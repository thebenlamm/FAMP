---
phase: 04-federation-cli-unwire-federation-ci-preservation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - ARCHITECTURE.md
autonomous: true
requirements: [MIGRATE-03]
must_haves:
  truths:
    - "README.md headlines 'FAMP today is local-first; FAMP at v1.0 is federated' (D-16/D-17 staged framing)"
    - "CLAUDE.md '## Project' section uses the staged framing"
    - "ROADMAP.md v0.9 milestone callout adds the explicit 'today: local-first; trigger to v1.0' sentence"
    - "MILESTONES.md v0.9 section is past-tense ship state, not 'in design'"
    - "ARCHITECTURE.md headers reflect v0.9 shipping (not 'in design')"
    - "The federation-CLI tutorial blocks in README.md are deleted or replaced with a pointer to MIGRATION + tag"
    - "Every changed line traces directly to D-16/D-17 staged framing or to a FED/MIGRATE requirement"
    - "No drive-by polish — surgical changes only per CLAUDE.md global rule"
    - "just ci is green"
  artifacts:
    - path: "README.md"
      provides: "User-facing project README with v0.9-as-shipping local-first framing"
      contains: "FAMP today is local-first"
    - path: "CLAUDE.md"
      provides: "Project instructions with v0.9-as-shipping framing in '## Project' section"
      contains: "FAMP today is local-first"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with explicit today/trigger framing line in v0.9 callout"
      contains: "today: local-first"
    - path: ".planning/MILESTONES.md"
      provides: "Milestones changelog with v0.9 in past-tense ship state"
      contains: "FAMP today is local-first"
    - path: "ARCHITECTURE.md"
      provides: "Architecture doc with current/past tense headers reflecting v0.9 shipping"
      contains: "v0.9.0"
  key_links:
    - from: "README.md"
      to: "docs/MIGRATION-v0.8-to-v0.9.md"
      via: "pointer paragraph replacing the federation-CLI tutorial blocks"
      pattern: "MIGRATION-v0.8-to-v0.9"
    - from: "README.md"
      to: "v0.8.1-federation-preserved tag"
      via: "pointer paragraph for federation users"
      pattern: "v0.8.1-federation-preserved"
---

<objective>
Land the **D-16 / D-17 staged-framing** edits across the five documentation surfaces — README.md, CLAUDE.md, .planning/ROADMAP.md, .planning/MILESTONES.md, ARCHITECTURE.md — so the project narrative matches the v0.9-as-shipping reality.

The framing is **"FAMP today is local-first; FAMP at v1.0 is federated"** — staged, not identity-rewriting. The acronym starts with F. Two rewrites for one positioning move (now → "local-first"; v1.0 → back to "federated") would be wasteful and concede nothing real. Staged framing matches the trigger reality (local-first ships now, federation gates on Sofer-from-different-machine) and doesn't need rewriting back when v1.0 fires.

Per **CLAUDE.md surgical-changes rule** + CONTEXT.md `<specifics>`: every changed line MUST trace directly to D-16/D-17 staged framing or to the FED/MIGRATE requirement table. **No drive-by polish.** No reformatting of unrelated sections.

This is a Wave 1 plan — runs in parallel with Plans 04-01, 04-02, 04-03, 04-04, 04-07. Lands BEFORE the `v0.8.1-federation-preserved` tag per D-07 — the framing edits are part of the relabeling work that the tag predates being separate from.

**Important coordination:** The federation-CLI tutorial in README.md (lines 24, 58–61, 233–256, 271, 562–564 per RESEARCH Audit 10) describes verbs that are deleted in Plan 04-08 (Wave 2). This plan REPLACES those tutorial sections with a pointer to MIGRATION + tag — replacement happens NOW (not waiting for Wave 2), because the v0.8.1-federation-preserved tag will be cut at end of Wave 1 and the tag commit's README MUST already point users at the migration doc.

Purpose: Update the project narrative to match the v0.9 shipping reality with staged framing per D-16/D-17.
Output: 5 file edits, all surgical, single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md
@README.md
@CLAUDE.md
@ARCHITECTURE.md
@.planning/ROADMAP.md
@.planning/MILESTONES.md

<interfaces>
<!-- Per-file landing sites and verbatim replacement texts are fully drafted in RESEARCH Audit 10 (lines 622-688). -->
<!-- This plan is purely doc edits; no code interfaces. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-17 staged-framing edits across README, CLAUDE, ROADMAP, MILESTONES, ARCHITECTURE</name>
  <files>README.md, CLAUDE.md, .planning/ROADMAP.md, .planning/MILESTONES.md, ARCHITECTURE.md</files>
  <read_first>
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md Audit 10 (per-file landing sites with verbatim replacement texts, lines 622-688)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md D-16, D-17, and `<specifics>` "No drive-by polish" rule
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md "Documentation framing edits" section (lines 644-663)
    - README.md (current state — locate lines per Audit 10: 10, 24, 58-61, 233-256, 271, 562-564)
    - CLAUDE.md (current state — locate "## Project" section around line 211; lines 214, 221 with federation language)
    - ARCHITECTURE.md (current state — locate line 4 "## Current state (v0.8)" header and line 38 "v0.9 direction (in design)")
    - .planning/ROADMAP.md (current state — line 11 v0.9 milestone callout)
    - .planning/MILESTONES.md (current state — lines 24-53 v0.9 section with "implementation paused" hedge)
  </read_first>
  <action>
**Surgical-changes invariant:** every edit below MUST trace to D-16/D-17 staged framing OR to a deleted-CLI tutorial replacement. No reformatting of unrelated sections, no whitespace cleanup, no rephrasing of unrelated paragraphs.

**Edit 1: README.md — first paragraph after project title**

Locate the first paragraph after the project title (around line 10 per Audit 10). Replace the existing v0.9-is-in-design framing with:

> FAMP today is local-first: a UDS-backed broker for same-host agent messaging
> with zero crypto on the local path. FAMP at v1.0 is federated: cross-host
> messaging via a `famp-gateway` wrapping the local bus, all of v0.5.2's
> signature/canonical-JSON guarantees preserved. The v1.0 trigger condition
> is documented in [ARCHITECTURE.md](ARCHITECTURE.md).

Add ONE explicit sentence to the Quick Start preamble (above the `cargo install` line — should already exist post-Phase-3) reading:

> This is the v0.9 local-first path; if you need cross-host federation, see
> [docs/MIGRATION-v0.8-to-v0.9.md](docs/MIGRATION-v0.8-to-v0.9.md).

**Edit 2: README.md — delete or replace the federation-CLI tutorial block**

Lines 24, 58-61, 233-256, 271, 562-564 currently describe the deleted CLI verbs (`famp setup / listen / send`, `famp peer import`, etc.). Replace this entire tutorial block with a single one-paragraph pointer:

> The v0.8 federation CLI (`famp init / setup / listen / peer add / peer import`)
> was removed in v0.9. See [docs/MIGRATION-v0.8-to-v0.9.md](docs/MIGRATION-v0.8-to-v0.9.md)
> for the migration path; the `v0.8.1-federation-preserved` git tag is the
> escape hatch for users who genuinely need cross-host messaging today (frozen,
> bug fixes ship via the v1.0 federation gateway when it lands).

**Surgical scope:** delete only the federation-CLI-tutorial-shaped content. Preserve all bus / broker / `famp register` / `famp send` / Quick Start content. If a paragraph mixes federation tutorial with bus tutorial, edit only the federation lines.

Line 562 (the "v0.9: local-first bus — in design" Backlog/Future entry) should change to "v0.9: local-first bus — shipping now" or be removed if it's a duplicate of content covered elsewhere. Keep the change SURGICAL.

**Edit 3: CLAUDE.md — '## Project' section staged framing**

Locate the `## Project` section (around line 211). Replace the existing one-line description with:

> **FAMP today is local-first** (v0.9): a UDS-backed broker for same-host agent
> messaging. **FAMP at v1.0 is federated**: cross-host messaging via
> `famp-gateway` wrapping the local bus. See [ARCHITECTURE.md](ARCHITECTURE.md)
> for the full layered model (Layer 0 protocol primitives → Layer 1 local bus →
> Layer 2 federation gateway).

Lines 214 and 221 (federation-transport descriptions in present tense) get rewritten to past tense:

- Line 214 area: change `\`famp listen\` HTTPS daemon per identity; TOFU-pinned peers; ...` to `In v0.8 the federation transport used \`famp listen\` HTTPS daemons with TOFU-pinned peers; v0.9 replaces this with the local bus...`
- Line 221 area: change `The federation transport (\`famp listen\`, \`famp send\`, etc.) still uses \`FAMP_HOME\` per identity.` to past tense or remove if redundant with the bus rewrite — preserve any v0.8.x bridge / MCP context that's still accurate.

**Surgical scope:** keep the surrounding "## Architecture" / "## GSD Workflow" / "## Constraints" sections unchanged.

**Edit 4: .planning/ROADMAP.md — v0.9 milestone callout**

Locate the v0.9 milestone callout (around line 11). Add ONE explicit sentence:

> **Today (v0.9):** local-first; broker handles same-host messaging.
> **Trigger to v1.0:** Sofer-from-different-machine; 4-week clock starts at v0.9.0.

Insert this as a one-paragraph block at the END of the v0.9 milestone callout, NOT replacing existing content. The Core Value line (line 3) stays as-is per D-17 — already accurate.

**Edit 5: .planning/MILESTONES.md — v0.9 section header and prose**

Locate the v0.9 section (around lines 24-53). Replace the section header from `## v0.9 Local-First Bus (In Design — 2026-04-17)` (or whatever stale-state framing is present) to:

> ## v0.9 Local-First Bus (Shipped: TBD-2026-05-XX)
>
> 4 phases, 85 requirements. UDS-backed broker, zero crypto on local path,
> IRC-style channels, durable per-name mailboxes, stable MCP tool surface.
> **FAMP today is local-first; FAMP at v1.0 is federated.** Federation-tagged
> tests preserved under `crates/famp/tests/_deferred_v1/`; escape-hatch tag
> `v0.8.1-federation-preserved` exists for federation users.

Replace the "implementation paused pending ~2 weeks" hedge text on lines 26-27 with the past-tense ship framing. The TBD-2026-05-XX placeholder is intentional — Ben fills the actual date when the v0.9.0 tag is cut.

If MILESTONES.md lines 27, 49-50 reference `scripts/famp-local`, update to `docs/history/v0.9-prep-sprint/famp-local/famp-local` — Plan 04-03 archived the script and Plan 04-05 (this plan) carries the path-fix forward in MILESTONES.md atomic with the framing edit.

**Edit 6: ARCHITECTURE.md — header tense flips**

Per Audit 10 line 686:

- Line 4: change `## Current state (v0.8)` to `## Past state (v0.8)`.
- Line 38: change `## v0.9 direction (in design)` to `## v0.9 (shipping at v0.9.0 tag)` or similar past-tense / ship-state framing.

The body text at lines 38-79 is already correctly framed per Phase 3 D-13; only the headers flip. Do NOT rewrite the body paragraphs.

**Verification at end of all edits:**

```bash
grep -c "FAMP today is local-first" README.md CLAUDE.md .planning/MILESTONES.md
# Expected: each file ≥ 1; total ≥ 3

grep -c "today: local-first\|Today (v0.9)" .planning/ROADMAP.md
# Expected: ≥ 1

grep -c "Past state (v0.8)\|shipping at v0.9.0" ARCHITECTURE.md
# Expected: ≥ 1

# Confirm federation-CLI tutorial blocks removed from README.md
grep -E "famp setup|famp listen|famp peer add|famp peer import" README.md | wc -l
# Expected: ≤ 1 (the migration-doc pointer paragraph references them in past tense; otherwise should be zero)

# Confirm MIGRATION + tag references present
grep -c "MIGRATION-v0.8-to-v0.9\|v0.8.1-federation-preserved" README.md
# Expected: ≥ 2 (one each in the pointer paragraph)

# Confirm scripts/famp-local references gone from MILESTONES
grep -c "scripts/famp-local" .planning/MILESTONES.md
# Expected: 0 (path updated to docs/history/v0.9-prep-sprint/famp-local/famp-local)

just ci 2>&1 | tail -10
# Expected: green
```

This is a single atomic commit titled `docs(04): staged-framing edits across README, CLAUDE, ROADMAP, MILESTONES, ARCHITECTURE` (Audit 11 commit 9).
  </action>
  <verify>
    <automated>just ci > /dev/null 2>&1 &amp;&amp; [ $(grep -c "FAMP today is local-first" README.md) -ge 1 ] &amp;&amp; [ $(grep -c "FAMP today is local-first" CLAUDE.md) -ge 1 ] &amp;&amp; [ $(grep -c "FAMP today is local-first" .planning/MILESTONES.md) -ge 1 ] &amp;&amp; [ $(grep -c "MIGRATION-v0.8-to-v0.9" README.md) -ge 1 ] &amp;&amp; [ $(grep -c "v0.8.1-federation-preserved" README.md) -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "FAMP today is local-first" README.md` returns ≥ 1.
- `grep -c "FAMP today is local-first" CLAUDE.md` returns ≥ 1.
- `grep -c "FAMP today is local-first" .planning/MILESTONES.md` returns ≥ 1.
- `grep -c "Today (v0.9)" .planning/ROADMAP.md` returns ≥ 1.
- `grep -c "Past state (v0.8)\|shipping at v0.9" ARCHITECTURE.md` returns ≥ 1.
- `grep -c "MIGRATION-v0.8-to-v0.9" README.md` returns ≥ 1 (pointer paragraph in place of federation-CLI tutorial).
- `grep -c "v0.8.1-federation-preserved" README.md` returns ≥ 1.
- `grep -c "scripts/famp-local" .planning/MILESTONES.md` returns 0 (path updated to archive path).
- The federation-CLI tutorial blocks in README.md (RESEARCH Audit 10 lines 24, 58-61, 233-256, 271) are deleted or rewritten to past tense / pointer prose. Specifically: `grep -E '^\./target/release/famp setup' README.md` returns 0 lines (the v0.8 tutorial command is gone).
- `just ci` exits 0.
- `git diff --stat` for the commit shows only the 5 files in `files_modified` were touched.
  </acceptance_criteria>
  <done>
The five doc surfaces carry the D-16/D-17 staged framing; the federation-CLI tutorial in README is replaced with a migration-doc pointer; surgical-changes invariant holds; just ci is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation narrative | The framing IS the project's identity claim; getting it wrong (overcorrect to "FAMP is local-first" identity rewrite) creates a future rewrite cost when v1.0 fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-overcorrection | Information disclosure (narrative) | Project identity | mitigate | D-16 explicitly forbids identity rewrite; staged framing is the explicit decision. Acceptance criteria check for "today is local-first" wording, NOT "is local-first" standalone. |
| T-04-drive-by | Tampering (scope creep) | Doc files | mitigate | CLAUDE.md surgical-changes rule + CONTEXT.md `<specifics>` "No drive-by polish" — git diff stat must show only the 5 files. |
</threat_model>

<verification>
- The five files carry the staged framing wording.
- README federation-CLI tutorial is replaced with a migration-doc pointer.
- MILESTONES.md scripts/famp-local references updated to the archive path.
- No reformatting of unrelated sections.
- just ci is green.
</verification>

<success_criteria>
- D-16/D-17 staged framing lands in all five files.
- Federation-CLI tutorial blocks in README replaced with MIGRATION + tag pointer.
- Single atomic commit titled `docs(04): staged-framing edits ...` lands all changes.
- Surgical-changes invariant holds.
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-05-SUMMARY.md` documenting: which paragraphs in README were deleted, which paragraphs were added, and confirmation that the surgical-changes invariant held (git diff stat shows only the 5 files).
</output>
