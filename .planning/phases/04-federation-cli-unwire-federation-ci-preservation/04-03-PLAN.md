---
phase: 04-federation-cli-unwire-federation-ci-preservation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/history/v0.9-prep-sprint/famp-local/famp-local
  - docs/history/v0.9-prep-sprint/famp-local/README.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MIGRATE-04]
must_haves:
  truths:
    - "scripts/famp-local no longer exists at its v0.8 path"
    - "docs/history/v0.9-prep-sprint/famp-local/famp-local exists and is the same 1316-line bash script (preserved via git mv)"
    - "docs/history/v0.9-prep-sprint/famp-local/README.md exists and marks the script as frozen"
    - "Backlog 999.6 in ROADMAP.md references the new path, not scripts/famp-local"
    - "just ci is green"
  artifacts:
    - path: "docs/history/v0.9-prep-sprint/famp-local/famp-local"
      provides: "Archived prep-sprint scaffolding (frozen, not maintained)"
      min_lines: 1300
    - path: "docs/history/v0.9-prep-sprint/famp-local/README.md"
      provides: "Frozen marker explaining the script is not maintained"
      min_lines: 5
  key_links:
    - from: ".planning/ROADMAP.md (backlog 999.6)"
      to: "docs/history/v0.9-prep-sprint/famp-local/famp-local"
      via: "path reference in backlog goal/context"
      pattern: "docs/history/v0.9-prep-sprint/famp-local"
---

<objective>
Archive `scripts/famp-local` to `docs/history/v0.9-prep-sprint/famp-local/` as design-provenance for the v0.9 prep sprint (T1–T9 UX validation), and update the ROADMAP.md backlog 999.6 entry to reference the new path. Per **D-14**, this is an ARCHIVE not a delete — the script is frozen, but it stays grep-able for the design lineage and stays addressable by the 999.6 backlog item which is still actionable.

Per **D-15** the ROADMAP.md update lands ATOMICALLY in the same commit as the archive move (AUDIT-05 atomic discipline). Per **D-04** the move uses `git mv` (preserves blame).

Per RESEARCH Audit 8: `scripts/famp-local` is a SINGLE 1316-line bash script, NOT a directory (despite some CONTEXT.md prose calling it a "directory"). The archive is a single-file move.

This is a Wave 1 plan — runs in parallel with Plans 04-01, 04-02, 04-04, 04-05, 04-07. Lands BEFORE the `v0.8.1-federation-preserved` tag per D-07 (the archive is a "deletion-shaped" operation that the tag predates would lie about).

Purpose: Preserve design provenance of the prep-sprint scaffolding while removing it from the active scripts/ directory.
Output: 1 file move, 1 new README, 1 ROADMAP.md edit (lines ~314-324) — all in a single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md

<interfaces>
<!-- This plan is purely a file-move + doc-edit. No code interfaces. -->
<!-- ROADMAP.md backlog 999.6 lines 314-324 reference the v0.8 path; D-15 mandates the path update lands in the same commit as the archive move. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv scripts/famp-local into docs/history/v0.9-prep-sprint/famp-local/, add freeze-marker README, update ROADMAP backlog 999.6 path</name>
  <files>docs/history/v0.9-prep-sprint/famp-local/famp-local, docs/history/v0.9-prep-sprint/famp-local/README.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md Audit 8 (archive scope: single 1316-line bash file; ROADMAP lines 314-324; MILESTONES.md lines 27, 49-50; REQUIREMENTS.md line 132)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md D-14, D-15 (archive operation; backlog 999.6 path update; atomic commit)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md "docs/history/v0.9-prep-sprint/famp-local/README.md" section (lines 383-399 — README template)
    - .planning/ROADMAP.md lines 314-324 (the backlog 999.6 entry)
  </read_first>
  <action>
**Step 1: Verify the source exists and confirm it's a file (not a directory)**

```bash
file scripts/famp-local
wc -l scripts/famp-local
```

Expected: `Bourne-Again shell script text executable` and `1316 scripts/famp-local`. If `scripts/famp-local` is a DIRECTORY rather than a file, escalate to Ben — the research says single-file but live tree may have changed.

**Step 2: Create destination directory and perform the git mv**

```bash
mkdir -p docs/history/v0.9-prep-sprint/famp-local/
git mv scripts/famp-local docs/history/v0.9-prep-sprint/famp-local/famp-local
```

**Step 3: Write the freeze-marker README**

Create `docs/history/v0.9-prep-sprint/famp-local/README.md` with content (per PATTERNS.md analog):

```markdown
# famp-local — frozen v0.9 prep-sprint scaffolding

This script is **frozen**, not maintained. It embodies the v0.9 prep-sprint
UX validation (T1–T9) — every workaround, every `update_zprofile_init` quirk,
every flag encodes a lesson learned during the v0.8.x → v0.9 migration design.

Bug fixes ship via the live `famp` binary or the `famp-local hook` subcommand
(Phase 2 / HOOK-04a). Do not edit this file.

See [`docs/MIGRATION-v0.8-to-v0.9.md`](../../../MIGRATION-v0.8-to-v0.9.md) for
the user-facing migration path, and ROADMAP backlog 999.6
(`update_zprofile_init` should sandbox on non-default `FAMP_LOCAL_ROOT`) for
the one open issue against this archived script.
```

**Step 4: Update ROADMAP.md backlog 999.6 path**

In `.planning/ROADMAP.md` lines 314-324 (the `Phase 999.6` block), replace every literal occurrence of `scripts/famp-local` with `docs/history/v0.9-prep-sprint/famp-local/famp-local`. There are three specific mentions per RESEARCH Audit 8:

- Line ~316 (Goal): `Make \`scripts/famp-local\`'s \`update_zprofile_init\` ...` → `Make \`docs/history/v0.9-prep-sprint/famp-local/famp-local\`'s \`update_zprofile_init\` ...`
- Line ~321 (Context): `... \`scripts/famp-local\`'s \`update_zprofile_init\` calls ...` → `... \`docs/history/v0.9-prep-sprint/famp-local/famp-local\`'s \`update_zprofile_init\` calls ...`
- Any other in-paragraph reference to `scripts/famp-local` within lines 314-324 → swap to the new path.

Also: if the backlog text says `scripts/famp-local` standalone (without backticks) in a sentence, update it. Use a targeted sed or manual edit; do NOT use a global `s/scripts\/famp-local/.../g` over the whole file (other unrelated lines may legitimately reference paths).

**Step 5: NOT in this plan** — `.planning/MILESTONES.md` lines 27, 49-50 also reference `scripts/famp-local`. Those references land in Plan 04-05 (staged-framing edits) which is the natural home for the MILESTONES.md edit, atomic with the other framing changes. Same for `.planning/REQUIREMENTS.md` line 132 (MIGRATE-04 wording) which lands in Plan 04-08's REQUIREMENTS sweep. Coordinate cross-plan: this plan ONLY updates `.planning/ROADMAP.md` backlog 999.6.

**Step 6: Verify and commit-ready**

```bash
test -f docs/history/v0.9-prep-sprint/famp-local/famp-local
test -f docs/history/v0.9-prep-sprint/famp-local/README.md
test ! -e scripts/famp-local
grep -c "scripts/famp-local" .planning/ROADMAP.md
# Expected: 0 (the backlog references should all be updated; if non-zero, find and fix the remaining ones)
just ci 2>&1 | tail -5
```

Per D-15 the move + README + ROADMAP edit land in ONE atomic commit titled `chore(04): archive scripts/famp-local under docs/history/v0.9-prep-sprint/` (Audit 11 commit 7).

**Step 7: Spot-check other consumers** that may reference `scripts/famp-local`:

```bash
grep -rn "scripts/famp-local" . --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=target 2>/dev/null
```

If hits remain in `README.md`, `CLAUDE.md`, `docs/`, or other planning docs — those are handled by Plan 04-05 (framing) or already in the staged-framing edit landing-site list per RESEARCH Audit 10. Document any remaining hits in the SUMMARY for Plan 04-05's awareness, but do not edit them in this plan (atomic-commit hygiene).

If hits remain in `.planning/MILESTONES.md` or `.planning/REQUIREMENTS.md`: those are handled by Plan 04-05 / Plan 04-08 respectively. Confirmed scope. Do not touch.

If hits remain in code files (e.g., `crates/`, `tests/`): escalate to Ben — that would indicate runtime code referencing the script path, which is unexpected.
  </action>
  <verify>
    <automated>test -f docs/history/v0.9-prep-sprint/famp-local/famp-local && test -f docs/history/v0.9-prep-sprint/famp-local/README.md && test ! -e scripts/famp-local && [ "$(grep -c 'scripts/famp-local' .planning/ROADMAP.md)" = "0" ] && just ci > /dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `test -f docs/history/v0.9-prep-sprint/famp-local/famp-local` returns true.
- `wc -l docs/history/v0.9-prep-sprint/famp-local/famp-local` returns ≥ 1300 (preserves the 1316-line script).
- `git log --follow docs/history/v0.9-prep-sprint/famp-local/famp-local | head -5` shows pre-2026-05-03 commits (proves history preserved via git mv).
- `test -f docs/history/v0.9-prep-sprint/famp-local/README.md` returns true.
- `wc -l docs/history/v0.9-prep-sprint/famp-local/README.md` returns ≥ 5.
- `grep -c "frozen" docs/history/v0.9-prep-sprint/famp-local/README.md` returns ≥ 1.
- `grep -c "MIGRATION-v0.8-to-v0.9" docs/history/v0.9-prep-sprint/famp-local/README.md` returns ≥ 1.
- `grep -c "999.6\|update_zprofile_init" docs/history/v0.9-prep-sprint/famp-local/README.md` returns ≥ 1.
- `test ! -e scripts/famp-local` returns true (the source no longer exists at the v0.8 path).
- `grep -c "scripts/famp-local" .planning/ROADMAP.md` returns 0 (all backlog 999.6 references updated to the new path).
- `grep -c "docs/history/v0.9-prep-sprint/famp-local/famp-local" .planning/ROADMAP.md` returns ≥ 2 (the backlog 999.6 references now point to the new path; expect at least the goal mention + context mention).
- `just ci` exits 0.
  </acceptance_criteria>
  <done>
The script is archived at the new path with history preserved; the freeze-marker README explains that the script is not maintained; ROADMAP backlog 999.6 references the new path; nothing in `scripts/` references the old name; `just ci` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/` (active tools) ↔ `docs/history/` (frozen archives) | A move from active to frozen status — must not leave orphan references in the active tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-orphan-ref | Information disclosure | Stale references to `scripts/famp-local` after the move | mitigate | Verified ROADMAP.md backlog 999.6 references updated; spot-check pass enumerates remaining references for Plan 04-05/04-08 to address. |
| T-04-blame-loss | Information disclosure (provenance) | Script's git history | mitigate | Used `git mv`; verified `git log --follow` returns pre-move commits. |
</threat_model>

<verification>
- The script archive lives at `docs/history/v0.9-prep-sprint/famp-local/famp-local` with full git history preserved.
- The freeze-marker README explains the script is not maintained and points to MIGRATION + 999.6 backlog.
- ROADMAP backlog 999.6 entry references the new archive path, not `scripts/famp-local`.
- `just ci` is green; no test references the old path.
</verification>

<success_criteria>
- `scripts/famp-local` no longer exists.
- `docs/history/v0.9-prep-sprint/famp-local/famp-local` exists with the same 1316 lines and pre-move git history.
- The freeze README is in place.
- ROADMAP backlog 999.6 path is updated.
- One atomic commit titled `chore(04): archive scripts/famp-local under docs/history/v0.9-prep-sprint/` lands all changes (D-15).
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-03-SUMMARY.md` documenting: the move SHA, any remaining `scripts/famp-local` references found in the spot-check (with note that those land in Plan 04-05 / 04-08), and confirmation the archive's git history is intact via `git log --follow`.
</output>
