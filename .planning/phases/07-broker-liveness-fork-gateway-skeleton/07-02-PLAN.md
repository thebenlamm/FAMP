---
phase: 07-broker-liveness-fork-gateway-skeleton
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/famp-bus/src/broker/handle/tests.rs
autonomous: true
requirements: [LIVE-01]
must_haves:
  truths:
    - "Two-or-more broker clients registered with the SAME pid all remain in broker.state.clients across a Tick sweep while that pid is alive (LIVE-01)."
    - "When that shared pid is marked dead, the same sweep reaps ALL of those clients on the next Tick — no orphan holders (LIVE-02 mechanism, proven deterministically)."
  artifacts:
    - "crates/famp-bus/src/broker/handle/tests.rs: new pure-broker test(s) using the existing TestEnv + FakeLiveness harness, proving register() imposes no pid-uniqueness constraint."
  key_links:
    - "register() sets state.pid = Some(pid) unconditionally (handle.rs:358) — the no-uniqueness fact Design A rides; this test pins it."
    - "tick() reaps by is_alive(pid) alone (handle.rs:973-991) — the deterministic FakeLiveness flip proves reap-on-death without a wall-clock sleep."
---

<objective>
Deterministically pin the load-bearing fact behind Design A: the broker's `register()` puts no pid-uniqueness constraint on clients, so N principals sharing one live PID all survive the `kill(pid,0)` sweep — and all reap together the instant that PID is marked dead.

Purpose: Provide the fast, non-flaky LIVE-01 proof (pure `Broker<E>` + `FakeLiveness`, no subprocess, no sleep) recommended by 07-RESEARCH.md Pitfall 4, independent of the gateway crate existing.
Output: One or more pure-broker unit tests in the existing `handle/tests.rs`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-RESEARCH.md
@.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces (whole-phase, for source-grounding drift exclusion)

This plan adds ONLY pure-broker tests to the existing `crates/famp-bus/src/broker/handle/tests.rs`. The whole-phase new symbols (crate `famp-gateway`, `GatewayError`, `ProxiedPrincipal`, `GatewayRegistry`, `[[bin]] famp-gateway`, `BusClient::connect_no_spawn`) are produced by plans 01 and 03 — see 07-01-PLAN.md `<artifacts_produced>`. No new production symbols are created here.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Pure-broker LIVE-01 test — N clients, one PID, survive-then-reap</name>
  <files>crates/famp-bus/src/broker/handle/tests.rs</files>
  <read_first>
    - crates/famp-bus/src/broker/handle/tests.rs:1-60 (existing `TestEnv` harness: `Rc<RefCell<FakeLiveness>>`, `hello_canonical` helper, `register(broker, client, name, pid, now)` helper)
    - crates/famp-bus/src/broker/handle.rs:262-376 (`register()` — no pid-uniqueness check; `state.pid = Some(pid)` at :358)
    - crates/famp-bus/src/broker/handle.rs:973-991 (`tick()` — filters `!is_alive(pid)`, disconnects each dead client)
    - crates/famp-bus/src/liveness.rs:18-41 (`FakeLiveness` test double — how to mark a pid alive/dead)
    - 07-PATTERNS.md section "handle/tests.rs — extend with LIVE-01"
  </read_first>
  <action>
    Add a new test to `crates/famp-bus/src/broker/handle/tests.rs` using the existing `TestEnv` + `FakeLiveness` helpers (do NOT create a new test file, do NOT modify production code). Name it `live01_shared_pid_clients_survive_sweep_and_reap_together` (or split into two focused tests if clearer). Body: mark a single pid (e.g. `4242`) alive in `FakeLiveness`; `hello_canonical` + `register` two (or three) distinct client/name pairs — `alice`, `bob` — all passing the SAME `pid=4242` (the Design A shape: one process backing many principals). Drive a `Tick` at `now`; assert BOTH clients remain present in `broker.state.clients`. Then mark pid `4242` dead in `FakeLiveness`, drive another `Tick`; assert BOTH clients are now gone from `broker.state.clients` (reaped together, no orphan). Assert via the returned `Out` events and/or `broker.state.clients` membership — match the assertion style already used by the sweep tests in the same file. Falsification note for the executor: a mistaken pid-uniqueness guard in `register()` would make the second `register` fail before the sweep is even reached — the test must show both registrations SUCCEED first.
    No new test dependency, no `#[expect_used]` in the test (project convention: `just lint` runs clippy pedantic on tests too).
  </action>
  <verify>
    <automated>cargo test -p famp-bus --lib live01_shared_pid && just lint</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p famp-bus --lib live01_shared_pid` passes (both shared-PID clients survive the live Tick; both reaped on the dead Tick).
    - The test registers >= 2 distinct names with an identical `pid` argument and asserts both `register` calls succeed (pins the no-uniqueness fact).
    - `just lint` (clippy pedantic, `-D warnings`, no `expect_used` in the new test) exits 0.
    - No production source changed: `git diff --name-only -- crates/famp-bus/src/ | grep -v 'handle/tests.rs' | wc -l` is 0.
  </acceptance_criteria>
  <done>A deterministic pure-broker test proves N clients sharing one PID both register successfully, both survive the sweep while the PID is alive, and both reap on the same Tick once the PID dies — with no production code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → in-process Broker<E> | Pure in-process test using `FakeLiveness`; no sockets, no subprocess, no real OS PID probe. No new trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02b | Tampering | test-only change to handle/tests.rs | low | accept | Test-only addition; touches no production path, no wire format, no broker behavior. Verified by the diff-scope acceptance gate above. |
| T-07-SC | Tampering | cargo installs | low | accept | N/A — no new external packages; test reuses the existing in-crate `FakeLiveness` harness. |
</threat_model>

<verification>
- `cargo test -p famp-bus --lib` green including the new test.
- `just lint` clean.
- Diff limited to `crates/famp-bus/src/broker/handle/tests.rs` (no production code touched).
</verification>

<success_criteria>
LIVE-01 is proven deterministically at the pure-broker layer: shared-PID clients survive the sweep while alive and reap together when the PID dies, confirming `register()` imposes no pid-uniqueness constraint.
</success_criteria>

<output>
Create `.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-02-SUMMARY.md` when done.
</output>
