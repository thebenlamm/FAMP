---
phase: 07-broker-liveness-fork-gateway-skeleton
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crates/famp-gateway/tests/common/child_guard.rs
  - crates/famp-gateway/tests/liveness.rs
  - crates/famp-gateway/tests/no_cross_talk.rs
autonomous: true
requirements: [LIVE-02, GW-04]
must_haves:
  truths:
    - "A real famp-gateway process backing N principals keeps them all live in `famp inspect identities` while it runs; SIGKILLing it reaps ALL N within one sweep interval (~1s), leaving no orphan holders (LIVE-02, and LIVE-01 at subprocess level)."
    - "A single gateway process backing principals A and B never lets a message addressed to A appear in B's mailbox (GW-04)."
  artifacts:
    - "crates/famp-gateway/tests/common/child_guard.rs — copied ChildGuard RAII kill-on-drop helper."
    - "crates/famp-gateway/tests/liveness.rs — LIVE-02 subprocess test (spawn broker + gateway bin, poll inspect, SIGKILL, poll for reaping with a bounded deadline)."
    - "crates/famp-gateway/tests/no_cross_talk.rs — GW-04 isolation test."
  key_links:
    - "Every spawned child (broker, gateway bin) wrapped in ChildGuard — leak-safe on panic (project incident class)."
    - "Reaping asserted by polling `famp inspect identities` with a bounded deadline, never `sleep()`-then-assert (07-RESEARCH.md Pitfall 4)."
    - "The gateway bin from plan 01 is the genuine killable PID LIVE-02 requires."
---

<objective>
Prove Design A end-to-end at the process level: a real `famp-gateway` process backing multiple principals keeps them live across the broker's sweep, reaps them all cleanly on process death, and never cross-wires one principal's mail into another's.

Purpose: Convert the mechanism (plan 01) and the deterministic broker fact (plan 02) into operator-observable LIVE-02 + GW-04 guarantees via real subprocess tests.
Output: Subprocess integration tests under `crates/famp-gateway/tests/`, ChildGuard-wrapped, poll-with-deadline.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-RESEARCH.md
@.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-PATTERNS.md
@.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces (whole-phase, for source-grounding drift exclusion)

This plan adds test-only files under `crates/famp-gateway/tests/`. It consumes — does not create — the production symbols `famp-gateway` bin, `GatewayRegistry`, `ProxiedPrincipal`, `BusClient::connect_no_spawn` (produced by plan 01; see 07-01-PLAN.md `<artifacts_produced>`) and the pure-broker LIVE-01 test (plan 02). New test symbols only: `ChildGuard`, `spawn_broker_subprocess`, `spawn_gateway_subprocess`, and the test fns.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: LIVE-02 — gateway exit reaps all proxied principals (SIGKILL + poll)</name>
  <files>crates/famp-gateway/tests/common/child_guard.rs, crates/famp-gateway/tests/liveness.rs</files>
  <read_first>
    - crates/famp/tests/broker_proxy_semantics.rs (full — the SIGKILL-a-holder-then-poll-with-timeout template; `spawn_broker_subprocess`, poll loop, `Command::cargo_bin`)
    - crates/famp/tests/common/child_guard.rs (full, ~41 lines — the ChildGuard RAII helper to copy verbatim)
    - crates/famp-gateway/src/main.rs (the bin's arg surface: `--socket <path>` + principal names — how the test invokes it)
    - crates/famp/src/cli/broker/mod.rs:51-53 (`TICK_INTERVAL = Duration::from_secs(1)` — the sweep interval the poll deadline must exceed)
    - 07-RESEARCH.md Pitfall 3 (ChildGuard mandatory), Pitfall 4 (poll-with-deadline, never sleep), Pitfall 6 (nextest list-hang fallback)
    - 07-VALIDATION.md Per-Task Verification Map (7-01-02)
  </read_first>
  <action>
    Copy `crates/famp/tests/common/child_guard.rs` verbatim to `crates/famp-gateway/tests/common/child_guard.rs` (RAII kill+wait on drop). Create `crates/famp-gateway/tests/liveness.rs` following the `broker_proxy_semantics.rs` shape: a `spawn_broker_subprocess(sock)` helper (`Command::cargo_bin("famp").args(["broker","--socket",sock])`, stdio null) and a `spawn_gateway_subprocess(sock, names)` helper (`Command::cargo_bin("famp-gateway").arg("--socket").arg(sock).args(names)`), BOTH wrapped in `ChildGuard`. Use a unique temp socket path per test (tempdir).
    Test `live02_gateway_exit_reaps_all_principals`: spawn broker; spawn a gateway process backing >= 2 principals (`alice`, `bob`); poll `famp inspect identities --json --socket <sock>` (via `Command::cargo_bin("famp")`) with a bounded deadline (e.g. up to ~5s, checking every ~100ms) until BOTH names appear as live holders (this also covers LIVE-01 at subprocess level). Then SIGKILL the gateway process (drop/kill its ChildGuard child, or `libc`/`Child::kill`). Poll `famp inspect identities` again with a bounded deadline generously exceeding one `TICK_INTERVAL` (e.g. up to ~5s) and assert BOTH `alice` and `bob` are gone — no orphan holders. NEVER `std::thread::sleep(fixed)`-then-assert; always poll-until-condition-or-deadline.
    If `cargo nextest` stalls in the list phase, fall back to `cargo test -p famp-gateway --test liveness` (Pitfall 6).
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test liveness live02_gateway_exit_reaps_all_principals -- --nocapture && just lint</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p famp-gateway --test liveness live02_gateway_exit_reaps_all_principals` passes: both principals observed live while the gateway runs, both absent within the post-kill deadline.
    - The test uses poll-with-deadline (no fixed `thread::sleep` immediately followed by the reaping assertion): `grep -c 'sleep' crates/famp-gateway/tests/liveness.rs` bounded to poll-interval usage only (reviewer confirms no sleep-then-assert).
    - Every spawned child is ChildGuard-wrapped: `grep -c 'ChildGuard' crates/famp-gateway/tests/liveness.rs` is >= 2 (broker + gateway).
    - `just lint` exits 0 (no `expect_used` regressions in the new test file).
  </acceptance_criteria>
  <done>A real gateway process keeps its N principals live across the sweep and, on SIGKILL, all N reap within one sweep interval with no orphans — proven by a ChildGuard-wrapped, poll-with-deadline subprocess test.</done>
</task>

<task type="auto">
  <name>Task 2: GW-04 — no cross-talk between two proxied principals</name>
  <files>crates/famp-gateway/tests/no_cross_talk.rs</files>
  <read_first>
    - crates/famp/tests/broker_proxy_semantics.rs (send + inspect patterns, `Command::cargo_bin` usage)
    - crates/famp-gateway/tests/liveness.rs (the spawn helpers + ChildGuard from Task 1 — reuse them; do not re-copy child_guard.rs)
    - crates/famp-gateway/src/registry.rs (GatewayRegistry demux — the isolation under test)
    - crates/famp/src/cli/ (how `famp send` addresses a target and `famp inspect messages --to <name>` reports a mailbox — confirm exact flag surface before use)
    - 07-RESEARCH.md GW-04 section (per-name mailbox isolation; gateway must not misroute)
  </read_first>
  <action>
    Create `crates/famp-gateway/tests/no_cross_talk.rs` (reusing the `#[path = "common/child_guard.rs"] mod child_guard;` include and the broker/gateway spawn helpers — extract shared helpers into `tests/common/` if cleaner, otherwise duplicate minimally). Test `gw04_no_cross_talk_between_proxied_principals`: spawn broker; spawn ONE gateway process backing principals `alice` and `bob`; from a separate sender (a `famp send`-style CLI invocation or a raw bus `Send`) deliver a uniquely-tagged message addressed to `alice`. Poll/assert `famp inspect messages --to alice` (confirm exact CLI flag from the read_first inspection) contains the tagged message AND `famp inspect messages --to bob` does NOT contain it (and bob's mailbox is empty of alice's traffic). Assert the isolation with a bounded-deadline poll for the positive side and a direct check for the negative side after the positive has landed. All spawned children ChildGuard-wrapped.
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test no_cross_talk gw04_no_cross_talk_between_proxied_principals && just lint</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p famp-gateway --test no_cross_talk gw04_no_cross_talk_between_proxied_principals` passes: the tagged message appears in `alice`'s mailbox and is absent from `bob`'s.
    - One gateway process backs >= 2 principals in the test (single `spawn_gateway_subprocess` call with both names).
    - All spawned children ChildGuard-wrapped: `grep -c 'ChildGuard' crates/famp-gateway/tests/no_cross_talk.rs` is >= 2.
    - `just lint` exits 0.
  </acceptance_criteria>
  <done>A single gateway backing two principals is proven to isolate their mailboxes — a message to alice never appears in bob's mailbox — via a ChildGuard-wrapped subprocess test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real broker + gateway subprocesses | Real UDS sockets over a per-test tempdir; children are leak-guarded by ChildGuard. No network, no cross-host surface (Phase 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Denial of Service (test hygiene) | leaked broker/gateway child processes | medium | mitigate | Every spawned child wrapped in `ChildGuard` (RAII kill+wait on drop) so a panicking test cannot leave orphan brokers/gateways or stale sockets — enforced by the `grep -c 'ChildGuard'` acceptance gates. |
| T-07-03 | Repudiation (observability) | shared-PID rows in `famp inspect identities` | low | accept | Expected Design A signature; the LIVE-02 test reads identity presence/absence by name, not by PID uniqueness, so the shared-PID rows do not confuse the assertion. |
| T-07-SC | Tampering | cargo installs | low | accept | N/A — no new external packages; tests use the already-present `assert_cmd` dev-dep and a copied in-repo ChildGuard helper. |
</threat_model>

<verification>
- `cargo test -p famp-gateway --test liveness` and `--test no_cross_talk` both green.
- `cargo nextest run -p famp-bus -p famp-gateway -p famp` green at wave merge (fall back to `cargo test` per-crate if nextest list-phase hangs).
- `just lint` clean.
- No production source changed in this plan (test-only): the only non-test diff, if any, is extracting shared spawn helpers into `tests/common/`.
- No change under `crates/famp-bus/` or `crates/famp/src/cli/broker/` → no `just install` / broker restart required.
</verification>

<success_criteria>
LIVE-02 and GW-04 are proven at the real-process level: a killable gateway backing multiple principals reaps them all cleanly on exit within one sweep interval, and isolates their mailboxes with no cross-talk — all via leak-safe, poll-with-deadline subprocess tests.
</success_criteria>

<output>
Create `.planning/phases/07-broker-liveness-fork-gateway-skeleton/07-03-SUMMARY.md` when done.
</output>
