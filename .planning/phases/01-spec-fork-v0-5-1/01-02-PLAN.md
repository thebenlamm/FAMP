---
phase: 01-spec-fork-v0-5-1
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - FAMP-v0.5.1-spec.md
autonomous: true
requirements: [SPEC-02, SPEC-03, SPEC-04, SPEC-07, SPEC-08, SPEC-18, SPEC-19]
must_haves:
  truths:
    - "Canonical JSON section cites RFC 8785 verbatim with no paraphrase"
    - "Domain separation prefix is specified as `FAMP-sig-v1\\0` (12 bytes)"
    - "Signature binds the `to` field (recipient anti-replay is normative text)"
    - "Ed25519 encoding is locked: raw 32/64-byte, unpadded base64url, verify_strict"
    - "Clock skew ±60s and validity 300s are RECOMMENDED with federation caps"
    - "Idempotency key is 128-bit random, 22-char unpadded base64url, (sender,recipient) scoped"
    - "Artifact IDs are `sha256:<hex>` lowercase 64 chars over canonical JSON of artifact body"
  artifacts:
    - path: "FAMP-v0.5.1-spec.md"
      provides: "§4a, §7.1a, §7.1b, §13.1, §13.2, §3.6a populated"
      contains: "RFC 8785, FAMP-sig-v1, unpadded base64url, sha256:<hex>, ±60, 128-bit, recipient anti-replay"
  key_links:
    - from: "§7.1a domain separation"
      to: "§7.1c worked example"
      via: "cross-reference forward to Plan 06 worked example"
      pattern: "§7.1c"
    - from: "§4a canonical JSON"
      to: "§3.6a artifact IDs"
      via: "artifact hash is SHA-256 over canonical JSON"
      pattern: "canonical JSON"
---

<objective>
Wave 1 foundations: rewrite the normative content for canonical JSON, Ed25519 signing/encoding, recipient binding, numeric defaults, idempotency, and artifact identifiers. Every rewrite is a verbatim RFC citation + the decisions locked in CONTEXT.md. No state-machine content (Plan 04), no Agent Card content (Plan 03), no body schemas (Plan 05), no worked example bytes (Plan 06).

Purpose: These are the sections Phase 2 consumes to implement `famp-canonical` and `famp-crypto`. Every normative requirement must be grep-verifiable and citation-anchored.
Output: Seven rewritten sub-sections in `FAMP-v0.5.1-spec.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md
@.planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md
@.planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md
@FAMP-v0.5-spec.md
@docs/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4a Canonical JSON — RFC 8785 normative rewrite (SPEC-02)</name>
  <read_first>
    - FAMP-v0.5.1-spec.md (the stub from Plan 01)
    - FAMP-v0.5-spec.md §4 (the Core Invariants / Canonical JSON section being replaced)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-05..D-08
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §2.1 (RFC 8785 verbatim quotes, lines 118-137) and §6 Δ04/Δ05
    - docs/PITFALLS.md Pitfalls P1 (UTF-16 sort), P2 (number formatting), P3 (no Unicode normalization)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Replace the `## §4a Canonical JSON` stub with normative content. The section MUST contain (verbatim where quoted):

    1. Opening sentence (per D-05): "Canonical JSON for FAMP is **RFC 8785 JSON Canonicalization Scheme (JCS)**."
    2. Cite RFC 8785 §3.2.3 with the pull quote: "JSON object members MUST be sorted based on the UTF-16 code units of their names." Follow with the normative warning (per RESEARCH §2.1 + PITFALLS P1): "An implementation that compares keys as UTF-8 byte strings or as Rust `str::cmp` output is non-conformant. Supplementary-plane characters (U+10000+) sort by UTF-16 surrogate pair order (D800–DFFF range), not by codepoint."
    3. Cite RFC 8785 §3.2.2.3 with the pull quote: "JSON numbers MUST be represented as specified by Section 7.1.12.1 of ECMAScript (ECMA-262), which is equivalent to the 'Number.prototype.toString' method." Add the normative clauses (per RESEARCH §2.1): (a) `NaN`, `+Infinity`, `-Infinity` MUST be rejected at the serializer boundary with a typed error; (b) integers with absolute value > 2^53 MUST be represented as JSON strings per RFC 8785 §6 guidance (PITFALLS P2); (c) `-0` MUST render as `0`; (d) the reference formatter is the cyberphone test corpus, NOT `ryu` default output.
    4. Cite RFC 8785 §3.1 (duplicate-key rejection, per D-06): "Implementations MUST reject JSON input containing duplicate object keys at parse. Silently deduplicating duplicate keys is non-conformant. RFC 8259 §4 treats this as SHOULD; FAMP upgrades to MUST via RFC 8785 §3.1."
    5. No-Unicode-normalization clause (per PITFALLS P3): "Canonical JSON MUST NOT apply Unicode normalization (NFC, NFD, NFKC, NFKD) to string values. Bytes are passed through unchanged."
    6. Forbidden serde features (per D-08): "The `serde_json` features `arbitrary_precision` and `preserve_order` are incompatible with JCS and MUST NOT be enabled by conforming Rust implementations."
    7. A forward-reference line: "Two worked canonical-JSON examples are provided in §7.1c." (Plan 06 populates §7.1c.)
    8. A "disagreement clause": "Where the text of this specification and RFC 8785 disagree on any edge case, RFC 8785 is authoritative."
    9. Append to the Changelog section: `v0.5.1-Δ04 — §4a Canonical JSON — PITFALLS P1/P2/P3 — RFC 8785 JCS made normative with §3.2.3 and §3.2.2.3 pull-quotes, duplicate-key rejection, no-Unicode-normalization clause.` and `v0.5.1-Δ05 — §4a Canonical JSON — CONTEXT D-08 — serde arbitrary_precision and preserve_order forbidden; NaN/±Infinity rejected; integers > 2^53 serialized as strings.`
  </action>
  <verify>
    <automated>rg -q 'RFC 8785' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '3\.2\.3' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '3\.2\.2\.3' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'arbitrary_precision' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ04' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ05' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'RFC 8785' FAMP-v0.5.1-spec.md` passes (SPEC-02 anchor)
    - `rg -q 'UTF-16 code units' FAMP-v0.5.1-spec.md` passes (§3.2.3 pull quote)
    - `rg -q 'Number.prototype.toString' FAMP-v0.5.1-spec.md` passes (§3.2.2.3 pull quote)
    - `rg -q 'arbitrary_precision' FAMP-v0.5.1-spec.md` AND `rg -q 'preserve_order' FAMP-v0.5.1-spec.md` both pass (D-08)
    - `rg -q 'v0\.5\.1-Δ04' FAMP-v0.5.1-spec.md` passes (changelog entry)
    - `rg -q 'v0\.5\.1-Δ05' FAMP-v0.5.1-spec.md` passes (changelog entry)
  </acceptance_criteria>
  <done>§4a is a normative RFC-citation block with no paraphrase; PITFALLS P1/P2/P3 are resolved in text; Δ04/Δ05 changelog entries present.</done>
</task>

<task type="auto">
  <name>Task 2: §7.1a/§7.1b/§7.1 — Domain separation, Ed25519 encoding, recipient binding (SPEC-03, SPEC-04, SPEC-19)</name>
  <read_first>
    - FAMP-v0.5-spec.md §7.1 (envelope definition being amended)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-09..D-12
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §2.2 (RFC 8032 verbatim quotes, lines 138-149), §2.4 (RFC 4648 §5, lines 163-172), §6 Δ08/Δ09/Δ10
    - docs/PITFALLS.md Pitfall P4 (Ed25519 encoding) and P5 (domain separation)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Replace the `## §7.1a Domain separation` and `## §7.1b Ed25519 encoding` stubs with normative content, and amend the (placeholder) §7.1 signature paragraph:

    **§7.1a Domain separation prefix** (per D-09, Δ08):
    - Normative bytes: `prefix = b"FAMP-sig-v1\x00"` — 12 bytes total (11 ASCII: `F A M P - s i g - v 1`, plus one NUL byte).
    - Hex: `46 41 4d 50 2d 73 69 67 2d 76 31 00` — include this line verbatim (SPEC-03 hex anchor).
    - Signing formula: `signature = Ed25519.sign(sk, prefix || canonical_json_bytes)` where `canonical_json_bytes` is the RFC 8785 output of the envelope with the `signature` field omitted.
    - Normative: "Verification MUST apply the identical prefix in the identical position. The prefix is prepended; it is not appended, not interleaved, and not included as a JSON field."
    - Forward reference: "A byte-level worked example is provided in §7.1c."
    - Rationale (per PITFALLS P5): "Domain separation prevents cross-protocol signature confusion where a FAMP signature could be mistaken for a signature over arbitrary JSON in an unrelated protocol."

    **§7.1 signature paragraph amendment — `to` binding (per D-11, Δ09):**
    Add the normative sentence (SPEC-04 anchor): "The `to` field is part of the canonical JSON that is signed by the sender. A signed envelope binds `to` the recipient: a signed envelope addressed to agent A MUST NOT be replayable to agent B. This property is FAMP's recipient anti-replay guarantee."

    **§7.1b Ed25519 encoding** (per D-12, Δ10):
    - Cite RFC 8032 §5.1.2 (public key encoding), §5.1.6 (sign), §5.1.7 (verify).
    - Normative: "Ed25519 public keys are raw 32 bytes (compressed Edwards point per RFC 8032 §5.1.2). Signatures are raw 64 bytes (R || S concatenation per RFC 8032 §5.1.6). Wire encoding for both is **unpadded base64url** per RFC 4648 §5, Table 2 alphabet (`A-Z a-z 0-9 - _`)."
    - Lengths: "Encoded length: 43 chars for a 32-byte public key, 86 chars for a 64-byte signature, 22 chars for a 16-byte idempotency key."
    - Decoder rejection list: "Decoders MUST reject: (a) any `=` padding character; (b) the standard (non-url) alphabet characters `+` and `/`; (c) embedded whitespace; (d) trailing garbage."
    - Strict verification (PITFALLS P4): "Signature verification MUST use the **strict** form per RFC 8032 §5.1.7 — reject `R` that decodes to a small-order point, reject non-canonical `S` (the upper bound `S < L` check). This matches `ed25519-dalek 2.2`'s `verify_strict`. Cofactor-tolerant verification (the raw §5.1.7 equation `[8]SB = [8]R + [8]kA`) is non-conformant for FAMP."
    - Weak key rejection: "Weak (8-torsion) public keys MUST be rejected at trust-list ingress, not only at verify time."

    **Changelog appends** (per Δ08/Δ09/Δ10):
    - `v0.5.1-Δ08 — §7.1a Domain separation — PITFALLS P5 — Fixed prefix b"FAMP-sig-v1\\x00" (12 bytes); prepended to canonical JSON before Ed25519 sign/verify.`
    - `v0.5.1-Δ09 — §7.1 — v0.5 reviewer finding (cross-recipient replay) — signature binds the to field; recipient anti-replay made normative.`
    - `v0.5.1-Δ10 — §7.1b Ed25519 encoding — PITFALLS P4 — Raw 32/64-byte, unpadded base64url (RFC 4648 §5); verify_strict semantics normative; decoder rejection list specified.`
  </action>
  <verify>
    <automated>rg -q 'FAMP-sig-v1' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '46 41 4d 50 2d 73 69 67 2d 76 31 00' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'recipient anti-replay' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'unpadded base64url' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'verify_strict' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'RFC 8032' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'RFC 4648' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ08' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ09' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ10' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'FAMP-sig-v1' FAMP-v0.5.1-spec.md` passes (SPEC-03 anchor)
    - `rg -q '46 41 4d 50 2d 73 69 67 2d 76 31 00' FAMP-v0.5.1-spec.md` passes (hex prefix)
    - `rg -q 'recipient anti-replay' FAMP-v0.5.1-spec.md` passes (SPEC-04 anchor)
    - `rg -q 'unpadded base64url' FAMP-v0.5.1-spec.md` passes (SPEC-19 anchor)
    - `rg -q 'verify_strict' FAMP-v0.5.1-spec.md` passes (SPEC-19 strict-verify)
    - `rg -q 'RFC 8032' FAMP-v0.5.1-spec.md` AND `rg -q 'RFC 4648' FAMP-v0.5.1-spec.md` both pass
    - `rg -q 'v0\.5\.1-Δ08'`, `v0\.5\.1-Δ09`, `v0\.5\.1-Δ10` all pass
  </acceptance_criteria>
  <done>§7.1a, §7.1b, and the §7.1 recipient-binding amendment are all present with RFC citations and changelog entries.</done>
</task>

<task type="auto">
  <name>Task 3: §13.1, §13.2, §3.6a — Numeric defaults, idempotency, artifact IDs (SPEC-07, SPEC-08, SPEC-18)</name>
  <read_first>
    - FAMP-v0.5-spec.md §13.1 (freshness window), §13.2 (idempotency), §3.6/§14 (artifact IDs)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-15, D-16, D-28
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §6 Δ14/Δ15/Δ25
    - docs/PITFALLS.md (any finding on defaults — search for "±30" or "idempotency")
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Replace the `## §13.1 Freshness and clock skew`, `## §13.2 Idempotency`, and `## §3.6a Artifact identifiers` stubs:

    **§13.1 Freshness and clock skew** (per D-15, Δ14):
    - "Clock skew tolerance is RECOMMENDED to be **±60 seconds**. (v0.5 specified ±30 seconds; this is a reviewer-driven adjustment.)"
    - "Default envelope validity window is RECOMMENDED to be **300 seconds** (5 minutes)."
    - "Federations MAY tighten these defaults but MUST NOT loosen beyond the documented caps: clock skew MUST NOT exceed ±300 seconds; validity window MUST NOT exceed 1800 seconds (30 minutes)."
    - "These defaults are referenced by §12.3a (transfer-timeout tiebreak) and §9.5a (EXPIRED vs deliver) as the δ guard band."

    **§13.2 Idempotency** (per D-16, Δ15):
    - "The envelope `idempotency_key` field, when present, MUST be **128 bits of cryptographic randomness**, encoded as unpadded base64url (22 characters, per §7.1b encoding rules)."
    - "Idempotency scope is the tuple `(sender_principal, recipient_principal)`: a receiver MUST deduplicate incoming messages using the tuple `(envelope.id, envelope.idempotency_key, content_hash)` where `content_hash` is SHA-256 over the canonical JSON of the envelope body."
    - "The replay cache MUST be bounded (implementation-defined capacity). Eviction policy MUST NOT be key-dependent in a way that allows an attacker to force eviction of targeted entries."
    - "An idempotency key that does not match the 128-bit / 22-char format is rejected at ingress with `malformed`."

    **§3.6a Artifact identifiers** (per D-28, Δ25):
    - "Artifact identifiers use the scheme `sha256:<hex>` — the literal prefix `sha256:` followed by 64 lowercase hexadecimal characters."
    - "The hash is SHA-256 computed over the **canonical JSON** of the artifact body (per §4a), NOT over the raw input bytes. This ensures two implementations that canonicalize differently are detected as non-conformant rather than silently producing different artifact IDs."
    - "Alternative hash algorithms are reserved via the `sha<N>:` prefix scheme (e.g., `sha512:`) but NO alternative algorithm is defined in v0.5.1. Implementations MUST reject artifact IDs with any prefix other than `sha256:`."

    **Changelog appends:**
    - `v0.5.1-Δ14 — §13.1 — CONTEXT D-15 (PITFALLS freshness-window finding) — Clock skew tolerance bumped from ±30s to ±60s RECOMMENDED; validity window 300s RECOMMENDED; federation caps ±300s/1800s MUST NOT exceed.`
    - `v0.5.1-Δ15 — §13.2 — CONTEXT D-16 — Idempotency key locked to 128-bit random, 22-char unpadded base64url; scope (sender, recipient); replay cache tuple (id, idempotency_key, content_hash).`
    - `v0.5.1-Δ25 — §3.6a — CONTEXT D-28 — Artifact IDs locked to sha256:<hex> lowercase 64 chars over canonical JSON of artifact body; sha<N>: reserved.`
  </action>
  <verify>
    <automated>rg -q '±60' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '300 seconds' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'idempotency.{0,30}128-bit' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'sha256:&lt;hex&gt;' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ14' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ15' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ25' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '±60' FAMP-v0.5.1-spec.md` AND `rg -q '300.{0,10}seconds' FAMP-v0.5.1-spec.md` both pass (SPEC-07 anchor)
    - `rg -q 'idempotency.{0,30}128-bit' FAMP-v0.5.1-spec.md` passes (SPEC-08 anchor)
    - `rg -q 'sha256:<hex>' FAMP-v0.5.1-spec.md` passes (SPEC-18 anchor — note literal `<hex>`)
    - `rg -q 'v0\.5\.1-Δ14'`, `v0\.5\.1-Δ15`, `v0\.5\.1-Δ25` all pass
    - `just spec-lint 2>&1 | rg -q 'SPEC-07.*PASS'` and `SPEC-08.*PASS` and `SPEC-18.*PASS`
  </acceptance_criteria>
  <done>§13.1, §13.2, and §3.6a are normative; federation caps documented; artifact-ID scheme locked.</done>
</task>

</tasks>

<verification>
After this plan: anchors for SPEC-02, SPEC-03, SPEC-04, SPEC-07, SPEC-08, SPEC-18, SPEC-19 all pass `just spec-lint`. Plan 03 runs in parallel on §6.1/§6.3.
</verification>

<success_criteria>
- All 7 acceptance-criteria grep checks in this plan pass
- `just spec-lint 2>&1 | rg -c 'PASS'` increases by 7 over Plan 01's baseline
- No previously-passing anchors regress
</success_criteria>

<output>
Create `.planning/phases/01-spec-fork-v0-5-1/01-02-SUMMARY.md` listing each sub-section rewritten, the RFC citations added, and the Δ changelog entries appended.
</output>
