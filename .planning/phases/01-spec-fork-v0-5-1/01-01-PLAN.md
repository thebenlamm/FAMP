---
phase: 01-spec-fork-v0-5-1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - FAMP-v0.5.1-spec.md
  - Justfile
  - scripts/spec-lint.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "FAMP-v0.5.1-spec.md exists at repo root and is grep-targetable"
    - "just spec-lint recipe exists and is wired into just ci"
    - "FAMP_SPEC_VERSION = \"0.5.1\" constant block present in the stub"
  artifacts:
    - path: "FAMP-v0.5.1-spec.md"
      provides: "Stub spec file with version constant and changelog heading"
    - path: "Justfile"
      provides: "spec-lint recipe wired into ci"
    - path: "scripts/spec-lint.sh"
      provides: "ripgrep anchor checks for all 20 SPEC-xx anchors"
  key_links:
    - from: "Justfile"
      to: "scripts/spec-lint.sh"
      via: "spec-lint recipe invokes bash scripts/spec-lint.sh"
    - from: "Justfile ci target"
      to: "spec-lint recipe"
      via: "ci depends on spec-lint"
---

<objective>
Wave 0 scaffold: create the empty spec file, version constant block, and the grep-anchor lint tooling so every later task has a file to write into and an automated check to run against. No normative spec content beyond the version constant.

Purpose: All subsequent plans in this phase edit `FAMP-v0.5.1-spec.md` and run `just spec-lint`. Neither exists yet.
Output: Stub spec file + `spec-lint` recipe + `scripts/spec-lint.sh`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md
@.planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md
@Justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub FAMP-v0.5.1-spec.md with version constant and changelog heading</name>
  <read_first>
    - FAMP-v0.5-spec.md (source being forked — confirm title + top-of-doc shape)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md (D-01..D-04, D-29)
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §§1,6 (Δ01/Δ02/Δ28 structure)
    - .planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md (anchors to satisfy)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Create a new file at repo root `FAMP-v0.5.1-spec.md` containing:

    1. Title line: `# FAMP — Federated Agent Messaging Protocol, v0.5.1`
    2. Subtitle: `Reviewer-audited revision of v0.5` (per Δ02)
    3. A "Conventions" box citing RFC 2119 / RFC 8174 verbatim (the standard BCP 14 boilerplate — see RESEARCH §2.5).
    4. A "Spec-version constant" block (near top of doc, per D-04) containing the EXACT line:
       ```
       FAMP_SPEC_VERSION = "0.5.1"
       ```
       followed by the normative sentence (per D-29): "Implementations MUST emit this exact string, case-sensitive, in any envelope header or Agent Card field that references the spec version. A message with a mismatched version is rejected with `unsupported_version`."
    5. A placeholder `## v0.5.1 Changelog` heading at the bottom (fulfills SPEC-01 anchor `v0.5.1 Changelog`) with a short intro sentence: "Each entry below cites the reviewer finding that drove the change. Entries are stable references of the form `v0.5.1-Δnn`." Leave the body empty (Plan 06 populates Δ01..Δ28).
    6. Placeholder section stubs (one-line H2 headings only) for the major sections that later plans will populate: `## §4a Canonical JSON`, `## §7.1a Domain separation`, `## §7.1b Ed25519 encoding`, `## §7.1c Worked signature example`, `## §6.1 Agent Card (revised)`, `## §6.3 Card versioning`, `## §13.1 Freshness and clock skew`, `## §13.2 Idempotency`, `## §9.5a EXPIRED vs deliver tiebreak`, `## §9.6a Terminal precedence`, `## §9.6b Conditional-lapse precedence`, `## §10.3a Supersession round counting`, `## §11.2a Capability snapshot`, `## §11.5a Competing-instance resolution`, `## §12.3a Transfer-timeout tiebreak`, `## §7.3a FSM-observable whitelist`, `## §8a Body schemas`, `## §3.6a Artifact identifiers`. These stubs are placeholders only; subsequent plans overwrite them with normative text.

    This plan implements SPEC-20 (version constant) and creates the scaffold for SPEC-01 (changelog). The normative changelog content is finalized in Plan 06.
  </action>
  <verify>
    <automated>test -f FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'FAMP_SPEC_VERSION\s*=\s*"0\.5\.1"' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0.5.1 Changelog' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'RFC 2119' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f FAMP-v0.5.1-spec.md` passes
    - `rg -q 'FAMP_SPEC_VERSION\s*=\s*"0\.5\.1"' FAMP-v0.5.1-spec.md` passes (SPEC-20 anchor)
    - `rg -q 'v0.5.1 Changelog' FAMP-v0.5.1-spec.md` passes (SPEC-01 anchor)
    - `rg -q 'RFC 2119' FAMP-v0.5.1-spec.md` passes (Conventions box)
    - All 18 placeholder H2 stubs listed in the action are present (`rg -c '^## §' FAMP-v0.5.1-spec.md` ≥ 18)
  </acceptance_criteria>
  <done>Stub spec file exists; version constant and changelog heading grep-match; all later plans have a file to edit.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/spec-lint.sh with all 20 SPEC-xx grep anchors</name>
  <read_first>
    - .planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md (the anchor table — lines 45-66)
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §7 (validation architecture, lines 493-540)
    - FAMP-v0.5.1-spec.md (from Task 1 — the file to be linted)
  </read_first>
  <files>scripts/spec-lint.sh</files>
  <action>
    Create `scripts/spec-lint.sh` as a bash script (starts with `#!/usr/bin/env bash`, `set -euo pipefail`) that runs `rg -q '<anchor>' FAMP-v0.5.1-spec.md` for each of the following 20 anchors (one check per SPEC-xx), printing a pass/fail line for each and exiting non-zero on any failure. Use the EXACT anchor expressions from 01-VALIDATION.md:

    - SPEC-01: `rg -q 'v0.5.1 Changelog' FAMP-v0.5.1-spec.md`
    - SPEC-02: `rg -q 'RFC 8785' FAMP-v0.5.1-spec.md`
    - SPEC-03: `rg -q 'FAMP-sig-v1' FAMP-v0.5.1-spec.md`
    - SPEC-04: `rg -q 'recipient.{0,20}anti-replay|binds.{0,10}\`to\`' FAMP-v0.5.1-spec.md`
    - SPEC-05: `rg -q 'federation_credential' FAMP-v0.5.1-spec.md`
    - SPEC-06: `rg -q 'card_version' FAMP-v0.5.1-spec.md && rg -q 'min_compatible_version' FAMP-v0.5.1-spec.md`
    - SPEC-07: `rg -q '±60' FAMP-v0.5.1-spec.md && rg -q '300.{0,10}seconds' FAMP-v0.5.1-spec.md`
    - SPEC-08: `rg -q 'idempotency.{0,30}128-bit' FAMP-v0.5.1-spec.md`
    - SPEC-09: `rg -q 'ack.disposition.{0,50}terminal' FAMP-v0.5.1-spec.md`
    - SPEC-10: `rg -q 'envelope-level.{0,20}whitelist|FSM.{0,20}inspects' FAMP-v0.5.1-spec.md`
    - SPEC-11: `rg -q 'transfer.{0,10}timeout.{0,20}tiebreak' FAMP-v0.5.1-spec.md`
    - SPEC-12: `rg -q 'EXPIRED.{0,20}deliver' FAMP-v0.5.1-spec.md`
    - SPEC-13: `rg -q 'conditional.{0,10}lapse' FAMP-v0.5.1-spec.md`
    - SPEC-14: `rg -q 'COMMITTED_PENDING_RESOLUTION' FAMP-v0.5.1-spec.md`
    - SPEC-15: `rg -q 'supersession.{0,30}round' FAMP-v0.5.1-spec.md`
    - SPEC-16: `rg -q 'capability.{0,20}snapshot.{0,20}commit-time' FAMP-v0.5.1-spec.md`
    - SPEC-17: loop `for b in commit propose deliver control delegate; do rg -q "\`$b\` body" FAMP-v0.5.1-spec.md || { echo "SPEC-17: missing $b body"; exit 1; }; done`
    - SPEC-18: `rg -q 'sha256:<hex>' FAMP-v0.5.1-spec.md`
    - SPEC-19: `rg -q 'unpadded base64url' FAMP-v0.5.1-spec.md`
    - SPEC-20: `rg -q 'FAMP_SPEC_VERSION\s*=\s*"0\.5\.1"' FAMP-v0.5.1-spec.md`
    - CHG-COUNT: `[ "$(rg -c '^v0\.5\.1-Δ' FAMP-v0.5.1-spec.md || echo 0)" -ge 20 ]` — but allow 0 until Plan 06 lands; wrap in `|| true` for now with a WARN print. Actually, do NOT wrap — leave it strict; Plan 06 must make it pass. Mark it as SPEC-01-FULL to distinguish from the heading-only SPEC-01 check.

    Each check prints `[PASS] SPEC-xx` or `[FAIL] SPEC-xx: <anchor>` to stderr. Track failures in a counter; at end, print summary and exit with failure count.

    Mark the file executable (`chmod +x` — do this in the action). The Task 1 stub file will FAIL most of these; that is expected — later plan waves make them pass.
  </action>
  <verify>
    <automated>test -x scripts/spec-lint.sh &amp;&amp; bash -n scripts/spec-lint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/spec-lint.sh` passes (file exists and is executable)
    - `bash -n scripts/spec-lint.sh` passes (shell syntax valid)
    - `rg -c 'SPEC-' scripts/spec-lint.sh` returns ≥ 20 (all anchors present)
    - `rg -q 'FAMP-sig-v1' scripts/spec-lint.sh` passes (SPEC-03 anchor wired)
    - Running `./scripts/spec-lint.sh; echo $?` returns non-zero (stub fails as expected); `./scripts/spec-lint.sh 2>&1 | rg -q 'SPEC-20.*PASS'` confirms at least the SPEC-20 anchor from Task 1 already passes
  </acceptance_criteria>
  <done>Grep-anchor lint script exists and fails only on anchors not yet written; SPEC-20 already passes from Task 1's stub.</done>
</task>

<task type="auto">
  <name>Task 3: Add just spec-lint recipe and wire into just ci</name>
  <read_first>
    - Justfile (current recipes — do not regress any)
    - scripts/spec-lint.sh (from Task 2)
    - .planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md (Wave 0 requirements, lines 72-78)
  </read_first>
  <files>Justfile</files>
  <action>
    Edit the existing `Justfile` at repo root:

    1. Add a new recipe `spec-lint:` whose body is `bash scripts/spec-lint.sh`.
    2. Find the existing `ci:` recipe (per STATE.md this runs `fmt-check → lint → build → test → test-doc`). Add `spec-lint` to its dependency list: `ci: fmt-check lint build test test-doc spec-lint` (or the equivalent just dependency-list syntax for the existing file — preserve the existing order, append `spec-lint` at the end).
    3. Do NOT add `--no-verify` or any softening flag. The rule from CLAUDE.md is non-negotiable: if spec-lint fails, `just ci` must fail.

    Note: During this plan's execution, `just ci` will FAIL because the stub file doesn't satisfy 19 of the 20 anchors. That is the correct state — Wave 1/2/3 plans make it pass. This plan's acceptance does NOT require `just ci` to be green; it requires that the recipe exists and is wired.
  </action>
  <verify>
    <automated>just --list | rg -q 'spec-lint' &amp;&amp; rg -q 'spec-lint' Justfile</automated>
  </verify>
  <acceptance_criteria>
    - `just --list | rg -q 'spec-lint'` passes (recipe registered)
    - `rg -q '^spec-lint:' Justfile` passes (recipe defined)
    - `rg -q 'ci:.*spec-lint' Justfile` passes (ci depends on spec-lint)
    - `just spec-lint; echo $?` exits non-zero (expected — stub fails most anchors; this proves the recipe runs the script)
  </acceptance_criteria>
  <done>`just spec-lint` runs the anchor script; `just ci` now includes spec-lint as a gate; the recipe exits non-zero until later waves populate the spec.</done>
</task>

</tasks>

<verification>
- Stub file exists at `FAMP-v0.5.1-spec.md` with SPEC-20 anchor satisfied.
- `just spec-lint` is wired and runnable.
- Every Wave 1+ plan can now write into the stub and verify via `just spec-lint`.
</verification>

<success_criteria>
- `test -f FAMP-v0.5.1-spec.md` passes
- `test -x scripts/spec-lint.sh` passes
- `just --list | rg -q 'spec-lint'` passes
- `rg -q 'FAMP_SPEC_VERSION\s*=\s*"0\.5\.1"' FAMP-v0.5.1-spec.md` passes
</success_criteria>

<output>
Create `.planning/phases/01-spec-fork-v0-5-1/01-01-SUMMARY.md` describing the stub, the lint recipe, and which anchors currently pass (SPEC-20 + SPEC-01 heading) vs fail (everything else).
</output>
