---
phase: 01-spec-fork-v0-5-1
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - FAMP-v0.5.1-spec.md
autonomous: true
requirements: [SPEC-17]
must_haves:
  truths:
    - "Inline body schemas exist for all 5 message classes: commit, propose, deliver, control, delegate"
    - "Every body schema declares additionalProperties: false / deny_unknown_fields semantics"
    - "Every body schema uses the literal anchor phrase `<class> body` (e.g., `commit body`)"
    - "Extensions are prohibited inside body and must live under envelope-level extensions map"
  artifacts:
    - path: "FAMP-v0.5.1-spec.md"
      provides: "§8a Body schemas populated with 5 field-level tables"
      contains: "commit body, propose body, deliver body, control body, delegate body, additionalProperties: false"
  key_links:
    - from: "§8a delegate body"
      to: "§12.3a transfer-timeout tiebreak"
      via: "transfer_deadline field + transfer_commit_race target"
      pattern: "transfer_commit_race"
    - from: "§8a commit body"
      to: "§11.2a capability snapshot"
      via: "capability_snapshot field bound to card_version"
      pattern: "capability_snapshot"
---

<objective>
Wave 3 body schemas: populate §8a with inline field-level schemas for the five message classes (`commit`, `propose`, `deliver`, `control`, `delegate`) that did not have fully-specified bodies in v0.5. Every field gets a name, JSON type, required/optional status, and constraint notes. Serialized after Plan 04 so that §12.3a (`transfer_commit_race`) and §11.2a (`capability_snapshot`) are already in place.

Purpose: Phase 3 (`famp-envelope`) consumes these schemas directly — each table maps one-to-one onto a Rust `serde` struct with `deny_unknown_fields`.
Output: One large sub-section `§8a Body schemas` with five tables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md
@.planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md
@.planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md
@FAMP-v0.5-spec.md
@docs/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §8a intro + propose and commit body schemas</name>
  <read_first>
    - FAMP-v0.5-spec.md §10.2 (proposal must-contain), §11.2 (commit binds), §7.1 (envelope shape)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-25, D-26, D-27
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §4.1 (propose body table, lines 267-288) and §4.2 (commit body table, lines 289-305)
    - docs/PITFALLS.md Pitfall P2 (no u64/i64 over 2^53), Pitfall P6 (deny_unknown_fields)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Replace the `## §8a Body schemas` stub with the intro + first two schemas.

    **Intro paragraph:**
    "Body schemas are defined inline in this specification as field-per-line tables. Every schema declares `additionalProperties: false` semantics: unknown body fields MUST be rejected at decode. Extensions MUST live under the envelope-level `extensions` map (§7.1); they MUST NOT appear inside any body. Numeric fields bounded by 2^53 MUST be represented as JSON numbers; values with absolute magnitude > 2^53 MUST be represented as JSON strings per §4a (RFC 8785 §6 guidance). The `ack`, `announce`, `describe`, and `request` body definitions from v0.5 are retained unchanged (see §7.4 and §8.1-8.4). Five new normative schemas follow: `propose`, `commit`, `deliver`, `control`, `delegate`."

    **§8a.1 `propose` body**

    Paste the full table from RESEARCH §4.1 (lines 267-288). Use Markdown table format:

    | Field | JSON type | Req/Opt | Constraint notes |
    |---|---|---|---|
    | `scope` | object | REQUIRED | Opaque to FSM; domain-specific work description. MUST be present (INV-4 precursor). |
    | `bounds` | object | REQUIRED | MUST include ≥2 keys from {deadline, budget, hop_limit, policy_domain, authority_scope, max_artifact_size, confidence_floor, recursion_depth} per §9.3 / INV-4. |
    | `bounds.deadline` | string (RFC 3339) | OPTIONAL | Absolute time. |
    | `bounds.budget` | object | OPTIONAL | `{amount: string, unit: string}` — amount is a string to avoid 2^53 precision loss (PITFALLS P2). |
    | `bounds.hop_limit` | integer | OPTIONAL | ≥ 0 ≤ 2^53. |
    | `bounds.policy_domain` | string | OPTIONAL | Opaque identifier. |
    | `bounds.authority_scope` | string (enum) | OPTIONAL | One of §5.3 levels. |
    | `bounds.max_artifact_size` | integer | OPTIONAL | Bytes, ≤ 2^53. |
    | `bounds.confidence_floor` | number | OPTIONAL | 0.0–1.0. |
    | `bounds.recursion_depth` | integer | OPTIONAL | ≥ 0 ≤ 255. |
    | `terms` | object | OPTIONAL | §10.2 SHOULD-contain; opaque. |
    | `delegation_permissions` | object | OPTIONAL | `{form_allowed: [assist\|subtask\|transfer], ceiling: {...}}`; if absent, delegation forbidden per INV-3. |
    | `artifact_expectations` | object | OPTIONAL | Opaque; input/output format hints. |
    | `policy_references` | array of string | OPTIONAL | Policy IDs. |
    | `natural_language_summary` | string | SHOULD | Human/LLM-readable, no length cap; canonicalized per §4a (no Unicode normalization per PITFALLS P3). |
    | `modifications` | array of string | OPTIONAL | Courtesy field per §10.3 Rule 3 — MUST NOT be used as normative source of truth. |
    | `conditions` | array of object | OPTIONAL | For conditional proposals; §11.4. Each: `{expression: string, evaluator: string, deadline: string}`. |

    Add before the table the exact header phrase: "**`propose` body** (additionalProperties: false)" — this satisfies SPEC-17's per-class anchor check.

    **§8a.2 `commit` body**

    Paste the full table from RESEARCH §4.2. Use the same Markdown format. Include ALL fields:
    - `scope` (REQUIRED object)
    - `scope_subset` (OPTIONAL boolean, default false) — FSM-inspected per §7.3a
    - `bounds` (REQUIRED object, same shape as propose.bounds)
    - `accepted_policies` (REQUIRED array of string)
    - `delegation_permissions` (OPTIONAL object, frozen at commit per §11.2a)
    - `reporting_obligations` (OPTIONAL object)
    - `terminal_condition` (REQUIRED object, machine-evaluable completion)
    - `capability_snapshot` (REQUIRED object `{card_version: integer, capabilities: [...]}`) — cross-reference §11.2a
    - `conditions` (OPTIONAL array of conditional-commitment objects)
    - `natural_language_summary` (SHOULD string)

    Add before the table the exact header phrase: "**`commit` body** (additionalProperties: false)".

    Cross-reference after the commit body table: "The `capability_snapshot` field is bound to the committing card's `card_version` per §11.2a and §6.3. The `scope_subset` field is FSM-inspected per §7.3a."

    **Changelog append:** (reserving Δ24 for the full body schemas block — but since we're splitting across two tasks, break Δ24 into Δ24a/Δ24b. Actually, use a single Δ24 and append after Task 3 below. In THIS task, append a partial-progress entry.)
    Actually, simpler: use a single consolidated entry at the end of Task 3. Do NOT append to changelog in this task; note in the task summary that Δ24 is deferred to Task 3.
  </action>
  <verify>
    <automated>rg -q '`propose` body' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '`commit` body' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'additionalProperties: false' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'capability_snapshot' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'scope_subset' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `` rg -q '`propose` body' FAMP-v0.5.1-spec.md `` passes
    - `` rg -q '`commit` body' FAMP-v0.5.1-spec.md `` passes
    - `rg -q 'additionalProperties: false' FAMP-v0.5.1-spec.md` passes
    - `rg -q 'capability_snapshot' FAMP-v0.5.1-spec.md` passes (field name)
    - `rg -q 'scope_subset' FAMP-v0.5.1-spec.md` passes (FSM-inspected field)
    - `rg -q 'terminal_condition' FAMP-v0.5.1-spec.md` passes
  </acceptance_criteria>
  <done>§8a intro + propose + commit body tables present; cross-links to §11.2a/§6.3/§7.3a in place.</done>
</task>

<task type="auto">
  <name>Task 2: §8a.3 deliver body + §8a.4 control body schemas</name>
  <read_first>
    - FAMP-v0.5-spec.md §12.4 (deliver), §15.1 (error categories for control disposition)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-27
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §4.3 (deliver body, lines 306-316) and §4.4 (control body, lines 318-326)
    - FAMP-v0.5.1-spec.md §12.3a (Plan 04 output — provides `transfer_commit_race` target value to wire)
    - FAMP-v0.5.1-spec.md §3.6a (Plan 02 output — provides `sha256:<hex>` artifact ID scheme)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Append the deliver and control body tables to §8a.

    **§8a.3 `deliver` body**

    Paste from RESEARCH §4.3 (lines 306-316):

    | Field | JSON type | Req/Opt | Constraint notes |
    |---|---|---|---|
    | `interim` | boolean | REQUIRED | FSM-inspected (§7.3a). `false` = terminal delivery (requires envelope-level `terminal_status`); `true` = progress update (MUST NOT carry `terminal_status`). |
    | `artifacts` | array of object | OPTIONAL | Each: `{id: "sha256:<hex>", media_type: string, size: integer}`. Per §3.6a. |
    | `result` | object | OPTIONAL | Domain payload; opaque to FSM. |
    | `usage_metrics` | object | OPTIONAL | `{tokens_used: integer, compute_ms: integer, cost: {amount: string, unit: string}}`. Numbers bounded by 2^53; larger values as strings. |
    | `error_detail` | object | OPTIONAL | REQUIRED iff envelope `terminal_status = failed`. `{category: ErrorCategory, message: string, diagnostic: object?}`. |
    | `provenance` | object | REQUIRED on terminal deliveries (optional on interim) | §14.2: `{originating_task, commitment_lineage, delegation_lineage?, artifact_lineage?, policy_context}`. Canonicalized per §4a. |
    | `natural_language_summary` | string | SHOULD | Human/LLM readable. |

    Header phrase: "**`deliver` body** (additionalProperties: false)".

    **§8a.4 `control` body**

    Paste from RESEARCH §4.4 (lines 318-326), AND include `transfer_commit_race` in the `target` enum values (this is the new value introduced by §12.3a per Plan 04):

    | Field | JSON type | Req/Opt | Constraint notes |
    |---|---|---|---|
    | `target` | string (enum) | REQUIRED | FSM-inspected (§7.3a). One of: `task`, `conversation`, `commitment`, `delegation`, `proposal`, `transfer_commit_race`. Defines what the control action operates on. `transfer_commit_race` is introduced by §12.3a for delegates that self-check inside the transfer-timeout δ guard band. |
    | `action` | string (enum) | REQUIRED | One of: `cancel`, `supersede`, `close`, `cancel_if_not_started`, `revert_transfer`. Combined with envelope `relation` per §7.3a whitelist. |
    | `disposition` | string (enum) | OPTIONAL | One of §15.1 error categories where relevant: `condition_failed`, `capacity_exceeded`, `policy_blocked`, `unauthorized`, `conflict`. For conditional lapse (§11.4, §9.6b), MUST be `condition_failed`. |
    | `reason` | string | SHOULD | Human-readable justification. |
    | `affected_ids` | array of string | OPTIONAL | Extra targets (e.g., list of commitment IDs closed by a conversation close). |

    Header phrase: "**`control` body** (additionalProperties: false)".

    Cross-reference: "The `transfer_commit_race` target value is introduced by §12.3a (transfer-timeout tiebreak). The `condition_failed` disposition is used by §9.6b (conditional-lapse precedence)."

    No changelog entry in this task (deferred to Task 3 for consolidated Δ24).
  </action>
  <verify>
    <automated>rg -q '`deliver` body' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '`control` body' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'transfer_commit_race' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'interim' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'error_detail' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `` rg -q '`deliver` body' FAMP-v0.5.1-spec.md `` passes
    - `` rg -q '`control` body' FAMP-v0.5.1-spec.md `` passes
    - `rg -q 'transfer_commit_race' FAMP-v0.5.1-spec.md` passes (cross-task link to §12.3a)
    - `rg -q 'error_detail' FAMP-v0.5.1-spec.md` passes (deliver field)
    - `rg -q 'cancel_if_not_started' FAMP-v0.5.1-spec.md` passes (control action enum)
    - `rg -q 'condition_failed' FAMP-v0.5.1-spec.md` passes (control disposition; wired to §9.6b)
  </acceptance_criteria>
  <done>deliver and control body tables present; transfer_commit_race enum wired; disposition values cross-linked.</done>
</task>

<task type="auto">
  <name>Task 3: §8a.5 delegate body schema + consolidated Δ24 changelog entry</name>
  <read_first>
    - FAMP-v0.5-spec.md §12.3 (delegation forms), §12.5 (delegation ceiling)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-27
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §4.5 (delegate body, lines 327-339)
    - FAMP-v0.5.1-spec.md §12.3a (Plan 04 — references `transfer_deadline` default)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Append the final body schema table and the consolidated Δ24 changelog entry.

    **§8a.5 `delegate` body**

    Paste from RESEARCH §4.5 (lines 327-339):

    | Field | JSON type | Req/Opt | Constraint notes |
    |---|---|---|---|
    | `form` | string (enum) | REQUIRED | One of `assist`, `subtask`, `transfer`. Per §12.3. Not FSM-inspected directly (form governs downstream obligations, not state transitions of the parent task — except `transfer` which triggers §12.3a tiebreak evaluation). |
    | `commitment_ref` | string | REQUIRED | Commitment ID (not message ID) under which delegation is authorized. |
    | `downstream` | string | REQUIRED | Principal identity of delegate (format `agent:<authority>/<name>`, per §5.1). |
    | `scope` | object | REQUIRED | What portion is delegated. |
    | `bounds` | object | REQUIRED | Same shape as `commit.bounds`. MUST be within parent commitment bounds. |
    | `delegation_ceiling` | object | OPTIONAL | `{max_hops: integer, max_fanout: integer?, allowed_delegates: [string]?, forbidden_delegates: [string]?, policy_inheritance: boolean}`. Per §12.5. |
    | `transfer_deadline` | string (RFC 3339) | REQUIRED iff `form=transfer` | Default 5 minutes from envelope `ts`. Per §12.3 item 5 and §12.3a tiebreak. |
    | `natural_language_summary` | string | SHOULD | Human-readable. |

    Header phrase: "**`delegate` body** (additionalProperties: false)".

    Append to §8a a "Closing note" paragraph: "Validation note: implementations MUST use `deny_unknown_fields` (or language-equivalent strict decoding) for every body schema. A body field not listed in the schema above is rejected at decode with error `malformed`. Extensions that need additional data MUST use the envelope-level `extensions` map per §7.1 and MUST NOT reuse any field name appearing in these schemas or in the §7.3a FSM whitelist."

    **Changelog append (consolidated Δ24):**
    `v0.5.1-Δ24 — §8a Body schemas — PITFALLS P6 (deny_unknown_fields discipline) / CONTEXT D-25/D-26/D-27 — Add inline body schemas for propose, commit, deliver, control, delegate. Every schema declares additionalProperties: false. New control target transfer_commit_race introduced by §12.3a. Numeric values > 2^53 represented as strings per P2. Extensions MUST NOT appear inside body — envelope-level extensions map only.`
  </action>
  <verify>
    <automated>rg -q '`delegate` body' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'transfer_deadline' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'delegation_ceiling' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ24' FAMP-v0.5.1-spec.md &amp;&amp; for b in commit propose deliver control delegate; do rg -q "\`$b\` body" FAMP-v0.5.1-spec.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `` rg -q '`delegate` body' FAMP-v0.5.1-spec.md `` passes
    - `rg -q 'transfer_deadline' FAMP-v0.5.1-spec.md` passes (cross-task link to §12.3a)
    - `rg -q 'delegation_ceiling' FAMP-v0.5.1-spec.md` passes
    - `rg -q 'v0\.5\.1-Δ24' FAMP-v0.5.1-spec.md` passes (changelog entry)
    - The SPEC-17 loop check passes: `for b in commit propose deliver control delegate; do rg -q "\`$b\` body" FAMP-v0.5.1-spec.md || exit 1; done; echo $?` returns 0
    - After this task, `just spec-lint 2>&1 | rg -q 'SPEC-17.*PASS'`
  </acceptance_criteria>
  <done>All 5 body schemas present; Δ24 changelog entry present; SPEC-17 grep anchor passes.</done>
</task>

</tasks>

<verification>
After this plan: SPEC-17 anchor passes `just spec-lint` (all 5 body-class headers present). Combined with Plan 04 (Wave 2), all 9 FSM + body-schema SPEC requirements are satisfied.
</verification>

<success_criteria>
- All 5 `<class> body` anchors present
- `rg -q 'additionalProperties: false'` passes
- `transfer_commit_race` wired from §12.3a to §8a.4 control body
- `capability_snapshot` wired from §11.2a to §8a.2 commit body
- Changelog Δ24 present
</success_criteria>

<output>
Create `.planning/phases/01-spec-fork-v0-5-1/01-05-SUMMARY.md` listing all 5 schemas and confirming cross-task wiring to §12.3a and §11.2a.
</output>
