---
phase: 01-spec-fork-v0-5-1
plan: 06
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04, 01-05]
files_modified:
  - FAMP-v0.5.1-spec.md
autonomous: true
requirements: [SPEC-01]
user_setup: []
must_haves:
  truths:
    - "§7.1c worked Ed25519 signature example is present with byte-exact hex output"
    - "The worked example bytes were computed from an EXTERNAL JCS reference implementation, not self-generated (PITFALLS P10)"
    - "The v0.5.1 Changelog contains ≥25 entries of shape `v0.5.1-Δnn` covering Δ01..Δ28"
    - "Every Δnn entry cites the reviewer/PITFALLS finding and the SPEC-xx requirement it addresses"
    - "Editorial Δ27 marks §23 open questions Q1/Q2/Q3 as deferred; Q6 as RESOLVED by Δ24"
  artifacts:
    - path: "FAMP-v0.5.1-spec.md"
      provides: "§7.1c worked example + full v0.5.1 Changelog populated"
      contains: "signing_input_bytes, [0-9a-f]{128}, v0.5.1-Δ01 through v0.5.1-Δ28"
  key_links:
    - from: "§7.1c worked example"
      to: "§7.1a domain separation + §4a canonical JSON"
      via: "example concretely instantiates the prefix + JCS rules"
      pattern: "FAMP-sig-v1"
    - from: "v0.5.1 Changelog"
      to: "every preceding sub-section"
      via: "each Δ entry cites the section it amends"
      pattern: "v0.5.1-Δ"
---

<objective>
Wave 3 finalization: compute and commit the concrete bytes for the §7.1c Ed25519 worked example (conformance vector #1 for Phase 8), then populate the complete v0.5.1 Changelog section with all 28 Δ entries.

**CRITICAL — PITFALLS P10 RULE:** The worked-example bytes MUST be generated by an **EXTERNAL** JCS reference implementation (cyberphone's JavaScript reference at https://github.com/cyberphone/json-canonicalization, or Python `jcs` library at https://pypi.org/project/jcs/, or any other independent implementation). The bytes MUST NOT be computed by our own Rust code — our `famp-canonical` does not exist yet, and if it did, it would be the implementation under test, not the source of truth. Task 1 below documents exactly which external tool was used and the exact command invoked, so the provenance of the bytes is auditable.

Purpose: SPEC-01 (changelog + worked example) is the capstone of Phase 1. Phase 8 conformance vectors reuse the §7.1c bytes byte-for-byte.
Output: §7.1c populated with concrete hex; v0.5.1 Changelog populated with Δ01..Δ28; optionally `famp-conformance/fixtures/vector-01-signature.json` seed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md
@.planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md
@.planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md
@FAMP-v0.5-spec.md
@docs/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §7.1c worked Ed25519 signature example with externally-generated bytes (PITFALLS P10)</name>
  <read_first>
    - FAMP-v0.5.1-spec.md §7.1a (domain separation prefix from Plan 02)
    - FAMP-v0.5.1-spec.md §7.1b (Ed25519 encoding from Plan 02)
    - FAMP-v0.5.1-spec.md §4a (canonical JSON rules from Plan 02)
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §5 (Worked Ed25519 Example Skeleton, lines 341-449) — THIS IS THE STRUCTURE TO FILL IN
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-09, D-10, D-12
    - docs/PITFALLS.md Pitfall P10 (vectors cannot be self-generated)
    - RFC 8032 §7.1 Test 1 (the secret key source — the keypair is published in the RFC itself)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Populate `## §7.1c Worked signature example`. Follow the structure in RESEARCH §5 verbatim; the task is to fill in the `<HEX_A>`, `<LEN_A>`, `<HEX_B>`, `<B64_B>` placeholders with **externally-computed** values.

    **Step 1: Document the external reference tool used.**
    At the top of §7.1c, include a "Provenance of bytes" block:
    "The bytes in this worked example were computed using [TOOL], not by any FAMP implementation. Per the project's PITFALLS P10 rule, conformance vectors MUST NOT be self-generated. The exact invocation is recorded in this spec so that any reviewer can reproduce the bytes independently."

    Choose ONE of the following external tools and use it (in priority order):
    1. **Python `jcs` package** (simplest): `pip install jcs && python3 -c 'import jcs, json; print(jcs.canonicalize(json.loads(open("envelope.json").read())).hex())'`
    2. **cyberphone JavaScript reference**: https://github.com/cyberphone/json-canonicalization/tree/master/javascript
    3. **Any other published JCS implementation** (document which and why).

    For Ed25519 signing, use:
    1. **Python `cryptography` package** with RFC 8032 §7.1 Test 1 key: `pip install cryptography && python3 signer.py`
    2. Or OpenSSL `openssl pkeyutl -sign` with a PEM-encoded Test 1 key.

    The executor MAY create a throwaway script `scripts/compute-vector-01.sh` that installs the tools, runs them, and emits the hex bytes to stdout. The script is not a project artifact (do not commit it under `scripts/` alongside `spec-lint.sh`); it is scratch work. The committed artifact is the hex bytes pasted into §7.1c.

    **Step 2: Fill in §5.1 Test keypair block** — paste verbatim from RESEARCH §5.1 (the RFC 8032 §7.1 Test 1 keys, which are already published worldwide). Include the boxed warning: "This key pair is from RFC 8032 §7.1 Test 1 and is published worldwide. It MUST NOT be used for any production signing."

    **Step 3: Fill in §5.2 Minimal envelope (pre-canonical)** — paste verbatim the JSON block from RESEARCH §5.2. Verify it has no JSON `number` literals (only strings, booleans, objects) — this sidesteps RFC 8785 §3.2.2.3 float edge cases (RESEARCH §9 risk 2).

    **Step 4: Fill in §5.3 Canonical JSON (JCS output)** —
    - Run the external JCS tool on the §5.2 envelope.
    - Paste the resulting bytes as hex: `canonical_json_bytes (hex) = <the actual hex>`
    - Paste the length: `canonical_json_bytes (length) = <N> bytes`
    - Confirm the UTF-16 sort order: `authority < body < causality < class < famp < from < id < scope < to < ts` (ASCII subset — simple alphabetical order applies).

    **Step 5: Fill in §5.4 Domain-separation prefix** — paste verbatim from RESEARCH §5.4. This is a fixed 12-byte string, no computation needed. Include the byte-by-byte breakdown table from RESEARCH line 400.

    **Step 6: Fill in §5.5 Signing input** — `signing_input_bytes = prefix_bytes || canonical_json_bytes`. Paste the concatenated hex (12 prefix bytes + N canonical bytes). Length = 12 + N.

    **Step 7: Fill in §5.6 Signature** —
    - Using the RFC 8032 §7.1 Test 1 secret key and the §5.5 signing_input_bytes, compute Ed25519 signature via the external tool.
    - Paste the resulting 64 bytes as hex: `signature (64 bytes, hex) = <R_hex><S_hex>` (128 hex chars total, split into R and S if desired for clarity).
    - Paste the unpadded base64url encoding (86 chars): `signature (unpadded base64url, 86 chars) = <b64>`.

    **Step 8: Fill in §5.7 Re-embedded envelope** — paste verbatim the JSON block from RESEARCH §5.7 with the `<B64_B>` placeholder replaced by the actual base64url signature from Step 7.

    **Step 9: Verification procedure** — paste verbatim the 8-step procedure from RESEARCH §5.7 (lines 440-449).

    **Step 10: Anchor sentence** — include the exact phrase "**ed25519 vector #1**" somewhere in §7.1c so the VALIDATION anchor `ed25519.{0,30}vector.{0,30}#1` matches. Recommended: add a heading "**Conformance vector #1 — ed25519 signature**" at the top of §7.1c.

    **Changelog append:**
    `v0.5.1-Δ13 — §7.1c Worked signature example — PITFALLS P5 + P10 — Concrete bytes for RFC 8032 §7.1 Test 1 key; canonical JSON + signature generated by [TOOL] (external reference, not self-generated); designated as Phase 8 conformance vector #1.`

    The executor MUST record the actual tool name in the Δ13 changelog entry (e.g., "generated by Python jcs 0.2.1 + cryptography 42.0.5").
  </action>
  <verify>
    <automated>rg -q 'FAMP-sig-v1' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'ed25519.{0,30}vector.{0,30}#1' FAMP-v0.5.1-spec.md &amp;&amp; rg -q '[0-9a-f]{128}' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'Provenance of bytes' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ13' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'RFC 8032.*Test 1' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'ed25519.{0,30}vector.{0,30}#1' FAMP-v0.5.1-spec.md` passes (anchor sentence)
    - `rg -q '[0-9a-f]{128}' FAMP-v0.5.1-spec.md` passes (≥1 128-char lowercase-hex run — the signature)
    - `rg -q 'canonical_json_bytes' FAMP-v0.5.1-spec.md` passes (hex block present)
    - `rg -q 'signing_input_bytes' FAMP-v0.5.1-spec.md` passes (concatenation block present)
    - `rg -q 'Provenance of bytes' FAMP-v0.5.1-spec.md` passes (P10 audit block present)
    - `rg -q 'Python jcs|cyberphone|openssl|cryptography' FAMP-v0.5.1-spec.md` passes (external tool named)
    - `rg -q 'v0\.5\.1-Δ13' FAMP-v0.5.1-spec.md` passes
    - The hex signature section has a `46414d502d7369672d7631 00` prefix substring (signing input starts with the domain prefix hex)
  </acceptance_criteria>
  <done>§7.1c is a complete, byte-exact, externally-sourced Ed25519 worked example; P10 provenance documented in spec text; Δ13 changelog entry cites the external tool.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the complete v0.5.1 Changelog (Δ01..Δ28)</name>
  <read_first>
    - FAMP-v0.5.1-spec.md (the current state — all prior plans have each appended their own Δ entries to the changelog, so this task's job is to ADD the remaining Δ entries that no other plan owned: Δ01, Δ02, Δ03, Δ26, Δ27, Δ28, plus any missing editorial entries)
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §6 (the full Δ01..Δ28 change inventory, lines 451-490)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-03 (changelog entry shape)
    - docs/PITFALLS.md (cross-verify every Δ entry cites a real finding)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    The `## v0.5.1 Changelog` section currently contains a subset of entries appended by Plans 02-05. This task ensures it contains the full ordered set Δ01..Δ28 as cataloged in RESEARCH §6 (lines 455-484 approximately).

    **Step 1: Audit existing entries.** Run `rg '^v0\.5\.1-Δ' FAMP-v0.5.1-spec.md | sort -u` to list which Δ numbers are already present. Expected from prior plans: Δ04, Δ05, Δ08, Δ09, Δ10, Δ11, Δ12, Δ13, Δ14, Δ15, Δ16, Δ17, Δ18, Δ19, Δ20, Δ21, Δ22, Δ23, Δ24, Δ25 (20 entries).

    **Step 2: Add the missing entries.** The following Δ entries have NOT been appended by any prior plan and MUST be added in this task. Use the exact entry shape `v0.5.1-Δnn — <section touched> — <finding id/reviewer> — <resolution summary>` from CONTEXT.md D-03:

    - `v0.5.1-Δ01 — top-of-doc — PITFALLS P14 (spec drift) — Add Spec-version constant block FAMP_SPEC_VERSION = "0.5.1"; mandate envelope famp field = "0.5.1" exact; mismatches rejected as unsupported_version.` (Satisfies SPEC-20 at the top-of-doc level.)
    - `v0.5.1-Δ02 — front matter — editorial — Bump title to "FAMP v0.5.1" with subtitle "Reviewer-audited revision of v0.5"; add Conventions box citing RFC 2119 / RFC 8174.`
    - `v0.5.1-Δ03 — global — editorial — Normalize normative keywords (MUST/SHOULD/MAY) to RFC-capital form in new and amended sections only (scope-limited per RESEARCH §9 risk 5 to avoid a mass-edit diff).`
    - `v0.5.1-Δ26 — §19 — CONTEXT D-29 — Amend §19 Conformance Levels to require implementations to emit FAMP_SPEC_VERSION = "0.5.1" exactly; reject mismatches as unsupported_version. Ties §19 levels to the version constant.`
    - `v0.5.1-Δ27 — §23 — editorial / CONTEXT deferred items — Mark §23 Open Questions Q1 (multi-party commitment) Q2 (streaming deliver) Q3 (cross-federation delegation) as "deferred to post-v0.5.1 per CONTEXT.md"; mark Q6 (deliver body structure) as RESOLVED by Δ24.`
    - `v0.5.1-Δ28 — bottom of doc — editorial — This section. Catalog of all 28 structural deltas from v0.5 to v0.5.1 with reviewer-finding citations; normative record of every change.`

    Plus any of the following if missing (they should already be present but verify): Δ04, Δ05, Δ08, Δ09, Δ10, Δ11, Δ12, Δ13, Δ14, Δ15, Δ16, Δ17, Δ18, Δ19, Δ20, Δ21, Δ22, Δ23, Δ24, Δ25.

    **Step 3: Apply the Δ01, Δ02, Δ26, Δ27 content edits to their target sections.** Adding a changelog entry is only half the work — the entry documents a change that must also be present in the section body:
    - For Δ01: verify §top-of-doc already has the FAMP_SPEC_VERSION block (Plan 01 Task 1 added this). If not, add it.
    - For Δ02: verify the title and subtitle are already present (Plan 01 Task 1 added these). If not, add them.
    - For Δ26: find the §19 Conformance Levels section (inherit from v0.5 if not present yet in v0.5.1). Add a normative sentence: "All conformance levels (L1, L2, L3) REQUIRE implementations to emit `FAMP_SPEC_VERSION = \"0.5.1\"` exactly; messages with a different version string MUST be rejected with `unsupported_version`." Create a `## §19 Conformance Levels` stub if absent and include this single sentence.
    - For Δ27: find the §23 Open Questions section (inherit from v0.5 if needed). Add inline markers: "Q1 — *deferred to post-v0.5.1 per CONTEXT.md.*", "Q2 — *deferred.*", "Q3 — *deferred.*", "Q6 — *RESOLVED by Δ24 (§8a Body schemas).*". Create a `## §23 Open Questions` stub if absent.

    **Step 4: Final audit.** After Step 2 and Step 3, run `rg -c '^v0\.5\.1-Δ[0-9]{2}' FAMP-v0.5.1-spec.md`. The count MUST be ≥ 26 (all of Δ01..Δ28 present — note Δ06, Δ07 are optional worked-example sub-entries captured inside Δ04 and Δ13 in this plan's ordering; if you prefer explicit Δ06/Δ07 for the two canonical-JSON worked examples, add them. RESEARCH §6 lists them as separate rows, so add them explicitly):
    - `v0.5.1-Δ06 — §4a — PITFALLS P1 + CONTEXT D-07 — Add worked example A (ASCII mixed-case key sort) with exact hex byte output. [DEFERRED: bytes to be computed during a future pass or inherited from §7.1c's external JCS tool.]`
    - `v0.5.1-Δ07 — §4a — PITFALLS P1 + CONTEXT D-07 — Add worked example B (supplementary-plane U+1F600 emoji key) with exact hex byte output proving UTF-16 surrogate-pair sort. [DEFERRED bytes as above.]`

    Note that Δ06 and Δ07 may be marked as DEFERRED if the executor decides not to compute the canonical-JSON worked examples in this phase — they are a nice-to-have on top of SPEC-02. The §4a section already has the normative RFC 8785 content; the worked examples are illustrative. Document the decision explicitly in the Δ entry.

    **Step 5: Verify changelog count ≥ 26.** The final count should be 26-28 entries, all of shape `v0.5.1-Δnn`. The validation anchor from 01-VALIDATION.md (`rg -c 'v0\.5\.1-Δ[0-9]{2}' ... >= 20`) will pass.
  </action>
  <verify>
    <automated>[ "$(rg -c '^v0\.5\.1-Δ[0-9]{2}' FAMP-v0.5.1-spec.md)" -ge 26 ] &amp;&amp; rg -q 'v0\.5\.1-Δ01' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ26' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ27' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ28' FAMP-v0.5.1-spec.md &amp;&amp; just spec-lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '^v0\.5\.1-Δ[0-9]{2}' FAMP-v0.5.1-spec.md` returns ≥ 26
    - Δ01, Δ02, Δ03, Δ26, Δ27, Δ28 all present (the entries added specifically by this task)
    - `rg -q 'v0.5.1 Changelog' FAMP-v0.5.1-spec.md` passes (SPEC-01 anchor — already satisfied by Plan 01 stub, but verify it is still present)
    - §19 Conformance Levels has the `FAMP_SPEC_VERSION` emission requirement (for Δ26)
    - §23 Open Questions has Q1/Q2/Q3 marked deferred and Q6 marked RESOLVED (for Δ27)
    - **`just spec-lint` exits 0** — all 20 SPEC anchors pass + the changelog count check passes
    - `just ci` exits 0 (full pre-push gate green)
  </acceptance_criteria>
  <done>Changelog is complete with ≥26 Δ entries; §19 and §23 amendments in place; `just spec-lint` is green; Phase 1 deliverable is complete.</done>
</task>

</tasks>

<verification>
After this plan:
- `just spec-lint` exits 0 (all 20 SPEC-xx anchors pass)
- `just ci` exits 0 (Phase 0 gate + spec-lint all green)
- §7.1c worked example bytes are externally sourced per PITFALLS P10
- Changelog has ≥26 Δ entries covering every material change from v0.5 to v0.5.1
</verification>

<success_criteria>
- `just spec-lint && just ci` exits 0
- `rg -c '^v0\.5\.1-Δ[0-9]{2}' FAMP-v0.5.1-spec.md` ≥ 26
- `rg -q '[0-9a-f]{128}' FAMP-v0.5.1-spec.md` passes (signature hex)
- `rg -q 'Provenance of bytes' FAMP-v0.5.1-spec.md` passes (P10 compliance documented)
</success_criteria>

<output>
Create `.planning/phases/01-spec-fork-v0-5-1/01-06-SUMMARY.md` documenting:
1. Which external JCS tool was used for the §7.1c bytes (exact tool + version)
2. The exact command/script invoked to compute the bytes
3. The final Δ count in the changelog
4. Confirmation that `just spec-lint && just ci` is green
5. Flag for Phase 8: the §7.1c bytes should be extracted into `famp-conformance/fixtures/vector-01-signature.json` when Phase 8 begins.
</output>
