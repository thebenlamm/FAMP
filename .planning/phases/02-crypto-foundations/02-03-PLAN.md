---
phase: 02-crypto-foundations
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json
  - crates/famp-crypto/tests/vectors/famp-sig-v1/PROVENANCE.md
  - crates/famp-crypto/tests/worked_example.rs
  - crates/famp-crypto/README.md
  - crates/famp-crypto/src/lib.rs
  - justfile
  - .github/workflows/ci.yml
autonomous: true
requirements: [CRYPTO-04, CRYPTO-08, SPEC-03]
must_haves:
  truths:
    - "The §7.1c worked-example bytes are committed verbatim from FAMP-v0.5.1-spec.md (externally sourced, NOT self-generated)"
    - "canonicalize_for_signature(unsigned_envelope) produces bytes byte-identical to fixture.signing_input_hex"
    - "TrustedVerifyingKey::from_bytes(fixture.public_key_hex) + verify_canonical_bytes(fixture.canonical, fixture.signature) returns Ok(())"
    - "`just ci` (or equivalent) runs the rfc8032_vectors + worked_example + weak_key_rejection test files as blocking gates"
    - "famp-crypto README documents the DOMAIN_PREFIX, the §7.1c worked example, and the constant-time verification rationale (D-22/D-23)"
    - "Wrapper audit is documented inline in lib.rs or README, enumerating every sign/verify error path and confirming no secret-dependent short-circuit"
  artifacts:
    - path: "crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json"
      provides: "§7.1c cross-language interop fixture — the single most important artifact in this phase"
      contains: "domain_prefix_hex"
    - path: "crates/famp-crypto/tests/vectors/famp-sig-v1/PROVENANCE.md"
      provides: "Documents Python `jcs 0.2.1` + `cryptography 46.0.7` provenance of bytes"
    - path: "crates/famp-crypto/tests/worked_example.rs"
      provides: "Byte-exact gate against §7.1c fixture"
    - path: "crates/famp-crypto/README.md"
      provides: "Worked-example walk-through, DOMAIN_PREFIX documentation, CT rationale"
      contains: "FAMP-sig-v1"
  key_links:
    - from: "crates/famp-crypto/tests/worked_example.rs"
      to: "crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json"
      via: "include_str!"
      pattern: "worked-example.json"
    - from: "justfile"
      to: "cargo nextest run -p famp-crypto"
      via: "`just test-crypto` recipe"
      pattern: "famp-crypto"
---

<objective>
Commit the §7.1c worked-example fixture, wire the byte-exact interop gate, document the wrapper audit for CRYPTO-08, write the famp-crypto README, and promote the crate's test suite to a blocking CI job alongside Phase 1's canonical-JSON gate.

Purpose: This plan delivers the Phase 2 success criterion — "the worked Ed25519 example from PITFALLS P10 verifies byte-exact in Rust" — and turns the whole phase into a CI-enforced interop contract.
Output: Committed §7.1c fixture (bytes from spec, not Rust-generated); green worked_example test; README with worked example + CT rationale; CI wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-crypto-foundations/02-CONTEXT.md
@.planning/phases/02-crypto-foundations/02-RESEARCH.md
@crates/famp-crypto/src/lib.rs
@crates/famp-crypto/src/prefix.rs
@crates/famp-crypto/src/verify.rs
@crates/famp-crypto/src/sign.rs
@crates/famp-crypto/src/keys.rs
@justfile
@.github/workflows/ci.yml

<interfaces>
From Plan 02, available for the test:
```rust
famp_crypto::canonicalize_for_signature(&serde_json::Value) -> Result<Vec<u8>, CryptoError>
famp_crypto::verify_canonical_bytes(&TrustedVerifyingKey, &[u8], &FampSignature) -> Result<(), CryptoError>
famp_crypto::TrustedVerifyingKey::from_bytes(&[u8; 32]) -> Result<Self, CryptoError>
famp_crypto::FampSignature::from_bytes([u8; 64]) -> Self
```

Spec bytes (from 02-RESEARCH.md §"Exact Spec Bytes"):
- DOMAIN_PREFIX hex: `46414d502d7369672d763100`
- §7.1c.1 test keypair (RFC 8032 Test 1):
  - secret: `9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60`
  - public: `d75a980182b10ab7d54bfed3c964073a0ee172f3daa62325af021a68f707511a`
- §7.1c.3 canonical JSON (324 bytes, exact string in research)
- §7.1c.6 signature hex: `9366aaced854c7898735908d2e2d973208905fd80e2f93fe505710f58f0ed1fc92e3b9d7a19b30b2cf184f703552dafcf91ca81321f57fa689d1a96865d0b608`
- §7.1c.6 signature b64url: `k2aqzthUx4mHNZCNLi2XMgiQX9gOL5P-UFcQ9Y8O0fyS47nXoZswss8YT3A1Utr8-RyoEyH1f6aJ0aloZdC2CA`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit §7.1c worked-example.json fixture + PROVENANCE.md</name>
  <files>crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json, crates/famp-crypto/tests/vectors/famp-sig-v1/PROVENANCE.md</files>
  <read_first>
    - FAMP-v0.5.1-spec.md §7.1a, §7.1c.1, §7.1c.3, §7.1c.4, §7.1c.5, §7.1c.6 (PRIMARY SOURCE — bytes are copied verbatim from the spec, NOT computed by Rust)
    - .planning/phases/02-crypto-foundations/02-CONTEXT.md D-13 through D-18
    - .planning/phases/02-crypto-foundations/02-RESEARCH.md "Exact Spec Bytes" section + Pitfall 3 (self-generated bytes anti-pattern)
  </read_first>
  <action>
    1. Create `crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json`. Schema (per D-15) — all hex fields lowercase, no `0x` prefix, no whitespace:
       ```json
       {
         "name": "famp-v0.5.1-section-7.1c-worked-example",
         "spec_version": "0.5.1",
         "spec_section": "7.1c",
         "source": "FAMP-v0.5.1-spec.md §7.1c (externally sourced via Python jcs==0.2.1 + cryptography==46.0.7; see PROVENANCE.md)",
         "domain_prefix_hex": "46414d502d7369672d763100",
         "secret_key_hex": "9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60",
         "public_key_hex": "d75a980182b10ab7d54bfed3c964073a0ee172f3daa62325af021a68f707511a",
         "unsigned_envelope_json": "{\"famp\":\"0.5.1\",\"id\":\"01890a3b-2c4d-7e5f-8a1b-0c2d3e4f5a6b\",\"from\":\"agent:example.test/alice\",\"to\":\"agent:example.test/bob\",\"scope\":\"standalone\",\"class\":\"ack\",\"ts\":\"2026-04-13T00:00:00Z\",\"authority\":\"advisory\",\"causality\":{\"rel\":\"acknowledges\",\"ref\":\"01890a3b-1111-7222-8333-444444444444\"},\"body\":{\"disposition\":\"accepted\"}}",
         "canonical_json_hex": "<COPY FROM SPEC §7.1c.3 — hex of the 324-byte canonical form>",
         "signing_input_hex": "<COPY FROM SPEC §7.1c.5 — 336 bytes total, equals domain_prefix_hex concatenated with canonical_json_hex>",
         "signature_hex": "9366aaced854c7898735908d2e2d973208905fd80e2f93fe505710f58f0ed1fc92e3b9d7a19b30b2cf184f703552dafcf91ca81321f57fa689d1a96865d0b608",
         "signature_b64url": "k2aqzthUx4mHNZCNLi2XMgiQX9gOL5P-UFcQ9Y8O0fyS47nXoZswss8YT3A1Utr8-RyoEyH1f6aJ0aloZdC2CA",
         "signed_envelope_json": "<COPY FROM SPEC §7.1c re-embedded form — the unsigned envelope with `signature` field added>"
       }
       ```
       **CRITICAL — the four `<COPY FROM SPEC>` fields are byte-literal from the v0.5.1 spec.** Read the spec file, locate each numbered section, and copy the hex strings exactly. If any `<COPY FROM SPEC>` placeholder remains in the committed file, the plan is not done.

       **ANTI-PATTERN (Pitfall 3):** Do NOT run Rust code to generate `canonical_json_hex` or `signing_input_hex` and then paste its output. The bytes exist in the spec. Rust's job is to MATCH them, not produce them. The worked_example.rs test in Task 2 will re-derive these values at runtime and `assert_eq!` against the fixture — that is the interop proof.

       **Cross-check:** After transcription, `signing_input_hex` MUST equal `domain_prefix_hex + canonical_json_hex` (string concatenation). If it does not, the transcription is wrong.

    2. Create `crates/famp-crypto/tests/vectors/famp-sig-v1/PROVENANCE.md`:
       ```markdown
       # §7.1c Worked Example — Provenance

       The byte values in `worked-example.json` are sourced verbatim from
       `FAMP-v0.5.1-spec.md` §7.1c, which in turn were generated using the
       following Python tooling:

       - `jcs==0.2.1` (RFC 8785 JSON canonicalization)
       - `cryptography==46.0.7` (Ed25519 sign/verify)

       The keypair is RFC 8032 §7.1 Test 1 (published worldwide — test use only).

       ## Python generator (reference — DO NOT RUN TO REGENERATE)

       The spec documents the Python script used. It is preserved here for
       reviewer inspection only. Committed bytes MUST match the spec, not the
       re-run output of this script (per PITFALLS P10: "self-generated bytes
       defeat the interop purpose of the fixture").

       ```python
       # jcs==0.2.1, cryptography==46.0.7
       import jcs, base64
       from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey

       envelope = {
           "famp": "0.5.1",
           "id": "01890a3b-2c4d-7e5f-8a1b-0c2d3e4f5a6b",
           "from": "agent:example.test/alice",
           "to": "agent:example.test/bob",
           "scope": "standalone",
           "class": "ack",
           "ts": "2026-04-13T00:00:00Z",
           "authority": "advisory",
           "causality": {"rel": "acknowledges", "ref": "01890a3b-1111-7222-8333-444444444444"},
           "body": {"disposition": "accepted"},
       }
       canonical = jcs.canonicalize(envelope)                       # bytes
       PREFIX = b"FAMP-sig-v1\x00"
       signing_input = PREFIX + canonical
       sk_hex = "9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60"
       sk = Ed25519PrivateKey.from_private_bytes(bytes.fromhex(sk_hex))
       sig = sk.sign(signing_input)
       print("canonical_hex    =", canonical.hex())
       print("signing_input    =", signing_input.hex())
       print("signature_hex    =", sig.hex())
       print("signature_b64url =", base64.urlsafe_b64encode(sig).rstrip(b"=").decode())
       ```

       ## How to verify the fixture against the spec

       1. Open `FAMP-v0.5.1-spec.md` at §7.1c.
       2. For each field in `worked-example.json`, locate the matching hex
          string in the spec and confirm byte-for-byte equality.
       3. Do NOT run the Python script above to "refresh" the file.
       ```
  </action>
  <verify>
    <automated>test -f crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json &amp;&amp; test -f crates/famp-crypto/tests/vectors/famp-sig-v1/PROVENANCE.md &amp;&amp; jq -e '.signature_b64url == "k2aqzthUx4mHNZCNLi2XMgiQX9gOL5P-UFcQ9Y8O0fyS47nXoZswss8YT3A1Utr8-RyoEyH1f6aJ0aloZdC2CA"' crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json &amp;&amp; ! grep -q 'COPY FROM SPEC' crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.domain_prefix_hex == "46414d502d7369672d763100"' worked-example.json` succeeds
    - `jq -e '.secret_key_hex == "9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60"' worked-example.json` succeeds
    - `jq -e '.public_key_hex == "d75a980182b10ab7d54bfed3c964073a0ee172f3daa62325af021a68f707511a"' worked-example.json` succeeds
    - `jq -e '.signature_hex | length == 128' worked-example.json` succeeds (64 bytes = 128 hex chars)
    - `jq -e '.canonical_json_hex | length > 0' worked-example.json` succeeds and does NOT equal the literal "<COPY FROM SPEC ..." placeholder
    - `jq -e '.signing_input_hex | startswith("46414d502d7369672d763100")' worked-example.json` succeeds
    - `jq -r '.signing_input_hex' worked-example.json` length equals `12 + (jq -r '.canonical_json_hex' worked-example.json length / 2) × 2` (i.e. signing_input = prefix || canonical)
    - `grep -q 'jcs==0.2.1' PROVENANCE.md`
    - `grep -q 'cryptography==46.0.7' PROVENANCE.md`
    - `! grep -q 'COPY FROM SPEC' crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json`
  </acceptance_criteria>
  <done>§7.1c fixture file committed verbatim from spec; provenance documented; no placeholder strings remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-exact §7.1c worked_example test + wire `just` recipe + CI gate</name>
  <files>crates/famp-crypto/tests/worked_example.rs, justfile, .github/workflows/ci.yml</files>
  <read_first>
    - crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json (from Task 1)
    - crates/famp-crypto/src/lib.rs (Plan 02 — confirm public re-exports)
    - justfile (current recipes)
    - .github/workflows/ci.yml (current jobs — model `test-crypto` after the existing `test-canonical` job from Phase 1)
    - .planning/phases/02-crypto-foundations/02-CONTEXT.md D-21
    - .planning/phases/02-crypto-foundations/02-RESEARCH.md "§7.1c Worked-Example Test Structure"
  </read_first>
  <behavior>
    - Test MUST assert that `famp_crypto::canonicalize_for_signature(parsed_unsigned_envelope)` equals `hex::decode(fixture.signing_input_hex)` — byte-exact.
    - Test MUST assert that `famp_crypto::verify_canonical_bytes(vk, hex::decode(fixture.canonical_json_hex), FampSignature::from_hex(fixture.signature_hex))` returns `Ok(())`.
    - Test MUST assert that `fixture.signing_input_hex` starts with `fixture.domain_prefix_hex` (guard against fixture transcription error).
    - Test MUST assert that `TrustedVerifyingKey::from_bytes(fixture.public_key_hex)` returns `Ok(_)` (key is non-weak — it is the RFC 8032 Test 1 pubkey, which is a legitimate Ed25519 point).
  </behavior>
  <action>
    1. Create `crates/famp-crypto/tests/worked_example.rs`:
       ```rust
       use famp_crypto::{
           canonicalize_for_signature, verify_canonical_bytes,
           FampSignature, TrustedVerifyingKey,
       };

       #[derive(serde::Deserialize)]
       struct WorkedExample {
           domain_prefix_hex: String,
           public_key_hex: String,
           unsigned_envelope_json: String,
           canonical_json_hex: String,
           signing_input_hex: String,
           signature_hex: String,
       }

       #[test]
       fn section_7_1c_worked_example_byte_exact() {
           let raw = include_str!("vectors/famp-sig-v1/worked-example.json");
           let f: WorkedExample = serde_json::from_str(raw).expect("fixture parses");

           // Guard: fixture transcription sanity
           assert_eq!(f.domain_prefix_hex, "46414d502d7369672d763100");
           assert!(
               f.signing_input_hex.starts_with(&f.domain_prefix_hex),
               "signing_input_hex MUST begin with domain_prefix_hex"
           );
           assert_eq!(
               f.signing_input_hex.len(),
               f.domain_prefix_hex.len() + f.canonical_json_hex.len(),
               "signing_input_hex MUST equal prefix || canonical concatenation"
           );

           // Reproduce signing input from the unsigned envelope and assert byte-exact match
           let unsigned: serde_json::Value =
               serde_json::from_str(&f.unsigned_envelope_json).expect("unsigned envelope parses");
           let actual_signing_input = canonicalize_for_signature(&unsigned)
               .expect("canonicalize_for_signature");
           let expected_signing_input = hex::decode(&f.signing_input_hex).expect("hex decode");
           assert_eq!(
               actual_signing_input, expected_signing_input,
               "canonicalize_for_signature output MUST match fixture byte-for-byte"
           );

           // Reconstruct key + signature from fixture and verify
           let pk_bytes: [u8; 32] = hex::decode(&f.public_key_hex).unwrap().try_into().unwrap();
           let vk = TrustedVerifyingKey::from_bytes(&pk_bytes)
               .expect("public key must pass ingress");

           let canonical = hex::decode(&f.canonical_json_hex).expect("canonical hex");
           let sig_bytes: [u8; 64] = hex::decode(&f.signature_hex).unwrap().try_into().unwrap();
           let sig = FampSignature::from_bytes(sig_bytes);

           verify_canonical_bytes(&vk, &canonical, &sig)
               .expect("§7.1c worked-example signature MUST verify byte-exact");
       }
       ```
    2. Add a `test-crypto` recipe to `justfile`. Model it after the existing `test-canonical` recipe from Phase 1. Exact recipe:
       ```make
       # Run famp-crypto tests as a blocking gate
       test-crypto:
           cargo nextest run -p famp-crypto
           cargo test -p famp-crypto --doc
       ```
       Also ensure `just ci` (or whatever the existing aggregate recipe is called) depends on `test-crypto`. Read the current justfile first to find the aggregate target and add `test-crypto` to its dependency list or command chain.

    3. Update `.github/workflows/ci.yml` — add a `test-crypto` job modeled after the existing `test-canonical` job (Plan 01-03 wired it). It MUST run `just test-crypto` (or `cargo nextest run -p famp-crypto`) and MUST block the downstream workspace test matrix on failure. No `continue-on-error: true`. Executor: read the current ci.yml, find `test-canonical`, duplicate its structure with `test-crypto` naming, and add it to the `needs:` list of whatever job currently depends on `test-canonical`.
  </action>
  <verify>
    <automated>cargo nextest run -p famp-crypto --test worked_example &amp;&amp; just test-crypto</automated>
  </verify>
  <acceptance_criteria>
    - `cargo nextest run -p famp-crypto --test worked_example` shows `section_7_1c_worked_example_byte_exact ... ok`
    - `grep -q 'test-crypto' justfile`
    - `just test-crypto` exits 0
    - `grep -q 'test-crypto' .github/workflows/ci.yml`
    - `! grep -A3 'test-crypto:' .github/workflows/ci.yml | grep -q 'continue-on-error'` (no soft-fail)
    - Downstream workspace test job in ci.yml lists `test-crypto` in its `needs:` array (use `grep -B2 -A10 'needs:' .github/workflows/ci.yml` to verify)
  </acceptance_criteria>
  <done>§7.1c byte-exact gate green; `just test-crypto` recipe exists and is wired into `just ci`; CI has a blocking `test-crypto` job.</done>
</task>

<task type="auto">
  <name>Task 3: README with worked example, CT rationale, wrapper audit</name>
  <files>crates/famp-crypto/README.md, crates/famp-crypto/src/lib.rs</files>
  <read_first>
    - crates/famp-crypto/src/sign.rs
    - crates/famp-crypto/src/verify.rs
    - crates/famp-crypto/src/keys.rs
    - crates/famp-canonical/README.md (if it exists — mirror its style)
    - .planning/phases/02-crypto-foundations/02-CONTEXT.md D-22 through D-24
    - .planning/phases/02-crypto-foundations/02-RESEARCH.md §"Common Pitfalls" (P1, P2, P5, P7)
  </read_first>
  <action>
    1. Create `crates/famp-crypto/README.md` with these REQUIRED sections (titles must match for grep-based acceptance checks):

       - `# famp-crypto` — crate overview (2-3 sentences): FAMP-specific Ed25519 sign/verify with domain separation.
       - `## Domain separation` — documents `DOMAIN_PREFIX = b"FAMP-sig-v1\x00"` (hex `46414d502d7369672d763100`), explains that every signature is over `DOMAIN_PREFIX || canonical_json_bytes`, and points at `canonicalize_for_signature` as the sanctioned path.
       - `## Worked example (§7.1c)` — shows the Rust code from `tests/worked_example.rs` (abbreviated), names the Python provenance (`jcs 0.2.1` + `cryptography 46.0.7`), and links to `tests/vectors/famp-sig-v1/PROVENANCE.md`.
       - `## Public API` — lists `FampSigningKey`, `TrustedVerifyingKey`, `FampSignature`, `sign_value`, `sign_canonical_bytes`, `verify_value`, `verify_canonical_bytes`, `canonicalize_for_signature`, `Signer`, `Verifier`, `DOMAIN_PREFIX`. States explicitly that `ed25519_dalek::VerifyingKey` is NOT re-exported and there is NO path to `verify` (only `verify_strict`).
       - `## Weak-key rejection at ingress` — describes CRYPTO-02/03 enforcement: `TrustedVerifyingKey::from_bytes` calls `VerifyingKey::from_bytes` + `is_weak()`; points at `tests/vectors/must-reject/weak-keys.json`.
       - `## Constant-time verification (CRYPTO-08)` — documents the D-22/D-23 claim verbatim:
         > 1. Cryptographic constant-time properties are delegated to `ed25519-dalek`'s `verify_strict`.
         > 2. This wrapper introduces no avoidable pre-verification branching on secret-dependent data.
         > 3. Weak-key ingress rejection and decode validation happen BEFORE the key is trusted — this is policy validation on public material, not secret-dependent runtime branching.
         > 4. No statistical timing tests (dudect-style) run in CI — they are environment-sensitive and deferred.
       - `## Wrapper audit` — enumerates every error path in `sign_*` / `verify_*` and confirms none short-circuits on secret material. Required bullets (copy verbatim, adjusted if a path differs from current source):
         - `sign_value`: canonicalization error from `famp_canonical::canonicalize` is a serialization error over PUBLIC value data, not secret. Short-circuit on canonical error happens before any secret-key access. ✅
         - `sign_canonical_bytes`: no branching — always prepends prefix and calls `dalek::SigningKey::sign`. ✅
         - `verify_value`: canonicalization error branches on PUBLIC value data. No secret involved. ✅
         - `verify_canonical_bytes`: unconditionally builds signing input, unconditionally calls `verify_strict`. Error mapping (`VerificationFailed`) discards upstream detail and does not leak timing. ✅
         - `TrustedVerifyingKey::from_bytes`: branches on PUBLIC key bytes (encoding check + `is_weak`). The key is not yet trusted at this point — no secret material. ✅
         - `FampSigningKey::from_bytes`: infallible, no branching. ✅
         - `FampSignature` equality uses `subtle::ConstantTimeEq::ct_eq`. ✅
       - `## What's NOT in this crate` — lists deferred items from 02-CONTEXT.md `<deferred>`: `sign_envelope`/`verify_envelope` (→ `famp-envelope`), FIPS/`aws-lc-rs`, `no_std`, dudect tests, Agent Card/trust-store policy, protocol error taxonomy mapping.

    2. Update `crates/famp-crypto/src/lib.rs` crate-level doc comment (`//!` block) to cross-reference the README:
       ```rust
       #![forbid(unsafe_code)]
       //! FAMP v0.5.1 — `famp-crypto`: Ed25519 sign/verify with domain separation.
       //!
       //! See `README.md` for the worked §7.1c example, constant-time rationale,
       //! and wrapper audit (CRYPTO-08).
       //!
       //! # Quick start
       //! ```no_run
       //! use famp_crypto::{FampSigningKey, sign_value, verify_value};
       //! use serde_json::json;
       //! let sk = FampSigningKey::from_bytes([0u8; 32]);
       //! // NOTE: [0u8; 32] is a test seed only — never use in production.
       //! let vk = sk.verifying_key();
       //! let v = json!({"hello": "world"});
       //! let sig = sign_value(&sk, &v).unwrap();
       //! verify_value(&vk, &v, &sig).unwrap();
       //! ```
       ```
  </action>
  <verify>
    <automated>cargo test -p famp-crypto --doc &amp;&amp; grep -q 'FAMP-sig-v1' crates/famp-crypto/README.md &amp;&amp; grep -q 'verify_strict' crates/famp-crypto/README.md &amp;&amp; grep -q 'Wrapper audit' crates/famp-crypto/README.md  <verify>
    <automated>cargo test -p famp-crypto --doc && grep -q 'FAMP-sig-v1' crates/famp-crypto/README.md && grep -q 'verify_strict' crates/famp-crypto/README.md && grep -q 'Wrapper audit' crates/famp-crypto/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/famp-crypto/README.md`
    - `grep -q '# famp-crypto' crates/famp-crypto/README.md`
    - `grep -q '## Domain separation' crates/famp-crypto/README.md`
    - `grep -q '## Worked example' crates/famp-crypto/README.md`
    - `grep -q '## Public API' crates/famp-crypto/README.md`
    - `grep -q '## Weak-key rejection' crates/famp-crypto/README.md`
    - `grep -q '## Constant-time verification' crates/famp-crypto/README.md`
    - `grep -q '## Wrapper audit' crates/famp-crypto/README.md`
    - `grep -q "What's NOT in this crate" crates/famp-crypto/README.md`
    - `grep -q 'FAMP-sig-v1' crates/famp-crypto/README.md`
    - `grep -q 'verify_strict' crates/famp-crypto/README.md`
    - `grep -q 'jcs 0.2.1' crates/famp-crypto/README.md`
    - `grep -q 'cryptography 46.0.7' crates/famp-crypto/README.md`
    - `grep -q 'famp-crypto' crates/famp-crypto/src/lib.rs` (crate-level doc comment present)
    - `cargo test -p famp-crypto --doc` passes (doctest in lib.rs crate comment runs green)
  </acceptance_criteria>
  <done>README documents worked example, CT rationale, wrapper audit; crate-level doc comment present; doctest green.</done>
</task>

</tasks>

<verification>
- `cargo nextest run -p famp-crypto` all green (all three plans' tests)
- `cargo test -p famp-crypto --doc` green
- `just test-crypto` exits 0
- `cargo clippy -p famp-crypto --all-targets -- -D warnings` clean
- CI workflow has a `test-crypto` blocking job
- Phase 2 goal achieved: §7.1c worked-example verifies byte-exact in Rust; `verify_strict`-only exposure compiler-enforced; RFC 8032 vectors green; weak-key rejection proven; base64url strict codec proven
</verification>

<success_criteria>
- `crates/famp-crypto/tests/vectors/famp-sig-v1/worked-example.json` contains byte-literal spec values (no `<COPY FROM SPEC>` placeholder)
- `section_7_1c_worked_example_byte_exact` test asserts `canonicalize_for_signature` output equals fixture signing_input_hex AND `verify_canonical_bytes` returns Ok
- `PROVENANCE.md` documents `jcs==0.2.1` + `cryptography==46.0.7` sourcing
- `justfile` has `test-crypto` recipe wired into `just ci`
- `.github/workflows/ci.yml` has `test-crypto` job that blocks workspace test matrix
- `crates/famp-crypto/README.md` documents DOMAIN_PREFIX, worked example, public API, weak-key rejection, CT rationale (D-22/D-23), wrapper audit (D-24), and deferred items
- Phase 2 requirements CRYPTO-01..08, SPEC-03, SPEC-19 all satisfied by the combination of Plans 01+02+03
</success_criteria>

<output>
After completion, create `.planning/phases/02-crypto-foundations/02-03-SUMMARY.md`
</output>
