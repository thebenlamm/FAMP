---
phase: 02-uds-wire-cli-mv-mcp-rewire-hook-subcommand
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/famp/tests/broker_lifecycle.rs
  - crates/famp/tests/broker_spawn_race.rs
  - crates/famp/tests/broker_crash_recovery.rs
  - crates/famp/tests/cli_dm_roundtrip.rs
  - crates/famp/tests/cli_channel_fanout.rs
  - crates/famp/tests/cli_inbox.rs
  - crates/famp/tests/cli_sessions.rs
  - crates/famp/tests/mcp_bus_e2e.rs
  - crates/famp/tests/hook_subcommand.rs
autonomous: true
requirements: []
notes: >
  Pure infrastructure plan extracted from 02-01 to keep that plan under the
  15-file blocker threshold. No requirement IDs — every stub here is owned
  by a downstream plan that fills it (02-10/02-11/02-12/02-13). Coverage
  audit: requirement IDs originally listed on 02-01 (BROKER-02, BROKER-05)
  remain on 02-01 unchanged; this plan adds zero new requirement coverage.
must_haves:
  truths:
    - "All 9 Wave-0 test stub files exist on disk so plan 02-01 can run `cargo build --workspace --tests` without 'file not found' errors"
    - "Every stub file carries the canonical header (`#![cfg(unix)]` + clippy/unused-deps allows) so when the owning plan replaces the body the header is already pinned"
    - "Every stub function carries `#[ignore = \"stub: implementation lands in plan 02-XX\"]` and panics with `unimplemented!(\"filled in by plan 02-XX\")` so accidentally un-ignoring a stub fails LOUDLY rather than silently passing"
    - "No test function defined in this plan is unignored — un-ignoring is the EXCLUSIVE right of the owning downstream plan"
  artifacts:
    - path: "crates/famp/tests/broker_lifecycle.rs"
      provides: "Wave-0 stub for BROKER-01, BROKER-04, CLI-11, BROKER-05 (filled by plan 02-11)"
      contains: "test_broker_accepts_connection"
    - path: "crates/famp/tests/broker_spawn_race.rs"
      provides: "Wave-0 stub for TEST-04 (filled by plan 02-11)"
      contains: "test_broker_spawn_race"
    - path: "crates/famp/tests/broker_crash_recovery.rs"
      provides: "Wave-0 stub for TEST-03 (filled by plan 02-11)"
      contains: "test_kill9_recovery"
    - path: "crates/famp/tests/cli_dm_roundtrip.rs"
      provides: "Wave-0 stub for TEST-01 + CLI-01/03/05/08 side-effects (filled by plan 02-12)"
      contains: "test_dm_roundtrip"
    - path: "crates/famp/tests/cli_channel_fanout.rs"
      provides: "Wave-0 stub for TEST-02 (filled by plan 02-12)"
      contains: "test_channel_fanout"
    - path: "crates/famp/tests/cli_inbox.rs"
      provides: "Wave-0 stub for CLI-04 / CLI-10 (filled by plan 02-12)"
      contains: "test_inbox_ack_cursor"
    - path: "crates/famp/tests/cli_sessions.rs"
      provides: "Wave-0 stub for CLI-06 / CLI-07 sessions list (filled by plan 02-12)"
      contains: "test_sessions_list"
    - path: "crates/famp/tests/mcp_bus_e2e.rs"
      provides: "Wave-0 stub for TEST-05 (filled by plan 02-13)"
      contains: "test_mcp_bus_e2e"
    - path: "crates/famp/tests/hook_subcommand.rs"
      provides: "Wave-0 stub for HOOK-01..03 (filled by plan 02-10)"
      contains: "test_hook_add"
  key_links:
    - from: "02-VALIDATION.md Per-Task Verification Map"
      to: "9 stub files in crates/famp/tests/"
      via: "every Test File (Wave 0) entry in the map points to a file created by this plan"
      pattern: "crates/famp/tests/.*\\.rs"
---

<objective>
Pre-create the 9 Wave-0 test stub files referenced by `02-VALIDATION.md` so plan 02-01 can stay below the 15-file blocker threshold and so every downstream test plan (02-10, 02-11, 02-12, 02-13) has a known-good landing pad to overwrite.

Purpose: The plan-checker flagged 02-01 at 19 files_modified — 4 over the blocker threshold. Splitting 9 stubs into a dedicated infrastructure plan keeps 02-01 at 10 files (substantive: deps, bus_client, identity, nfs_check) and isolates the stub creation as a pure mechanical step. This plan adds NO requirement coverage of its own; every Wave-0 file's requirement IDs are owned by the plan that fills the stubs.

Output: 9 Rust test files, each containing the canonical header + N `#[ignore]`-gated stub functions whose names match exactly the `Automated Command` column in `02-VALIDATION.md` Per-Task Verification Map. Total file size of each stub: ≤30 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-CONTEXT.md
@.planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-VALIDATION.md

<interfaces>
<!-- Canonical stub header (pinned). Every Wave-0 stub MUST start with these 3 lines verbatim. -->

```rust
#![cfg(unix)]
#![allow(unused_crate_dependencies)]
#![allow(clippy::unwrap_used, clippy::expect_used)]
```

<!-- Canonical stub function template. Replace OWNER with the filling plan number (10/11/12/13). -->

```rust
#[test]
#[ignore = "stub: implementation lands in plan 02-OWNER"]
fn TEST_NAME() {
    unimplemented!("filled in by plan 02-OWNER");
}
```

<!-- Per-VALIDATION.md test-name → file → owner map (locked, do not improvise): -->

| Test File | Test Functions (one #[ignore] per row) | Owner |
|-----------|-----------------------------------------|-------|
| `broker_lifecycle.rs` | `test_broker_accepts_connection`, `test_broker_idle_exit`, `test_sessions_jsonl_diagnostic_only`, `test_nfs_warning` | 02-11 |
| `broker_spawn_race.rs` | `test_broker_spawn_race` | 02-11 |
| `broker_crash_recovery.rs` | `test_kill9_recovery` | 02-11 |
| `cli_dm_roundtrip.rs` | `test_register_blocks`, `test_dm_roundtrip`, `test_inbox_list`, `test_await_unblocks`, `test_whoami` | 02-12 |
| `cli_channel_fanout.rs` | `test_channel_fanout` | 02-12 |
| `cli_inbox.rs` | `test_inbox_ack_cursor` | 02-12 |
| `cli_sessions.rs` | `test_sessions_list` | 02-12 |
| `mcp_bus_e2e.rs` | `test_mcp_bus_e2e` | 02-13 |
| `hook_subcommand.rs` | `test_hook_add`, `test_hook_list`, `test_hook_remove` | 02-10 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 9 Wave-0 test stub files</name>
  <read_first>
    - .planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-VALIDATION.md (Per-Task Verification Map — confirm test-function names match the `Automated Command` column for every row whose Test File is marked W0)
    - crates/famp/tests/mcp_stdio_tool_calls.rs (lines 1-3 — copy the canonical 3-line header verbatim)
  </read_first>
  <action>
    Create the 9 stub files listed in the `<interfaces>` map above. Each file uses the canonical header (3 lines, verbatim) followed by N `#[test] #[ignore = ...]` stubs (one per test function in that file's row of the map).

    Concrete byte template (substitute TEST_NAME and OWNER):
    ```rust
    #![cfg(unix)]
    #![allow(unused_crate_dependencies)]
    #![allow(clippy::unwrap_used, clippy::expect_used)]

    // 02-00 stub: filled by plan 02-OWNER. Do NOT un-ignore here — un-ignoring
    // is the exclusive right of the owning plan when it replaces the body.

    #[test]
    #[ignore = "stub: implementation lands in plan 02-OWNER"]
    fn TEST_NAME() {
        unimplemented!("filled in by plan 02-OWNER");
    }
    ```

    For files with multiple test functions (`broker_lifecycle.rs`, `cli_dm_roundtrip.rs`, `hook_subcommand.rs`), repeat the `#[test] #[ignore] fn ...` block once per row, sharing the single 3-line header.

    Owner number map (pin EXACTLY):
    - `broker_lifecycle.rs` → owner `11`
    - `broker_spawn_race.rs` → owner `11`
    - `broker_crash_recovery.rs` → owner `11`
    - `cli_dm_roundtrip.rs` → owner `12`
    - `cli_channel_fanout.rs` → owner `12`
    - `cli_inbox.rs` → owner `12`
    - `cli_sessions.rs` → owner `12`
    - `mcp_bus_e2e.rs` → owner `13`
    - `hook_subcommand.rs` → owner `10`

    Do NOT add `use` imports, helpers, fixtures, or shared modules — every line of substantive content lands when the owning plan replaces the file. The stub MUST compile against the existing `crates/famp/Cargo.toml` (zero added deps in this plan).

    After writing all 9 files, verify with:
    ```
    ls -1 crates/famp/tests/{broker_lifecycle,broker_spawn_race,broker_crash_recovery,cli_dm_roundtrip,cli_channel_fanout,cli_inbox,cli_sessions,mcp_bus_e2e,hook_subcommand}.rs | wc -l
    # expect: 9
    cargo build --workspace --tests
    # expect: exit 0
    ```

    NOTE: The `cargo build` step requires that `crates/famp/Cargo.toml` already declares `tempfile` / `assert_cmd` / etc. as dev-deps. In this plan we do NOT touch Cargo.toml — every stub uses ONLY std + `unimplemented!()`. If a future plan needs imports, the OWNER plan adds them when filling the stub. This plan stays compile-clean against the current Cargo.toml on Phase-1's HEAD.
  </action>
  <acceptance_criteria>
    - All 9 stub files exist: `ls crates/famp/tests/{broker_lifecycle,broker_spawn_race,broker_crash_recovery,cli_dm_roundtrip,cli_channel_fanout,cli_inbox,cli_sessions,mcp_bus_e2e,hook_subcommand}.rs` lists 9 files
    - Every stub starts with `#![cfg(unix)]` (`grep -L '#!\[cfg(unix)\]' crates/famp/tests/{broker_lifecycle,broker_spawn_race,broker_crash_recovery,cli_dm_roundtrip,cli_channel_fanout,cli_inbox,cli_sessions,mcp_bus_e2e,hook_subcommand}.rs` returns no matching files)
    - Every test function is `#[ignore]`-gated (`grep -c '#\[ignore' crates/famp/tests/broker_lifecycle.rs` returns 4; `cli_dm_roundtrip.rs` returns 5; `hook_subcommand.rs` returns 3; the other 6 return 1 each — total 19 across 9 files)
    - Test names match `02-VALIDATION.md` Per-Task Verification Map exactly (manual check against the table)
    - `cargo build --workspace --tests` exits 0
    - `cargo nextest run -p famp --no-fail-fast --status-level skip 2>&1 | grep -F 'IGNORED' | wc -l | awk '$1 >= 19 { exit 0 } { exit 1 }'` exits 0 (every stub reports IGNORED, not FAILED)
    - Each stub file is ≤30 lines (`wc -l crates/famp/tests/broker_lifecycle.rs` ≤ 30; same for all 9)
    - No `use` import statements in any stub (`grep -c '^use ' crates/famp/tests/broker_lifecycle.rs` returns 0; same for all 9)
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --tests &amp;&amp; cargo nextest run -p famp --no-fail-fast --status-level skip 2&gt;&amp;1 | grep -F 'IGNORED' | wc -l | awk '$1 &gt;= 19 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    9 stub files exist with the canonical header and the exact set of `#[ignore]`-gated test functions named in `02-VALIDATION.md`. Workspace builds with new stub files in place; no test runs to FAILED.
  </done>
</task>

</tasks>

<verification>
- 9 stub files exist on disk
- `cargo build --workspace --tests` exits 0
- All 19 stub functions report as IGNORED in nextest, never FAILED
- Wave-0 sign-off in 02-VALIDATION.md frontmatter remains `wave_0_complete: false` (gets flipped by 02-01 once Cargo.toml + bus_client land — this plan only handles the stub-creation half of Wave-0)
</verification>

<success_criteria>
- 02-01 can drop its 9 stub-file paths from `files_modified` and stay below the 15-file blocker threshold
- 02-10 (and 02-11, 02-12, 02-13) can declare `depends_on: [00]` for stub-file presence without coupling to 02-01's substantive Cargo.toml/bus_client work
- Zero requirement IDs claimed by this plan; every Wave-0 file's requirements ride with the OWNER plan that fills the body
</success_criteria>

<output>
After completion, create `.planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-00-SUMMARY.md` documenting:
- File count (9) and total LoC (~150)
- Confirmation that no Cargo.toml change was needed
- IGNORED-count verification result
</output>
