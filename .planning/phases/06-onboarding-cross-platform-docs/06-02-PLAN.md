---
phase: 06-onboarding-cross-platform-docs
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-03]

must_haves:
  truths:
    - "Every downstream README reference agrees with the finalized three-tier block — no section still presents a daemon-free quickstart or an auto-spawn-only broker story that contradicts the daemon-first lead"
    - "The CLI command table lists the four `famp daemon` subcommands and the `--no-idle-exit` flag, and the register row's auto-spawn framing is corrected"
    - "The 'When NOT to Use FAMP' closed-laptop integrity boundary survives: the daemon keeps the BROKER reachable but does NOT service work autonomously — delivery still requires an open agent window"
    - "Header and intro prose read v0.11, not v0.9"
  artifacts:
    - path: "README.md"
      provides: "Five reconciled downstream sections (CLI table, Onboarding path, When-NOT-to-Use, Upgrading, Troubleshooting) + v0.11 version refresh"
      contains: "famp daemon"
  key_links:
    - from: "README.md Upgrading section"
      to: "famp daemon restart"
      via: "version-skew (VER-01) remediation after a binary upgrade"
      pattern: "famp daemon restart"
---

<objective>
Reconcile the FIVE downstream consistency edits (D-04) against the FINALIZED
three-tier block from plan 01, then apply the v0.9→v0.11 version-framing refresh
(D-05). These edits are NOT scope creep: without them the daemon-first quickstart
contradicts other parts of the same README.

This plan depends on plan 01 — every edit here must agree with the block plan 01
produced, not a quickstart still in motion.

Purpose: keep DOC-01 (daemon-first) internally non-contradictory; preserve the
DOC-03 platform/closed-laptop integrity boundary in the reworded "When NOT to
Use" section. Pure docs — no code changes.
Output: Reconciled README.md (CLI table, Onboarding path, When-NOT-to-Use,
Upgrading, Troubleshooting) + v0.11 header/intro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-onboarding-cross-platform-docs/06-CONTEXT.md
@.planning/REQUIREMENTS.md

<!-- This plan reconciles AGAINST plan 01's output. The current README state below the -->
<!-- three-tier block (plan 01) is what gets edited here. Read plan 01's finalized block first. -->
@.planning/phases/06-onboarding-cross-platform-docs/06-01-PLAN.md

<interfaces>
<!-- Verified command surface (same as plan 01). Quote verbatim. -->

`famp daemon --help` → four subcommands: install, uninstall, status, restart.
`famp broker --no-idle-exit` → run broker without the 300s idle self-terminate.
`famp -V` → `famp 0.11.0`. Workspace version unified to 0.11.0 (VER-02).
VER-01: a client hitting a bus_proto-mismatched (stale long-lived) daemon gets a
ProtocolMismatch error whose text names `famp daemon restart` as the remedy.

Integrity boundary (D-04 item 3, the INTEGRITY TRAP): the daemon keeps the message
BROKER reachable; it does NOT read inboxes or service work autonomously. Delivery
still requires an open agent window reading its inbox. The closed-laptop rule of
thumb still holds. The daemon does NOT enable background/production/unattended use.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile CLI table + Onboarding path + version refresh (DOC-01, DOC-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md — the finalized three-tier block (plan 01 output) AND the current
      CLI table (~L184-198), "MCP Integration / Onboarding (recommended path)"
      (~L218-258), header (L7 `Status: v0.9 Local-First Bus`), the version-note
      blockquote (~L9-11), and the v0.9-narrating intro prose (~L13-31).
    - .planning/phases/06-onboarding-cross-platform-docs/06-CONTEXT.md — D-04 items
      1 (CLI table) and 2 (Onboarding path), D-05 (version refresh).
    - .planning/phases/06-onboarding-cross-platform-docs/06-01-PLAN.md <interfaces>
      — exact daemon subcommand + flag surface.
  </read_first>
  <action>
    **D-04 item 1 — CLI table (~L184).** The current "Full CLI" table has zero
    `daemon` rows. Add rows for the daemon lifecycle and the bridge flag:

    | `famp daemon install` | Install the broker as a persistent user-level service (launchd/systemd --user); idempotent |
    | `famp daemon status` | Report daemon state (not-installed / installed-down / running); exit 0/2/1 |
    | `famp daemon restart` | Restart the daemon, picking up a new on-disk binary after `cargo install` |
    | `famp daemon uninstall` | Stop and remove the service; idempotent |
    | `famp broker --no-idle-exit` | Run the broker in the foreground with no 300s idle exit (no-install bridge) |

    Fix the `register` row: the current text "Bind a local identity and start the
    broker if needed" overclaims auto-spawn as universal. Reword to reflect the
    demoted-but-live auto-spawn framing from plan 01, e.g. "Bind a local identity
    (auto-spawns a broker for unsandboxed clients if none is running; sandboxed
    clients require a daemon or the bridge)."

    **D-04 item 2 — "Onboarding (recommended path)" (~L225).** This is a second
    hidden quickstart whose step 1 is `install-claude-code` with no daemon — it
    directly contradicts the daemon-first lead. Reconcile it: make its first step
    reference the daemon (e.g. "If you have not already, install the broker service
    once: `famp daemon install` (see [Quick Start]) — then wire the MCP integration:
    `famp install-claude-code`"). Do NOT duplicate the full quickstart here; point
    back to it and keep this section focused on the per-window MCP register flow.

    **D-05 — version refresh.** Update the header L7 `Status: v0.9 Local-First Bus`
    → `v0.11 Broker Daemon`. Update the version-note blockquote (~L9-11) and the
    v0.9-narrating intro prose (~L13-31) so the milestone framing reads v0.11: the
    library version in Cargo.toml is now `0.11.0` (not `0.1.0` — VER-02 unified it),
    and `famp -V` reports `0.11.0`. Keep the "v0.5.1 is the protocol spec" framing
    (spec version is unchanged at 0.5.2). Do NOT change the v1.0-federation framing.
    Leave deeper historical sections (Current Milestones list ~L626) describing past
    milestones as-is; only refresh the lead header + intro so v0.9 does not sit a few
    lines above a v0.11 daemon quickstart.
  </action>
  <verify>
    <automated>grep -q 'famp daemon install' README.md && grep -q 'famp daemon status' README.md && grep -q 'famp daemon restart' README.md && grep -q 'famp daemon uninstall' README.md && grep -q '0.11' README.md && ! grep -q 'v0.9 Local-First Bus' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLI table contains all four daemon subcommands: `grep -q 'famp daemon install' README.md`, `grep -q 'famp daemon status' README.md`, `grep -q 'famp daemon restart' README.md`, `grep -q 'famp daemon uninstall' README.md`
    - `grep -q -- '--no-idle-exit' README.md` (bridge flag row present)
    - register-row overclaim fixed: README no longer contains the exact string `start the broker if needed` (`grep -q 'start the broker if needed' README.md` returns nothing)
    - Onboarding path references the daemon: within the "Onboarding (recommended path)" section, `famp daemon install` appears (no longer leads with bare `install-claude-code`)
    - version refresh: header no longer reads v0.9 — `grep -q 'v0.9 Local-First Bus' README.md` returns nothing; `grep -q '0.11' README.md` passes (and `grep -q '0.1.0' README.md` for the stale crate-version note returns nothing OR is corrected to 0.11.0)
  </acceptance_criteria>
  <done>
    CLI table has four daemon rows + `--no-idle-exit`; register row reworded;
    Onboarding path points at the daemon; header/intro read v0.11; spec framing intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reword "When NOT to Use" (integrity trap) + Upgrading + Troubleshooting (DOC-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md — "When NOT to Use FAMP" (~L131-152, esp. L139 the line "There is
      no autonomous daemon servicing scheduled work"), "Upgrading" (~L118-129),
      "Troubleshooting" (~L642-656, esp. the "Stuck after a binary upgrade" bullet ~L655).
    - .planning/phases/06-onboarding-cross-platform-docs/06-CONTEXT.md — D-04 items
      3 (When-NOT-to-Use INTEGRITY TRAP + the reword target in <specifics>), 4
      (Upgrading + VER-01 daemon-restart step), 5 (Troubleshooting daemon
      status/restart).
    - .planning/phases/06-onboarding-cross-platform-docs/06-01-PLAN.md <interfaces>
      — VER-01 ProtocolMismatch names `famp daemon restart`.
  </read_first>
  <action>
    **D-04 item 3 — "When NOT to Use FAMP" (INTEGRITY TRAP, ~L139).** The current
    line "There is no autonomous daemon servicing scheduled work" now collides
    verbally with `famp daemon install`. Replace it with the reword target from
    CONTEXT.md <specifics>, verbatim:

    > The daemon keeps the message broker running; it does not service work
    > autonomously — delivery still requires an open agent window reading its inbox.

    Preserve the surrounding paragraph's meaning: FAMP delivery requires an open
    Claude Code / Codex window actively reading the inbox; close the window, the
    inbox stalls. KEEP the rule-of-thumb line intact: "if the use case survives a
    closed laptop, FAMP is not the right layer." Do NOT let the reword imply the
    daemon enables background / production / unattended use — that would overrun
    DOC-03 and weaken a load-bearing integrity boundary. The daemon restores broker
    PRESENCE; it does not restore agent ATTENDANCE.

    **D-04 item 4 — "Upgrading" (~L118).** The current steps are `git pull` /
    `cargo install` then "restart any open Claude Code windows." With a long-lived
    daemon you must ALSO restart the daemon or VER-01 version-skew fires (the stale
    long-lived daemon vs the upgraded client; the client gets a ProtocolMismatch
    error naming `famp daemon restart`). Add a step after the cargo install:

    ```bash
    # If you installed the broker as a service, pick up the new binary:
    famp daemon restart
    ```

    Add a one-line note that a client hitting a not-yet-restarted daemon will get a
    version-skew error telling it to run `famp daemon restart` (VER-01).

    **D-04 item 5 — "Troubleshooting" (~L642).** Update the "Stuck after a binary
    upgrade" bullet so it covers the daemon: "Stuck after a binary upgrade — restart
    all Claude Code windows AND, if you run the broker as a service, run
    `famp daemon restart` so the daemon picks up the new binary." Add a new bullet:
    "Not sure if the broker is up — run `famp daemon status` (RUNNING / INSTALLED_DOWN
    / NOT_INSTALLED)." Keep the existing broker.log / stale-socket bullets.
  </action>
  <verify>
    <automated>grep -q 'it does not service work' README.md && ! grep -q 'There is no autonomous daemon servicing scheduled work' README.md && grep -q 'survives a closed laptop' README.md && grep -q 'famp daemon restart' README.md && grep -q 'famp daemon status' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - reworded line present: `grep -q 'it does not service work autonomously' README.md`
    - old colliding line gone: `grep -q 'There is no autonomous daemon servicing scheduled work' README.md` returns nothing
    - closed-laptop rule preserved: `grep -q 'survives a closed laptop' README.md`
    - no background/production overclaim introduced: the reworded paragraph does NOT claim unattended/background/production capability (`grep -i 'while no one is at a keyboard' README.md` still present in the "needs a real backend" list, confirming the boundary is preserved)
    - Upgrading has the daemon-restart step: `famp daemon restart` appears within the "Upgrading" section
    - Troubleshooting references both `famp daemon status` and `famp daemon restart`
  </acceptance_criteria>
  <done>
    When-NOT-to-Use reworded to preserve the closed-laptop boundary (broker
    presence ≠ autonomous service); Upgrading adds `famp daemon restart` (VER-01);
    Troubleshooting adds daemon status/restart.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README prose → developer action | A reader follows a documented step; an inconsistent or boundary-eroding edit misleads them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information disclosure (misinformation) | Reconciled downstream sections | mitigate | Every edit reconciles against plan 01's finalized block and quotes the verified command surface; plan 03 re-verifies against the live binary. |
| T-06-04 | (integrity-boundary erosion) | "When NOT to Use FAMP" reword | mitigate | The closed-laptop rule of thumb and the "needs a real backend" list survive verbatim; reword distinguishes broker presence from autonomous service — no background/production/unattended claim. |
</threat_model>

<verification>
- Five sections reconciled (CLI table, Onboarding path, When-NOT-to-Use, Upgrading, Troubleshooting); header/intro read v0.11.
- Integrity boundary preserved: reworded line present, closed-laptop rule intact, no background/production overclaim (acceptance greps above).
- VER-01 surfaced: `famp daemon restart` present in Upgrading + Troubleshooting.
</verification>

<success_criteria>
- All five D-04 sections agree with plan 01's three-tier block.
- D-05 version refresh applied to header + intro (v0.11; crate version 0.11.0).
- "When NOT to Use" reworded without weakening the closed-laptop boundary.
</success_criteria>

<output>
After completion, create
`.planning/phases/06-onboarding-cross-platform-docs/06-02-SUMMARY.md`.
</output>
