---
phase: 06-onboarding-cross-platform-docs
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [README.md]
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "Every command and exit-code claim the README makes is verified against the actual installed binary — README quotes match real `famp ... --help` / `famp -V` / status output"
    - "The DOC-01 fresh-clone macOS walkthrough (install daemon → register from Claude Code → register from Codex) completes end-to-end with no broker-babysitting"
    - "No documented behavior asserts something the binary does not do; UNCERTAIN/human-only Phase 5 items (live launchctl idempotency, Linux behavior) are not claimed as live-verified"
  artifacts:
    - path: "README.md"
      provides: "Final README, every command-quote confirmed accurate against the binary"
      contains: "famp daemon install"
  key_links:
    - from: "README documented commands"
      to: "installed ~/.cargo/bin/famp binary"
      via: "grep README quotes against live --help / -V / status output"
      pattern: "famp daemon"
---

<objective>
D-07 is the #1 way this phase fails: docs that document a command that doesn't
match the binary. This plan is the hard accuracy gate. It runs every documented
command against the actual installed binary and confirms the README quotes match
real output, then drives the fresh-clone DOC-01 walkthrough end-to-end.

Split into two tasks:
- Task 1 (auto): side-effect-free verification — grep README quotes against
  `famp ... --help`, `famp -V`, and the documented status render/exit-codes/EPERM
  string. No service install, no live mutation.
- Task 2 (checkpoint:human-verify, BLOCKING): the side-effecting live launchctl
  lifecycle (install → status RUNNING → restart → uninstall) and the fresh-clone
  macOS Claude+Codex end-to-end walkthrough. Human-only because it loads a real
  LaunchAgent and requires two real agent tools.

Purpose: prove DOC-01/02/03 accurate against shipped Phase 5/Phase 4 behavior.
Output: confirmed-accurate README (with any drift corrected in Task 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-onboarding-cross-platform-docs/06-CONTEXT.md
@.planning/REQUIREMENTS.md

<!-- The authoritative shipped-behavior record. Task 2's human steps mirror its human-verification section. -->
@.planning/phases/05-daemon-service-management-version-safety/05-VERIFICATION.md
@.planning/phases/06-onboarding-cross-platform-docs/06-01-PLAN.md
@.planning/phases/06-onboarding-cross-platform-docs/06-02-PLAN.md

<interfaces>
<!-- VERIFIED-AGAINST-BINARY expected output. Task 1 confirms README matches these EXACTLY. -->

`famp -V` → exactly: `famp 0.11.0`

`famp daemon --help` → four subcommands listed: install, uninstall, status, restart.

`famp daemon status` render (note: TWO-space separators; literal `broker build:`):
  - RUNNING (exit 0):        `state: RUNNING  pid=<n>  socket=<path>  broker build: 0.11.0`
  - INSTALLED_DOWN (exit 2): `state: INSTALLED_DOWN  service=<path>  evidence=<...>`
  - NOT_INSTALLED (exit 1):  `state: NOT_INSTALLED  service file not found at <path>`

`famp broker --help` → `--no-idle-exit  Run the broker without the 300s idle self-terminate`

EPERM-on-bind sandbox message (BOOT-01, spawn.rs):
  `can't create a broker inside a sandbox; run `famp daemon install` from a normal shell`

Idempotency: `famp daemon --help` asserts "Idempotent: re-running is safe when the
service is already installed." Phase 5 VERIFICATION truth #8 marks LIVE launchctl
idempotency UNCERTAIN (behaviorally human-needed) — README must describe INTENDED
idempotency (as --help does), NOT claim CI-verified idempotency. Live 2x-install
confirmation belongs to Task 2 (human).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Side-effect-free accuracy grep — README quotes vs live binary</name>
  <files>README.md</files>
  <read_first>
    - README.md — the final state after plans 01 + 02 (every documented command,
      flag, exit code, and idempotency claim).
    - .planning/phases/05-daemon-service-management-version-safety/05-VERIFICATION.md
      — the behavioral spot-checks (lines ~108-118) and human-verification section
      for which claims are live-verified vs human-only.
    - The <interfaces> block above — the exact expected strings.
  </read_first>
  <action>
    Run each command below against the INSTALLED binary (`famp` on PATH =
    `~/.cargo/bin/famp` — the deployment target, not `target/release/famp`) and
    confirm the README's quotes match. This task performs NO service install and NO
    live mutation — it only reads `--help` / `-V` / non-installing surfaces.

    Verification commands (capture output, compare to README):
    1. `famp -V` → must print `famp 0.11.0`. Confirm README's version-refresh
       (plan 02) matches; no `0.1.0` or `v0.9` left in the lead.
    2. `famp daemon --help` → lists install/uninstall/status/restart. Confirm the
       README CLI table rows and quickstart use these exact subcommand names.
    3. `famp daemon status --help` → confirm the documented three states and exit
       codes (0 running / 1 not-installed / 2 installed-but-down) match the README
       Troubleshooting/CLI descriptions.
    4. `famp broker --help` → confirm `--no-idle-exit` exists with the documented
       "300s idle self-terminate" semantics; confirm the README bridge command and
       table row match.
    5. Confirm the EPERM sandbox remedy string the README quotes (if any) matches
       the binary's message: `can't create a broker inside a sandbox; run `famp
       daemon install` from a normal shell` (grep the source as the source of truth:
       `grep -n "can't create a broker inside a sandbox" crates/famp/src/bus_client/spawn.rs`).
    6. Confirm the README does NOT claim CI-verified or exit-code-based idempotency
       (Phase 5 marks live launchctl idempotency UNCERTAIN) — only the intended
       "re-running is safe" framing from `famp daemon --help`.

    If ANY README quote disagrees with live output, CORRECT the README to match the
    binary (the binary is the source of truth, never the reverse). Record each
    command's actual output in the SUMMARY so the human task (Task 2) can focus on
    the side-effecting paths.

    Do NOT run `famp daemon install` / `restart` / `uninstall` in this task — those
    load a real LaunchAgent and belong to Task 2.
  </action>
  <verify>
    <automated>test "$(famp -V)" = "famp 0.11.0" && famp daemon --help 2>&1 | grep -q install && famp daemon --help 2>&1 | grep -q restart && famp broker --help 2>&1 | grep -q -- '--no-idle-exit' && grep -q "can't create a broker inside a sandbox" crates/famp/src/bus_client/spawn.rs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `famp -V` output is exactly `famp 0.11.0` AND README lead/version-note matches (no residual `0.1.0` crate-version note, no `v0.9 Local-First Bus` header): `grep -q '0.11' README.md` AND `grep -q 'v0.9 Local-First Bus' README.md` returns nothing
    - `famp daemon --help` lists install/uninstall/status/restart AND each appears in the README CLI table
    - `famp broker --help` contains `--no-idle-exit` AND README documents the bridge with that flag
    - status three-state exit codes (0/1/2) as documented in README match `famp daemon status --help` text
    - EPERM remedy string in spawn.rs (`can't create a broker inside a sandbox`) is the source of truth; any README quote of it matches verbatim
    - README contains no CI-verified / exit-code idempotency claim: `grep -i 'exit 37' README.md` and `grep -i 'CI-verified' README.md` both return nothing
    - SUMMARY records the actual captured output of commands 1-5
  </acceptance_criteria>
  <done>
    Every side-effect-free README command-quote confirmed against the live binary;
    any drift corrected in README; actual outputs recorded in SUMMARY; no
    idempotency overclaim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live launchctl lifecycle + fresh-clone Claude+Codex E2E (human-verify, BLOCKING)</name>
  <files>README.md</files>
  <action>Block on human confirmation that the README quickstart matches live behavior on a real macOS machine: the side-effecting daemon lifecycle (install / status RUNNING / restart / uninstall) and the fresh-clone Claude+Codex end-to-end walkthrough. See the checkpoint fields below for the exact steps. Correct any README mismatch the human reports before phase close.</action>
  <read_first>
    - README.md — the final state after plans 01 + 02 (the quickstart, bridge,
      Platform support, CLI table, Upgrading, Troubleshooting as the human follows them).
    - .planning/phases/05-daemon-service-management-version-safety/05-VERIFICATION.md
      — human-verification #1 (live launchctl idempotency) and #3 (live three-state
      status); these define the side-effecting behaviors this step confirms.
    - 06-03-SUMMARY.md (Task 1 output) — the captured side-effect-free command
      outputs, so the human focuses only on the side-effecting paths.
  </read_first>
  <what-built>
    The README's three-tier getting-started block (daemon quickstart + no-install
    bridge + platform support) plus the five reconciled sections. Task 1 already
    verified all side-effect-free command quotes against the binary. This step
    verifies the SIDE-EFFECTING paths that load a real service and require two real
    agent tools — they cannot be automated safely (loading a LaunchAgent mutates the
    developer's machine; the E2E needs live Claude Code + Codex windows). Mirrors
    Phase 5 VERIFICATION human-verification #1 (live launchctl idempotency) and #3
    (live three-state status).
  </what-built>
  <how-to-verify>
    On a macOS machine, from a fresh clone (or a clean checkout), follow the README
    quickstart EXACTLY as written and confirm each step matches reality:

    1. **Daemon install + idempotency (DOC-01 / DAEMON-01).**
       - Run the README's `famp daemon install`. Expect a clean install and a
         loaded `com.famp.broker` LaunchAgent.
       - Run `famp daemon status`. Expect exit 0 and a line of the form
         `state: RUNNING  pid=<n>  socket=<path>  broker build: 0.11.0`
         (two-space separators, literal `broker build:`).
       - Run `famp daemon install` a SECOND time. Expect it to succeed as a no-op
         (no duplicate registration) — confirming the README's "re-running is safe"
         idempotency claim against live launchctl (Phase 5 left this behaviorally
         UNCERTAIN; this is the live confirmation).

    2. **Restart + status (DOC-01 / DAEMON-05 / DAEMON-03).**
       - Run `famp daemon restart`. Run `famp daemon status` again — expect exit 0
         and a RUNNING render (PID may change).

    3. **Fresh-clone Claude + Codex E2E (DOC-01 acceptance — the load-bearing test).**
       - Following ONLY the README quickstart: `famp install-claude-code`,
         `famp install-codex`.
       - Open a Claude Code window, `/famp-register alice`.
       - Open a Codex window (sandboxed), `register as bob`.
       - From alice's window: "send bob a message saying ship it". Confirm bob
         receives it — with NO broker-babysitting (you never started a broker by
         hand; the daemon is serving the sandboxed Codex client).
       - This proves the daemon (not auto-spawn) is what lets the sandboxed Codex
         window connect.

    4. **No-install bridge (DOC-02 — optional cross-check).**
       - `famp daemon uninstall`. In one unsandboxed terminal: `famp broker
         --no-idle-exit`. Confirm a fresh `register as <name>` from another window
         connects to that broker. Ctrl-C the terminal; confirm the broker dies with
         it (the documented bridge tradeoff).

    5. **Uninstall cleanup (DAEMON-04).**
       - `famp daemon uninstall` (twice — second run exits 0). Confirm
         `famp daemon status` then reports `NOT_INSTALLED` (exit 1) and the
         LaunchAgent is gone.

    Report any step where the README's wording, command, or expected output does
    not match what actually happened — those are doc bugs to fix before phase close.
  </how-to-verify>
  <why_human>
    Steps 1-2 and 5 load and mutate a real `com.famp.broker` LaunchAgent on the
    developer's machine (Phase 5 gated these behind FAMP_RUN_LAUNCHCTL_TESTS and
    marked them human-needed — VERIFICATION human-verification #1, #3). Step 3
    requires two live agent tools (Claude Code + Codex) that cannot be driven from a
    test harness. None of this is side-effect-free; it must not run autonomously.
  </why_human>
  <acceptance_criteria>
    - Running `famp daemon install` twice leaves exactly one `com.famp.broker` registration (no duplicate) — confirms README's "re-running is safe" idempotency claim live.
    - `famp daemon status` prints a RUNNING render matching the README form `state: RUNNING  pid=<n>  socket=<path>  broker build: 0.11.0` and exits 0.
    - From a fresh Codex window (`register as bob`), alice's message arrives with NO hand-started broker — the daemon served the sandboxed client (DOC-01 acceptance).
    - `famp daemon uninstall` run twice both exit 0; `famp daemon status` then reports `NOT_INSTALLED` (exit 1) and the LaunchAgent is gone.
    - Any README wording/command/output the human reports as not matching reality is corrected in README before phase close.
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if every README step matched reality, or describe each
    mismatch (which command / expected vs actual) so the docs can be corrected.
  </resume-signal>
  <verify>Human has typed "approved"; every README quickstart step matched live behavior (or reported mismatches were corrected in README).</verify>
  <done>Live daemon lifecycle + fresh-clone Claude+Codex E2E confirmed against the README; DOC-01/02/03 verified accurate against the binary; phase may close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README documented command → installed binary behavior | The phase's central risk: a documented command that misbehaves or whose quoted output is stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information disclosure (documentation inaccuracy) | Every documented command/exit-code/output quote | mitigate | Task 1 greps every side-effect-free quote against the live binary and corrects drift; Task 2 (human) confirms the side-effecting lifecycle + the fresh-clone Claude+Codex E2E. UNCERTAIN/human-only Phase 5 behaviors are documented as intended, never as CI-verified. |
</threat_model>

<verification>
- Task 1: `famp -V` == `famp 0.11.0`; daemon/broker `--help` match README; no idempotency overclaim; drift corrected.
- Task 2: human confirms live install/status(RUNNING)/restart/uninstall + fresh-clone Claude+Codex E2E; mismatches reported and fixed.
</verification>

<success_criteria>
- Every documented command/claim verified against the installed binary (auto for side-effect-free, human for side-effecting).
- DOC-01 fresh-clone macOS walkthrough (install → register Claude → register Codex) completes with no broker-babysitting.
- No README claim asserts an UNCERTAIN/human-only Phase 5 behavior as live-verified.
</success_criteria>

<output>
After completion, create
`.planning/phases/06-onboarding-cross-platform-docs/06-03-SUMMARY.md`.
</output>
