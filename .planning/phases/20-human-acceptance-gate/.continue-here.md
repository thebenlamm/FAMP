---
context: phase
phase: 20-human-acceptance-gate
task: 2
total_tasks: 2
status: blocked_human_action
last_updated: 2026-08-06T02:48:14.037Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was confirmed at the execution checkpoint.

- [ ] CONSTRAINT: External evidence integrity — this development workspace cannot substitute for DOC-07's untouched supported host. The clean-host rehearsal must run externally, and automation must not fill or approve its evidence.

**Do not proceed until the constraint is acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Local substitution | Calling this existing development machine, a container, or a cleaned-up host “fresh” would invalidate DOC-07. | blocking | Require a pre-install clean signal from `scripts/phase20-clean-box-preflight.sh` on an untouched supported host; contamination produces `invalid`, never cleanup-and-continue. |
| Fabricated acceptance | Repository tests can validate the evidence schema but cannot establish machine ownership, network independence, no coaching, or human comprehension. | blocking | Keep owner-attributed UTC evidence and the blocking human checkpoints; never synthesize `pass`. |

<current_state>
Phase 20, Plan 20-02, Task 2 of 2. Plan 20-01 is complete. Plan 20-02 Task 1 committed the candidate rehearsal record at `5ca0538`. The working tree was clean before this handoff was written. Plan 20-03 has not started.
</current_state>

<completed_work>

Completed:

- Phase 19 fully executed and independently verified.
- Phase 20 context, research, validation strategy, three serial plans, and independent plan-checker approval.
- Plan 20-01: follower guide, semantic documentation tests, clean-host preflight, evidence validator, and blank templates. Summary commit: `f848c9e`.
- Plan 20-02 Task 1: repository-local readiness suite passed and rehearsal candidate frozen. Commit: `5ca0538`.
- Frozen guide commit: `f848c9e747ad769a162408249a8dd084f34e2350`.
- Frozen guide SHA-256: `43f793114a9e51cf2a94c86dea47077cc1b800c2b344d81fa0bcc04eb6e1a01c`.
</completed_work>

<remaining_work>

- Plan 20-02 Task 2: run the genuine supported clean-host rehearsal and populate `20-REHEARSAL.md` with redacted, owner-attributed evidence.
- Validate the rehearsal with `cargo test -p famp --test phase20_evidence_schema` and `scripts/phase20-evidence-check.sh rehearsal .planning/phases/20-human-acceptance-gate/20-REHEARSAL.md`.
- If the rehearsal passes, finish Plan 20-02 and create `20-02-SUMMARY.md`.
- Plan 20-03: run the genuine second-person, different-network, no-VPN/no-key-copy UAT; capture both receiver-owned terminal task JSON records and all seven uncoached PAIR-05 paraphrases; then close verification.
</remaining_work>

<decisions_made>

- The follower follows one frozen, linear release-binary guide using `famp pair`; legacy manual key copying is excluded.
- Remote traffic is processed explicitly through Inbox and a terminal reply because Phase 19's Local-only wake gate is intentional and Phase 21 is not a dependency.
- Evidence has exactly three outcomes: `pass`, `product_or_guide_failure`, or `invalid`.
- Automation validates structure and redaction but never manufactures external or human facts.
</decisions_made>

<blockers>

- A genuine untouched macOS arm64/x86_64 or supported Linux x86_64 host is required for DOC-07. There is no valid local substitute.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current position and resume action.
2. `.planning/phases/20-human-acceptance-gate/20-02-PLAN.md` — exact checkpoint and completion contract.
3. `.planning/phases/20-human-acceptance-gate/20-REHEARSAL.md` — candidate record to populate; currently unresolved by design.
4. `docs/FOLLOWER-SETUP.md` — frozen guide used for the rehearsal.
5. `.planning/phases/20-human-acceptance-gate/20-CONTEXT.md` — locked D-01 through D-11 decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat sender exit zero as delivery; use the receiving owner's terminal `famp inspect tasks --id <task_id> --json`.
- Do not edit the guide mid-run and continue. Classify the attempt, repair/reset, and begin a new clean attempt.
- Do not include invite codes, keys, tokens, raw transcripts, or unredacted home paths in evidence.
- Do not begin Plan 20-03 before Plan 20-02 passes and has a summary.

## Infrastructure State

- No background server, watcher, or async external job is running as part of this pause.
- Branch: `main`.
- Before the handoff commit, HEAD was `5ca0538` and the branch was 15 commits ahead of `origin/main`.

<context>
Repository-local work is green. The next step is deliberately external and evidence-driven. Resume only after the clean-host record is populated, or use the resumed session to guide that external run without weakening the checkpoint.
</context>

<next_action>
Start with: on the untouched supported host, run `scripts/phase20-clean-box-preflight.sh` before installing anything. If it emits the clean signal, follow `docs/FOLLOWER-SETUP.md` exactly and populate `.planning/phases/20-human-acceptance-gate/20-REHEARSAL.md`. Then run the two validation commands and resume with `$gsd-resume-work`, reporting `rehearsal recorded` on success.
</next_action>
