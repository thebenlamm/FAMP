---
phase: 20-human-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/FOLLOWER-SETUP.md
  - scripts/phase20-clean-box-preflight.sh
  - scripts/phase20-evidence-check.sh
  - crates/famp/tests/follower_setup_doc_accuracy.rs
  - crates/famp/tests/phase20_clean_box_preflight.rs
  - crates/famp/tests/phase20_evidence_schema.rs
  - .planning/phases/20-human-acceptance-gate/20-REHEARSAL-TEMPLATE.md
  - .planning/phases/20-human-acceptance-gate/20-ACCEPTANCE-TEMPLATE.md
autonomous: true
requirements: [DOC-06, DOC-07, UAT-02]
must_haves:
  truths:
    - "D-01/D-02/D-04: A follower has one linear, copy-pasteable guide from release-binary installation through pairing, gateway readiness, explicit remote Inbox/task processing, and receiver-owned terminal proof without depending on Phase 21."
    - "D-03: A read-only preflight fails before mutation when Rust, FAMP binaries/state, or an existing broker/service contaminates the clean-host premise."
    - "D-08/D-09/D-10/D-11: Blank evidence templates encode owner-attributed receiver proof, redaction, two distinct task directions, seven-message comprehension, and exactly three outcomes without defaulting to pass."
    - "PAIR-05 human comprehension remains explicitly open; automation synchronizes the seven shipped messages but never claims to measure comprehension."
  artifacts:
    - path: "docs/FOLLOWER-SETUP.md"
      provides: "DOC-06 linear follower walkthrough"
    - path: "README.md"
      provides: "Public follower-setup entry point"
    - path: "scripts/phase20-clean-box-preflight.sh"
      provides: "DOC-07 pre-install clean-host assertion"
    - path: "crates/famp/tests/follower_setup_doc_accuracy.rs"
      provides: "semantic role, ordering, production-command, and message-synchronization gates"
    - path: ".planning/phases/20-human-acceptance-gate/20-REHEARSAL-TEMPLATE.md"
      provides: "blank DOC-07 evidence schema"
    - path: ".planning/phases/20-human-acceptance-gate/20-ACCEPTANCE-TEMPLATE.md"
      provides: "blank UAT-02 and PAIR-05 evidence schema"
    - path: "scripts/phase20-evidence-check.sh"
      provides: "runnable populated-record shape, outcome, and redaction gate"
  key_links:
    - from: "docs/FOLLOWER-SETUP.md"
      to: "crates/famp/tests/follower_setup_doc_accuracy.rs"
      via: "ordered semantic assertions against shipping CLI help and pairing messages"
      pattern: "famp pair|famp inspect tasks|famp inbox"
    - from: "scripts/phase20-clean-box-preflight.sh"
      to: "crates/famp/tests/phase20_clean_box_preflight.rs"
      via: "isolated pristine and contaminated HOME/PATH/FAMP_HOME cases"
      pattern: "phase20-clean-box-preflight"
    - from: "20-*-TEMPLATE.md"
      to: "crates/famp/tests/phase20_evidence_schema.rs"
      via: "required-field, status, placeholder, ownership, and redaction-shape validation"
      pattern: "pass|product_or_guide_failure|invalid"
  assumptions:
    - id: DOC-06
      category: unclassified
      status: unresolved
      flagged_unverified: true
      human_backstop: "The clean-host rehearsal and real follower must confirm the guide is understandable and complete."
    - id: DOC-07
      category: unclassified
      status: unresolved
      flagged_unverified: true
      human_backstop: "Only a real supported clean host can establish absence of prior Rust/FAMP state and successful release installation."
    - id: UAT-02
      category: unclassified
      status: unresolved
      flagged_unverified: true
      human_backstop: "Only the genuine second-person event can establish personhood, independence, no coaching, and receiver-owned terminal outcomes."
  prohibitions:
    - requirement_id: DOC-06
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "The guide MUST NOT minimize terminal-equivalent pairing consent or place the warning after the follower has entered the code."
    - requirement_id: DOC-07
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "The phase MUST NOT describe a container, source build, or previously used host as fresh-machine release-path validation."
    - requirement_id: UAT-02
      category: integrity
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "The phase MUST NOT classify a run as passing from sender-side success, coaching, same-network or VPN use, copied keys, or evidence reported by someone other than the receiving owner."
    - requirement_id: UAT-02
      category: privacy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "Checked-in evidence MUST NOT contain invite codes, private keys, authentication tokens, raw transcripts, or unredacted home paths."
---

<objective>
Create the production follower guide, semantic gates, clean-host preflight, and blank evidence infrastructure that make the scarce external runs deterministic and auditable.

Purpose: DOC-06 must be useful before a second person sees it, while DOC-07 and UAT-02 require honest external facts that repository automation can only prepare and validate—not invent.
Output: a linear guide, non-vacuous automated gates, and fail-closed evidence templates.
</objective>

<execution_context>
@/home/ben/Workspace/caprun/.codex/gsd-core/workflows/execute-plan.md
@/home/ben/Workspace/caprun/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-human-acceptance-gate/20-CONTEXT.md
@.planning/phases/20-human-acceptance-gate/20-RESEARCH.md
@.planning/phases/20-human-acceptance-gate/20-PATTERNS.md
@docs/PAIRING.md
@docs/QUARANTINE.md
@docs/GATEWAY-SETUP.md
@.planning/phases/19-auto-wake-gate/19-VERIFICATION.md
</context>

<assumption_delta_decision>
primary_noun: second person
decision: no-change
rationale: Phase 20 exercises another participant but does not generalize or persist a participant identity model.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: Ship the end-to-end follower path with semantic executable gates</name>
  <files>README.md, docs/FOLLOWER-SETUP.md, crates/famp/tests/follower_setup_doc_accuracy.rs</files>
  <read_first>README.md, docs/PAIRING.md, docs/QUARANTINE.md, docs/GATEWAY-SETUP.md, crates/famp/src/cli/send/mod.rs, crates/famp/src/cli/inbox/mod.rs, crates/famp/tests/gateway_setup_doc_accuracy.rs, crates/famp/tests/pair_cli.rs, .planning/phases/19-auto-wake-gate/19-VERIFICATION.md</read_first>
  <action>
    Write `docs/FOLLOWER-SETUP.md` as the single linear DOC-06 path per D-01 and D-02, and add a concise README follower link so it is the public entry point rather than a hidden phase artifact. Start from a supported unprepared machine and the published prebuilt installers; name Ben/inviter and follower/redeemer at every asymmetric step; use `famp pair invite`, `redeem`, and inviter `status`/pin confirmation; put the QUAR-15 terminal-equivalent consent warning immediately before redemption; require gateway restart after pins and fresh ready signals before sending. Use two distinct task IDs: Ben-to-follower inspected by the follower and follower-to-Ben inspected by Ben. State that send success is local acceptance only. Because Phase 19 intentionally blocks remote auto-wake, include the currently shipping explicit Inbox/task-processing steps that let each receiver handle remote work, then inspect `COMPLETED`, `FAILED`, or `CANCELLED`; do not depend on Phase 21. Link deeper operator docs only for troubleshooting, never as missing happy-path steps.

    Build `follower_setup_doc_accuracy.rs` from the `gateway_setup_doc_accuracy.rs` relation-and-order pattern per D-04. Normalize Markdown, assert installer-before-pairing, consent-immediately-before-redeem, inviter/redeemer direction, inviter pin plus restart before readiness, readiness before sends, explicit Inbox processing after remote delivery, and receiver-specific inspection after each direction. Launch the shipping `famp` binary for relevant `pair`, `send`, `inbox`, and `inspect tasks` help surfaces. Add negative semantic assertions against legacy blob exchange, shared VPN/key copying, source-build happy paths, sender success as remote proof, and Phase 21 dependency. Synchronize seven comprehension cards with the authored `PairingError` display values/constants from `pair_cli.rs`/pairing source while stating that PAIR-05's human half remains open per D-11.
  </action>
  <verify>
    <automated>cargo test -p famp --test follower_setup_doc_accuracy</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` links to `docs/FOLLOWER-SETUP.md` as the public follower entry point.
    - The guide orders release installation, pairing consent/redeem, inviter pin, gateway restart/readiness, two opposite-direction tasks, explicit receiver processing, and receiver-owned terminal inspection.
    - The explicit CLI processing path is `famp inbox list --as &lt;receiver&gt;`, followed by `famp send --as &lt;receiver&gt; --to &lt;sender-principal&gt; --task &lt;task-id&gt; --body &lt;result&gt; --terminal`, followed by `famp inspect tasks --id &lt;task-id&gt; --json`; the host-agent alternative names `famp_inbox` then `famp_send` in reply mode with the same task ID and default terminal close.
    - The semantic test launches shipping help surfaces and fails if roles/directions/order are inverted, sender success is treated as receiver proof, or Phase 21 is required.
  </acceptance_criteria>
  <done>D-01, D-02, D-04, D-08, and the Phase 19 boundary are encoded in one runnable production path whose semantic test fails on inverted roles/order or substituted proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a read-only, fail-closed clean-host preflight with falsification cases</name>
  <files>scripts/phase20-clean-box-preflight.sh, crates/famp/tests/phase20_clean_box_preflight.rs</files>
  <read_first>.planning/phases/20-human-acceptance-gate/20-CONTEXT.md, README.md, crates/famp/tests/installer_checksum_gate.rs, scripts/release-artifact-source-gate.sh</read_first>
  <behavior>
    - "A controlled pristine supported environment reports OS, architecture, UTC timestamp, redacted paths, and exits successfully without mutation."
    - "Each contamination class—rustc, cargo, famp, famp-gateway, FAMP_HOME, default FAMP state, broker/socket, or service—causes a non-zero exit with an actionable named diagnostic."
    - "No output reveals the absolute controlled home path, and a zero-check run cannot pass vacuously."
  </behavior>
  <action>
    Implement the D-03 preflight using POSIX shell strict mode and the repository's cumulative-failure pattern. It runs before any installer and only observes: OS/architecture, UTC time, PATH resolution of Rust/FAMP binaries, `FAMP_HOME`, default FAMP state, broker/socket, and supported service state. It must not clean, stop, uninstall, create, or repair anything. Emit one diagnostic per contamination plus a final unambiguous clean signal, redact home prefixes, and reject unsupported OS/architecture rather than silently treating them as clean.

    In the Rust integration test, run the script through `sh` with isolated `HOME`, `PATH`, and `FAMP_HOME` under `TempDir`; never touch the developer's state. Establish a pristine control and a separate negative case for every named contamination class. Assert status and diagnostic text, assert redaction, and add a mutation check that the controlled filesystem snapshot is unchanged. This automation validates the predicate's discrimination; it does not claim DOC-07's real external-host fact.
  </action>
  <verify>
    <automated>cargo test -p famp --test phase20_clean_box_preflight</automated>
  </verify>
  <acceptance_criteria>
    - A pristine isolated fixture exits zero and emits supported OS, architecture, UTC time, redacted paths, and one clean signal.
    - Independent fixtures for `rustc`, `cargo`, `famp`, `famp-gateway`, `FAMP_HOME`, default state, broker/socket, service, and unsupported platform each exit non-zero with a named diagnostic.
    - Before/after filesystem snapshots are identical and output contains no absolute controlled-home path.
  </acceptance_criteria>
  <done>D-03 has a read-only pre-install assertion with a passing control, named falsification cases, and no route for a contaminated or unsupported host to look valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define fail-closed rehearsal and acceptance evidence contracts</name>
  <files>.planning/phases/20-human-acceptance-gate/20-REHEARSAL-TEMPLATE.md, .planning/phases/20-human-acceptance-gate/20-ACCEPTANCE-TEMPLATE.md, scripts/phase20-evidence-check.sh, crates/famp/tests/phase20_evidence_schema.rs</files>
  <read_first>.planning/phases/20-human-acceptance-gate/20-CONTEXT.md, .planning/phases/20-human-acceptance-gate/20-RESEARCH.md, crates/famp/src/pairing/mod.rs, crates/famp/tests/pair_cli.rs, docs/PAIRING.md</read_first>
  <behavior>
    - "Templates contain unmistakable placeholders and no default pass result; an incomplete populated record is rejected."
    - "Exactly one final outcome is allowed: pass, product_or_guide_failure, or invalid."
    - "A pass requires every hard precondition, evidence owner, UTC timestamp, redacted evidence cell, two distinct task directions, and receiver terminal state."
    - "The acceptance schema requires the seven synchronized pairing messages, first-response paraphrases, no-coaching question log, and guide commit/digest while leaving the human judgment open."
    - "The acceptance schema requires separate Ben and follower rows whose owners and UTC capture times accompany redacted OS/architecture and installed `famp`/`famp-gateway` versions."
    - "Secret-pattern or unredacted-path findings reject pass and require human redaction review."
  </behavior>
  <action>
    Create separate blank rehearsal and acceptance templates per D-09 and D-10. Each evidence row names criterion, owner, capture command/attestation, UTC timestamp, redacted evidence, and result. Rehearsal additionally requires the pre-install clean result, supported OS/architecture, installed release versions, pairing/restart readiness, two distinct directions, and receiver-owned terminal JSON. The acceptance template must separately require, for both Ben's machine and the follower's machine, owner-attributed and UTC-timestamped redacted evidence of OS/architecture plus the installed `famp` and `famp-gateway` versions; generic supported-machine attestations do not satisfy these D-09 fields. Acceptance additionally requires independently administered machines/networks and no-VPN/no-key-copy attestations, a complete question/no-coaching log under D-05/D-06, the tested guide commit and digest, and all seven non-mutating D-11 message reviews with first paraphrase plus judgment. State that invalid runs require a fully clean rerun and product/guide failures require repair/reset rather than coaching-through.

    Implement `scripts/phase20-evidence-check.sh <rehearsal|acceptance> <record>` as the runnable fail-closed validator, plus a repository-local Rust test that exercises it. The validator must prove pristine templates are intentionally incomplete and reject populated acceptance records when either machine lacks its explicit owner, UTC timestamp, redacted OS/architecture, installed `famp` version, or installed `famp-gateway` version. It must also reject missing owners/fields elsewhere, duplicated task IDs, nonterminal receiver JSON, multiple/unknown outcomes, unresolved placeholders, or redaction findings, and accept only a fully complete synthetic fixture containing both machines' D-09 fields. Keep real `20-REHEARSAL.md` and `20-ACCEPTANCE.md` absent; this task must never fabricate them or populate human fields.
  </action>
  <verify>
    <automated>cargo test -p famp --test phase20_evidence_schema && ! scripts/phase20-evidence-check.sh rehearsal .planning/phases/20-human-acceptance-gate/20-REHEARSAL-TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Both templates retain visible placeholders and an unresolved outcome; neither can validate as a pass.
    - The validator accepts exactly one complete synthetic record and rejects missing ownership/time/evidence, duplicate task IDs, nonterminal receiver JSON, unknown/multiple outcomes, placeholders, and redaction findings.
    - The acceptance template and validator require owner-attributed, UTC-timestamped, redacted OS/architecture plus installed `famp` and `famp-gateway` versions separately for both Ben and follower machines.
    - The acceptance template contains all seven synchronized pairing messages, first-paraphrase fields, question log, no-coaching attestations, and guide commit/digest fields.
    - No populated `20-REHEARSAL.md` or `20-ACCEPTANCE.md` is produced by this task.
  </acceptance_criteria>
  <done>D-05 through D-11 have a non-vacuous evidence contract, while DOC-07, UAT-02, and PAIR-05 remain visibly unresolved until their external owners provide real observations.</done>
</task>

</tasks>

## Artifacts this phase produces

- `README.md` — public link to the follower guide.
- `docs/FOLLOWER-SETUP.md` — linear release-install, pairing, explicit processing, and bidirectional proof procedure.
- `scripts/phase20-clean-box-preflight.sh` — read-only external-host preflight.
- `scripts/phase20-evidence-check.sh` — fail-closed evidence record validator.
- `crates/famp/tests/follower_setup_doc_accuracy.rs` — semantic guide and shipping-command gate.
- `crates/famp/tests/phase20_clean_box_preflight.rs` — clean/contaminated preflight falsification suite.
- `crates/famp/tests/phase20_evidence_schema.rs` — template and populated-record contract suite.
- `.planning/phases/20-human-acceptance-gate/20-REHEARSAL-TEMPLATE.md` — blank rehearsal ledger.
- `.planning/phases/20-human-acceptance-gate/20-ACCEPTANCE-TEMPLATE.md` — blank real-person acceptance ledger.

<threat_model>
## Security Policy

OWASP ASVS Level 1 applies. Any unresolved critical or high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository guide -> follower | Human follows security-sensitive commands and consent language without coaching. |
| Host environment -> preflight record | Potentially stale or unsupported local state crosses into the clean-host claim. |
| Human/machine evidence -> git | Untrusted evidence may be spoofed, incomplete, or secret-bearing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | follower guide semantics | high | mitigate | Ordered role/direction/consent/readiness/proof tests in `follower_setup_doc_accuracy.rs` per D-04. |
| T-20-02 | Spoofing | clean-host claim | high | mitigate | Pre-install read-only predicate with pristine control and per-contaminant falsification tests per D-03. |
| T-20-03 | Information Disclosure | evidence templates | high | mitigate | Whitelisted fields, explicit forbidden-data rules, redaction scan, and mandatory human review per D-09. |
| T-20-04 | Repudiation | receiver proof | high | mitigate | Owner and capture-method fields plus distinct receiver-produced terminal JSON per D-08. |
| T-20-05 | Denial of Service | pairing attempt budget | medium | mitigate | Synchronize non-mutating comprehension cards; never induce live faults per D-11. |
| T-20-06 | Elevation of Privilege | remote auto-wake | high | mitigate | Guide requires explicit remote Inbox/task processing and preserves Phase 19's Local-only wake boundary. |
</threat_model>

<verification>
Run `cargo test -p famp --test follower_setup_doc_accuracy --test phase20_clean_box_preflight --test phase20_evidence_schema`, then `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings`. Confirm the evidence checker rejects both blank templates and no real rehearsal or acceptance record was created.
</verification>

<success_criteria>
- DOC-06 has a production guide and semantic relation/order gates.
- DOC-07 has a preflight and evidence schema but remains externally unresolved.
- UAT-02 and PAIR-05 have explicit human backstops and no fabricated evidence.
- All D-01 through D-11 decisions are cited and encoded across guide, gates, and templates.
- No new package, API integration, UI, or schema change is introduced.
</success_criteria>

<output>
Create `.planning/phases/20-human-acceptance-gate/20-01-SUMMARY.md` when done.
</output>
