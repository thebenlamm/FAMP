---
phase: 19-auto-wake-gate
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - crates/famp/tests/auto_wake_gate.rs
  - crates/famp/tests/quarantine_surfaces.rs
autonomous: true
requirements: [QUAR-12, QUAR-13, QUAR-14]
must_haves:
  truths:
    - "A real Gateway-origin socket sender reports woken=false and a parked Await remains pending."
    - "A later Local-origin socket sender wakes the same Await, which returns only the Local record."
    - "The held Gateway record remains visible through an explicit Inbox read from zero with Origin::Gateway."
    - "No test can pass by filtering a client-side Await result after the broker already consumed it."
  artifacts:
    - path: "crates/famp/tests/auto_wake_gate.rs"
      provides: "Single-broker real-socket behavioral proof of remote-held/local-wake/Inbox-visible"
      contains: "remote_is_held_until_local_wakes_and_remains_in_inbox"
    - path: "crates/famp/tests/quarantine_surfaces.rs"
      provides: "Phase 14 rendering suite with obsolete remote-Await-wakes claim removed"
  key_links:
    - from: "crates/famp/tests/auto_wake_gate.rs"
      to: "crates/famp-bus/src/broker/handle.rs"
      via: "Gateway SendOk.woken=false and pending Await before any rendered output"
      pattern: "woken"
    - from: "crates/famp/tests/auto_wake_gate.rs"
      to: "crates/famp/src/cli/inbox/list.rs"
      via: "explicit Inbox after Await returns the same remote marker with Gateway origin"
      pattern: "inbox_run_at_structured"
---

<objective>
Prove QUAR-12 through QUAR-14 over a real broker socket in one production-shaped scenario, and replace the Phase 14 test whose expected Gateway Await wake is intentionally invalidated.

Purpose: A client-side cursor-consuming filter could mimic rendered no-wake output. The test must observe broker SendOk state and the still-pending Await before a Local send, then prove durability through explicit Inbox on the same broker and mailbox.
Output: New `auto_wake_gate` integration target plus a regression-clean quarantine rendering suite with truthful expectations.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.codex/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-auto-wake-gate/19-RESEARCH.md
@.planning/phases/19-auto-wake-gate/19-PATTERNS.md
@.planning/phases/19-auto-wake-gate/19-VALIDATION.md
@.planning/phases/19-auto-wake-gate/19-01-SUMMARY.md
@.planning/phases/14-inbound-content-is-data-quarantine/14-02-SUMMARY.md

<interfaces>
- `BusClient::register(... origin: Some(Origin::Gateway))` provides the production wire provenance fixture.
- `await_cmd::run_at_structured(&sock, AwaitArgs)` is the CLI Await seam; the test checks task completion state, not only returned rendering.
- `inbox::list::run_at_structured(&sock, ListArgs)` performs the explicit human-read path and returns stamped records.
- `BusReply::SendOk.delivered[*].woken` is the broker-side behavioral signal required to rule out client filtering.
</interfaces>
</context>

## Artifacts this phase produces

- New file: `crates/famp/tests/auto_wake_gate.rs`.
- New test: `remote_is_held_until_local_wakes_and_remains_in_inbox`.
- New CLI behavior proof: parked `famp await` ignores Gateway traffic, wakes on the subsequent Local record, and `famp inbox list` can still retrieve the Gateway record.
- Removed conflicting test behavior: `quarantine_surfaces::await_marks_gateway_origin` no longer claims Gateway content satisfies Await.
- Updated test-module truth list in `crates/famp/tests/quarantine_surfaces.rs`; existing local Await, explicit Inbox/wait-reply, register-tail, channel-log, and body-free wake tests remain.

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-02-01: Prove the broker gate through one real-socket sequence</name>
  <files>crates/famp/tests/auto_wake_gate.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md — QUAR-12, QUAR-13, and all three QUAR-14 clauses.
    - .planning/phases/19-auto-wake-gate/19-RESEARCH.md — real-socket acceptance shape and anti-client-filter requirement.
    - .planning/phases/19-auto-wake-gate/19-PATTERNS.md — exact harness/import/sequence analog.
    - .planning/phases/19-auto-wake-gate/19-VALIDATION.md — required target command and task ID.
    - crates/famp/tests/auto_wake_gate.rs — create this file; confirm it is absent before writing.
    - crates/famp/tests/quarantine_surfaces.rs — broker/register/marker/valid-envelope harness analog.
    - crates/famp/src/cli/await_cmd/mod.rs — `AwaitArgs` and structured outcome contract.
    - crates/famp/src/cli/inbox/list.rs — `ListArgs` and structured explicit-read contract.
    - crates/famp/src/bus_client/mod.rs — direct client registration and Send reply contract.
  </read_first>
  <behavior>
    - One broker and one Bob mailbox are used for every assertion.
    - Gateway marker R produces `SendOk.woken == false`; a bounded delay proves the already-parked Await task is unfinished.
    - Local marker L produces a completed Await containing exactly L with Local origin and no R.
    - Explicit Inbox from zero subsequently finds R by runtime-generated ID/marker with Gateway origin.
  </behavior>
  <action>
Create the Unix-only integration test using the existing spawned broker, registration wait, runtime, UUID marker, and fully decodable `audit_log` envelope patterns. Register Bob through the normal local CLI and park `await_run_at_structured` before sending. Register the remote sender through a direct `BusClient` with `origin: Some(Origin::Gateway)`, send runtime marker R, assert the DM SendOk row is successful and `woken == false`, and after a bounded short delay assert the Await task is not finished. Register a normal local sender through the existing CLI/proxy pattern, send marker L, and assert the same Await task returns exactly L with Local origin. Finally call `inbox_run_at_structured` from zero for Bob, locate R by its runtime-generated ID/marker, and assert Gateway origin. Use valid typed envelopes throughout; malformed input could be skipped and create a false green. Do not add a client-side filter or use separate brokers/mailboxes for the three clauses.
  </action>
  <acceptance_criteria>
    - The test asserts `SendOk.woken == false` before inspecting any Await result.
    - The test proves the parked task stays unfinished after remote send, then finishes only after Local send.
    - Await returns one Local record and explicit Inbox returns the earlier Gateway record from the same mailbox.
    - All markers and envelope IDs are generated at runtime, and all envelopes decode as valid `audit_log` messages.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p famp --test auto_wake_gate remote_is_held_until_local_wakes_and_remains_in_inbox -- --exact &amp;&amp; cargo test -p famp --test auto_wake_gate &amp;&amp; just check-no-tokio-in-bus</automated>
  </verify>
  <done>The socket-level proof cannot be satisfied by client suppression: broker output says no remote wake, the waiter remains parked, Local wakes it, and explicit Inbox still contains the remote record.</done>
</task>

<task type="auto">
  <name>Task 19-02-02: Replace the conflicting Phase 14 Await expectation</name>
  <files>crates/famp/tests/quarantine_surfaces.rs</files>
  <read_first>
    - .planning/phases/19-auto-wake-gate/19-RESEARCH.md — Pitfall 4 and State of the Art.
    - .planning/phases/19-auto-wake-gate/19-PATTERNS.md — quarantine-surfaces division of responsibility.
    - .planning/phases/19-auto-wake-gate/19-VALIDATION.md — required regression command.
    - crates/famp/tests/quarantine_surfaces.rs — modified file, especially module truth list, `await_marks_gateway_origin`, local Await control, and inbox-first wait-reply test.
    - crates/famp/tests/auto_wake_gate.rs — Task 19-02-01's replacement behavioral proof.
  </read_first>
  <action>
Remove the obsolete `await_marks_gateway_origin` test and rewrite the module-level truth list so it no longer says Gateway content satisfies `famp_await`. Point the module description to `auto_wake_gate` as the behavioral replacement. Retain `await_leaves_local_origin_verbatim` as the Local control and retain Gateway rendering coverage through explicit human-read and inbox-first `wait-reply` paths. Do not add an Await bypass or weaken Phase 14's per-record rendering assertions merely to keep the old behavior alive.
  </action>
  <acceptance_criteria>
    - No test or module comment claims a Gateway-origin message completes parked Await.
    - `await_leaves_local_origin_verbatim`, `wait_reply_marks_gateway_origin`, channel-log, and wake-payload body suppression remain present and green.
    - The new `auto_wake_gate` target is the named replacement for remote Await behavior.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p famp --test quarantine_surfaces &amp;&amp; cargo test -p famp --test auto_wake_gate &amp;&amp; cargo fmt --all -- --check &amp;&amp; just check-no-tokio-in-bus</automated>
  </verify>
  <done>The Phase 14 rendering suite remains comprehensive without asserting the auto-wake behavior Phase 19 deliberately removes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → real UDS broker | Direct Gateway registration and normal Local registration exercise broker authority over the actual wire. |
| Await → explicit Inbox | The same durable mailbox is observed through separate automatic and human-initiated cursor paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-05 | Tampering | behavioral proof | high | mitigate | Assert broker SendOk.woken and pending task before rendering so client-side suppression cannot pass. |
| T-19-06 | Denial of Service | remote-before-local ordering | medium | mitigate | Use one mailbox sequence R then L and require L to complete Await despite R preceding it. |
| T-19-07 | Information Disclosure | test diagnostics | low | accept | Runtime markers are synthetic and broker subprocess output remains suppressed; no real peer content or keys enter fixtures. |
</threat_model>

<verification>
- `cargo test -p famp --test auto_wake_gate`
- `cargo test -p famp --test quarantine_surfaces`
- `cargo fmt --all -- --check`
- `just check-no-tokio-in-bus`
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Remote no-wake and Local unchanged over shipping surfaces | 19-02 | COVERED | One real socket sequence.
REQ | QUAR-12 | Gateway held, Local wakes | 19-02 | COVERED | Same Await task and broker.
REQ | QUAR-13 | Broker enforcement, not CLI filter | 19-02 | COVERED | SendOk and pending-task assertions precede CLI result.
REQ | QUAR-14 | Held record remains explicitly readable | 19-02 | COVERED | Inbox from zero finds R with Gateway origin.
RESEARCH | — | Real socket, valid envelopes, conflicting test replacement | 19-02 | COVERED | Both tasks map directly.
CONTEXT | — | No Phase 19 CONTEXT.md exists | — | N/A | No locked or deferred decisions.
</source_audit>

<success_criteria>
- The new integration test proves all three QUAR-14 clauses using one broker/mailbox.
- Broker-side signals make a client-only filter incapable of passing the test.
- The obsolete Phase 14 Gateway-Await-wakes assertion is removed, while rendering regressions stay green.
</success_criteria>

<output>
Create `.planning/phases/19-auto-wake-gate/19-02-SUMMARY.md` when done.
</output>
