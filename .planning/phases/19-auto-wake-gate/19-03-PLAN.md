---
phase: 19-auto-wake-gate
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - docs/QUARANTINE.md
  - docs/CONFIGURATION.md
  - README.md
  - crates/famp/src/cli/mcp/server.rs
  - CLAUDE.md
  - docs/CLAUDE-CODE-CONTEXT-GUIDE.md
  - docs/CHANNEL-DISCUSSION-GUIDE.md
  - docs/HOST-WAKE-ADAPTERS.md
  - .planning/phases/19-auto-wake-gate/19-VALIDATION.md
autonomous: true
requirements: [QUAR-12, QUAR-13, QUAR-14, QUAR-15]
must_haves:
  truths:
    - "Every live MCP descriptor and operator guide says only Local records satisfy Await/listen-mode auto-wake; Gateway and Unknown remain available to explicit Inbox."
    - "Documentation does not claim remote mailbox admission, host re-entry, steering, laundering, or mailbox growth is prevented."
    - "wait-reply's inbox-first explicit read and local-only parked-Await fallback are described distinctly."
    - "The existing pairing artifact still carries the single-authored QUAR-15 warning before the five-word code."
  artifacts:
    - path: "docs/QUARANTINE.md"
      provides: "Narrow auto-wake boundary, limitations, claim-to-test map, and unchanged consent warning"
    - path: "docs/CONFIGURATION.md"
      provides: "Operator contract for await, wait-reply timing, and explicit inbox"
    - path: "README.md"
      provides: "Accurate CLI/listen-mode summaries"
    - path: "crates/famp/src/cli/mcp/server.rs"
      provides: "Accurate live famp_await tool descriptor served to MCP clients"
      contains: "Only Local-origin records satisfy parked famp_await"
    - path: "CLAUDE.md"
      provides: "Accurate project-level listen-mode contract"
    - path: "docs/CLAUDE-CODE-CONTEXT-GUIDE.md"
      provides: "Accurate manual Await and background listen-mode retrieval flows"
    - path: "docs/CHANNEL-DISCUSSION-GUIDE.md"
      provides: "Accurate Local-only channel auto-wake and explicit retrieval guidance"
    - path: "docs/HOST-WAKE-ADAPTERS.md"
      provides: "Accurate host-neutral wake-adapter contract"
    - path: ".planning/phases/19-auto-wake-gate/19-VALIDATION.md"
      provides: "Reconciled task IDs, commands, measured runtimes, and Nyquist sign-off"
  key_links:
    - from: "docs/QUARANTINE.md"
      to: "crates/famp/tests/auto_wake_gate.rs"
      via: "claim-to-test map names the behavioral triad"
      pattern: "auto_wake_gate"
    - from: "docs/QUARANTINE.md"
      to: "crates/famp/src/pairing/consent.rs"
      via: "existing exact CONSENT_WARNING bytes stay single-authored and drift-tested"
      pattern: "CONSENT_WARNING"
    - from: "crates/famp/src/cli/mcp/server.rs"
      to: "~/.cargo/bin/famp"
      via: "just install refreshes the live MCP descriptor consumed by agent sessions"
      pattern: "famp_await"
---

<objective>
Make every operator-facing claim match the shipped local-only Await boundary, preserve the Phase 18 pairing consent guarantee by regression, and close the phase validation ledger with measured commands.

Purpose: The security value depends on describing the exact automatic-ingestion boundary. Remote records are still appended and explicitly readable, local re-authorship still launders origin, and host re-entry is downstream of Broker Await. QUAR-15 is already implemented and must be claimed without churn.
Output: Updated live MCP descriptor and operator-guide truth, passing QUAR-15 regressions, and a reconciled Nyquist validation contract.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.codex/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-auto-wake-gate/19-RESEARCH.md
@.planning/phases/19-auto-wake-gate/19-PATTERNS.md
@.planning/phases/19-auto-wake-gate/19-VALIDATION.md
@.planning/phases/19-auto-wake-gate/19-01-SUMMARY.md
@.planning/phases/19-auto-wake-gate/19-02-SUMMARY.md
@.planning/phases/14-inbound-content-is-data-quarantine/14-05-SUMMARY.md
@.planning/phases/18-cross-person-trust-bootstrap-pairing/18-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Updated file: `docs/QUARANTINE.md`, including the new `auto_wake_gate` claim-to-test reference and the exact boundary/limitations.
- Updated file: `docs/CONFIGURATION.md`, including local-only `famp await`, explicit Inbox retention, and wait-reply timing asymmetry.
- Updated file: `README.md`, including local-origin wording in CLI and listen-mode summaries plus explicit remote retrieval guidance.
- Updated file: `crates/famp/src/cli/mcp/server.rs`, whose live `famp_await` descriptor states Local-only automatic delivery and explicit `famp_inbox` retrieval for Gateway/Unknown records.
- Updated files: `CLAUDE.md`, `docs/CLAUDE-CODE-CONTEXT-GUIDE.md`, `docs/CHANNEL-DISCUSSION-GUIDE.md`, and `docs/HOST-WAKE-ADAPTERS.md`, with Local-only listen-mode/host-wake semantics and explicit remote Inbox recovery.
- Updated file: `.planning/phases/19-auto-wake-gate/19-VALIDATION.md`, with final task IDs 19-01-01 through 19-03-02, exact commands, measured runtimes, statuses, `wave_0_complete: true`, and `nyquist_compliant: true` only after all required commands pass.
- No new pairing symbol, function, field, test, or warning string. Existing `CONSENT_WARNING`, `consent_warning_matches_quarantine_doc`, and `artifact_code_offset_greater_than_consent_and_install_lines` are regression-verified unchanged.

<tasks>

<task type="auto">
  <name>Task 19-03-01: Document the exact automatic-ingestion boundary</name>
  <files>docs/QUARANTINE.md, docs/CONFIGURATION.md, README.md, crates/famp/src/cli/mcp/server.rs, CLAUDE.md, docs/CLAUDE-CODE-CONTEXT-GUIDE.md, docs/CHANNEL-DISCUSSION-GUIDE.md, docs/HOST-WAKE-ADAPTERS.md</files>
  <read_first>
    - .planning/ROADMAP.md — all five Phase 19 success criteria and the held-by-default road not taken.
    - .planning/REQUIREMENTS.md — QUAR-12 through QUAR-15 wording.
    - .planning/phases/19-auto-wake-gate/19-RESEARCH.md — pitfalls 4-7 and narrow claim requirements.
    - .planning/phases/19-auto-wake-gate/19-PATTERNS.md — exact documentation sections and replacement claims.
    - docs/QUARANTINE.md — modified file, existing threat model, one-hop limitation, mechanical guarantees, and consent section.
    - docs/CONFIGURATION.md — modified file, await/wait-reply/inbox command sections.
    - README.md — modified file, CLI summaries and listen-mode sections.
    - crates/famp/src/cli/mcp/server.rs — modified live MCP descriptor; update only `famp_await` truth and retain the twelve-tool registry contract.
    - CLAUDE.md — modified project-level Listen Mode section and mandatory `just install` convention for MCP server changes.
    - docs/CLAUDE-CODE-CONTEXT-GUIDE.md — modified listen-mode/manual-Await flows and retrieval guidance.
    - docs/CHANNEL-DISCUSSION-GUIDE.md — modified channel recovery and auto-wake/N^2 guidance.
    - docs/HOST-WAKE-ADAPTERS.md — modified host-neutral core and Stop-hook flow descriptions.
    - crates/famp/tests/auto_wake_gate.rs — actual behavioral proof produced by the completed Plan 19-02 dependency.
    - crates/famp/src/pairing/consent.rs — exact QUAR-15 warning source; do not duplicate or edit it.
  </read_first>
  <action>
Update `docs/QUARANTINE.md` to replace the obsolete remote-auto-wake text and `await_marks_gateway_origin` claim with the exact shipped boundary: Gateway and Unknown records are appended but cannot satisfy Broker Await; Local records retain automatic wake; explicit Inbox still sees remote records. Name `auto_wake_gate::remote_is_held_until_local_wakes_and_remains_in_inbox` as the end-to-end proof. Keep the consent section and exact warning bytes unchanged. State explicitly that the gate is not held-by-default ingress, does not prevent remote mailbox growth, does not stop explicit reads, does not preserve provenance through local re-authorship, and does not prove host UI/model re-entry or prevent other re-entry mechanisms.

Add the local-only Await contract directly below the Await flags in `docs/CONFIGURATION.md`. Explain that wait-reply first performs an explicit Inbox scan—which may return a remote reply already present—then its parked Await fallback follows the local-only gate. Update README command and listen-mode prose to Local-origin wake language, with `famp inbox`/`famp_inbox` as the explicit remote retrieval path. In `docs/QUARANTINE.md`, `docs/CONFIGURATION.md`, `README.md`, `CLAUDE.md`, `docs/CLAUDE-CODE-CONTEXT-GUIDE.md`, `docs/CHANNEL-DISCUSSION-GUIDE.md`, and `docs/HOST-WAKE-ADAPTERS.md`, include this stable contract sentence verbatim: "Only Local-origin records satisfy a parked `famp await`; Gateway- and Unknown-origin records remain available through explicit Inbox reads." Keep every security claim scoped to automatic Await wake, and phrase limitations affirmatively without quoting rejected security promises.

Update only the `famp_await` descriptor text in `crates/famp/src/cli/mcp/server.rs` for descriptor truth, preserving its schema and runtime behavior. Open with "Block until one or more new Local-origin messages arrive." and include this MCP-native sentence verbatim: "Only Local-origin records satisfy parked famp_await; Gateway- and Unknown-origin records remain available through explicit famp_inbox reads." Preserve task-filter and timeout guidance, but state that real-time delivery applies only to eligible Local-origin records. In `CLAUDE.md`, use "Agents auto-wake on Local-origin inbound messages without an explicit flag:" and the exact sentence "When a Local-origin message arrives, Claude wakes automatically and receives:" before the scrubbed notification example. In the context guide's Flow A diagram, use the exact step `  → Local-origin message arrives`, and explain that manual `famp_await` likewise returns only Local-origin records while remote records require explicit Inbox. In the channel guide, change the recovery contract to "`famp_await` delivers one eligible Local-origin message at a time."; use the exact statement "Listen mode (`listen:true`, the MCP default) fires only on **Local-origin channel messages**."; and direct Gateway/Unknown recovery to `famp_channel_log` plus explicit Inbox reads. In the host-adapter guide, use "On each Local-origin batch it prints **one scrubbed stdout line**" in the core description and "On a Local-origin message, emits" in the Stop-hook sequence; state that Gateway/Unknown batches do not wake adapters and remain explicitly readable from Inbox. These are wording/descriptor corrections only: do not change schemas, command behavior, host scripts, hooks, or broker policy in this task.
  </action>
  <acceptance_criteria>
    - Each of the seven modified Markdown surfaces contains the exact stable contract sentence for Local-only parked Await and explicit Gateway/Unknown Inbox visibility; the MCP descriptor contains its exact MCP-native equivalent.
    - The MCP Await descriptor, project Listen Mode section, context-flow diagram, channel N^2 section, and host-adapter core each contain their specified Local-origin wording and none retains its stale any-origin sentence.
    - `docs/QUARANTINE.md` names the new integration proof and no longer names `await_marks_gateway_origin` as live behavior.
    - The consent warning section remains byte-identical to `CONSENT_WARNING`.
    - The docs explicitly retain the limitations for mailbox ingress/growth, explicit reads, laundering, and host re-entry.
    - Across all eight operator-facing surfaces, no affirmative sentence claims that the gate blocks ingress, makes remote content safe, prevents steering or laundering, prevents mailbox growth, prevents host re-entry, or lets Gateway/Unknown traffic wake parked Await/listen mode.
    - The focused MCP descriptor test, Phase 19 integration regressions, no-tokio gate, and `just install` all pass; the installed MCP binary is refreshed as required by `CLAUDE.md`.
  </acceptance_criteria>
  <verify>
    <automated>set -e; contract='Only Local-origin records satisfy a parked `famp await`; Gateway- and Unknown-origin records remain available through explicit Inbox reads.'; for doc in docs/QUARANTINE.md docs/CONFIGURATION.md README.md CLAUDE.md docs/CLAUDE-CODE-CONTEXT-GUIDE.md docs/CHANNEL-DISCUSSION-GUIDE.md docs/HOST-WAKE-ADAPTERS.md; do rg -Fq "$contract" "$doc"; done; rg -Fq 'Block until one or more new Local-origin messages arrive.' crates/famp/src/cli/mcp/server.rs; rg -Fq 'Only Local-origin records satisfy parked famp_await; Gateway- and Unknown-origin records remain available through explicit famp_inbox reads.' crates/famp/src/cli/mcp/server.rs; rg -Fq 'Agents auto-wake on Local-origin inbound messages without an explicit flag:' CLAUDE.md; rg -Fq 'When a Local-origin message arrives, Claude wakes automatically and receives:' CLAUDE.md; rg -Fq '  → Local-origin message arrives' docs/CLAUDE-CODE-CONTEXT-GUIDE.md; rg -Fq '`famp_await` delivers one eligible Local-origin message at a time.' docs/CHANNEL-DISCUSSION-GUIDE.md; rg -Fq 'fires only on **Local-origin channel messages**' docs/CHANNEL-DISCUSSION-GUIDE.md; rg -Fq 'On each Local-origin batch it prints **one scrubbed stdout line**' docs/HOST-WAKE-ADAPTERS.md; rg -Fq 'On a Local-origin message, emits' docs/HOST-WAKE-ADAPTERS.md; ! rg -Fq 'Block until one or more new messages arrive.' crates/famp/src/cli/mcp/server.rs; ! rg -Fq 'famp_await delivers every message as it arrives' crates/famp/src/cli/mcp/server.rs; ! rg -Fq 'auto-wake on inbound messages' CLAUDE.md; ! rg -Fq 'When a message arrives, Claude wakes automatically and receives:' CLAUDE.md; ! rg -Fq '  → message arrives' docs/CLAUDE-CODE-CONTEXT-GUIDE.md; ! rg -Fq '`famp_await` delivers one message at a time.' docs/CHANNEL-DISCUSSION-GUIDE.md; ! rg -Fq 'fires on **every channel message**' docs/CHANNEL-DISCUSSION-GUIDE.md; ! rg -Fq 'On each inbound batch' docs/HOST-WAKE-ADAPTERS.md; ! rg -Fq 'On message, emits' docs/HOST-WAKE-ADAPTERS.md; for doc in docs/QUARANTINE.md docs/CONFIGURATION.md README.md; do for forbidden_claim in 'blocks remote mailbox ingress' 'remote ingress is blocked' 'remote mailbox ingress is blocked' 'blocks remote content' 'remote content is safe' 'all remote content is safe' 'comprehensive safety' 'provides comprehensive safety' 'prevents remote steering' 'remote agents cannot steer' 'steering is prevented' 'prevents provenance laundering' 'origin cannot be laundered' 'laundering is prevented' 'prevents mailbox growth' 'prevents remote mailbox growth' 'remote traffic cannot grow the mailbox' 'mailbox growth is prevented' 'prevents host re-entry' 'prevents host UI/model re-entry' 'host re-entry is prevented'; do if rg -Fqi "$forbidden_claim" "$doc"; then exit 1; fi; done; done; cargo test -p famp --lib tool_descriptors_has_exactly_twelve_named_tools &amp;&amp; cargo test -p famp --test auto_wake_gate &amp;&amp; cargo test -p famp --test quarantine_surfaces &amp;&amp; cargo test -p famp --test pair_cli consent_warning_matches_quarantine_doc &amp;&amp; just check-no-tokio-in-bus &amp;&amp; just install</automated>
  </verify>
  <done>All live operator-facing descriptor/guide surfaces match the exact broker boundary, the installed MCP binary exposes the corrected descriptor, and every named limitation plus the single-authored consent warning is preserved.</done>
</task>

<task type="auto">
  <name>Task 19-03-02: Regress QUAR-15 and close the Nyquist validation ledger</name>
  <files>.planning/phases/19-auto-wake-gate/19-VALIDATION.md</files>
  <read_first>
    - .planning/phases/19-auto-wake-gate/19-VALIDATION.md — modified ledger and provisional IDs/commands.
    - .planning/phases/19-auto-wake-gate/19-01-SUMMARY.md — broker implementation and focused command results.
    - .planning/phases/19-auto-wake-gate/19-02-SUMMARY.md — real-socket and conflicting-test results; wait for Plan 19-02 completion before final sign-off.
    - crates/famp/tests/pair_cli.rs — existing QUAR-15 tests, unchanged.
    - crates/famp/src/pairing/consent.rs — existing single-authored warning, unchanged.
    - docs/QUARANTINE.md — exact warning mirror and new claim map.
    - .planning/ROADMAP.md — five success criteria to map to final evidence.
  </read_first>
  <action>
Run the two existing QUAR-15 tests without modifying pairing code or its warning. Run every focused Phase 19 target and the full phase gate using plain `cargo test`; do not use nextest because project history records that it hangs. Record wall-clock runtimes and actual outcomes in `19-VALIDATION.md`, replace the provisional table with final task IDs 19-01-01, 19-01-02, 19-02-01, 19-02-02, 19-03-01, and 19-03-02, and make each row's command match its plan. Mark Wave 0 items complete only when their files/tests exist. Set `wave_0_complete: true`, `nyquist_compliant: true`, and validation sign-off checks only after all required focused commands, `cargo test --workspace`, `just lint`, `cargo fmt --all -- --check`, and `just check-no-tokio-in-bus` pass. If the full workspace command hits a documented infrastructure timeout, record the exact incomplete command and keep sign-off false rather than claiming success.
  </action>
  <acceptance_criteria>
    - Both existing QUAR-15 tests pass and no pairing production/test file is modified.
    - Validation rows use the final six task IDs and commands from these plans.
    - Runtime estimates are replaced with measured values and all Wave 0 gaps have evidence.
    - Nyquist/frontmatter/sign-off fields become true only when their stated commands actually pass; incomplete workspace verification remains explicitly incomplete.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p famp --test pair_cli consent_warning_matches_quarantine_doc &amp;&amp; cargo test -p famp --test pair_cli artifact_code_offset_greater_than_consent_and_install_lines &amp;&amp; cargo test -p famp-bus --lib auto_wake &amp;&amp; cargo test -p famp --test auto_wake_gate &amp;&amp; cargo test -p famp --test quarantine_surfaces &amp;&amp; cargo test --workspace &amp;&amp; just lint &amp;&amp; cargo fmt --all -- --check &amp;&amp; just check-no-tokio-in-bus</automated>
  </verify>
  <done>QUAR-15 remains intact by regression, all five roadmap criteria map to automated evidence, and the validation ledger reports only measured outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation → operator descriptor/documentation | Security claims in MCP metadata or guides can overstate what the broker actually enforces. |
| pairing artifact → human consent | Exact warning placement is the existing QUAR-15 mechanism and must not drift during unrelated gate work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Repudiation | MCP descriptor and security documentation | medium | mitigate | Assert Local-only semantics and remove stale any-origin wording across all eight live surfaces; map claims to named actor/socket tests. |
| T-19-09 | Elevation of Privilege | consent wording drift | medium | mitigate | Re-run exact warning/doc and warning-before-code tests; do not edit pairing artifacts. |
| T-19-10 | Information Disclosure | mailbox-growth claim | low | accept | The gate intentionally leaves remote records in the mailbox; document the residual rather than implying admission control. |
</threat_model>

<verification>
- QUAR-15: both exact `pair_cli` tests.
- Focused phase: actor, real-socket, and quarantine targets.
- Phase gate: workspace tests, lint, formatting, and no-tokio check.
- Per-file positive and forbidden-overclaim assertions cover the live MCP descriptor and all seven modified guides, followed by exact test-backed review and `just install`.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Accurate broker-enforced auto-wake boundary and Local continuity | 19-03 | COVERED | Docs and final evidence map.
REQ | QUAR-12 | Local-only Await | 19-03 | COVERED | Operator docs match implementation/tests.
REQ | QUAR-13 | Broker-side, not CLI drain | 19-03 | COVERED | Claim names broker behavioral proof.
REQ | QUAR-14 | Held but explicit Inbox-visible | 19-03 | COVERED | Docs preserve recovery path and test reference.
REQ | QUAR-15 | Consent warning in pairing artifact | 19-03 | COVERED | Regression-only; no pairing churn.
RESEARCH | — | Narrow claims, wait-reply asymmetry, mailbox/laundering/host limitations, and active Await/listen-mode surface truth | 19-03 | COVERED | MCP descriptor and all seven operator guides are enumerated with deterministic assertions.
CONTEXT | — | No Phase 19 CONTEXT.md exists | — | N/A | No locked or deferred decisions.
</source_audit>

<success_criteria>
- The live MCP descriptor, all active operator guides, and tests reflect the same Local-only Await contract.
- QUAR-15 remains single-authored, before the code, and unchanged.
- All five roadmap success criteria have named automated evidence in the validation ledger.
- No claim implies quarantine from mailbox ingress or protection from other host re-entry paths.
</success_criteria>

<output>
Create `.planning/phases/19-auto-wake-gate/19-03-SUMMARY.md` when done.
</output>
