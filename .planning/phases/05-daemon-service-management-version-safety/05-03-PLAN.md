---
phase: 05-daemon-service-management-version-safety
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md
autonomous: false
requirements: [DAEMON-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The literal plist XML has been reviewed by the mac-guardian skill against its checklist"
    - "guardian sign-off is recorded BEFORE any service-load task runs"
  artifacts:
    - path: ".planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md"
      provides: "recorded guardian verdict + reviewed plist hash/excerpt"
      contains: "guardian"
  key_links:
    - from: "sample-com.famp.broker.plist"
      to: "mac-guardian skill review"
      via: "guardian checklist evaluation"
      pattern: "com.famp.broker"
---

<objective>
BLOCKING PRE-LOAD GATE (DAEMON-02). Submit the literal plist XML produced in
Plan 02 to the `mac-guardian` skill, obtain sign-off against its review
checklist, and record the verdict. NO service may be loaded (`launchctl
bootstrap`) until this sign-off exists. This is the gate that sequences ahead of
Plan 04's first load.

Purpose: STATE.md v0.11 invariant #4 and ROADMAP success criterion 1 make this a
blocking external review gate, not advisory. A service that auto-launches a
binary at login must have its literal launch spec human/skill-reviewed before it
is ever loaded.

Output: GUARDIAN-SIGNOFF.md recording the reviewed plist, the guardian verdict,
and any required corrections (which, if present, loop back to Plan 02 before
Plan 04 proceeds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-daemon-service-management-version-safety/05-CONTEXT.md
@.planning/phases/05-daemon-service-management-version-safety/05-RESEARCH.md
@.planning/phases/05-daemon-service-management-version-safety/sample-com.famp.broker.plist
@$HOME/.claude/skills/mac-guardian/SKILL.md

<interfaces>
<!-- The mac-guardian skill lives at ~/.claude/skills/mac-guardian. -->
<!-- The artifact under review: the literal XML at
     .planning/phases/05-daemon-service-management-version-safety/sample-com.famp.broker.plist
     (produced by Plan 02 Task 2, byte-identical in shape to generate_plist output). -->
<!-- Locked shape checklist (RESEARCH § "Plist shape checklist"):
     Label=com.famp.broker; ProgramArguments=[absolute famp, broker, --no-idle-exit];
     RunAtLoad=true; KeepAlive=true (unconditional); ProcessType=Background;
     StandardOutPath=StandardErrorPath=<home>/.famp/broker.log; NO EnvironmentVariables;
     NO UserName/GroupName; all paths absolute (no ~). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run guardian review on the literal plist XML</name>
  <files>.planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md</files>
  <read_first>
    - .planning/phases/05-daemon-service-management-version-safety/sample-com.famp.broker.plist (the literal XML under review)
    - $HOME/.claude/skills/mac-guardian/SKILL.md (the review checklist the plist must pass)
    - 05-RESEARCH.md § "Plist shape checklist (for guardian review gate)"
    - STATE.md § "v0.11 Architectural Invariants" #4 (guardian is a blocking pre-load gate)
  </read_first>
  <action>
    Invoke the `mac-guardian` skill (read $HOME/.claude/skills/mac-guardian/SKILL.md
    and follow its review procedure) against the literal plist XML at
    `.planning/phases/05-daemon-service-management-version-safety/sample-com.famp.broker.plist`.

    Evaluate the plist against the guardian checklist AND the locked-shape
    checklist (RESEARCH § Plist shape checklist):
      - Label = com.famp.broker
      - ProgramArguments = [absolute famp binary, "broker", "--no-idle-exit"]
      - RunAtLoad = true
      - KeepAlive = true (unconditional <true/>, not a dict)
      - ProcessType = Background
      - StandardOutPath = StandardErrorPath = absolute <home>/.famp/broker.log
      - NO EnvironmentVariables key
      - NO UserName / GroupName key
      - all paths absolute (no tilde)

    Write `.planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md`
    recording: (a) the reviewed plist excerpt or path, (b) each checklist item
    PASS/FAIL, (c) guardian's overall verdict (APPROVED / CHANGES-REQUIRED), and
    (d) any required corrections.

    If guardian returns CHANGES-REQUIRED: record the corrections, then STOP and
    report that Plan 02's generate_plist must be revised before Plan 04 (load)
    can proceed. Do NOT mark this plan complete with an unresolved CHANGES-REQUIRED.
  </action>
  <acceptance_criteria>
    - `.planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md` exists
    - It contains the word `guardian`
    - It contains an explicit verdict line matching `APPROVED` (or, if not approved, a CHANGES-REQUIRED block — in which case this plan is NOT complete and Plan 02 is revised first)
    - The reviewed plist still contains `com.famp.broker` and no `EnvironmentVariables`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md && grep -qi 'guardian' .planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md && echo OK</automated>
  </verify>
  <done>guardian has reviewed the literal plist XML and the verdict is recorded in GUARDIAN-SIGNOFF.md. If APPROVED, Plan 04 may load the service. If CHANGES-REQUIRED, Plan 02 is revised first.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human authorization of guardian-approved plist before first load</name>
  <files>.planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md</files>
  <action>Block on human confirmation that the guardian verdict in GUARDIAN-SIGNOFF.md is APPROVED before Plan 04 may load the service. See checkpoint fields below.</action>
  <what-built>The plist XML at sample-com.famp.broker.plist was generated to the locked shape (Plan 02) and run through the mac-guardian skill checklist (Task 1 above), with the verdict recorded in GUARDIAN-SIGNOFF.md.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/05-daemon-service-management-version-safety/GUARDIAN-SIGNOFF.md` — confirm guardian's verdict is APPROVED and every checklist item is PASS.
    2. Spot-check the literal plist at `.planning/phases/05-daemon-service-management-version-safety/sample-com.famp.broker.plist`: KeepAlive unconditional, ProcessType=Background, absolute log path, no EnvironmentVariables, no tilde.
    3. Confirm you are comfortable that THIS literal launch spec is what gets loaded at login before Plan 04 runs `launchctl bootstrap`.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize Plan 04 to load the service, or describe required plist changes (which loop back to Plan 02).</resume-signal>
  <verify>GUARDIAN-SIGNOFF.md verdict is APPROVED and human has typed "approved"</verify>
  <done>Human has authorized first load; Plan 04 (which depends_on Plan 03) may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed launch spec → login-time auto-execution | The plist will auto-launch a binary at every login. This gate is the human/skill control before that capability goes live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering / EoP | An unreviewed plist auto-launching a binary at login is the highest-leverage misconfiguration in this phase | mitigate | This BLOCKING gate is the mitigation: the literal XML is guardian-reviewed against the locked checklist before any `launchctl bootstrap`. Plan 04 depends_on this plan, so load cannot precede sign-off. |
| T-05-08 | Information Disclosure | A plist with EnvironmentVariables could leak secrets into the world-readable log | mitigate | Guardian checklist explicitly verifies NO EnvironmentVariables key on the literal XML. |
</threat_model>

<verification>
- GUARDIAN-SIGNOFF.md exists with an APPROVED verdict and per-item checklist results
- The human checkpoint is approved before Plan 04 (the loading plan) runs
- If CHANGES-REQUIRED: Plan 02 is revised and this plan re-run before Plan 04
</verification>

<success_criteria>
- DAEMON-02 (approval half): the literal plist XML is guardian-approved BEFORE first load, satisfying ROADMAP success criterion 1's blocking review gate.
- No `launchctl bootstrap` has run prior to this sign-off (enforced by wave/depends_on ordering: Plan 04 depends_on Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/05-daemon-service-management-version-safety/05-03-SUMMARY.md`
</output>
