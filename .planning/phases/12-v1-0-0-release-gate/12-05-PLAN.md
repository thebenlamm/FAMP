---
phase: 12-v1-0-0-release-gate
plan: 05
type: execute
wave: 3
depends_on: [12-04]
files_modified:
  - .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md
autonomous: false
requirements: [REL-05]
must_haves:
  truths:
    - "The `v1.0.0` tag points at EXACTLY the SHA attested in `12-CI-ATTESTATION.md` — not its parent, not its child, not whatever `HEAD` happens to be: `git rev-parse v1.0.0^{commit}` equals the recorded 40-character SHA, and that SHA's own check-runs are re-queried green immediately before the tag is created (REL-03 / REL-05)."
    - "The tag is created against the recorded SHA by value (`git tag -a v1.0.0 <sha>`), never against an implicit `HEAD`, so a parallel window moving the branch — a documented failure mode in this repo, where multi-window sessions share one git HEAD — cannot silently retarget the tag; and the pre-tag re-query requires every check-run to report a completed status, never an in-progress one (REL-05)."
    - "The tag is annotated (not lightweight, not GPG-signed), matching the `v0.9` / `v0.11` / `v1.0.0-rc.1` convention: `git cat-file -t v1.0.0` returns `tag`."
    - "The annotation reproduces design review C §16's nine-item checklist in full, each item marked satisfied with its own evidence citation — items 1, 2, 3, 4, 5, 7 by citation to `11-VERIFICATION.md`, item 6 to the CI attestation record, item 8 to REL-01, item 9 to REL-02 (REL-05)."
    - "Any limitation statement in the annotation is verified current against the shipped code before it ships. §16's proposed wording — that `famp send` does not initiate or complete the task FSM — predates Phase 11's ADDR-02 and is now FALSE: remote sends are typed and FSM-driving (`--new-task` to Request, `--task` to Commit, `--task --terminal` to Deliver), live-proven in `11-HUMAN-UAT.md` where a task reached COMPLETED on both hosts. The annotation ships the accurate statement or none — never the stale one (REL-05, success criterion 6)."
    - "Ben explicitly confirmed the tag before it was created. The tag is outward-facing and effectively permanent; the ROADMAP hard constraint requires his confirmation, and the checkpoint presents the exact annotation text and the limitation-wording decision for that confirmation."
  artifacts:
    - ".planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md — the drafted annotation, reviewed at the checkpoint and used verbatim as the tag body"
    - "the annotated git tag `v1.0.0` on the attested SHA, pushed to origin"
  key_links:
    - "`12-CI-ATTESTATION.md` `## Tag Candidate` SHA → `git tag -a v1.0.0 <sha>` → `git rev-parse v1.0.0^{commit}` — the value must survive unchanged through all three, and is re-asserted after tagging"
    - "`12-CI-ATTESTATION.md` `## §16 Re-Attestation by Citation` table → the nine-item checklist reproduced in the annotation body — one source, reproduced not re-derived"
    - "`11-HUMAN-UAT.md`'s live terminal-FSM record → the limitation-statement decision — the evidence that falsifies §16's proposed wording"
  prohibitions:
    - "MUST NOT let the `v1.0.0` tag annotation imply capabilities outside the shipped scope. It must not claim public-internet or relay reachability, cross-person trust bootstrap, a signed peer directory, freshness/replay defense, or a conformance vector pack — every one of those is explicitly deferred to v1.1 or to the separate Gate B trigger in REQUIREMENTS.md, and a release note that blurs them is an overclaim a reader cannot falsify from the tag alone."
    - "MUST NOT create or push the `v1.0.0` tag without Ben's explicit confirmation in this session. The tag is outward-facing and effectively permanent; no configuration, auto-advance setting, or inferred approval substitutes for it."
---

<objective>
REL-05's tag half — draft the `v1.0.0` annotation, get Ben's explicit confirmation on it and on the limitation-wording decision, then create and push the annotated tag on exactly the SHA that `12-CI-ATTESTATION.md` proved green.

Purpose: this is the last action of the v1.0 federation milestone and the only genuinely irreversible one in the phase. Everything before it is `git revert`-able; a published tag is outward-facing and effectively permanent.

Output: a reviewed annotation record and the pushed `v1.0.0` tag.

**Three things that make this plan's ordering non-negotiable.**

1. **The tag is a one-way door.** A `checkpoint:decision` sits immediately before the tag task. `auto_advance` does not apply to it.
2. **The tag must be created by SHA value, never by `HEAD`.** This repo's FAMP multi-window sessions share a single git HEAD, and a parallel window's checkout has silently moved it before. Plan 12-04's bump commit is the last CI-triggering commit, but plan 12-04's own attestation record — and this plan's annotation draft — are `.planning/` commits that land after it and move `HEAD` forward without producing any check-runs. Tagging `HEAD` would therefore point `v1.0.0` at a docs-only SHA with zero CI evidence, silently converting REL-03 from satisfied to unsatisfiable.
3. **The proposed limitation sentence from §16 is stale.** It must not ship unedited. That is success criterion 6, and the checkpoint makes the choice explicit rather than leaving it to the drafter.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md
@.planning/phases/12-v1-0-0-release-gate/12-04-SUMMARY.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `12-TAG-ANNOTATION.md` | new phase-record file (the drafted annotation body) | `.planning/phases/12-v1-0-0-release-gate/` |
| `## Proposed Annotation` / `## Limitation Statement Decision` / `## Scope Guard` | new section headings inside that file | same |
| `v1.0.0` | new annotated git tag | the repository, on the attested SHA |

<tasks>

<task type="auto">
  <name>Task 1: draft the v1.0.0 annotation with the §16 checklist reproduced and a verified-current limitation statement</name>
  <files>.planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md</files>
  <read_first>
    - `.planning/phases/12-v1-0-0-release-gate/12-CI-ATTESTATION.md` — the `## Tag Candidate` SHA and the complete `## §16 Re-Attestation by Citation` table (this plan REPRODUCES that table; it does not re-derive the citations)
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § REL-05 "§16 Exact release ruling — verbatim" (the nine items, so the PDF need not be reopened) and § "Prior-tag annotation convention" (the shape every prior milestone tag uses)
    - `git show v1.0.0-rc.1` — the immediately-prior annotation, whose closing line states that v1.0.0 proper still requires the §16 nine-item checklist; this annotation closes that loop explicitly
    - `.planning/REQUIREMENTS.md` lines 93-123 (the v2/deferred requirements and the `## Out of Scope` table) — the authoritative list of what this release must NOT be read as claiming
    - `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-HUMAN-UAT.md` § 4 — the live record of a task reaching a terminal state on both hosts, driven by the shipping client; the evidence that falsifies §16's proposed limitation wording
    - `.planning/phases/12-v1-0-0-release-gate/12-REL-02-REVIEW.md` `## Triage` and `## Outcome` — any documented-accept item that belongs in a known-gaps callout
  </read_first>
  <action>
    Write `.planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md` containing the exact text proposed for the tag body, plus two supporting sections.

    `## Proposed Annotation` holds the tag body, following the shape every prior milestone tag uses: a one-line title; a `Delivered:` paragraph; a `Key accomplishments:` bullet list; a line giving the phase, plan, and requirement counts for the milestone; a known-gaps or deferred callout; and then the §16 nine-item checklist reproduced in full, each item marked satisfied with its own evidence citation copied from `12-CI-ATTESTATION.md`'s citation table — items 1, 2, 3, 4, 5, 7 citing `11-VERIFICATION.md`, item 6 citing the CI attestation SHA and run ids, item 8 citing REL-01's doc and pinning test, item 9 citing the REL-02 review record. Close by explicitly discharging the sentence at the end of the `v1.0.0-rc.1` annotation, which stated that v1.0.0 proper still required this checklist. Include the attested SHA in the body so the annotation is self-locating.

    `## Limitation Statement Decision` presents the choice rather than making it silently. Record §16's proposed wording verbatim, record why it is now false (ADDR-02 made remote sends typed and FSM-driving; `11-HUMAN-UAT.md` records a task reaching a terminal state on both hosts through the shipping client), and lay out two defensible options: (A) ship NO limitation sentence, because the specific gap it described no longer exists; or (B) ship a replacement built from REL-01's verified statement — that a successful send confirms only local acceptance into the gateway-backed outbound mailbox, not remote delivery, verification, or far-side FSM advancement. Recommend (B) with a one-line reason: the fire-and-forget boundary is real and operator-relevant even though the FSM gap is not, and shipping the accurate bounded limitation is more useful than shipping none. Do not decide — Task 2 is where the decision is made.

    `## Scope Guard` lists what the annotation must NOT be read as claiming, derived from `REQUIREMENTS.md`'s deferred and out-of-scope sections: public-internet relay or NAT traversal, cross-person trust bootstrap, a signed peer directory, freshness/replay-cache enforcement, the capability/approval/tool-admission plane, and the conformance vector pack (which ships on the separate Gate B trigger, not with this tag). Then confirm, phrase by phrase, that the drafted annotation body claims none of them.

    Do not create the tag in this task. Commit the record with `git add -f`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md &amp;&amp; grep -c '^## ' .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The file contains all three headings `## Proposed Annotation`, `## Limitation Statement Decision`, `## Scope Guard`.
    - The `## Proposed Annotation` section contains nine numbered checklist items and `grep -c '11-VERIFICATION.md' .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md` returns at least `6`.
    - The attested 40-character SHA from `12-CI-ATTESTATION.md` appears verbatim in the annotation body: `grep -c "$(grep -oE '[0-9a-f]{40}' .planning/phases/12-v1-0-0-release-gate/12-CI-ATTESTATION.md | head -1)" .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md` returns at least `1`.
    - `## Limitation Statement Decision` quotes §16's proposed wording, states why it is false, cites `11-HUMAN-UAT.md`, and presents exactly two options with a recommendation.
    - `## Scope Guard` names all six deferred/out-of-scope areas, and the `## Proposed Annotation` body does not claim any of them: `awk '/^## Proposed Annotation/,/^## Limitation/' .planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md | grep -Eci 'public.internet|NAT traversal|relay|vector pack|cross-person'` returns `0`, or every hit is inside an explicit deferral sentence quoted in the SUMMARY.
    - No git tag was created by this task: `git tag -l 'v1.0.0'` returns empty.
  </acceptance_criteria>
  <reversibility rating="reversible">A draft record; nothing outward-facing is created.</reversibility>
  <done>A complete, citation-backed annotation draft exists with the limitation-wording choice framed for decision, and no tag has been created.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ben confirms the v1.0.0 tag and the limitation-statement wording</name>
  <decision>Whether to create and push the outward-facing `v1.0.0` tag on the attested SHA, and which limitation statement (if any) ships in its annotation.</decision>
  <context>
    This is the one-way door of the v1.0 federation milestone. Everything before it in Phase 12 is `git revert`-able; a published tag is outward-facing and effectively permanent. The ROADMAP's hard constraint states it directly: tagging `v1.0.0` is outward-facing — confirm with Ben before the tag lands.

    Present, in the checkpoint message: the attested SHA and its check-run summary from `12-CI-ATTESTATION.md` (total count, all-success confirmation, run ids); the full drafted annotation body from `12-TAG-ANNOTATION.md`; the §16 nine-item checklist with each item's evidence citation; any `documented-accept` disposition from the REL-02 review that a reader should know about; and the deployed-binary staleness note (the installed binaries on both dogfooded machines still report the release-candidate version until reinstalled — this is expected, not a failed bump).

    Do NOT proceed on `auto_advance`, on an inferred approval, or on any prior message. This checkpoint requires an explicit answer in this session.
  </context>
  <options>
    <option id="tag-with-boundary-limitation">
      <name>Tag v1.0.0, ship the REL-01 fire-and-forget limitation statement (recommended)</name>
      <pros>Ships an accurate, bounded limitation a user can act on — a successful send confirms local acceptance into the gateway-backed outbound mailbox, not remote delivery, verification, or far-side FSM advancement. Consistent with the §6 guide text, the `famp send --help` text, and the README, all of which now say the same thing. Discharges §16's intent (be honest about the boundary) without repeating its now-false claim.</pros>
      <cons>Adds a limitation line to an otherwise clean release note; a reader skimming only the tag might over-weight it.</cons>
    </option>
    <option id="tag-no-limitation">
      <name>Tag v1.0.0 with no limitation statement</name>
      <pros>§16's proposed limitation described an FSM gap that ADDR-02 closed, so strictly nothing on §16's list remains to disclose. Cleanest possible release note.</pros>
      <cons>The fire-and-forget exit-code boundary is real and operator-relevant even though the FSM gap is not; omitting it puts the whole burden on the guide.</cons>
    </option>
    <option id="hold">
      <name>Do not tag yet</name>
      <pros>Preserves the one-way door. Correct if anything in the annotation, the attestation, or the REL-02 triage reads wrong on review.</pros>
      <cons>v1.0 stays at the release candidate; the milestone does not close.</cons>
    </option>
  </options>
  <resume-signal>Reply with `tag-with-boundary-limitation`, `tag-no-limitation`, `hold`, or corrections to the annotation text.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: create and push the annotated v1.0.0 tag on the attested SHA</name>
  <files>.planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md</files>
  <precondition>Ben selected a tagging option at the Task 2 checkpoint in this session. The attested SHA recorded in `12-CI-ATTESTATION.md` is still reachable from the remote (`git branch -r --contains &lt;sha&gt;` lists `origin/main`) and has not been force-moved. If Ben selected `hold`, this task does not run.</precondition>
  <read_first>
    - `.planning/phases/12-v1-0-0-release-gate/12-CI-ATTESTATION.md` `## Tag Candidate` — the exact 40-character SHA
    - `.planning/phases/12-v1-0-0-release-gate/12-TAG-ANNOTATION.md` `## Proposed Annotation` — the body, as amended by any correction Ben gave at the checkpoint
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § "Prior-tag annotation convention" — prior tags are plain annotated tags, NOT GPG-signed; do not introduce signing here
  </read_first>
  <action>
    Apply whatever amendment Ben gave at the checkpoint to `12-TAG-ANNOTATION.md` first, and fold in the chosen limitation option, so the record on disk matches what actually ships.

    Re-verify before tagging, in this order, and abort on any failure. Read the SHA from `12-CI-ATTESTATION.md` into a variable and use only that variable from here on — never `HEAD`, and never re-derive it. Confirm the object exists and is a commit. Confirm it is on `origin/main`. Re-query its check-runs one final time and require a non-zero total with every check-run completed and successful — the state could have changed since the attestation was written, and a stale attestation is exactly the mistake this phase exists to avoid.

    Create the annotated tag against that SHA value explicitly. Use the annotation body from the record file rather than retyping it, so what ships is byte-identical to what Ben reviewed. Do not GPG-sign — prior tags on this repo are plain annotated tags. Then push the tag to origin.

    After pushing, assert that the tag resolves to the intended commit and that its body carries the nine-item checklist. Record in the SUMMARY: the tag's own object id, the commit it points at, the chosen limitation option, and a final note that the installed binaries on the dogfooded machines still report the release-candidate version until someone reinstalls, so this is not later mistaken for a failed bump.
  </action>
  <verify>
    <automated>git cat-file -t v1.0.0</automated>
    <automated>git cat-file -p v1.0.0</automated>
  </verify>
  <acceptance_criteria>
    - `git cat-file -t v1.0.0` returns `tag` — annotated, not lightweight.
    - `git rev-parse v1.0.0^{commit}` equals the 40-character SHA recorded in `12-CI-ATTESTATION.md` `## Tag Candidate`, character for character.
    - `git rev-parse v1.0.0^{commit}` does NOT equal `git rev-parse HEAD` unless HEAD happens to be that same commit — record both values in the SUMMARY so the distinction is auditable.
    - `git cat-file -p v1.0.0 | grep -c '1\.'` shows the annotation body is present, and the body contains nine checklist items: `git cat-file -p v1.0.0 | grep -cE '^\s*[1-9]\.'` returns at least `9`.
    - `git cat-file -p v1.0.0 | grep -ci 'does not initiate or complete the task FSM'` returns `0` — the stale limitation wording did not ship.
    - If `tag-with-boundary-limitation` was chosen: `git cat-file -p v1.0.0 | grep -ci 'gateway-backed outbound mailbox'` returns at least `1`.
    - `git ls-remote --tags origin v1.0.0` returns a matching ref — the tag reached origin.
    - `gh api "/repos/thebenlamm/FAMP/commits/$(git rev-parse v1.0.0^{commit})/check-runs" --jq '[.check_runs[] | select(.conclusion != "success")] | length'` returns `0` with `.total_count` at least `11` — re-asserted after the tag exists, against the tag's own resolved commit.
    - `git cat-file -p v1.0.0 | grep -c 'BEGIN PGP'` returns `0` — plain annotated tag, matching the prior-tag convention.
  </acceptance_criteria>
  <reversibility rating="one-way">A published tag is outward-facing and effectively permanent. Deleting and re-pushing a release tag is visible to anyone who fetched it and is treated here as irreversible. Gated by the Task 2 `checkpoint:decision` immediately above.</reversibility>
  <done>`v1.0.0` is an annotated tag on exactly the CI-attested SHA, its body reproduces the §16 checklist with citations, it ships no stale limitation claim, and it is pushed to origin — the v1.0 federation milestone is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository state → published tag | The single irreversible, outward-facing action of the milestone. Everything upstream is revertible; this is not. |
| local git HEAD → tag target | FAMP multi-window sessions share one git checkout; a parallel window can move HEAD between the attestation and the tag. |
| drafted annotation → published claim | The annotation is read by people who cannot check the code; every claim in it must be citation-backed. |

## STRIDE Threat Register (OWASP ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-01 | Tampering | tag target commit | high | mitigate | Tagging `HEAD` would point `v1.0.0` at a `.planning/` docs-only commit with zero check-runs, silently converting REL-03 from satisfied to unsatisfiable — and a parallel window sharing this repo's HEAD is a documented, previously-observed failure. Mitigation: the SHA is read from `12-CI-ATTESTATION.md` into a variable, the tag is created against that value explicitly, and `git rev-parse v1.0.0^{commit}` is asserted equal to it after the fact. |
| T-12-05-02 | Repudiation | tag annotation | high | mitigate | A release note asserting satisfaction of a nine-item security checklist without per-item evidence cannot be audited later. Mitigation: every §16 item carries its own citation copied from the attestation record, and the checklist item count is asserted from the pushed tag object itself. |
| T-12-05-03 | Spoofing | release scope claims | medium | mitigate | An annotation implying open-internet reachability, cross-person trust, or a conformance vector pack would misrepresent what a user can safely do with this release. Mitigation: the `## Scope Guard` section enumerates all six deferred areas and the acceptance criteria grep the annotation body against them. |
| T-12-05-04 | Elevation of privilege | the tag decision itself | high | mitigate | An agent tagging an outward-facing release without the owner's confirmation is acting beyond its authority. Mitigation: a blocking `checkpoint:decision` immediately precedes the tag task, the task's precondition names it, and a prohibition forbids substituting `auto_advance` or an inferred approval. |
| T-12-05-05 | Tampering | stale attestation | high | mitigate | Remote state can change between writing the attestation and creating the tag. Mitigation: the check-runs query is re-run immediately before tagging AND again after, against the tag's own resolved commit. |
| T-12-05-06 | Spoofing | tag authenticity | low | accept | The tag is not GPG-signed. Accepted deliberately: every prior milestone tag on this repo (`v0.9`, `v0.11`, `v1.0.0-rc.1`) is a plain annotated tag, and introducing signing at the release moment would break the convention without a stated requirement. Recorded as an accepted, conventional gap rather than an oversight. |
| T-12-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and touches no manifest. Gate: `git status --porcelain` must show no change under `crates/`, no `Cargo.toml`, and no `Cargo.lock` at tag time; a dirty manifest at the tag moment means the tagged tree is not the attested tree — abort. |
</threat_model>

<verification>
- `git cat-file -t v1.0.0` returns `tag`; `git rev-parse v1.0.0^{commit}` equals the attested SHA character for character.
- The tag body carries all nine §16 items with citations, ships no stale FSM-limitation claim, and is not GPG-signed.
- The tag's resolved commit is re-queried green (total count at least 11, all conclusions success) AFTER the tag exists.
- `git ls-remote --tags origin v1.0.0` confirms the tag reached origin.
- Ben's checkpoint selection is recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
REL-05 is fully closed and the v1.0 federation milestone ships: `v1.0.0` is an annotated tag on exactly the CI-attested commit, its annotation reproduces the §16 nine-item checklist with per-item evidence, its limitation statement is verified current against shipped behavior (or deliberately absent), it claims nothing outside the shipped scope, and it landed only after Ben's explicit confirmation.
</success_criteria>

## Probe reconciliation — this plan, and the PHASE-WIDE total

**This plan.** The probe surfaced 3 items for REL-03 and 4 for REL-05; plan 12-04 authored 5 of those, and this plan authors the remaining **2** — the ones that only become checkable once a tag exists. Reconciliation for this plan: **2 authored into `must_haves.truths` + 0 flagged = 2.** No silent drops.

| # | Req | Category | Disposition | Where |
|---|-----|----------|-------------|-------|
| 12 | REL-03 | adjacency | covered | truth 1 (the tag SHA must be EXACTLY the attested SHA — an adjacent commit is a different SHA with different or zero check-runs, so equality is asserted character for character and the check-runs are re-queried against the tag's own resolved commit) |
| 22 | REL-05 | concurrency | covered | truth 2 (interruption / parallel execution: tag by SHA value not `HEAD`, so a parallel window sharing this repo's git HEAD cannot retarget the tag; and no conclusion is evaluated while any check-run is still in progress) |

**Phase-wide reconciliation (all 5 plans).** The spec-less probe fallback surfaced **22 applicable edge items** across REL-01..REL-05, all initially `unresolved`. Every one is accounted for:

| Plan | Requirement | Items | Covered | Backstop | Flagged assumption |
|------|-------------|-------|---------|----------|--------------------|
| 12-01 | REL-01 | 6 | 5 | 0 | 1 (precision — no arithmetic or numeric contract exists in a prose doc edit plus string-containment assertions) |
| 12-02 | REL-02 | 5 | 4 | 1 (precision — numeric federation-field contract, honestly not mechanically checkable inside a v1.0 release gate that defers freshness/replay to v1.1) | 0 |
| 12-03 | REL-04 | 4 | 4 | 0 | 0 |
| 12-04 | REL-03 (2 of 3) + REL-05 (3 of 4) | 5 | 5 | 0 | 0 |
| 12-05 | REL-03 (1 of 3) + REL-05 (1 of 4) | 2 | 2 | 0 | 0 |
| **Total** | | **22** | **20** | **1** | **1** |

**No-silent-drop equality: 22 probe-surfaced items == 20 authored as plain `must_haves.truths` + 1 authored as a structured `{ statement, verification: backstop }` truth + 1 surfaced as an explicit flagged assumption = 22.** Zero items were dismissed and zero were dropped. The single flagged assumption is recorded in plan 12-01's reconciliation table with its one-line reason; it is a genuine poor-fit probe for this phase, not an unexamined gap.

**Prohibition probe, phase-wide.** Two-stage recall → precision run in-prompt over REL-01..REL-05. Raw recall produced roughly ten candidates per requirement; the precision stage dropped every routine-engineering item (partial-bump correctness, markdown-table integrity, grep-gate brittleness — all owned by the edge probe or ordinary code review) and kept **7** values/safety/integrity/transparency prohibitions, distributed as: 12-01 ×1 (no stale limitation claim), 12-02 ×2 (no schedule-driven accept; no verdict-echo review), 12-03 ×1 (no evidence-free status flip or phantom back-fill), 12-04 ×1 (no manufactured green gate), 12-05 ×2 (no out-of-scope capability claim; no tag without explicit confirmation). All are authored **descriptor-less** into each plan's `must_haves.prohibitions` block — no `check_kind` / `check_target` / `check_rule` / `check_violation_fixture` / `check_clean_fixture` — so each disposes flagged-unverified downstream. One canon-referral breadcrumb was emitted and dropped rather than minted (open-relay / arbitrary-mailbox-placement is canon security already owned by `SEC-02` and `/gsd-secure-phase`), recorded in plan 12-02's threat model.

<output>
Create `.planning/phases/12-v1-0-0-release-gate/12-05-SUMMARY.md` when done, recording Ben's checkpoint selection verbatim, the tag object id, and the commit the tag resolves to. `.planning/` is gitignored in this repo — use `git add -f`. Commit any planning artifact AFTER the tag is created, never before, so nothing can move the tag target.
</output>
