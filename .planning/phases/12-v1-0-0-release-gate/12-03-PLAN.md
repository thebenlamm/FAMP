---
phase: 12-v1-0-0-release-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md
autonomous: true
requirements: [REL-04]
must_haves:
  truths:
    - "UAT-01's two representations in `.planning/REQUIREMENTS.md` flip together and neither is left behind: the checklist entry near line 81 and the traceability-table row near line 153 both reflect the recorded PASS from `11-HUMAN-UAT.md`, so `grep -c 'UAT-01' .planning/REQUIREMENTS.md` still returns 2 while zero of those 2 occurrences remains in an incomplete state (REL-04a)."
    - "The completed-checkbox marker uses the lowercase `[x]` form already used by every other completed item in `.planning/REQUIREMENTS.md`, so the file's existing convention — and any grep gate written against it — matches without a case or Unicode variant."
    - "`.planning/ROADMAP.md`'s Phase 11 plan list is no longer missing a plan: `grep -c '11-08-PLAN.md' .planning/ROADMAP.md` returns at least 1, and the Phase 11 `**Plans:**` count line reads 8 rather than 7 (REL-04c)."
    - "`11-08-PLAN.md` appears AFTER `11-07-PLAN.md` in `.planning/ROADMAP.md` (byte-offset comparison) and is listed under Wave 3, matching its own frontmatter (`wave: 3`, `depends_on: [11-07]`, `requirements: [SEC-02, SEC-03, SEC-04]`) — the ordering reflects the real dependency, not merely the presence of the string."
    - "Phase 11's top-level checklist box in `.planning/ROADMAP.md` reflects its recorded `status: passed` / `score: 7/7` from `11-VERIFICATION.md` (REL-04c)."
    - "`11-VERIFICATION.md`'s WARNING paragraph no longer leaves the `ADDR-04` question open: it carries a one-line addendum pointing at the standing resolution note in `.planning/REQUIREMENTS.md` (the block at lines 49-56), so a future reader of the verification report cannot re-open a closed question (REL-04b)."
  artifacts:
    - "corrected .planning/REQUIREMENTS.md (UAT-01 checklist + traceability row)"
    - "corrected .planning/ROADMAP.md (Phase 11 top-level box, plan count 8, 11-08 entry in Wave 3)"
    - "amended .planning/phases/11-.../11-VERIFICATION.md (ADDR-04 WARNING addendum pointing at the resolution note)"
  key_links:
    - "`11-HUMAN-UAT.md` frontmatter `verdict: PASS` → REQUIREMENTS.md UAT-01 checklist entry → REQUIREMENTS.md traceability row — one recorded verdict, three representations that must agree"
    - "`11-08-PLAN.md` frontmatter (`wave: 3`, `depends_on: [11-07]`) → ROADMAP.md Phase 11 Wave 3 list entry — the roadmap entry must be derived from the plan file, not invented"
    - "`11-VERIFICATION.md` ADDR-04 WARNING → REQUIREMENTS.md lines 49-56 standing resolution note → commit `04171bd` (the message slip's origin)"
  prohibitions:
    - "MUST NOT close a release-record item by marking it complete without the underlying evidence. Every status flip in this plan cites the recorded verdict or plan file it is derived from, and a dangling reference is resolved by pointing at the existing explanation — never by back-filling a phantom requirement into REQUIREMENTS.md to make the reference resolve."
---

<objective>
REL-04 — make the release record internally consistent before the `v1.0.0` tag lands, closing the three named defects from the `11-VERIFICATION.md` WARNING.

Purpose: the tag annotation re-attests §16 items 1-5 and 7 *by citation to the release record*. A record that says UAT-01 is `Pending` while `11-HUMAN-UAT.md` records `verdict: PASS`, or that lists 7 of Phase 11's 8 shipped plans, undermines the citation the tag depends on. This is the cheapest possible defect to fix now and the most embarrassing one to discover after a public tag.

Output: three corrected planning documents, committed before the version bump.

**Sequencing note (load-bearing).** Every file this plan touches is under `.planning/` or matches `**/*.md`, both of which `.github/workflows/ci.yml` lists in `paths-ignore`. A commit from this plan therefore produces **zero check-runs — not a pass, not a skip, literally no check-run object**. That is fine, and it is exactly why this plan must land BEFORE the version bump rather than after: REL-03 requires a real green CI run at the exact SHA that receives the tag, and a hygiene commit sitting last would make that unsatisfiable by any evidence. Plan 12-04 declares `depends_on: [12-01, 12-02, 12-03]` to enforce this ordering mechanically rather than by prose reminder.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `- [x] 11-08-PLAN.md — …[SEC-02, SEC-03, SEC-04] (depends 11-07)` | new ROADMAP plan-list entry under Phase 11 Wave 3 | `.planning/ROADMAP.md` |
| ADDR-04 resolution addendum sentence | new sentence appended to the existing WARNING paragraph | `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md` |

No new code symbols, no new files. Every edit is a scoped replacement inside an existing document.

<tasks>

<task type="auto">
  <name>Task 1: reconcile UAT-01's two representations in REQUIREMENTS.md against its recorded PASS</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 79-92 (the `### UAT — Phase 11` section and the `### Release (REL)` section immediately after it) and lines 124-160 (the `## Traceability` table)
    - `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-HUMAN-UAT.md` frontmatter (the recorded `verdict` value — cite it, do not assume it)
    - `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md` lines 30-32 (truth #7 and the 7/7 score) — the corroborating evidence for the flip
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § "REL-04 — Release-Record Hygiene (three exact defects)" subsection (a)
  </read_first>
  <action>
    Confirm the evidence first: read `11-HUMAN-UAT.md`'s frontmatter and quote its recorded verdict value in the SUMMARY. Only then edit.

    In `.planning/REQUIREMENTS.md`, locate the UAT-01 entry in the `### UAT — Phase 11` section (near line 81) and change its checklist marker from the incomplete form to the completed form, using the lowercase-x marker already used by every other completed item in this file. Leave the requirement's descriptive text unchanged — this is a status flip, not a rewrite. Then locate the UAT-01 row in the `## Traceability` table (near line 153) and change its Status cell to `Complete`, matching the exact spelling used by the 23 completed rows above it.

    Do NOT touch the five REL-01..REL-05 rows or their checklist entries — those are this phase's own in-flight requirements and are closed by the phase's verification step, not by REL-04. Do NOT alter the `## Coverage` counts (29 total / 29 mapped is unchanged by a status flip). Use scoped `Edit` replacements; never rewrite the whole file.

    Append a one-line provenance note to the file's footer `*Last updated:*` line recording that UAT-01's status was reconciled to its recorded verdict under REL-04, with the date.
  </action>
  <verify>
    <automated>grep -c 'UAT-01' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'UAT-01' .planning/REQUIREMENTS.md` returns `2` — both representations still exist; neither was deleted instead of flipped.
    - `grep -c '^- \[ \] \*\*UAT-01' .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c '^- \[x\] \*\*UAT-01' .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c '| UAT-01 | Phase 11 | Pending |' .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c '| UAT-01 | Phase 11 | Complete |' .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c '| Phase 11 | Pending |' .planning/REQUIREMENTS.md` returns `0` — no other Phase 11 row was left behind.
    - `grep -c '^- \[ \] \*\*REL-0' .planning/REQUIREMENTS.md` returns `5` — the in-flight REL rows were NOT collaterally checked.
    - `grep -c 'v1 requirements: 29 total' .planning/REQUIREMENTS.md` returns `1` — coverage counts untouched.
    - The SUMMARY quotes `11-HUMAN-UAT.md`'s recorded verdict value as the evidence for the flip.
  </acceptance_criteria>
  <reversibility rating="reversible">Two scoped status-cell edits in a planning document.</reversibility>
  <done>UAT-01 reads Complete in both places, backed by a cited recorded verdict, with no collateral edits to in-flight requirements or coverage counts.</done>
</task>

<task type="auto">
  <name>Task 2: complete Phase 11's ROADMAP entry (11-08 in Wave 3, plan count 8, top-level box) and close the ADDR-04 loop in 11-VERIFICATION.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 24-30 (the top-level `## Phases` checklist — note Phase 9 at line 26 and Phase 10 at line 27 are ALSO unchecked) and lines 265-290 (the `### Phase 11` detail section: the `**Plans:** 7 plans` line and the Wave 1 / Wave 2 / Wave 3 / Wave 4 lists)
    - `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-08-PLAN.md` frontmatter lines 1-14 (`wave: 3`, `depends_on: [11-07]`, `requirements: [SEC-02, SEC-03, SEC-04]`) and its `<objective>` opening — the source of truth for the entry's wave placement and one-line description
    - `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md` lines 1-8 (frontmatter `status`/`score`) and line 112 (the WARNING paragraph whose final sentence still calls ADDR-04 an unresolved dangling reference)
    - `.planning/REQUIREMENTS.md` lines 49-56 (the standing `ADDR-04 does not exist — resolved` note that the addendum will point at)
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § REL-04 subsections (b) and (c), including the out-of-scope flag on Phase 9/10
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, make three scoped edits to the Phase 11 material. First, add a plan entry for `11-08-PLAN.md` to the **Wave 3** list in the `### Phase 11` detail section, placed after the `11-05-PLAN.md` entry so it follows `11-07-PLAN.md` in document order; derive its one-line description and bracketed requirement list from `11-08-PLAN.md`'s own frontmatter and objective (its three requirements are SEC-02, SEC-03, SEC-04 and it depends on 11-07) and match the exact shape of the sibling entries, including the completed-checkbox marker since the plan shipped and `11-VERIFICATION.md` attributes SEC-01..04 to `11-07, 11-08`. Second, correct the `**Plans:**` count line for Phase 11 from seven to eight, preserving the existing parenthetical revision note verbatim. Third, mark Phase 11's top-level checklist box (line 28) complete, appending a completion citation in the same shape the Phase 5/6/7/8 entries use, citing `11-VERIFICATION.md`'s recorded status and score.

    Leave Phase 9's and Phase 10's top-level boxes ALONE. `12-RESEARCH.md` § REL-04(c) flags them as also-stale but explicitly out of REL-04's literal scope, which names only Phase 11. Record them in the SUMMARY as a flagged follow-up so the observation is not lost — do not silently fix them and do not silently drop them.

    Use `Edit` for every change. Never pass whole-file content to `Write` for `ROADMAP.md` — a full-file rewrite destroys phase entries outside the diff window.

    Then, in `.planning/phases/11-.../11-VERIFICATION.md`, append one sentence to the end of the existing WARNING paragraph (line 112) stating that the ADDR-04 reference is now resolved and pointing the reader at the standing note in `.planning/REQUIREMENTS.md` (lines 49-56), which names the originating commit and records that the trust-boundary work the message referred to is covered by SEC-01. Do NOT rewrite or delete the original WARNING text — this is a historical verification record; append, do not revise. Do NOT create an ADDR-04 requirement anywhere.

    Commit all three files from Tasks 1 and 2 together with `git add -f` (`.planning/` is gitignored). Expect and accept zero CI check-runs for this commit — that is the documented `paths-ignore` behavior, not a failure.
  </action>
  <verify>
    <automated>grep -c '11-08-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '11-08-PLAN.md' .planning/ROADMAP.md` returns `1`.
    - `grep -n '11-07-PLAN.md' .planning/ROADMAP.md` reports a smaller line number than `grep -n '11-08-PLAN.md' .planning/ROADMAP.md` — 11-08 follows 11-07 in document order, matching `depends_on: [11-07]`.
    - The `11-08-PLAN.md` entry sits inside the Phase 11 Wave 3 block: `awk '/^\*\*Wave 3\*\*/,/^\*\*Wave 4\*\*/' .planning/ROADMAP.md | grep -c '11-08-PLAN.md'` returns `1`.
    - The 11-08 entry names all three of `SEC-02`, `SEC-03`, `SEC-04`.
    - `sed -n '269p' .planning/ROADMAP.md` (the Phase 11 `**Plans:**` line, or wherever it now sits) contains `8 plans` and no longer contains `7 plans`; `awk '/^### Phase 11:/,/^### Phase 12:/' .planning/ROADMAP.md | grep -c '7 plans'` returns `0`.
    - `awk '/^### Phase 11:/,/^### Phase 12:/' .planning/ROADMAP.md | grep -c '11-0[1-8]-PLAN.md'` returns `8` — all eight plans listed exactly once.
    - `grep -c '^- \[x\] \*\*Phase 11:' .planning/ROADMAP.md` returns `1`.
    - `grep -c '^- \[ \] \*\*Phase 9:' .planning/ROADMAP.md` returns `1` and `grep -c '^- \[ \] \*\*Phase 10:' .planning/ROADMAP.md` returns `1` — the out-of-scope boxes were deliberately left alone, and the SUMMARY records them as a flagged follow-up.
    - `awk '/^### Phase 12:/,0' .planning/ROADMAP.md | grep -c 'REL-01, REL-02, REL-03, REL-04, REL-05'` returns `1` — Phase 12's own entry survived the edit intact.
    - `grep -c 'ADDR-04' .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-VERIFICATION.md` returns at least `1`, and the WARNING paragraph now also contains a reference to `REQUIREMENTS.md`.
    - `grep -c 'ADDR-04' .planning/REQUIREMENTS.md` is unchanged from its pre-task value (the standing note only; no phantom requirement was minted) — record the before and after counts in the SUMMARY.
    - `git show --stat HEAD` lists all three files; `git status --porcelain .planning/` is clean after the forced add.
  </acceptance_criteria>
  <reversibility rating="reversible">Scoped edits to three planning documents; single-commit revert with no code or build impact.</reversibility>
  <done>The release record is internally consistent: Phase 11 shows all 8 plans and its true completion state, UAT-01 reads Complete, and the ADDR-04 loop is closed by pointer rather than by inventing a requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release record → tag annotation | The `v1.0.0` tag re-attests §16 items 1-5 and 7 *by citation* to these documents. A false or incomplete record propagates directly into an outward-facing, effectively permanent artifact. |
| verification record → future reader | `11-VERIFICATION.md` is an immutable-by-convention historical record; edits to it must be additive so a later reader can still see what the verifier originally observed. |

## STRIDE Threat Register (OWASP ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Repudiation | `.planning/REQUIREMENTS.md` traceability + checklist | medium | mitigate | A record claiming an unverified item is complete makes the tag's citation unfalsifiable. Task 1 requires the recorded verdict be read and quoted BEFORE the flip, and the acceptance criteria prove no in-flight REL row was collaterally checked. |
| T-12-03-02 | Tampering | `.planning/ROADMAP.md` | high | mitigate | A whole-file `Write` on ROADMAP.md silently destroys phase entries outside the diff window (a known GSD failure mode). Mitigation: `Edit`-only, plus an explicit acceptance criterion that Phase 12's own entry and its requirement list survive the edit. |
| T-12-03-03 | Tampering | `11-VERIFICATION.md` | medium | mitigate | Rewriting a historical verification record erases the evidence trail. Mitigation: the addendum is append-only, the original WARNING text is preserved verbatim, and the acceptance criteria check for the added pointer rather than for removed text. |
| T-12-03-04 | Spoofing | `ADDR-04` | medium | mitigate | Resolving a dangling reference by minting a phantom requirement would fabricate a verification history for work that was never separately specified. Mitigation: the plan resolves by pointer only, and an acceptance criterion pins the `ADDR-04` occurrence count in REQUIREMENTS.md as unchanged. |
| T-12-03-05 | Information disclosure | planning documents | low | accept | These files contain no credentials, keys, hostnames, or personal data — only requirement text and verification verdicts. Accepted with no mitigation. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and touches no manifest. Gate: `git show --stat HEAD` must list only files under `.planning/`; any `Cargo.toml`/`Cargo.lock` in that commit means the wrong files were staged — stop and re-stage. |
</threat_model>

<verification>
- Every grep-based acceptance criterion above returns the stated value.
- `11-HUMAN-UAT.md`'s recorded verdict is quoted in the SUMMARY as the evidence for the UAT-01 flip.
- `11-08-PLAN.md`'s frontmatter is quoted in the SUMMARY as the source of the ROADMAP entry's wave and requirement list.
- Phase 9/10's still-unchecked top-level boxes are recorded in the SUMMARY as a flagged, deliberately-deferred follow-up.
- No code, no manifests, no build. This commit legitimately produces zero CI check-runs.
</verification>

<success_criteria>
REL-04 is closed: all three named defects are corrected with cited evidence, no phantom requirement was minted, no historical record was rewritten, no out-of-scope item was silently fixed or silently dropped, and the commit lands before the version bump so REL-03's green-gate attestation stays satisfiable.
</success_criteria>

## Probe reconciliation (spec-less fallback — REL-04)

The probe surfaced **4 applicable edge items for REL-04**. Reconciliation: **4 authored into `must_haves.truths` + 0 flagged assumptions = 4.** No silent drops.

| # | Category | Disposition | Where |
|---|----------|-------------|-------|
| 15 | adjacency | covered | truth 1 (UAT-01's two adjacent representations flip together; neither is deleted instead of flipped — pinned by the count-still-2 criterion) |
| 16 | empty | covered | truth 3 (the missing plan entry and the off-by-one count — the empty slot in the Phase 11 wave list) |
| 17 | encoding | covered | truth 2 (which literal form counts as "checked": lowercase `[x]` matching the file's existing convention, so the grep gate matches without a case or Unicode variant) |
| 18 | ordering | covered | truth 4 (11-08 must follow 11-07 in document order, reflecting `depends_on: [11-07]`, not merely be present somewhere) |

Prohibition probe (in-prompt, two-stage): 1 kept for REL-04, authored descriptor-less into `must_haves.prohibitions` above (integrity class — no evidence-free status flip, no phantom-requirement back-fill). Routine-engineering candidates ("must not corrupt the markdown table", "must not leave a trailing space") were dropped in the precision stage.

<output>
Create `.planning/phases/12-v1-0-0-release-gate/12-03-SUMMARY.md` when done. `.planning/` is gitignored in this repo — use `git add -f` for every file this plan touches, including the summary.
</output>
