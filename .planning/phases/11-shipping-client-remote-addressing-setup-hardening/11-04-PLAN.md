---
phase: 11-shipping-client-remote-addressing-setup-hardening
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-03, 11-07]
files_modified:
  - crates/famp/tests/fixtures/cross_machine/alice.crt
  - crates/famp/tests/fixtures/cross_machine/alice.key
  - crates/famp/tests/fixtures/cross_machine/bob.crt
  - crates/famp/tests/fixtures/cross_machine/bob.key
  - crates/famp/tests/fixtures/cross_machine/README.md
  - crates/famp-gateway/tests/common/gateway_harness.rs
  - crates/famp-gateway/tests/e2e_cross_host_delivery.rs
  - crates/famp-gateway/tests/e2e_shipping_surface.rs
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The committed cross_machine TLS fixtures are CA:FALSE (critical) + extendedKeyUsage=serverAuth, verifying on BOTH Apple SecTrust (macOS) and webpki (Linux)."
    - "A shipping-surface integration test drives the FIXED `famp send` (not the raw bus client / not the injector) cross-host and reaches delivery into the remote mailbox."
    - "A FULL-CYCLE shipping test drives request→commit→deliver→ack via the real `famp send` (mode flags) and asserts BOTH sides converge on a TERMINAL FSM state (mirrors e2e_cross_host_delivery.rs:707-774) — this is what makes UAT-01's terminal-FSM gate reachable via the shipping surface."
    - "A NEGATIVE test proves a `local.bus`-authority `from` through the federated path yields a TYPED, OBSERVABLE rejection — asserted by capturing gateway-A stderr (the enriched relay-failure line contains the ingress slug `unpinned_key` + `403`/`401`) AND asserting non-delivery on B — NOT by matching a Rust error type across the process boundary."
    - "The two-host harness (Side/famp_cmd/spawn_gateway/poll_inbox_contains) is extracted into tests/common/gateway_harness.rs and #[path]-included by BOTH e2e files; extraction is mechanical (no behavioral edits) and e2e_cross_host_delivery.rs stays green."
    - "The existing macOS CI matrix leg (ci.yml test job) exercises the Apple-verifier path with the regenerated fixtures."
  artifacts:
    - "regenerated crates/famp/tests/fixtures/cross_machine/{alice,bob}.{crt,key} + corrected README"
    - "NEW crates/famp-gateway/tests/common/gateway_harness.rs (extracted shared harness)"
    - "NEW crates/famp-gateway/tests/e2e_shipping_surface.rs (happy-path + full-cycle terminal + negative)"
  key_links:
    - "fixed famp send (plan 03, mode-branched class) -> local bus -> gateway egress (plan 07 own-domain check) -> HTTPS (regen fixtures) -> ingress verify -> remote mailbox -> reply legs -> terminal FSM both sides"
  prohibitions:
    - "Do NOT weaken or #[ignore] the E2E; it runs in `just ci` on both OS legs."
    - "The ONLY permitted edit to e2e_cross_host_delivery.rs is the mechanical harness extraction (move helpers to common/gateway_harness.rs, #[path]-include) — NO behavioral change; it stays green."
    - "Do NOT match on a Rust error type (`UnpinnedKey`) across the two subprocess boundaries — assert the observable gateway-A stderr text + non-delivery instead."
    - "Throwaway artifacts (wire_proof_inject.rs / probe_tls.rs) are NOT git-tracked — confirm via `git ls-files` and skip deletion; focus on ADDING."
---

<objective>
D-08 + D-09 / TEST-03 — replace the throwaway injector with a real shipping-surface regression net, and fix the platform-conditional fixtures. First run the falsification control: confirm the CURRENT ECDSA/no-EKU fixtures fail on macOS (Apple SecTrust `EkuError`) — proving the failure mode — then regenerate to the CA:FALSE+serverAuth recipe and confirm green on both legs. Then add an integration test that drives the FIXED `famp send` cross-host (mirroring the E2E harness) plus a negative test that a `local.bus`-authority envelope through the federated path yields a typed error.

Purpose: prove the fix works from the shipping surface, on both verifiers, permanently in CI.
Output: regenerated fixtures + corrected README + a new additive e2e test file (happy + negative).
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-RESEARCH.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-PATTERNS.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-03-SUMMARY.md
@.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: falsification-control + regenerate cross_machine fixtures to CA:FALSE+serverAuth (D-08)</name>
  <files>crates/famp/tests/fixtures/cross_machine/alice.crt, crates/famp/tests/fixtures/cross_machine/alice.key, crates/famp/tests/fixtures/cross_machine/bob.crt, crates/famp/tests/fixtures/cross_machine/bob.key, crates/famp/tests/fixtures/cross_machine/README.md</files>
  <read_first>
    - crates/famp/tests/fixtures/cross_machine/README.md (stale — claims "Ed25519" and references the deleted `_gen_fixture_certs` example; both wrong per RESEARCH)
    - .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-RESEARCH.md "Pitfall 3" + "Canonical cross-platform cert recipe" (the openssl recipe; SANs `127.0.0.1`/`localhost` because the E2E binds loopback)
    - .github/workflows/ci.yml L104-118 (the `test` matrix job already includes `macos-latest` → exercises Apple SecTrust via rustls-platform-verifier)
    - crates/famp-gateway/tests/e2e_cross_host_delivery.rs (the consumer of these fixtures; binds loopback ~L339)
  </read_first>
  <action>
    FIRST (falsification control, RESEARCH Open Q2): determine whether `e2e_cross_host_delivery` currently PASSES on macOS with the existing ECDSA/no-EKU fixtures — run it on macOS locally (`cargo test -p famp-gateway --test e2e_cross_host_delivery`) or inspect recent CI runs of the `macos-latest` leg. Record the observed pass/fail in the SUMMARY. THEN BRANCH ON THE OUTCOME (this is the review MEDIUM — a control with no branch proves nothing):
      - If the pre-regen fixtures FAIL on macOS (Apple SecTrust `EkuError`, the expected finding-#5 mode): the regen→green transition IS the proof; proceed.
      - If the pre-regen fixtures PASS on macOS (the `--trust-cert` extra-root path is masking the EKU/CA check per Open Q2): a post-regen green proves NOTHING. In that case ADD a dedicated test exercising the SYSTEM-TRUST / no-extra-root client path (the config that actually failed in the live dogfood) — i.e. build the outbound client WITHOUT the fixture as an added root and assert it FAILS on the OLD-shape cert and PASSES on the regenerated CA:FALSE+serverAuth cert. Name explicitly, per the project's control rule, ONE must-fail expectation (old-shape cert rejected on the system-trust path) AND ONE must-pass expectation (regenerated cert accepted) so green-under-both is impossible.
    THEN regenerate `crates/famp/tests/fixtures/cross_machine/{alice,bob}.{crt,key}` using the canonical recipe: `openssl req -x509 -newkey rsa:2048 -nodes -days 800 -keyout <host>.key -out <host>.crt -subj "/CN=<host>" -addext "subjectAltName=IP:127.0.0.1,DNS:localhost" -addext "basicConstraints=critical,CA:FALSE" -addext "keyUsage=critical,digitalSignature,keyEncipherment" -addext "extendedKeyUsage=serverAuth"` (or an equivalent in-tree `rcgen` example setting `IsCa::NoCa` + `ExtendedKeyUsagePurpose::ServerAuth`). Rewrite the README to state the actual key type (RSA-2048), the CA:FALSE+serverAuth policy, the loopback SANs, and the regeneration recipe; remove the stale `_gen_fixture_certs` reference and the "Ed25519" claim. Confirm `openssl x509 -text` on each new cert shows `CA:FALSE` critical + `Extended Key Usage: TLS Web Server Authentication`.
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test e2e_cross_host_delivery</automated>
  </verify>
  <acceptance_criteria>
    - `openssl x509 -in crates/famp/tests/fixtures/cross_machine/alice.crt -text -noout | grep -A1 'Basic Constraints'` shows `CA:FALSE` (critical); `... | grep -A1 'Extended Key Usage'` shows `TLS Web Server Authentication`. Same for bob.crt.
    - The regenerated certs carry `127.0.0.1`/`localhost` SANs.
    - `e2e_cross_host_delivery` is green locally; README no longer contains `Ed25519` or `_gen_fixture_certs`.
    - The pre-regen macOS pass/fail is recorded in the SUMMARY AND the branch is honored: if it passed pre-regen, a dedicated system-trust/no-extra-root test exists with a named must-fail (old cert) + must-pass (new cert). If it failed pre-regen, that is documented as the control.
  </acceptance_criteria>
  <done>Fixtures regenerated to the cross-platform recipe; README corrected; e2e green; falsification control branched on its outcome (system-trust test added if the control passed pre-regen).</done>
</task>

<task type="auto">
  <name>Task 2: extract the two-host harness into tests/common/gateway_harness.rs (mechanical, no behavioral change)</name>
  <files>crates/famp-gateway/tests/common/gateway_harness.rs, crates/famp-gateway/tests/e2e_cross_host_delivery.rs</files>
  <read_first>
    - crates/famp-gateway/tests/e2e_cross_host_delivery.rs (the private helpers to MOVE: `Side` ~L150, `famp_cmd` ~L174, `spawn_gateway` ~L318, `poll_inbox_contains` ~L553, `connect_no_spawn` ~L526, plus `poll_terminal_state`, domain constants ALICE/BOB L86-92 — Rust integration-test binaries are separate crates, so a sibling test file CANNOT import these while they stay private to this file)
    - crates/famp-gateway/tests/common/child_guard.rs (the existing shared-helper pattern — `common/` is already the shared-module convention; mirror how it is `#[path]`/`mod`-included)
  </read_first>
  <action>
    Extract the shared two-host harness (`Side`, `famp_cmd`, `spawn_gateway`, `connect_no_spawn`, `poll_inbox_contains`, `poll_terminal_state`, the ALICE/BOB domain constants, and any small helpers Task 3 needs) OUT of `e2e_cross_host_delivery.rs` and INTO a new `crates/famp-gateway/tests/common/gateway_harness.rs`. `#[path = "common/gateway_harness.rs"] mod gateway_harness;` (or the established `common/` include idiom) from BOTH `e2e_cross_host_delivery.rs` and the new `e2e_shipping_surface.rs` (Task 3). This is a PURELY MECHANICAL move — NO behavioral edits to any test logic; the assertions, poll deadlines, and spawn sequence are byte-identical, just relocated. Make items `pub(crate)`/`pub` as needed for cross-file visibility. Guard: `cargo test -p famp-gateway --test e2e_cross_host_delivery` must stay green after the move (identical behavior — the falsification control).
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test e2e_cross_host_delivery</automated>
  </verify>
  <acceptance_criteria>
    - `crates/famp-gateway/tests/common/gateway_harness.rs` exists and holds `Side`/`famp_cmd`/`spawn_gateway`/`poll_inbox_contains`/`poll_terminal_state`/`connect_no_spawn` + domain constants.
    - `e2e_cross_host_delivery.rs` `#[path]`-includes the extracted module and no longer defines those helpers inline; its behavior is unchanged (green).
    - `cargo test -p famp-gateway --test e2e_cross_host_delivery` green; `just lint` clean.
  </acceptance_criteria>
  <done>Harness extracted to common/gateway_harness.rs, included from the existing e2e; e2e_cross_host_delivery stays green (control).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shipping-surface e2e — happy + full-cycle terminal + observable negative (D-09, review HIGH #1 / MEDIUM negative-observable)</name>
  <files>crates/famp-gateway/tests/e2e_shipping_surface.rs</files>
  <read_first>
    - crates/famp-gateway/tests/common/gateway_harness.rs (the extracted harness from Task 2 — `#[path]`-include it; do NOT copy-paste)
    - crates/famp-gateway/tests/e2e_cross_host_delivery.rs L700-775 (the full request→commit→deliver→ack cycle + `poll_terminal_state` convergence assertion the full-cycle test mirrors — but driven via the shipping `famp send` mode flags, not `send_bus_envelope`)
    - crates/famp/src/cli/send/mod.rs (the FIXED `famp send` this test drives: `--to agent:<domain>/<name> --new-task` = request; `--to ... --task <id>` = commit; `--to ... --task <id> --terminal` = deliver-terminal — the mode-branched class from plan 03 Task 2)
    - crates/famp-gateway/src/ingress.rs L141-151 (ingress returns the typed slug `unpinned_key` with HTTP 403/401 on an unpinned `from`) — the OBSERVABLE the negative test asserts on gateway-A stderr, NOT a cross-process Rust type match
    - crates/famp-gateway/src/egress.rs drain-loop eprintln ~L273 (plan-01-enriched relay-failure line — where gateway-A surfaces the ingress rejection)
    - git ls-files check: confirm `wire_proof_inject.rs` / `probe_tls.rs` are absent from the tree (skip any "delete" step)
  </read_first>
  <behavior>
    - Happy path: `famp send --to agent:<bob-domain>/bob --new-task "..."` from the A side delivers a typed request into B's real `bob` mailbox with a domain-qualified `from`.
    - Full-cycle terminal: drive request (A `--new-task`), then commit (B replies `--to agent:<alice-domain>/alice --task <id>`), then deliver-terminal (B `--task <id> --terminal`), then ack (A) — via the shipping client on each side — and assert BOTH sides converge on a TERMINAL FSM state via `poll_terminal_state` (mirrors e2e:761-774). This proves UAT-01's terminal-FSM gate is reachable through the shipping surface.
    - Negative (OBSERVABLE): an envelope whose `from` authority is `local.bus` pushed through the federated path is rejected; assert by capturing gateway-A's stderr and matching the enriched relay-failure line contains the ingress slug `unpinned_key` and `403`/`401`, AND assert NON-delivery on B — NOT by matching a Rust `UnpinnedKey` type across the process boundary.
    - ChildGuard for every spawned child; bounded polling (no fixed `sleep`).
  </behavior>
  <action>
    Create crates/famp-gateway/tests/e2e_shipping_surface.rs, `#[path]`-including the Task-2 `common/gateway_harness.rs`. (1) HAPPY: stand up the two-process loopback gateway pair with the regenerated fixtures + own-domain configured on each host (set `FAMP_OWN_DOMAIN` or the file so plan-07's egress check is exercised, and so the `famp send` client can stamp `from`); invoke the SHIPPING `famp send --to agent:<bob-domain>/bob --new-task` (spawn the built `famp` binary via `famp_cmd`, or call the send entrypoint) and assert delivery into B's `bob` mailbox with a domain-qualified `from`. (2) FULL-CYCLE: drive the four legs (request/commit/deliver-terminal/ack) each via the shipping client's mode flags, then assert `poll_terminal_state` on both sides converges to the SAME terminal state (e.g. COMPLETED). (3) NEGATIVE: drive a `from = agent:local.bus/...` envelope through the federated path; capture gateway-A stderr (the `spawn_gateway` child's stderr — do NOT discard it) and assert a TYPED, OBSERVABLE rejection with NON-delivery on B. RECONCILE WITH PLAN 07 (checker warning): plan-07 adds an egress `from.authority()==own-domain` check that fires (as `FromDomainMismatch`) LOCALLY on gateway-A *before* remote ingress WHENEVER own-domain is configured on gateway-A — pre-empting the `unpinned_key` slug. So either (preferred, deterministic) run THIS negative case with gateway-A own-domain UNSET, so plan-07's when-configured egress check is skipped, the envelope reaches remote ingress, and the stderr line carries the ingress slug `unpinned_key` + `403`/`401`; OR keep own-domain configured and assert the egress `FromDomainMismatch` line instead. Do NOT hard-code an assertion that is unreachable under the happy-path's own-domain-configured state. Either way assert NO delivery landed in B's mailbox (bounded poll times out empty). First `git ls-files | grep -E 'wire_proof_inject|probe_tls'` to confirm the throwaways are untracked; skip deletion. Keep `e2e_cross_host_delivery.rs` behavior untouched.
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test e2e_shipping_surface</automated>
  </verify>
  <acceptance_criteria>
    - Happy-path drives `famp send`'s remote path (not `send_bus_envelope`) and asserts delivery with a domain-qualified `from`.
    - A full-cycle test drives request→commit→deliver→ack via the shipping mode flags and asserts both sides converge on a terminal FSM state (COMPLETED).
    - The negative test asserts gateway-A stderr contains a typed rejection slug (`unpinned_key` + `403`/`401` when own-domain UNSET on A so it reaches ingress; OR the plan-07 egress `FromDomainMismatch` line when own-domain configured) AND asserts non-delivery on B — no cross-process Rust-type match, and no hard-coded slug that is unreachable under the configured state.
    - `grep -n 'sleep' crates/famp-gateway/tests/e2e_shipping_surface.rs` shows no fixed-sleep synchronization (bounded poll only); ChildGuard used for spawned children.
    - `cargo test -p famp-gateway --test e2e_shipping_surface` green; `e2e_cross_host_delivery` still green; `just ci` + `just lint` clean.
  </acceptance_criteria>
  <done>Shipping-surface e2e (happy + full-cycle terminal + observable negative) green; injector retired (confirmed untracked); existing E2E behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote gateway -> ingress verify | inbound envelope trust decision keyed on `from` |
| TLS channel (cross-platform) | Apple SecTrust vs webpki EKU/CA divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Spoofing | TLS cert passing one platform only | high | mitigate | CA:FALSE+serverAuth fixtures verified on both verifiers; macOS CI leg exercises Apple SecTrust |
| T-11-11 | Repudiation | silent drop of local.bus-authority envelope | high | mitigate | D-09 negative test asserts a TYPED reject, not a black hole (observability + fail-closed) |
| T-11-12 | Tampering | E2E regression net weakened | medium | mitigate | new test is additive + runs in `just ci` on both legs, not `#[ignore]`'d |

No new dependencies — no supply-chain (T-*-SC) row.
</threat_model>

<verification>
- `cargo test -p famp-gateway --test e2e_cross_host_delivery` green (regen fixtures).
- `cargo test -p famp-gateway --test e2e_shipping_surface` green (happy + negative).
- `just ci` green on both OS legs; `just lint` clean.
- openssl inspection confirms CA:FALSE+serverAuth on both certs.
</verification>

<success_criteria>
The shipping `famp send` cross-host path has a permanent CI regression net on both verifiers; a `local.bus`-authority envelope through the federated path fails typed; fixtures are cross-platform-correct; the throwaway injector is retired.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Regenerated `crates/famp/tests/fixtures/cross_machine/{alice,bob}.{crt,key}` (RSA-2048, CA:FALSE, serverAuth, loopback SANs)
- Corrected fixtures README
- NEW `crates/famp-gateway/tests/common/gateway_harness.rs` (mechanically-extracted shared two-host harness, #[path]-included by both e2e files)
- NEW `crates/famp-gateway/tests/e2e_shipping_surface.rs` (happy + full-cycle terminal via shipping mode flags + observable negative: gateway-A stderr `unpinned_key`/`403` + non-delivery on B)
- Falsification-control record (pre-regen macOS behavior + branch outcome) in the SUMMARY; system-trust/no-extra-root test if the control passed pre-regen
</artifacts>

<output>
Create `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-04-SUMMARY.md` when done
</output>
