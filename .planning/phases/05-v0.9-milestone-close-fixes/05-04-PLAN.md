---
phase: 05-v0.9-milestone-close-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - "BROKER-01"
  - "BROKER-02"
  - "BROKER-03"
  - "BROKER-04"
  - "BROKER-05"
  - "CLI-01"
  - "CLI-02"
  - "CLI-03"
  - "CLI-04"
  - "CLI-05"
  - "CLI-06"
  - "CLI-07"
  - "CLI-08"
  - "CLI-09"
  - "CLI-10"
  - "CLI-11"
  - "MCP-01"
  - "MCP-02"
  - "MCP-03"
  - "MCP-04"
  - "MCP-05"
  - "MCP-06"
  - "MCP-07"
  - "MCP-08"
  - "MCP-09"
  - "MCP-10"
  - "HOOK-01"
  - "HOOK-02"
  - "HOOK-03"
  - "HOOK-04a"
  - "TEST-01"
  - "TEST-02"
  - "TEST-03"
  - "TEST-04"
  - "TEST-05"
  - "CARRY-02"
requirements_addressed:
  - "BROKER-01"
  - "BROKER-02"
  - "BROKER-03"
  - "BROKER-04"
  - "BROKER-05"
  - "CLI-01"
  - "CLI-02"
  - "CLI-03"
  - "CLI-04"
  - "CLI-05"
  - "CLI-06"
  - "CLI-07"
  - "CLI-08"
  - "CLI-09"
  - "CLI-10"
  - "CLI-11"
  - "MCP-01"
  - "MCP-02"
  - "MCP-03"
  - "MCP-04"
  - "MCP-05"
  - "MCP-06"
  - "MCP-07"
  - "MCP-08"
  - "MCP-09"
  - "MCP-10"
  - "HOOK-01"
  - "HOOK-02"
  - "HOOK-03"
  - "HOOK-04a"
  - "TEST-01"
  - "TEST-02"
  - "TEST-03"
  - "TEST-04"
  - "TEST-05"
  - "CARRY-02"
gap_closure: true

must_haves:
  truths:
    - "All 36 Phase 2 requirement IDs (BROKER-01..05, CLI-01..11, MCP-01..10, HOOK-01..03 + HOOK-04a, TEST-01..05, CARRY-02) appear as `- [x]` in the body checklist sections of REQUIREMENTS.md."
    - "All 36 Phase 2 requirement IDs appear as `Complete` in the traceability table at the bottom of REQUIREMENTS.md."
    - "No requirement that was previously `[x]` / `Complete` is regressed to `[ ]` / `Pending`."
    - "CC-07 and HOOK-04b status remain `Pending (BROKEN — gap closure...)` / `Pending (PARTIAL — gap closure...)` (or are flipped to Complete only after their respective fix plans 05-01 and 05-02 ship — see note below)."
    - "Coverage line at bottom (`**Coverage:** 85/85 ...`) is preserved or refreshed; the active-requirements total (85) does NOT change."
    - "REQ-IDs out of Phase 2 (BUS, TDD, PROP, AUDIT, CARRY-03/04, CC-*, FED-*, MIGRATE-*, TEST-06, CARRY-01) are not modified."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 2 traceability rows and body checkboxes flipped from Pending → Complete (drift cleanup; no real-state change)."
      contains: "BROKER-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md (body section, lines ~55-103, 137)"
      to: ".planning/REQUIREMENTS.md (traceability table, lines ~177-265)"
      via: "Same REQ-ID rendered consistently in both views (checkbox state and table-row Status agree)."
      pattern: "BROKER-01"
    - from: ".planning/REQUIREMENTS.md (Phase 2 rows)"
      to: ".planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-VERIFICATION.md"
      via: "Source-of-truth for Complete status (36/36 verified, 2 manual UATs resolved 2026-04-30)."
      pattern: "Complete"
---

<objective>
Close milestone gap #4 (BOOKKEEPING DRIFT): the body section of
`.planning/REQUIREMENTS.md` shows the 36 Phase 2 requirements as `- [ ]`
and the traceability table at the bottom shows them as `Pending`, but
`02-VERIFICATION.md` already established 36/36 satisfied (34 automated +
2 manual UATs resolved 2026-04-30 — see STATE.md "Last Shipped" entry and
v0.9-MILESTONE-AUDIT.md `traceability_drift` block).

Purpose: Make REQUIREMENTS.md an honest snapshot of the verified state so
the next `/gsd-audit-milestone v0.9` run will not re-flag this drift, and
so a future reader cannot mistake a checkbox for a real gap.

Scope: PURE BOOKKEEPING. No code change. No new artifacts. No semantic
edit beyond flipping checkbox state and traceability `Pending` → `Complete`
on the 36 Phase 2 IDs. CC-07 and HOOK-04b remain `Pending` until plans
05-01 and 05-02 ship — they are flipped to `Complete` by their own
SUMMARYs, not by this plan.

Output: Updated `.planning/REQUIREMENTS.md` reflecting verified Phase 2
state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v0.9-MILESTONE-AUDIT.md
@.planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-VERIFICATION.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Two locations in REQUIREMENTS.md need flipping:

1. BODY CHECKBOXES (lines ~55-103, 137 — section by section):

   Lines 55-59  BROKER-01..05      [ ] -> [x]
   Lines 63-73  CLI-01..11         [ ] -> [x]
   Lines 77-86  MCP-01..10         [ ] -> [x]
   Lines 90-93  HOOK-01..03 + HOOK-04a [ ] -> [x]
                (HOOK-04b on line 94 stays [ ] until 05-02 ships)
   Lines 98-102 TEST-01..05        [ ] -> [x]
                (TEST-06 line 103 already [x] — leave alone)
   Line 137     CARRY-02 already [x] — VERIFY no change needed
                (Audit doc reports body as [ ] for CARRY-02 in some renderings;
                 verify by reading the actual file. If already [x], DO NOT touch.)

2. TRACEABILITY TABLE (lines ~177-267):

   Lines 207-243 (BROKER-01..05, CLI-01..11, MCP-01..10, HOOK-01..03,
                  HOOK-04a, TEST-01..05, CARRY-02):
       Status `Pending` -> `Complete`

   Lines NOT to touch:
     - HOOK-04b (line 237):  "Pending (PARTIAL — gap closure per v0.9-MILESTONE-AUDIT.md)"
       -> stays Pending until 05-02 ships
     - CC-07 (line 250):     "Pending (BROKEN — gap closure per v0.9-MILESTONE-AUDIT.md)"
       -> stays Pending until 05-01 ships
     - All other rows (BUS-*, TDD-*, PROP-*, AUDIT-*, CARRY-03, CARRY-04,
       CC-* except CC-07, FED-*, MIGRATE-*, TEST-06, CARRY-01) are already
       Complete — DO NOT MODIFY.

3. COVERAGE LINE (line ~267):
     "**Coverage:** 85/85 v0.9 active requirements mapped..."
     The total stays at 85. The Phase 2 sweep does NOT change the total;
     it only changes Pending->Complete counts. If the line carries a
     completed-counter (it does not in current text — verify by reading),
     refresh it. Otherwise leave as-is.

NOTE: This plan does NOT depend on 05-01 or 05-02 (Wave 1 placement is
deliberate). It flips ONLY the 36 already-satisfied Phase 2 IDs. CC-07
and HOOK-04b are flipped by their own SUMMARYs after the fix lands.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read full REQUIREMENTS.md and confirm exact starting state</name>
  <files>(no file changes — Task 2 writes)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file)
    - .planning/phases/02-uds-wire-cli-mv-mcp-rewire-hook-subcommand/02-VERIFICATION.md (confirm 36/36 verified — supports the flip)
    - .planning/v0.9-MILESTONE-AUDIT.md (traceability_drift block, lines 86-91)
    - .planning/STATE.md (Phase 02 line: "Verification PASS 36/36 (2 manual UATs resolved 2026-04-30)")
  </read_first>
  <action>
    Inspect the file once and record exact line numbers for every flip.
    Build a precise edit map (in working memory, not on disk) of the form:

      flip_map = [
        ("- [ ] **BROKER-01**:",  "- [x] **BROKER-01**:"),
        ... (35 more body flips for already-satisfied Phase 2 IDs) ...
      ]

      table_flip_map = [
        ("| BROKER-01 | Phase 2 | Pending |", "| BROKER-01 | Phase 2 | Complete |"),
        ... (35 more table flips) ...
      ]

    Pre-flight grep counts (record them — Task 2 will compare against post-flip):

      grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)' .planning/REQUIREMENTS.md
      # expected: 36 (the IDs to flip)

      grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[^|]+\| Phase 2 \| Pending' .planning/REQUIREMENTS.md
      # expected: 36 (matching table rows)

    Confirm CARRY-02 body line (line ~137): inspect whether it is already
    `[x]` (audit doc says traceability table lists CARRY-02 as Pending; the
    body line per current grep already shows [x]). If body already `[x]`,
    do NOT touch the body line; only flip the table row.

    Confirm CC-07 (line 250) and HOOK-04b (line 237) traceability rows are
    NOT in the flip set — both stay Pending.
  </action>
  <verify>
    <automated>BODY_PENDING=$(grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5])' .planning/REQUIREMENTS.md); TABLE_PENDING=$(grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Pending' .planning/REQUIREMENTS.md); echo "body_pending=$BODY_PENDING table_pending=$TABLE_PENDING"; [ "$BODY_PENDING" -ge 35 ] &amp;&amp; [ "$TABLE_PENDING" -eq 36 ]</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md has been read in full.
    - Pre-flight grep confirms ~36 Pending entries exist in the table for the targeted Phase 2 IDs.
    - Body-checkbox count for the targeted IDs is recorded (35 or 36 depending on CARRY-02 starting state).
    - HOOK-04b (line ~237) and CC-07 (line ~250) confirmed to remain Pending after this plan.
    - 02-VERIFICATION.md confirms 36/36 satisfied, justifying the flip.
  </acceptance_criteria>
  <done>
    Edit map complete; exact starting counts recorded; the 36 IDs to flip
    are unambiguously identified; CC-07 and HOOK-04b are confirmed
    excluded from the flip set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply Pending → Complete flip for the 36 Phase 2 IDs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (re-confirm content matches Task 1 expectations)
  </read_first>
  <action>
    Apply the edit map from Task 1 using the Edit tool. Each edit MUST use
    a unique-match context (the REQ-ID prefix is sufficiently unique for
    both body and table rows).

    Body-section edits (35 or 36 — confirm CARRY-02 starting state first):

      For each ID in {BROKER-01..05, CLI-01..11, MCP-01..10, HOOK-01..03,
      HOOK-04a, TEST-01..05}:
          Edit:
            old_string: `- [ ] **<ID>**:`
            new_string: `- [x] **<ID>**:`

      For CARRY-02 (line ~137):
          IF body shows `- [ ]`, flip to `- [x]`. Otherwise skip.

    Traceability-table edits (36 rows — exact for-each loop):

      For each ID in {BROKER-01..05, CLI-01..11, MCP-01..10, HOOK-01..03,
      HOOK-04a, TEST-01..05, CARRY-02}:
          Edit:
            old_string: `| <ID> | Phase 2 | Pending |`
            new_string: `| <ID> | Phase 2 | Complete |`

      DO NOT touch:
          | HOOK-04b | Phase 5 | Pending (PARTIAL — gap closure per v0.9-MILESTONE-AUDIT.md) |
          | CC-07    | Phase 5 | Pending (BROKEN — gap closure per v0.9-MILESTONE-AUDIT.md) |

    Coverage line: re-read line ~267. Current text reads
      "**Coverage:** 85/85 v0.9 active requirements mapped to exactly one
      phase. No orphans."
    Total (85) is unchanged. If the line includes a completed counter
    (it does not, per current text), refresh it. Otherwise leave as-is.

    After edits, run the post-flight grep gates:

      # Body checkboxes
      grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5])' \
          .planning/REQUIREMENTS.md
      # expected: 0 (all flipped)
      # NOTE: HOOK-04b body line stays `- [ ]`; it does not match this regex
      # because it ends with the (Phase 3) suffix; verify by inspecting the
      # raw file.

      # Table rows
      grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Complete' \
          .planning/REQUIREMENTS.md
      # expected: 36

      grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Pending' \
          .planning/REQUIREMENTS.md
      # expected: 0

      # Confirm CC-07 and HOOK-04b unchanged
      grep -F '| CC-07    | Phase 5 | Pending (BROKEN' .planning/REQUIREMENTS.md \
        || grep -F '| CC-07 | Phase 5 | Pending (BROKEN' .planning/REQUIREMENTS.md
      grep -F '| HOOK-04b | Phase 5 | Pending (PARTIAL' .planning/REQUIREMENTS.md

      # Coverage total preserved
      grep -F '**Coverage:** 85/85' .planning/REQUIREMENTS.md

    Run `just ci` once before commit (this should be a no-op since no code
    changed; the goal is to confirm no doc-validation hook trips on the
    REQUIREMENTS.md edits).
  </action>
  <verify>
    <automated>POST_BODY=$(grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5])' .planning/REQUIREMENTS.md); POST_TABLE_PENDING=$(grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Pending' .planning/REQUIREMENTS.md); POST_TABLE_COMPLETE=$(grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Complete' .planning/REQUIREMENTS.md); echo "body_pending=$POST_BODY table_pending=$POST_TABLE_PENDING table_complete=$POST_TABLE_COMPLETE"; [ "$POST_BODY" -eq 0 ] &amp;&amp; [ "$POST_TABLE_PENDING" -eq 0 ] &amp;&amp; [ "$POST_TABLE_COMPLETE" -eq 36 ] &amp;&amp; grep -qF '| CC-07' .planning/REQUIREMENTS.md &amp;&amp; grep -qE 'CC-07.*Pending \(BROKEN' .planning/REQUIREMENTS.md &amp;&amp; grep -qE 'HOOK-04b.*Pending \(PARTIAL' .planning/REQUIREMENTS.md &amp;&amp; grep -qF '**Coverage:** 85/85' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5])' .planning/REQUIREMENTS.md` returns 0 (all 35-36 body checkboxes flipped).
    - `grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Complete' .planning/REQUIREMENTS.md` returns exactly 36.
    - `grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Pending' .planning/REQUIREMENTS.md` returns 0.
    - `grep -E 'CC-07.*Pending \(BROKEN' .planning/REQUIREMENTS.md` exits 0 (CC-07 status preserved).
    - `grep -E 'HOOK-04b.*Pending \(PARTIAL' .planning/REQUIREMENTS.md` exits 0 (HOOK-04b status preserved).
    - `grep -F '**Coverage:** 85/85' .planning/REQUIREMENTS.md` exits 0 (coverage line preserved).
    - No requirement that was previously `- [x]` was flipped to `- [ ]` (verify with: pre-flight `[x]` body count <= post-flight `[x]` body count).
    - `just ci` exits 0 (sanity — no code changed; doc-validation hooks pass).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md body and traceability table both reflect verified
    Phase 2 state (36 IDs flipped to Complete). CC-07 and HOOK-04b remain
    Pending pending their own fix plans (05-01 / 05-02). Total active
    requirement count (85) preserved. No untouched-row regressions.
    Bookkeeping drift identified by the milestone audit is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation file | `.planning/REQUIREMENTS.md` is doc-only; no executable code path crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Repudiation | Status flipped to Complete without underlying verification (false positive — what the audit doc explicitly warned about) | mitigate | The flip is sourced from `02-VERIFICATION.md` (36/36 satisfied, audited 2026-04-29) and `STATE.md` "Phase 02" line. CC-07 and HOOK-04b are explicitly EXCLUDED from this flip — they stay Pending until 05-01 and 05-02 produce real evidence. |
| T-05-04-02 | Tampering | A previously-Complete entry is regressed to Pending by accident | mitigate | Acceptance criterion: `[x]` body-count post-flip MUST be >= pre-flip; explicit grep gates check CC-07 / HOOK-04b are still Pending. |

No `high`-severity threats. ASVS L1 surface is doc-only.
</threat_model>

<verification>
Phase-level verification commands (run after both tasks complete):

  # 1. Phase 2 body checkboxes all flipped
  test "$(grep -cE '^- \[ \] \*\*(BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5])' .planning/REQUIREMENTS.md)" = "0"

  # 2. Phase 2 table rows all Complete
  test "$(grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Complete' .planning/REQUIREMENTS.md)" = "36"

  # 3. Phase 2 table has zero Pending rows
  test "$(grep -cE '^\| (BROKER|CLI|MCP|HOOK-0[123]|HOOK-04a|TEST-0[1-5]|CARRY-02)[A-Za-z0-9-]* +\| Phase 2 +\| Pending' .planning/REQUIREMENTS.md)" = "0"

  # 4. CC-07 and HOOK-04b status preserved (Pending until 05-01/05-02 ship)
  grep -qE 'CC-07.*Pending \(BROKEN' .planning/REQUIREMENTS.md
  grep -qE 'HOOK-04b.*Pending \(PARTIAL' .planning/REQUIREMENTS.md

  # 5. Coverage total preserved at 85
  grep -qF '**Coverage:** 85/85' .planning/REQUIREMENTS.md

  # 6. Sanity: just ci green (doc edit only — should pass trivially)
  just ci
</verification>

<success_criteria>
- D. `.planning/REQUIREMENTS.md` shows the 36 Phase 2 IDs as `[x]` in the body and `Complete` in the traceability table; coverage count line preserved at 85/85 (phase success criterion D).
- CC-07 and HOOK-04b remain `Pending` (their flips happen in 05-01 / 05-02 SUMMARYs, not here).
- No previously-Complete row regressed to Pending.
- `just ci` is fully green at commit time.
</success_criteria>

<output>
After completion, create `.planning/phases/05-v0.9-milestone-close-fixes/05-04-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Single atomic commit:
  chore(05): flip Phase 2 traceability Pending -> Complete (REQUIREMENTS.md drift cleanup)
</output>
