---
phase: 04-federation-cli-unwire-federation-ci-preservation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MIGRATION-v0.8-to-v0.9.md
autonomous: true
requirements: [MIGRATE-01, MIGRATE-02, MIGRATE-04]
must_haves:
  truths:
    - "docs/MIGRATION-v0.8-to-v0.9.md exists with the CLI mapping table at the top"
    - "Document includes .mcp.json cleanup instructions"
    - "Document includes ~/.famp/ directory cleanup notes"
    - "Document references the v0.8.1-federation-preserved escape-hatch tag"
    - "Document references crates/famp/tests/_deferred_v1/ for federation engineering reference"
    - "Document is ≤200 lines, table-first, terse"
    - "scripts/famp-local archive path (docs/history/v0.9-prep-sprint/famp-local/) is referenced as deprecated/superseded (MIGRATE-04)"
  artifacts:
    - path: "docs/MIGRATION-v0.8-to-v0.9.md"
      provides: "User-facing migration guide for v0.8 → v0.9"
      min_lines: 60
  key_links:
    - from: "docs/MIGRATION-v0.8-to-v0.9.md"
      to: "v0.8.1-federation-preserved tag"
      via: "discoverability section pointing federation users at the escape hatch"
      pattern: "v0.8.1-federation-preserved"
    - from: "docs/MIGRATION-v0.8-to-v0.9.md"
      to: "crates/famp/tests/_deferred_v1/README.md"
      via: "federation-engineering-reference link"
      pattern: "_deferred_v1"
    - from: "docs/MIGRATION-v0.8-to-v0.9.md"
      to: "docs/history/v0.9-prep-sprint/famp-local/"
      via: "MIGRATE-04 deprecation pointer"
      pattern: "v0.9-prep-sprint"
---

<objective>
Create `docs/MIGRATION-v0.8-to-v0.9.md` — the user-facing migration guide for v0.8 federation CLI users moving to v0.9 local-first bus. Per **D-18** the doc is the unmissable backstop for the hard-delete (D-05): there are no soft-deprecation stubs, the unrecognized-subcommand error from clap is the discoverability floor, and this doc is the answer to "where did `famp setup` go?"

Required content per D-18:
1. **CLI mapping table** at the top of the doc (above any prose) — old verb → new verb / "removed" — with notes on flag-syntax preservation.
2. **`.mcp.json` cleanup instructions** — what to delete (legacy `FAMP_HOME` / `FAMP_LOCAL_ROOT` env vars in `mcpServers.famp.env`); what to add (`famp install-claude-code` rewrites this for you).
3. **`~/.famp/` directory cleanup** (optional; legacy `peers.toml`, `cert.pem`, `key.pem` files become inert but don't break anything).
4. **`v0.8.1-federation-preserved` tag pointer** — one paragraph: "this tag exists; it's frozen; don't expect bug fixes; v1.0 is the way forward."
5. **`crates/famp/tests/_deferred_v1/README.md` pointer** — for federation engineering reference.
6. **MIGRATE-04 deprecation pointer** — `docs/history/v0.9-prep-sprint/famp-local/` is the new home of the archived prep-sprint scaffolding.

Tone constraints (D-18): terse, table-first, ≤200 lines, bullet points beat paragraphs.

This is a Wave 1 plan — runs in parallel with Plans 04-01, 04-02, 04-03, 04-05, 04-07. Lands BEFORE the `v0.8.1-federation-preserved` tag per D-07; the doc references the tag, but creating the doc is part of the "framing edits" wave that the tag predates.

Purpose: Provide a single discoverability surface for federation users encountering the unrecognized-subcommand error.
Output: 1 new file at `docs/MIGRATION-v0.8-to-v0.9.md` (~80–150 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md
@docs/history/README.md

<interfaces>
<!-- This plan creates a new markdown doc. No code interfaces. -->
<!-- The skeleton is fully drafted in RESEARCH.md Audit 9 — copy the skeleton verbatim and adapt to the live state. -->
<!-- D-18 enumerates the required sections; the skeleton already covers all six. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/MIGRATION-v0.8-to-v0.9.md from the Audit 9 skeleton</name>
  <files>docs/MIGRATION-v0.8-to-v0.9.md</files>
  <read_first>
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md Audit 9 (full skeleton draft, lines 530-615)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md D-18 (six required sections; ≤200 lines, table-first, terse)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md "docs/MIGRATION-v0.8-to-v0.9.md" (lines 354-379) — tone reference
    - docs/history/README.md (closest in-tree tone analog — terse archive marker voice)
  </read_first>
  <action>
Create `docs/MIGRATION-v0.8-to-v0.9.md` with the following content (this is the Audit 9 skeleton, slightly polished):

```markdown
# Migration: FAMP v0.8 → v0.9

**Local-first bus replaces the federation TLS listener mesh for same-host agents.**
v0.9 ships a UDS-backed broker; cross-host messaging is deferred to v1.0
(`famp-gateway`, trigger-gated on the v1.0 readiness condition documented
in [ARCHITECTURE.md](../ARCHITECTURE.md)).

## TL;DR

- Run `famp install-claude-code` — auto-rewrites your `.mcp.json` and drops new slash commands.
- Switch `famp setup` / `famp init` → `famp register <name>`.
- `famp listen` is gone — the broker auto-spawns on first `famp register`.
- `famp peer add` / `famp peer import` are gone — same-host discovery is automatic via the broker.
- `famp send` keeps the same flag surface; only the transport changed.

## CLI mapping table

| v0.8 | v0.9 | Notes |
|------|------|-------|
| `famp init --home <dir>` | `famp register <name>` | Identity bind via `~/.famp-local/agents/<name>/`; no per-identity HOME dir |
| `famp setup --name <n> --home <d> --port <p>` | `famp register <name>` | Single command; broker handles port/socket |
| `famp listen` | (gone) | Broker auto-spawns at `~/.famp/bus.sock` on first `famp register` |
| `famp peer add --alias <a> --endpoint <u> --pubkey <pk>` | (gone) | Same-host discovery is automatic via broker |
| `famp peer import` | (gone) | Peer cards are v1.0 federation-internal |
| `famp send --to <a> --new-task "<x>"` (TLS) | `famp send --to <name> --new-task "<x>"` (UDS) | Same syntax; bus under the hood |
| `FAMP_HOME=/tmp/a` env var | (no longer meaningful) | `~/.famp/` is the sole root |
| `FAMP_TOFU_BOOTSTRAP=1` | (no longer meaningful) | No TLS on the local bus |
| `famp mcp` with `FAMP_HOME=...` in `.mcp.json` | `famp mcp` (no env vars) | Register identity inside the MCP session via `famp_register` tool |

## `.mcp.json` cleanup

`famp install-claude-code` does this for you. Manual cleanup, if you must:

1. Open `~/.claude.json` (or your project-scope `.mcp.json`).
2. Find the `mcpServers.famp` entry.
3. **Delete** any `env` keys: `FAMP_HOME`, `FAMP_LOCAL_ROOT`.
4. Confirm `command` points to your installed `famp` binary, with `args: ["mcp"]`.

## `~/.famp/` directory cleanup (optional)

Legacy v0.8 artifacts under `~/.famp/` (per-identity dirs containing
`config.toml`, `peers.toml`, `cert.pem`, `key.pem`) are no longer read by v0.9.
They don't break anything; delete at your leisure:

```bash
# Inspect first.
ls ~/.famp/
# Then remove the per-identity dirs (NOT the bus.sock or broker.log files).
rm -rf ~/.famp/<old-identity-dir>
```

v0.9 uses `~/.famp-local/agents/<name>/` for per-identity state (mailboxes,
sessions) and `~/.famp/bus.sock` + `~/.famp/broker.log` for the broker.

## If you genuinely need federation today

The `v0.8.1-federation-preserved` git tag is an escape hatch for users who
need cross-host messaging via the v0.8 TLS listener mesh:

```bash
git checkout v0.8.1-federation-preserved
cargo install --path crates/famp
```

This tag is **frozen**. Bug fixes ship via the v1.0 federation gateway
(`famp-gateway`, trigger-gated on the named v1.0 readiness condition).

## For federation engineering reference

Federation-tagged tests are preserved under
[`crates/famp/tests/_deferred_v1/`](../crates/famp/tests/_deferred_v1/). They
will be the starting test surface for `famp-gateway`'s integration suite when
the v1.0 federation milestone fires. See
[`crates/famp/tests/_deferred_v1/README.md`](../crates/famp/tests/_deferred_v1/README.md)
for the freeze explainer and reactivation criteria.

## Workspace internals

`famp-transport-http` and `famp-keyring` crates remain in the workspace as
**v1.0 federation internals**. They compile and stay tested in `just ci` via
the refactored [`crates/famp/tests/e2e_two_daemons.rs`](../crates/famp/tests/e2e_two_daemons.rs)
integration test (FED-03/04 — plumb-line-2 insurance). No top-level CLI
subcommand reaches them in v0.9.

## Archived prep-sprint scaffolding

The `scripts/famp-local` bash wrapper used during the v0.9 prep sprint
(T1–T9 UX validation) has been archived to
[`docs/history/v0.9-prep-sprint/famp-local/`](history/v0.9-prep-sprint/famp-local/).
It is **frozen, not maintained**; bug fixes ship via the live `famp` binary
or the `famp-local hook` subcommand. See ROADMAP backlog 999.6 for the one
open issue against the archived script.
```

Verify the doc fits the constraints:

```bash
wc -l docs/MIGRATION-v0.8-to-v0.9.md
# Expected: between 60 and 200

# Confirm all six required sections are present
grep -c "## TL;DR\|## CLI mapping table\|## \`.mcp.json\` cleanup\|## \`~/.famp/\` directory cleanup\|## If you genuinely need federation today\|## For federation engineering reference\|## Workspace internals\|## Archived prep-sprint scaffolding" docs/MIGRATION-v0.8-to-v0.9.md
# Expected: ≥ 6 (six section headers above plus TL;DR)
```

The single atomic commit titled `docs(04): MIGRATION-v0.8-to-v0.9.md` (Audit 11 commit 8) lands this file.

After writing, smoke-check the doc renders cleanly: open in any markdown viewer or run `cat docs/MIGRATION-v0.8-to-v0.9.md | head -50` and visually confirm the table at the top is properly formatted.

If `just ci` includes a markdown link-check or shellcheck recipe, run it:

```bash
just ci 2>&1 | tail -10
```

Confirm green.
  </action>
  <verify>
    <automated>test -f docs/MIGRATION-v0.8-to-v0.9.md && [ "$(wc -l < docs/MIGRATION-v0.8-to-v0.9.md)" -ge 60 ] && [ "$(wc -l < docs/MIGRATION-v0.8-to-v0.9.md)" -le 200 ] && grep -c "v0.8.1-federation-preserved" docs/MIGRATION-v0.8-to-v0.9.md && grep -c "_deferred_v1" docs/MIGRATION-v0.8-to-v0.9.md && grep -c "v0.9-prep-sprint" docs/MIGRATION-v0.8-to-v0.9.md</automated>
  </verify>
  <acceptance_criteria>
- `test -f docs/MIGRATION-v0.8-to-v0.9.md` returns true.
- `wc -l docs/MIGRATION-v0.8-to-v0.9.md` returns between 60 and 200.
- `grep -c "v0.8.1-federation-preserved" docs/MIGRATION-v0.8-to-v0.9.md` returns ≥ 1.
- `grep -c "_deferred_v1" docs/MIGRATION-v0.8-to-v0.9.md` returns ≥ 1.
- `grep -c "v0.9-prep-sprint\|history/v0.9-prep-sprint" docs/MIGRATION-v0.8-to-v0.9.md` returns ≥ 1 (MIGRATE-04 reference).
- `grep -c "famp register" docs/MIGRATION-v0.8-to-v0.9.md` returns ≥ 3 (mapping rows for setup/init/listen all reference register).
- `grep -c "famp-transport-http\|famp-keyring" docs/MIGRATION-v0.8-to-v0.9.md` returns ≥ 2 (Workspace internals section).
- `grep -E "^\| v0.8 \| v0.9 \| Notes \|" docs/MIGRATION-v0.8-to-v0.9.md` returns 1 line (the mapping table header).
- `just ci` exits 0.
  </acceptance_criteria>
  <done>
The migration doc exists at the canonical path, hits all six D-18 required sections, fits within the size constraints, and references the escape-hatch tag, the deferred-tests README, and the archive path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-discoverability surface | The doc IS the discoverability layer for federation users; if missing or wrong, users get only clap's "unrecognized subcommand" error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-undiscoverable | Information disclosure (negative) | Federation users hitting `famp setup` post-deletion | mitigate | Migration doc lives at the canonical `docs/MIGRATION-v0.8-to-v0.9.md` path; README + CLAUDE point at it; doc references the escape-hatch tag and the federation engineering reference. |
</threat_model>

<verification>
- The doc exists at `docs/MIGRATION-v0.8-to-v0.9.md`.
- All six D-18 required sections are present.
- Size is between 60 and 200 lines.
- Cross-references to the tag, the deferred tests README, and the script archive are present.
- `just ci` is green.
</verification>

<success_criteria>
- `docs/MIGRATION-v0.8-to-v0.9.md` is created with the table-first structure per D-18.
- Six required sections present.
- ≤200 lines, terse, table-first.
- One atomic commit titled `docs(04): MIGRATION-v0.8-to-v0.9.md` (Audit 11 commit 8) lands the file.
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-04-SUMMARY.md` documenting: the final line count of the doc, confirmation that all six D-18 sections are present, and any wording adjustments made vs the Audit 9 skeleton.
</output>
