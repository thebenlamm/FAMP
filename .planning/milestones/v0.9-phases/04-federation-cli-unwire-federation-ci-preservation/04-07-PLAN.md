---
phase: 04-federation-cli-unwire-federation-ci-preservation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CARRY-01]
must_haves:
  truths:
    - "REQUIREMENTS.md CARRY-01 row checkbox is flipped from [ ] to [x] with closing SHA ebd0854 referenced inline"
    - "ROADMAP.md Phase 4 traceability table CARRY-01 row marked Complete"
    - ".config/nextest.toml still carries the listen-subprocess pin (verified, unchanged)"
    - "just ci is green"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CARRY-01 row marked closed with inline SHA reference"
      contains: "[x] **CARRY-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 traceability table CARRY-01 row marked Complete"
      contains: "CARRY-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md (CARRY-01 row)"
      to: ".config/nextest.toml (listen-subprocess pin)"
      via: "inline SHA reference (ebd0854) — closing commit"
      pattern: "ebd0854"
---

<objective>
Reconcile the **CARRY-01 bookkeeping** per **D-22**. CARRY-01 is closed in code per STATE.md 2026-04-30 sweep — the `[[profile.default.test-groups]] listen-subprocess = max-threads = 4` pin is in `.config/nextest.toml` (verified at HEAD via RESEARCH Audit 4). The closing SHA is `ebd0854ff793b7b9112ff69665a235304f915533` (commit title: `chore(tech-debt): TD-1 — pin listen-subprocess nextest test-group at max-threads=4`).

This plan is **bookkeeping only — DO NOT redo the work**. It only:
1. Flips the `[ ]` checkbox on the CARRY-01 row in `.planning/REQUIREMENTS.md` to `[x]`, adding an inline reference to the closing commit SHA.
2. Updates the Phase 4 traceability table row in `.planning/ROADMAP.md` to mark CARRY-01 as Complete.
3. Verifies the pin still exists at HEAD (`.config/nextest.toml`).

This is a Wave 1 plan — runs in parallel with Plans 04-01, 04-02, 04-03, 04-04, 04-05. Lands BEFORE the `v0.8.1-federation-preserved` tag per D-07 (the bookkeeping is part of the relabeling work the tag predates).

Per Audit 11 commit 1: this is the FIRST commit of the phase, titled `chore(04): pin reqs/roadmap CARRY-01 to closing SHA ebd0854`.

Purpose: Close out CARRY-01 in REQUIREMENTS / ROADMAP without redoing the actual pin.
Output: 2 file edits (one checkbox + SHA reference; one traceability row), single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md
@.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.config/nextest.toml

<interfaces>
<!-- This plan only edits markdown files. -->
<!-- Closing SHA: ebd0854ff793b7b9112ff69665a235304f915533 (RESEARCH Audit 4). -->
<!-- The pin location: .config/nextest.toml lines 25-26 ([test-groups] listen-subprocess = { max-threads = 4 }). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the pin at HEAD; flip CARRY-01 checkbox in REQUIREMENTS.md and ROADMAP.md traceability table</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .config/nextest.toml (verify the pin is still there)
    - .planning/REQUIREMENTS.md (locate the CARRY-01 row — line ~136 per RESEARCH Audit 4)
    - .planning/ROADMAP.md (locate the Phase 4 traceability table CARRY-01 row — RESEARCH Audit 4 says "line 265" but verify against live state)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-CONTEXT.md D-22 (bookkeeping only, do not redo work)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-RESEARCH.md Audit 4 (closing SHA ebd0854, pin location, REQUIREMENTS/ROADMAP flip)
    - .planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-PATTERNS.md "REQUIREMENTS.md checkbox flips" (line 666-678)
  </read_first>
  <action>
**Step 1: Verify the pin still exists at HEAD**

```bash
grep -A1 "\[test-groups\]" .config/nextest.toml | grep "listen-subprocess.*max-threads.*4"
```

Expected: 1 line matching `listen-subprocess = { max-threads = 4 }` (or equivalent TOML form). The pin format may use single-line `listen-subprocess = { max-threads = 4 }` OR multi-line table form — both are acceptable as long as the value is 4.

If the pin is missing or the value differs from 4: **DO NOT proceed**. Escalate to Ben — it means TD-1 was reverted between research date and execution and CARRY-01 needs the actual work done, not just bookkeeping. Defer to a quick re-pin commit before this plan lands.

If the pin is present and equals 4: proceed.

**Step 2: Flip the CARRY-01 checkbox in REQUIREMENTS.md**

Locate the CARRY-01 row in `.planning/REQUIREMENTS.md` (RESEARCH Audit 4 says line ~136; verify against live state). The current text is:

```markdown
- [ ] **CARRY-01** (TD-1): `[[profile.default.test-groups]]` pinned for listen-subprocess tests (max-threads = 4) before listen subprocess tests proliferate further. Address in Phase 4 alongside `e2e_two_daemons` refactor.
```

Replace with:

```markdown
- [x] **CARRY-01** (TD-1): `[[profile.default.test-groups]]` pinned for listen-subprocess tests (max-threads = 4) before listen subprocess tests proliferate further. **Closed in commit `ebd0854` (2026-04-30) — pin lives in `.config/nextest.toml`.**
```

The change: `[ ]` → `[x]` and append `**Closed in commit `ebd0854` (2026-04-30) — pin lives in `.config/nextest.toml`.**` to the line. The trailing "Address in Phase 4 alongside..." sentence becomes the closing note. Per D-22 wording.

**Step 3: Update ROADMAP.md Phase 4 traceability row**

Locate the Phase 4 traceability table in `.planning/ROADMAP.md` (RESEARCH Audit 4 says "line 265" — verify against live state). Find the CARRY-01 row, which currently reads something like:

```markdown
| CARRY-01 | Phase 4 | Pending |
```

Replace with:

```markdown
| CARRY-01 | Phase 4 | Complete (closed in `ebd0854`) |
```

If the table format differs (e.g., uses different column counts or different status terminology), adapt to match the existing convention — the goal is the row reflects "Complete" plus the closing SHA reference.

**Step 4: Verify the edits**

```bash
grep -c "\[x\] \*\*CARRY-01\*\*" .planning/REQUIREMENTS.md
# Expected: 1

grep -c "ebd0854" .planning/REQUIREMENTS.md
# Expected: ≥ 1

grep -c "ebd0854\|CARRY-01.*Complete" .planning/ROADMAP.md
# Expected: ≥ 1 (the traceability row)

grep -c "\[ \] \*\*CARRY-01\*\*" .planning/REQUIREMENTS.md
# Expected: 0 (no orphan unchecked CARRY-01 rows)

# Verify nothing else changed
git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md
# Expected: only those 2 files; only ~2-3 lines changed
```

**Step 5: Confirm just ci is green**

```bash
just ci 2>&1 | tail -10
```

Expected: green. (This plan only edits markdown; no code/test impact.)

This is a single atomic commit titled `chore(04): pin reqs/roadmap CARRY-01 to closing SHA ebd0854` (Audit 11 commit 1).
  </action>
  <verify>
    <automated>[ $(grep -c "\\[x\\] \\*\\*CARRY-01\\*\\*" .planning/REQUIREMENTS.md) -eq 1 ] &amp;&amp; [ $(grep -c "ebd0854" .planning/REQUIREMENTS.md) -ge 1 ] &amp;&amp; [ $(grep -c "\\[ \\] \\*\\*CARRY-01\\*\\*" .planning/REQUIREMENTS.md) -eq 0 ] &amp;&amp; grep -c "listen-subprocess" .config/nextest.toml | grep -qE "^[1-9]" &amp;&amp; just ci > /dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `.config/nextest.toml` still carries the listen-subprocess pin: `grep "listen-subprocess.*max-threads.*4" .config/nextest.toml` returns ≥ 1 line.
- `grep -c "\[x\] \*\*CARRY-01\*\*" .planning/REQUIREMENTS.md` returns 1.
- `grep -c "\[ \] \*\*CARRY-01\*\*" .planning/REQUIREMENTS.md` returns 0 (no unchecked rows for CARRY-01).
- `grep -c "ebd0854" .planning/REQUIREMENTS.md` returns ≥ 1 (closing SHA referenced inline).
- `grep -c "ebd0854\|CARRY-01.*Complete" .planning/ROADMAP.md` returns ≥ 1 (traceability row updated).
- `git diff --stat` for the commit shows only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` were touched (no drive-by edits).
- `just ci` exits 0.
  </acceptance_criteria>
  <done>
CARRY-01 row in REQUIREMENTS.md is checked off with the closing SHA referenced inline; the Phase 4 traceability row in ROADMAP.md marks CARRY-01 as Complete; the pin in `.config/nextest.toml` is verified to still exist at HEAD; just ci is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bookkeeping vs work | If the pin disappeared between research date and execution, flipping the checkbox would lie — must verify before flipping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-bookkeeping-lies | Repudiation | CARRY-01 closure claim | mitigate | Step 1 of the action explicitly verifies the pin's continued existence at HEAD; if the pin is missing, escalate before flipping the checkbox. |
</threat_model>

<verification>
- The pin still exists in `.config/nextest.toml`.
- REQUIREMENTS.md CARRY-01 row is checked with the closing SHA inline.
- ROADMAP.md Phase 4 traceability row marks CARRY-01 Complete.
- just ci is green.
</verification>

<success_criteria>
- CARRY-01 bookkeeping closes per D-22 without redoing the actual pin work.
- One atomic commit titled `chore(04): pin reqs/roadmap CARRY-01 to closing SHA ebd0854` lands all changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-federation-cli-unwire-federation-ci-preservation/04-07-SUMMARY.md` documenting: the verification that the pin still exists at HEAD, the exact lines edited, and confirmation that the commit only touched the 2 expected files.
</output>
