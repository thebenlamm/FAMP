---
phase: 01-canonical-json-foundations
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - crates/famp-canonical/tests/conformance.rs
  - .planning/SEED-001.md
  - .github/workflows/ci.yml
  - Justfile
autonomous: false
requirements: [CANON-02, CANON-03]
must_haves:
  truths:
    - "RFC 8785 Appendix B vectors (all 27) pass byte-exact in CI"
    - "Sampled float corpus (100K) passes byte-exact in CI"
    - "Supplementary-plane emoji/CJK Ext B fixtures pass"
    - "weird.json cyberphone fixture round-trips byte-exact"
    - "SEED-001 decision (keep serde_jcs OR fork) is recorded with cited evidence in .planning/SEED-001.md"
    - "CI workflow runs just test-canonical and full just test as a hard gate"
  artifacts:
    - path: ".planning/SEED-001.md"
      provides: "SEED-001 decision record with per-vector evidence"
      contains: "Decision:"
    - path: ".github/workflows/ci.yml"
      provides: "CI gate enforcing famp-canonical tests on every PR"
      contains: "test-canonical"
    - path: "Justfile"
      provides: "test-canonical-full recipe (with full-corpus feature for nightly/release)"
      contains: "test-canonical-full"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "just test-canonical"
      via: "fail-on-error step"
      pattern: "test-canonical"
    - from: ".planning/SEED-001.md"
      to: "Plan 02 SUMMARY test results"
      via: "cited evidence per gate criterion"
      pattern: "Appendix B"
---

<objective>
Run the SEED-001 decision gate against the fully-wired test harness from
Plans 01–02, record the decision (keep serde_jcs vs. fork to
serde_json_canonicalizer vs. from-scratch fallback) in .planning/SEED-001.md
with cited per-vector evidence, and lock in CI enforcement so the gate
catches future regressions.

Per locked decision D-10 step 5, the decision MUST cite which vectors
passed/failed. Per D-11, the decision lives in `.planning/SEED-001.md`.

Purpose: Resolve the long-standing SEED-001 blocker, prove RFC 8785
conformance for FAMP's canonical-bytes contract, and turn the gate into a
permanent CI fixture.

Output: SEED-001 decision recorded; CI workflow enforcing the gate; Phase 1
success criteria #1, #2, #4, #5, #6, #7 satisfied (criteria #3 — duplicate
key — already done in Plan 02; #6 — fallback plan — already done in Plan 01).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-json-foundations/01-CONTEXT.md
@.planning/phases/01-canonical-json-foundations/01-RESEARCH.md
@.planning/phases/01-canonical-json-foundations/01-01-SUMMARY.md
@.planning/phases/01-canonical-json-foundations/01-02-SUMMARY.md
@crates/famp-canonical/tests/conformance.rs
@crates/famp-canonical/Cargo.toml
@Justfile
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run gate, capture evidence, write SEED-001 decision</name>
  <files>.planning/SEED-001.md, crates/famp-canonical/tests/conformance.rs</files>
  <read_first>
    - .planning/phases/01-canonical-json-foundations/01-02-SUMMARY.md (test results from Plan 02)
    - .planning/phases/01-canonical-json-foundations/01-RESEARCH.md (§"SEED-001 Decision Framework" — gate criteria table)
    - .planning/phases/01-canonical-json-foundations/01-CONTEXT.md (D-08, D-10, D-11)
    - crates/famp-canonical/tests/conformance.rs (current state — may need Appendix C/E added)
  </read_first>
  <action>
    1. **Augment conformance.rs with RFC 8785 Appendix C and E vectors.**
       Plan 01 transcribed Appendix B (27 float vectors). Add two more tests:

       a. `rfc8785_appendix_c_structured` — input/expected from RFC 8785
          Appendix C (structured object example):
          Input (ASCII):
          ```json
          {
            "numbers": [333333333.33333329, 1E30, 4.50, 2e-3, 0.000000000000000000000000001],
            "string": "\u20ac$\u000F\u000aA'\u0042\u0022\u005c\\\"/",
            "literals": [null, true, false]
          }
          ```
          Expected output (verbatim from RFC 8785 §C.1):
          ```
          {"literals":[null,true,false],"numbers":[333333333.3333333,1e+30,4.5,0.002,1e-27],"string":"€$\u000f\nA'B\"\\\\\"/"}
          ```
          (Pull exact bytes from https://datatracker.ietf.org/doc/html/rfc8785#appendix-C
           — the spec section is ~30 lines.)

       b. `rfc8785_appendix_e_complex` — RFC 8785 Appendix E nested-object
          example. Pull from https://datatracker.ietf.org/doc/html/rfc8785#appendix-E.

       Both tests parse the input via `serde_json::from_str::<serde_json::Value>`
       then call `famp_canonical::canonicalize` and compare bytes to the
       expected output.

    2. **Run the full gate** with verbose output:
       ```
       cargo nextest run -p famp-canonical --no-fail-fast 2>&1 | tee /tmp/famp-canonical-gate.txt
       ```

       Record the result for each criterion in the table below. If any vector
       fails, capture the EXACT mismatch (first 2-3 differing bytes/chars).

    3. **Write `.planning/SEED-001.md`** with this exact structure:

       ```markdown
       # SEED-001: serde_jcs RFC 8785 Conformance Gate Decision

       **Status:** RESOLVED (Phase 1, Plan 03)
       **Decision Date:** {today}
       **Decision:** {keep serde_jcs 0.2.0 | fork to serde_json_canonicalizer 0.3.2 | from-scratch fallback}

       ## Context
       Per CONTEXT.md D-08, the fallback plan was written FIRST (see
       `crates/famp-canonical/docs/fallback.md`, committed in Plan 01-01)
       before any conformance gate ran. The gate was wired in Plan 01-02
       and executed in Plan 01-03.

       ## Gate Criteria & Evidence

       | Criterion | Pass Condition | Result | Evidence |
       |-----------|---------------|--------|----------|
       | RFC 8785 Appendix B (27 float vectors) | All 27 pass | {PASS/FAIL} | `cargo nextest run -p famp-canonical conformance::rfc8785_appendix_b_all` — {output snippet} |
       | RFC 8785 Appendix C (structured object) | Byte-exact | {PASS/FAIL} | `... appendix_c_structured` — {output snippet} |
       | RFC 8785 Appendix E (complex object) | Byte-exact | {PASS/FAIL} | `... appendix_e_complex` — {output snippet} |
       | cyberphone weird.json | Byte-exact | {PASS/FAIL} | `... cyberphone_weird_fixture` — {output snippet} |
       | Sampled float corpus (100K, fixed prefix) | 100% match | {PASS/FAIL} | `... float_corpus_sampled` — {output snippet} |
       | Supplementary-plane fixtures | Expected sort order | {PASS/FAIL} | `... supplementary_plane_keys_sort_correctly` — {output snippet} |
       | NaN / Infinity rejection | Returns Err | {PASS/FAIL} | `... appendix_b_nan_rejected` — {output snippet} |
       | Duplicate-key rejection on parse | Returns DuplicateKey err | PASS (Plan 01-02) | duplicate_keys::* |

       ## Decision Rationale

       {If all PASS:}
       All gate criteria met. SEED-001 resolves to **keep serde_jcs 0.2.0** as
       the canonical engine, wrapped behind `famp-canonical::canonicalize`.
       The wrapper trait insulates downstream callers from any future
       serde_jcs API churn (the "unstable" self-label refers to API, not
       correctness — see RESEARCH §"SEED-001 Decision Framework").

       {If any FAIL:}
       Gate failed on {N} criteria: {list}. Per RESEARCH §"SEED-001 Decision
       Framework", the next step is to fork `serde_json_canonicalizer 0.3.2`
       (1.1M downloads, uses ryu-js 1.0.1, claims 100% RFC compatibility)
       rather than the from-scratch fallback. The from-scratch plan
       (`crates/famp-canonical/docs/fallback.md`) is reserved for the case
       where serde_json_canonicalizer ALSO fails the gate. A new phase plan
       (Plan 04) will swap the engine and re-run the gate.

       ## Carried Forward
       - Wrapper trait API (D-02) is independent of engine choice — no caller
         changes if the engine is swapped later.
       - 100M full corpus run is required at release-tag time (D-12); this
         plan only enforces the 100K sampled subset on every PR.

       ## References
       - .planning/phases/01-canonical-json-foundations/01-RESEARCH.md §"SEED-001 Decision Framework"
       - .planning/phases/01-canonical-json-foundations/01-CONTEXT.md D-08..D-11
       - crates/famp-canonical/docs/fallback.md
       - Gate run output: /tmp/famp-canonical-gate.txt (run {today})
       ```

       Fill in {Result} and {Evidence} cells with the actual gate output. Do
       not paraphrase — quote the test name and any failing assertion verbatim.

    4. Commit only AFTER:
       - `.planning/SEED-001.md` exists with all cells filled
       - The "Decision:" line is one of the three exact strings (no hedging)
       - If decision is "keep serde_jcs", the gate must be 100% green
  </action>
  <verify>
    <automated>cargo nextest run -p famp-canonical 2>&1 | tee /tmp/famp-canonical-gate.txt; test -f .planning/SEED-001.md && grep -q '^**Decision:**' .planning/SEED-001.md && grep -q 'Appendix B' .planning/SEED-001.md && grep -q 'float corpus' .planning/SEED-001.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/SEED-001.md` exists
    - Contains literal `**Decision:**` followed by one of: `keep serde_jcs`, `fork to serde_json_canonicalizer`, `from-scratch fallback`
    - Contains literal `**Status:** RESOLVED`
    - Contains an evidence row for each of: Appendix B, Appendix C, Appendix E, cyberphone weird.json, sampled float corpus, supplementary-plane fixtures, NaN rejection, duplicate-key rejection
    - `crates/famp-canonical/tests/conformance.rs` contains literal `appendix_c_structured` and `appendix_e_complex`
    - `cargo nextest run -p famp-canonical conformance::rfc8785_appendix_b_all` exits 0 (if decision is "keep serde_jcs")
    - `cargo nextest run -p famp-canonical conformance::cyberphone_weird_fixture` exits 0 (if decision is "keep serde_jcs")
    - `cargo nextest run -p famp-canonical float_corpus::float_corpus_sampled` exits 0 (if decision is "keep serde_jcs")
    - `cargo nextest run -p famp-canonical utf16_supplementary` exits 0 (if decision is "keep serde_jcs")
    - If any gate test fails, SEED-001.md decision MUST NOT be "keep serde_jcs"
  </acceptance_criteria>
  <done>SEED-001 decision recorded with cited evidence per D-10 step 5 and D-11.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire CI gate + nightly full-corpus recipe</name>
  <files>.github/workflows/ci.yml, Justfile</files>
  <read_first>
    - .github/workflows/ci.yml (current — confirm structure)
    - Justfile (current — confirm test-canonical recipe from Plan 01)
    - .planning/SEED-001.md (just-written decision)
  </read_first>
  <action>
    1. **Justfile** — append two recipes:
       ```
       # Run famp-canonical with full features (incl. supplementary fixtures, no full corpus)
       test-canonical-strict:
           cargo nextest run -p famp-canonical --no-fail-fast

       # Run famp-canonical with the 100M float corpus (nightly / release tags only)
       test-canonical-full:
           cargo nextest run -p famp-canonical --features full-corpus --no-fail-fast
       ```

    2. **`.github/workflows/ci.yml`** — read the current file. Add a job (or
       step in the existing test job) that runs `just test-canonical-strict`
       and fails the build on any test failure. Do NOT use `continue-on-error`.
       Do NOT skip on any platform.

       If the file does not exist, create a minimal version:
       ```yaml
       name: CI
       on:
         push:
           branches: [main]
         pull_request:
       jobs:
         test:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - uses: taiki-e/install-action@v2
               with:
                 tool: just,cargo-nextest
             - name: famp-canonical conformance gate (RFC 8785)
               run: just test-canonical-strict
             - name: full workspace tests
               run: just test
             - name: clippy
               run: just lint
             - name: fmt
               run: just fmt-check
       ```

       Add a separate scheduled workflow `.github/workflows/nightly-full-corpus.yml`
       that runs `just test-canonical-full` on cron `0 6 * * *` AND on release tags:
       ```yaml
       name: nightly-full-corpus
       on:
         schedule:
           - cron: '0 6 * * *'
         push:
           tags:
             - 'v*'
         workflow_dispatch:
       jobs:
         full-corpus:
           runs-on: ubuntu-latest
           timeout-minutes: 240
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - uses: taiki-e/install-action@v2
               with:
                 tool: just,cargo-nextest
             - name: download full 100M float corpus
               run: |
                 mkdir -p crates/famp-canonical/tests/vectors/full_corpus
                 curl -sSL https://github.com/cyberphone/json-canonicalization/releases/download/es6testfile/es6testfile100m.txt.gz \
                   | gunzip > crates/famp-canonical/tests/vectors/full_corpus/es6testfile100m.txt
                 echo "0f7dda6b0837dde083c5d6b896f7d62340c8a2415b0c7121d83145e08a755272  crates/famp-canonical/tests/vectors/full_corpus/es6testfile100m.txt" | sha256sum -c
             - name: run full corpus gate
               run: just test-canonical-full
       ```

       Note: D-12 mandates the full 100M is a required release gate. The
       nightly job satisfies the "nightly + release tags + manual dispatch"
       requirement literally.

    3. Run `just test-canonical-strict` locally to confirm green:
       ```
       just test-canonical-strict
       ```

    4. Validate workflow YAML with a parser if available, or at minimum
       `cat .github/workflows/ci.yml | head -50` to inspect.
  </action>
  <verify>
    <automated>just test-canonical-strict 2>&1 && test -f .github/workflows/ci.yml && grep -q 'test-canonical-strict' .github/workflows/ci.yml && grep -q 'test-canonical-strict:' Justfile && grep -q 'test-canonical-full:' Justfile && test -f .github/workflows/nightly-full-corpus.yml && grep -q '0f7dda6b0837dde083c5d6b896f7d62340c8a2415b0c7121d83145e08a755272' .github/workflows/nightly-full-corpus.yml</automated>
  </verify>
  <acceptance_criteria>
    - `Justfile` contains literal `test-canonical-strict:` and `test-canonical-full:`
    - `Justfile` `test-canonical-full` recipe contains literal `--features full-corpus`
    - `.github/workflows/ci.yml` exists and contains literal `just test-canonical-strict`
    - `.github/workflows/ci.yml` does NOT contain `continue-on-error` for the test-canonical step
    - `.github/workflows/nightly-full-corpus.yml` exists
    - `.github/workflows/nightly-full-corpus.yml` contains the cron `0 6 * * *`
    - `.github/workflows/nightly-full-corpus.yml` contains the SHA-256 `0f7dda6b0837dde083c5d6b896f7d62340c8a2415b0c7121d83145e08a755272`
    - `just test-canonical-strict` exits 0 locally
  </acceptance_criteria>
  <done>RFC 8785 gate is enforced on every PR; full 100M corpus runs nightly + on release tags + manual dispatch per D-12.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Phase 1 closeout review</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/SEED-001.md
    - crates/famp-canonical/docs/fallback.md
    - .github/workflows/ci.yml
    - .github/workflows/nightly-full-corpus.yml
  </read_first>
  <action>Pause for user verification per the how-to-verify steps below. Do not proceed until user types "approved".</action>
  <verify>
    <automated>just test-canonical-strict 2>&1 && just lint 2>&1</automated>
  </verify>
  <done>User has typed "approved" after running the verification steps; Phase 1 is closed.</done>
  <acceptance_criteria>
    - User confirms `.planning/SEED-001.md` shows Status: RESOLVED with cited evidence
    - User confirms `just test-canonical-strict` is green locally
    - User confirms `just lint` is clean
    - User confirms STATE.md no longer lists SEED-001 as a blocker
  </acceptance_criteria>
  <what-built>
    Phase 1 ships famp-canonical with:
    - canonicalize() / Canonicalize trait wrapping serde_jcs (CANON-01)
    - RFC 8785 Appendix B/C/E vectors green in CI (CANON-02)
    - 100K sampled float corpus green per PR; 100M nightly (CANON-03)
    - Supplementary-plane (emoji + CJK Ext B) fixtures green (CANON-04)
    - ECMAScript number formatting verified via Appendix B + corpus (CANON-05)
    - Duplicate-key rejection at parse via from_*_strict (CANON-06)
    - Fallback plan written on disk before gate ran (CANON-07)
    - sha256:<hex> artifact-ID helpers (SPEC-18)
    - SEED-001 resolved with cited evidence (.planning/SEED-001.md)
  </what-built>
  <how-to-verify>
    1. Read `.planning/SEED-001.md` and confirm:
       - Status is RESOLVED
       - Decision line is one of the three locked options
       - Each gate criterion has a real Pass/Fail value with evidence
    2. Run locally:
       ```
       just test-canonical-strict
       just lint
       cargo doc -p famp-canonical --no-deps --open
       ```
       Confirm tests are green, clippy is clean, doc renders without broken links.
    3. Skim `crates/famp-canonical/docs/fallback.md` — confirm it's a real
       written plan, not a placeholder.
    4. Inspect `.github/workflows/ci.yml` — confirm `just test-canonical-strict`
       runs and there is no `continue-on-error` on that step.
    5. Inspect `.github/workflows/nightly-full-corpus.yml` — confirm the cron
       and the SHA-256 verification step exist.
    6. STATE.md update: confirm SEED-001 is no longer listed as an open blocker.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix before Phase 1 closes.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/SEED-001.md` exists with RESOLVED status and cited evidence
- `just test-canonical-strict` exits 0
- `cargo nextest run -p famp-canonical conformance` — all green (assuming "keep serde_jcs")
- CI workflow enforces gate without continue-on-error
- Nightly workflow exists for full-corpus
- All Phase 1 success criteria from ROADMAP.md §"Phase 1: Canonical JSON Foundations" satisfied
</verification>

<success_criteria>
- CANON-01..07, SPEC-02, SPEC-18 all closed
- SEED-001 blocker removed from STATE.md
- Phase 1 ROADMAP success criteria #1, #2, #3, #4, #5, #6, #7 all green
- CI gate live and enforced
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-json-foundations/01-03-SUMMARY.md`
documenting:
- SEED-001 decision (verbatim line)
- Gate results table (per criterion)
- CI workflow files added
- STATE.md updates needed (SEED-001 removed from blockers, Phase 1 marked complete)
</output>
