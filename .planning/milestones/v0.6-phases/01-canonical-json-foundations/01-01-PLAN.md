---
phase: 01-canonical-json-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/famp-canonical/Cargo.toml
  - crates/famp-canonical/docs/fallback.md
  - crates/famp-canonical/tests/conformance.rs
  - crates/famp-canonical/tests/float_corpus.rs
  - crates/famp-canonical/tests/utf16_supplementary.rs
  - crates/famp-canonical/tests/duplicate_keys.rs
  - crates/famp-canonical/tests/artifact_id.rs
  - crates/famp-canonical/tests/vectors/input/.gitkeep
  - crates/famp-canonical/tests/vectors/output/.gitkeep
  - crates/famp-canonical/tests/vectors/supplementary/.gitkeep
  - Justfile
autonomous: true
requirements: [CANON-07, SPEC-02]
must_haves:
  truths:
    - "famp-canonical depends on serde_jcs, serde_json (with float_roundtrip), sha2, thiserror"
    - "Fallback plan exists on disk before any conformance gate runs (D-08)"
    - "Test harness files exist as failing/pending stubs ready for implementation"
    - "just test-canonical recipe runs only famp-canonical tests"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace serde_json pinned with float_roundtrip feature"
      contains: "float_roundtrip"
    - path: "crates/famp-canonical/Cargo.toml"
      provides: "Crate dependencies on serde_jcs/serde_json/sha2/thiserror"
      contains: "serde_jcs"
    - path: "crates/famp-canonical/docs/fallback.md"
      provides: "Written ~500 LoC fallback plan (RFC 8785 §3 sort + ryu-js + UTF-8 pass-through)"
    - path: "crates/famp-canonical/tests/conformance.rs"
      provides: "RFC 8785 Appendix B/C/E vector harness skeleton"
    - path: "crates/famp-canonical/tests/duplicate_keys.rs"
      provides: "Duplicate-key rejection test skeleton"
    - path: "crates/famp-canonical/tests/artifact_id.rs"
      provides: "sha256:<hex> helper test skeleton"
    - path: "Justfile"
      provides: "test-canonical recipe"
      contains: "test-canonical"
  key_links:
    - from: "Cargo.toml [workspace.dependencies]"
      to: "serde_json features"
      via: "float_roundtrip enabled"
      pattern: "float_roundtrip"
---

<objective>
Wave 0 scaffolding for famp-canonical. Per locked decision D-08/D-10, the
fallback plan MUST be written to disk BEFORE any RFC 8785 conformance gate
runs. This plan also fixes the workspace serde_json pin to add float_roundtrip
(per RESEARCH §Pitfall 1), wires crate dependencies, copies in cyberphone
test fixture vectors, and creates failing test stubs that subsequent waves fill in.

Purpose: Establish the test harness and contractual artifacts required by
D-10's fixed task ordering. No production code is written in this plan.

Output: A famp-canonical crate that builds with full deps, has a written
fallback plan on disk, and has test files that compile and run as failing
stubs (or `#[ignore]`) keyed to specific requirement IDs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-json-foundations/01-CONTEXT.md
@.planning/phases/01-canonical-json-foundations/01-RESEARCH.md
@.planning/phases/01-canonical-json-foundations/01-VALIDATION.md
@Cargo.toml
@crates/famp-canonical/Cargo.toml
@Justfile
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Workspace dep fix + famp-canonical Cargo.toml + Justfile recipe</name>
  <files>Cargo.toml, crates/famp-canonical/Cargo.toml, Justfile</files>
  <read_first>
    - Cargo.toml (current workspace deps — confirm serde_json line lacks float_roundtrip)
    - crates/famp-canonical/Cargo.toml (current stub — has empty [dependencies])
    - Justfile (existing recipes)
    - .planning/phases/01-canonical-json-foundations/01-RESEARCH.md (Pitfall 1 — float_roundtrip required)
  </read_first>
  <action>
    1. In root Cargo.toml [workspace.dependencies], replace the existing serde_json
       line with EXACTLY:
       `serde_json = { version = "1.0.149", default-features = false, features = ["std", "float_roundtrip"] }`
       Rationale: without float_roundtrip, parse-then-serialize is not byte-identical,
       breaking JCS guarantees (RESEARCH Pitfall 1).

    2. In crates/famp-canonical/Cargo.toml, replace the empty [dependencies] block
       with:
       ```toml
       [dependencies]
       serde_jcs = { workspace = true }
       serde = { workspace = true }
       serde_json = { workspace = true }
       sha2 = { workspace = true, features = ["std"] }
       thiserror = { workspace = true }

       [dev-dependencies]
       serde_json = { workspace = true }
       ```
       Do NOT add `arbitrary_precision` or `preserve_order` features anywhere — both
       break JCS (RESEARCH anti-patterns).

    3. In Justfile, append a new recipe:
       ```
       # Run famp-canonical test suite only (fast feedback loop)
       test-canonical:
           cargo nextest run -p famp-canonical
       ```

    4. Run `cargo build -p famp-canonical` to confirm everything resolves with
       the new feature flags. Crate is still empty (just lib.rs stub) so build
       must succeed.
  </action>
  <verify>
    <automated>cargo build -p famp-canonical 2>&1 && grep -q 'float_roundtrip' Cargo.toml && grep -q 'serde_jcs' crates/famp-canonical/Cargo.toml && grep -q 'test-canonical' Justfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'float_roundtrip' Cargo.toml` returns at least 1
    - `grep -q 'serde_jcs = { workspace = true }' crates/famp-canonical/Cargo.toml` succeeds
    - `grep -q 'sha2 = { workspace = true' crates/famp-canonical/Cargo.toml` succeeds
    - `grep -q 'thiserror = { workspace = true' crates/famp-canonical/Cargo.toml` succeeds
    - NO occurrence of `arbitrary_precision` or `preserve_order` in Cargo.toml or crates/famp-canonical/Cargo.toml
    - `grep -A1 '^test-canonical:' Justfile` shows `cargo nextest run -p famp-canonical`
    - `cargo build -p famp-canonical` exits 0
  </acceptance_criteria>
  <done>Workspace and crate deps wired; new feature flags resolve; just test-canonical recipe exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write famp-canonical/docs/fallback.md (BEFORE running any gate)</name>
  <files>crates/famp-canonical/docs/fallback.md</files>
  <read_first>
    - .planning/phases/01-canonical-json-foundations/01-CONTEXT.md (D-08, D-09, D-10 — fallback first)
    - .planning/phases/01-canonical-json-foundations/01-RESEARCH.md (§"Fallback Plan Outline (for famp-canonical/docs/fallback.md)" — 8-section outline)
    - FAMP-v0.5.1-spec.md §3.6a (sha256:<hex> requirement) — for cross-reference
  </read_first>
  <action>
    Create `crates/famp-canonical/docs/fallback.md` containing the from-scratch
    fallback plan. This document is REQUIRED by CANON-07 to exist on disk BEFORE
    the conformance gate runs (locked decision D-08, D-10 step 2). Per D-09, no
    code is written — this is a written plan only.

    The document MUST contain these eight sections, each with substantive content
    (not placeholders), drawn from RESEARCH.md "Fallback Plan Outline":

    1. ## Trigger conditions — Under what circumstances do we abandon serde_jcs
       AND serde_json_canonicalizer? (e.g., shared ryu-js bug affects both)
    2. ## RFC 8785 §3.2.2.3 Number Formatting — Use `ryu-js` crate directly
       (`boa-dev/ryu-js`); call `Buffer::format_finite(f64_value)`; reject
       non-finite via `f.is_finite()`. Show pseudocode.
    3. ## RFC 8785 §3.2.3 Key Sorting — Traverse `serde_json::Value::Object`
       entries; sort by `key.encode_utf16().collect::<Vec<u16>>()` comparison;
       reconstruct in sorted order. Show pseudocode.
    4. ## RFC 8785 §3.2.1 UTF-8 String Pass-Through — No normalization; raw
       UTF-8 output; escape only `"`, `\`, and control chars `\u0000`–`\u001F`.
    5. ## Integer handling — All integers cast to f64 via `as f64`; values
       above 2^53 lose precision (matches spec).
    6. ## Recursive structure traversal — Object, Array, String, Number, Bool,
       Null branches.
    7. ## Estimated LoC budget — Number formatter ~50, key sorter ~80, recursive
       serializer ~200, writer plumbing ~100, tests ~100. Total ~530.
    8. ## Verification approach — Diff against cyberphone Java reference output
       on the same inputs; reuse the conformance test harness from
       `tests/conformance.rs`.

    Add a short header noting:
    - "Written 2026-04-12 per D-08 (fallback-first discipline)"
    - "Status: contingency plan — not active. Active engine: serde_jcs 0.2.0 (pending SEED-001 gate)."
    - "Preferred fallback if gate fails: fork `serde_json_canonicalizer 0.3.2` (RESEARCH §SEED-001 Decision Framework). This document covers the from-scratch path of last resort."

    Target document length: 250–500 lines of substantive content.
  </action>
  <verify>
    <automated>test -f crates/famp-canonical/docs/fallback.md && [ "$(wc -l < crates/famp-canonical/docs/fallback.md)" -ge 100 ] && grep -q 'RFC 8785 §3.2.2.3' crates/famp-canonical/docs/fallback.md && grep -q 'encode_utf16' crates/famp-canonical/docs/fallback.md && grep -q 'ryu-js' crates/famp-canonical/docs/fallback.md</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/famp-canonical/docs/fallback.md` exists
    - File has >= 100 lines
    - Contains literal strings: `RFC 8785 §3.2.2.3`, `RFC 8785 §3.2.3`, `RFC 8785 §3.2.1`
    - Contains literal `encode_utf16`
    - Contains literal `ryu-js`
    - Contains literal `Buffer::format_finite`
    - Contains literal `serde_json_canonicalizer` (preferred fallback target reference)
    - Contains a section on integer-to-f64 casting
    - Contains an LoC budget section totaling ~500 lines
  </acceptance_criteria>
  <done>Written fallback plan exists on disk before any RFC 8785 gate runs, satisfying CANON-07 and D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test harness skeletons + cyberphone fixtures committed</name>
  <files>
    crates/famp-canonical/tests/conformance.rs,
    crates/famp-canonical/tests/float_corpus.rs,
    crates/famp-canonical/tests/utf16_supplementary.rs,
    crates/famp-canonical/tests/duplicate_keys.rs,
    crates/famp-canonical/tests/artifact_id.rs,
    crates/famp-canonical/tests/vectors/input/.gitkeep,
    crates/famp-canonical/tests/vectors/output/.gitkeep,
    crates/famp-canonical/tests/vectors/supplementary/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-canonical-json-foundations/01-RESEARCH.md (§"Code Examples" — full 27-vector Appendix B array verbatim; §"Pattern 5"; §"Float Corpus Sampled Driver"; §"Duplicate Key Rejection Test")
    - .planning/phases/01-canonical-json-foundations/01-VALIDATION.md (Wave 0 Requirements — list of test files)
    - crates/famp-canonical/src/lib.rs (current stub — confirm what's exported)
  </read_first>
  <behavior>
    Test files must compile against the current empty crate. Tests reference
    public symbols that DO NOT YET EXIST (canonicalize, from_str_strict,
    artifact_id_for_canonical_bytes, CanonicalError) and are therefore expected
    to FAIL TO COMPILE in this plan — so each file is gated with `#[ignore]`
    + `#[cfg(feature = "wave2_impl")]` OR alternatively the body uses a
    `compile_error!` placeholder. Choose: gate with `#![cfg(feature = "wave2_impl")]`
    at the top of each test file and add the feature to Cargo.toml as a no-op
    feature so files compile (unbuilt) until Plan 02 enables it.
  </behavior>
  <action>
    1. Add to `crates/famp-canonical/Cargo.toml`:
       ```toml
       [features]
       default = []
       wave2_impl = []
       full-corpus = []
       ```
       This lets test files reference future symbols behind `#[cfg(feature = "wave2_impl")]`
       so the crate compiles without them today.

    2. Create `crates/famp-canonical/tests/conformance.rs` with the FULL 27-entry
       RFC 8785 Appendix B vector array transcribed VERBATIM from
       01-RESEARCH.md §"Code Examples" → `rfc8785_appendix_b_all`. Wrap entire
       file body in `#![cfg(feature = "wave2_impl")]`. Include the
       `appendix_b_float_vectors` test, the `nan_rejected` test, and a
       `cyberphone_weird_fixture` test using `include_str!("vectors/input/weird.json")`
       + `include_bytes!("vectors/output/weird.json")`.

    3. Create `crates/famp-canonical/tests/float_corpus.rs` matching the
       skeleton in 01-RESEARCH.md §"Float Corpus Sampled Driver":
       - `const SAMPLE_SIZE_PR: usize = 100_000;`
       - `const SAMPLE_SEED: &str = "famp-canonical-float-corpus-v1";`
       - `#[test] fn float_corpus_sampled() { run_corpus_lines(SAMPLE_SIZE_PR); }`
       - `#[cfg(feature = "full-corpus")] #[test] fn float_corpus_full()`
       - Stub body for `run_corpus_lines` that loads from
         `tests/vectors/float_corpus_sample.txt` (file not yet present — Wave 2
         generates it). Header comment documents the cyberphone source URL and
         SHA-256 0f7dda6b0837dde083c5d6b896f7d62340c8a2415b0c7121d83145e08a755272.
         Wrap whole file in `#![cfg(feature = "wave2_impl")]`.

    4. Create `crates/famp-canonical/tests/utf16_supplementary.rs` with a stub
       test referencing `tests/vectors/supplementary/emoji_keys.json` (not yet
       authored). Wrap in `#![cfg(feature = "wave2_impl")]`. Header comment
       explains: oracle is cyberphone JS reference, fixtures TBD in Plan 02.

    5. Create `crates/famp-canonical/tests/duplicate_keys.rs` containing the
       `duplicate_key_is_error` and `non_duplicate_is_ok` tests verbatim from
       01-RESEARCH.md §"Duplicate Key Rejection Test". Wrap in
       `#![cfg(feature = "wave2_impl")]`.

    6. Create `crates/famp-canonical/tests/artifact_id.rs` with two tests:
       a. `sha256_known_input` — calls `artifact_id_for_canonical_bytes(b"")`
          and asserts result equals
          `"sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"`
          (SHA-256 of empty input — known constant).
       b. `sha256_lowercase_only` — asserts the returned string matches regex
          `^sha256:[0-9a-f]{64}$` (NO uppercase per spec §3.6a).
       Wrap in `#![cfg(feature = "wave2_impl")]`.

    7. Create directories `crates/famp-canonical/tests/vectors/input/`,
       `crates/famp-canonical/tests/vectors/output/`,
       `crates/famp-canonical/tests/vectors/supplementary/` each with a
       `.gitkeep` file. (Plan 02 fills with cyberphone fixtures.)

    8. Run `cargo build -p famp-canonical --tests` — must succeed because all
       test bodies are feature-gated off.
  </action>
  <verify>
    <automated>cargo build -p famp-canonical --tests 2>&1 && test -f crates/famp-canonical/tests/conformance.rs && test -f crates/famp-canonical/tests/float_corpus.rs && test -f crates/famp-canonical/tests/utf16_supplementary.rs && test -f crates/famp-canonical/tests/duplicate_keys.rs && test -f crates/famp-canonical/tests/artifact_id.rs && grep -q '0x4314_3ff3_c1cb_0959' crates/famp-canonical/tests/conformance.rs</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist
    - `crates/famp-canonical/tests/conformance.rs` contains literal `0x4314_3ff3_c1cb_0959` and `1424953923781206.2` (last RFC 8785 Appendix B vector) AND contains `0x0000_0000_0000_0000` (first vector)
    - `grep -c 'cfg(feature = "wave2_impl")' crates/famp-canonical/tests/conformance.rs` returns at least 1
    - `crates/famp-canonical/tests/duplicate_keys.rs` contains literal `{"a":1,"b":2,"a":3}`
    - `crates/famp-canonical/tests/artifact_id.rs` contains literal `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
    - `crates/famp-canonical/tests/float_corpus.rs` contains literal `SAMPLE_SIZE_PR` and `100_000` and `famp-canonical-float-corpus-v1`
    - `grep -q 'wave2_impl' crates/famp-canonical/Cargo.toml` succeeds
    - `grep -q 'full-corpus' crates/famp-canonical/Cargo.toml` succeeds
    - `cargo build -p famp-canonical --tests` exits 0
    - Vector subdirs exist: `tests/vectors/input/.gitkeep`, `tests/vectors/output/.gitkeep`, `tests/vectors/supplementary/.gitkeep`
  </acceptance_criteria>
  <done>Test scaffolding exists and compiles (feature-gated off). RFC 8785 Appendix B vectors transcribed verbatim. Wave 2 will enable the wave2_impl feature once production code lands.</done>
</task>

</tasks>

<verification>
- `cargo build -p famp-canonical` succeeds
- `cargo build -p famp-canonical --tests` succeeds
- `crates/famp-canonical/docs/fallback.md` exists with all 8 required sections
- `Cargo.toml` workspace has `float_roundtrip` enabled on serde_json
- Justfile has `test-canonical` recipe
- All 5 test files exist with verbatim RFC 8785 Appendix B vectors and stubs gated on `wave2_impl`
</verification>

<success_criteria>
Wave 0 scaffolding complete per D-10 step 1–3 of the locked sequence. Fallback
plan exists on disk. Test harness ready for Plan 02 implementation. No
production code written yet (per D-10 ordering).
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-json-foundations/01-01-SUMMARY.md`
documenting: workspace dep changes, fallback.md sections written, list of test
files created with their gate status, and any deviations from the plan.
</output>
