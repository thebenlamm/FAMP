---
phase: 10-test-reactivation-setup-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/famp/tests/_deferred_v1/TRIAGE.md
  - crates/famp/tests/_deferred_v1/README.md
  # deletes: crates/famp/tests/_deferred_v1/*.rs (27 files) + e2e_two_daemons.rs.deferred
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "crates/famp/tests/_deferred_v1/ contains no .rs test files and no .deferred file — only retirement documentation"
    - "Every one of the 27 retired tests has a one-line rationale recorded in the triage ledger (D-01)"
    - "cargo nextest run --workspace is green after deletion (no dangling references, no orphaned harness imports)"
  artifacts:
    - "crates/famp/tests/_deferred_v1/TRIAGE.md"
  key_links:
    - "Each ALREADY-COVERED ledger row names the specific currently-green covering test"
---

<objective>
Triage the 27 parked federation tests in `crates/famp/tests/_deferred_v1/` (TEST-01).
Per D-01/D-02 and the research's per-file audit, the honest outcome is
**27/27 RETIRE, 0 REACTIVATE**: every file traces to a hard-deleted v0.8 CLI
symbol (`init`/`setup`/`listen`/`peer::add`/old `send::run_at`/`run_on_listener`/
bare `HttpTransport`), so there is nothing to salvage onto the live
`famp-bus`/`famp-gateway` API. Both prior salvage candidates
(`send_principal_fallback.rs`, `conversation_restart_safety.rs`) were confirmed
to have no rewrite target.

Purpose: Close the deferred-corpus obligation honestly — delete the dead tests
and leave a documented rationale so the removal is auditable, not silent
(TEST-01 requires documented rationale for every removal).

Output: `_deferred_v1/TRIAGE.md` ledger (27-row RETIRE table), an updated
`_deferred_v1/README.md` retirement banner, and deletion of all 27 `.rs` files
plus `e2e_two_daemons.rs.deferred`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-test-reactivation-setup-docs/10-CONTEXT.md
@.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the TEST-01 triage ledger</name>
  <files>crates/famp/tests/_deferred_v1/TRIAGE.md</files>
  <read_first>
    - `.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md` §"TEST-01 Triage Table (all 27 files read in full)" — the authoritative per-file disposition table. Transcribe it verbatim.
    - `crates/famp/tests/_deferred_v1/README.md` — the original freeze rationale, so the ledger references it correctly.
  </read_first>
  <action>
    Create `crates/famp/tests/_deferred_v1/TRIAGE.md` as the TEST-01 rationale
    ledger (D-01, Claude's-discretion location resolved to a dedicated ledger
    file). Include: (1) a header stating all 27 files retire, 0 reactivate,
    dated 2026-07-27, citing D-01/D-02 and the v0.9 Phase 4 CLI-deletion +
    `v0.8.1-federation-preserved` escape-hatch tag as why the subject CLI is
    gone for good; (2) a 27-row table with columns File | Disposition |
    Rationale, transcribed verbatim from the RESEARCH §"TEST-01 Triage Table"
    (12 rows carry a `RETIRE — ALREADY-COVERED` disposition naming the specific
    currently-green covering test; the remaining 15 carry `RETIRE` with the
    named deleted symbol); (3) a closing note that the `_deferred_v1/` directory
    is retained only as this ledger + README banner, and that reactivation is
    not applicable because no retired test's behavior exists on a shipping
    surface today (the D-02 bar). Do NOT invent dispositions — every row must
    match the research table's File/Disposition/Rationale exactly.
  </action>
  <acceptance_criteria>
    - `crates/famp/tests/_deferred_v1/TRIAGE.md` exists.
    - The ledger table has exactly 27 file rows: `grep -c '^| ' crates/famp/tests/_deferred_v1/TRIAGE.md` counts the 27 data rows plus header/separator (verify 27 filenames present).
    - Every filename from the research table appears once: `for f in init_refuses init_home_env init_identity_incomplete init_no_leak init_force init_happy_path listen_truncated_tail listen_durability info_happy_path listen_bind_collision listen_smoke send_more_coming_requires_new_task listen_shutdown conversation_restart_safety peer_import setup_happy_path send_tofu_bootstrap_refused send_new_task_scope_instructions send_principal_fallback send_new_task peer_add send_deliver_sequence e2e_two_daemons mcp_session_bound_e2e send_terminal_advance_error_surfaces mcp_stdio_tool_calls listen_multi_peer_keyring; do grep -q "$f" crates/famp/tests/_deferred_v1/TRIAGE.md || echo "MISSING $f"; done` prints nothing.
    - At least 12 rows contain the `ALREADY-COVERED` marker: `grep -c 'ALREADY-COVERED' crates/famp/tests/_deferred_v1/TRIAGE.md` >= 12.
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/famp/tests/_deferred_v1/TRIAGE.md && grep -c 'ALREADY-COVERED' crates/famp/tests/_deferred_v1/TRIAGE.md</automated>
  </verify>
  <done>TRIAGE.md exists with all 27 files rationalized and >=12 ALREADY-COVERED pointers to named live tests.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 27 dead tests + update the README banner</name>
  <files>crates/famp/tests/_deferred_v1/README.md, crates/famp/tests/_deferred_v1/ (deletes 27 *.rs + e2e_two_daemons.rs.deferred)</files>
  <read_first>
    - `crates/famp/tests/_deferred_v1/README.md` — current content, to replace with a retirement banner.
    - `.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md` §"Mechanics of exclusion" — confirms these files are invisible to cargo (subdir, not the active glob), so deletion needs no Cargo.toml change; and §"Dead nextest.toml filter references" — the `listen-subprocess` filter is a harmless no-op that may be left as-is (do NOT expand nextest.toml scope; leaving stale-but-harmless config is zero-risk per the research recommendation).
  </read_first>
  <action>
    Delete all 27 `.rs` files under `crates/famp/tests/_deferred_v1/` and the
    `e2e_two_daemons.rs.deferred` file (the full retirement set from D-01). Do
    NOT delete `TRIAGE.md` (Task 1) or `README.md`. Do NOT touch
    `crates/famp/tests/common/` harness files, `Cargo.toml`, or
    `.config/nextest.toml` — the subdirectory files were never in the active
    test glob, so nothing references them and no build config changes. Then
    rewrite `_deferred_v1/README.md` as a short retirement banner: state the
    corpus was retired 2026-07-27 in Phase 10 per TEST-01/D-01, that all 27
    tests targeted the v0.9-Phase-4-deleted federation CLI with no live rewrite
    target, and point readers to `TRIAGE.md` for the per-file rationale. Run
    `just lint` (NOT plain clippy) since the workspace's compiled test surface
    changed. Executors run NON-isolated on `main` per project memory
    (`.planning/` is gitignored); the deleted test files under `crates/` commit
    normally.
  </action>
  <acceptance_criteria>
    - No `.rs` files remain in the subdir: `ls crates/famp/tests/_deferred_v1/*.rs 2>/dev/null | wc -l` prints 0.
    - The `.deferred` file is gone: `test ! -e crates/famp/tests/_deferred_v1/e2e_two_daemons.rs.deferred`.
    - Only `TRIAGE.md` and `README.md` remain in the subdir: `ls crates/famp/tests/_deferred_v1/` lists exactly those two files.
    - `README.md` points at the ledger: `grep -q 'TRIAGE.md' crates/famp/tests/_deferred_v1/README.md`.
    - Workspace still builds and the full suite is green: `cargo build --workspace --all-targets` succeeds and `cargo nextest run --workspace` reports 0 failures.
    - `just lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test $(ls crates/famp/tests/_deferred_v1/*.rs 2>/dev/null | wc -l) -eq 0 && cargo nextest run --workspace</automated>
  </verify>
  <done>All 27 .rs + the .deferred file deleted; subdir holds only TRIAGE.md + README.md; `cargo nextest run --workspace` green; `just lint` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure test-suite hygiene; deletes dead test files + adds Markdown. No runtime code, no input parsing, no network/crypto surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | Silent removal of a test that still guards live behavior | low | mitigate | 27-row TRIAGE.md ledger documents every removal; 12 rows name the live covering test; full-workspace nextest green proves no live coverage regressed |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan (deletion + Markdown only); RESEARCH confirms zero new dependencies workspace-wide |
</threat_model>

<verification>
- `cargo build --workspace --all-targets` succeeds.
- `cargo nextest run --workspace` reports 0 failures (was 969/969 green pre-phase per RESEARCH; deletion of dormant subdir files cannot change the runnable count).
- `just lint` exits 0.
- `crates/famp/tests/_deferred_v1/` contains only `TRIAGE.md` + `README.md`.
</verification>

<success_criteria>
Every deferred test is triaged with documented rationale (TEST-01); the dead corpus is deleted; the workspace stays green.
</success_criteria>

<output>
Create `.planning/phases/10-test-reactivation-setup-docs/10-01-SUMMARY.md` when done.
</output>
