---
phase: 10-test-reactivation-setup-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GATEWAY-SETUP.md
  - README.md
  - crates/famp/tests/gateway_setup_doc_accuracy.rs
  - crates/famp-gateway/tests/gateway_usage_doc_accuracy.rs
  - .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md
autonomous: true
requirements: [DOC-04]

must_haves:
  truths:
    - "docs/GATEWAY-SETUP.md documents the two-machine setup — bind address, out-of-band key exchange, connect/verify — using the exact shipping flag spellings (D-06)"
    - "A compiled accuracy gate fails if the guide's documented flags drift from famp-gateway's usage string or famp peer export/import --help (D-07)"
    - "10-HUMAN-UAT.md captures the unassisted two-machine walkthrough as a human-verified Gate A acceptance (D-07)"
  artifacts:
    - "docs/GATEWAY-SETUP.md"
    - "crates/famp/tests/gateway_setup_doc_accuracy.rs"
    - "crates/famp-gateway/tests/gateway_usage_doc_accuracy.rs"
    - ".planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md"
  key_links:
    - "README federation/quickstart section links to docs/GATEWAY-SETUP.md"
    - "Accuracy gates probe famp-gateway no-args usage stderr + famp peer export/import --help against the guide"
---

<objective>
Ship the two-machine gateway setup guide (DOC-04) and gate it against the
shipping binary so it cannot drift.

Per D-06: a standalone own-machines-first runbook at `docs/GATEWAY-SETUP.md`
(laptop ↔ dev server, direct or a Ben-controlled VPN, **no public relay**),
built from the exact CLI surface the research extracted verbatim from source.
Per D-07: accuracy is enforced by a grep-gate mirroring the v0.11 Phase 6
accuracy-against-binary pattern, and the "developer follows it unassisted and
reaches a working cross-host connection" clause is a HUMAN-UAT acceptance Ben
performs (the milestone's Gate A dogfood) — captured in `10-HUMAN-UAT.md`, not
claimed on the grep-gate alone.

Purpose: Make the v1.0 gateway reproducible by someone other than the author,
closing the milestone's onboarding gap.

Output: `docs/GATEWAY-SETUP.md`, a README link, two accuracy-gate tests, and
`10-HUMAN-UAT.md`.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-test-reactivation-setup-docs/10-CONTEXT.md
@.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/GATEWAY-SETUP.md and link it from README</name>
  <files>docs/GATEWAY-SETUP.md, README.md</files>
  <read_first>
    - `.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md` §"DOC-04: Exact CLI Surface (verbatim from source)" — the authoritative flag/path/example content; and §"Two-machine bootstrap sequence" — the guide's section order. Use these verbatim; do NOT re-derive from memory.
    - `crates/famp-gateway/src/main.rs` — cross-check the `usage:` string and flag spellings (`--socket`/`--listen`/`--tls-cert`/`--tls-key`/`--peer`/`--trust-cert`, positional principal names; no `--help`).
    - `crates/famp/src/cli/peer/export.rs` and `crates/famp/src/cli/peer/import.rs` — confirm `famp peer export --as <principal>` output shape and `famp peer import [<source>]` stdin/file + TOFU-pin-fails-closed behavior.
    - `README.md` §Quick Start (line ~170, the stale `docs/MIGRATION-v0.8-to-v0.9.md` federation pointer per RESEARCH) — the insertion point for the link.
  </read_first>
  <action>
    Write `docs/GATEWAY-SETUP.md` (D-06 filename resolved) as a copy-pasteable
    two-machine runbook, own-machines-first, explicitly stating no public relay.
    Sections in this order (from RESEARCH §"Two-machine bootstrap sequence"):
    (1) Prerequisites — daemon running on each host (`famp daemon install`),
    identity paths `~/.famp/gateway/identity.ed25519` and
    `~/.famp/gateway/peers.keyring`; (2) Gateway identity — generated on first
    use, idempotent; (3) Out-of-band key exchange — `famp peer export --as
    agent:hostA.example/gateway` on A → copy the single printed public line
    (principal + pubkey-b64url + key_id) over Signal/clipboard → `famp peer
    import` on B (stdin or file), and the reverse; call out the fingerprint
    (key_id) eyeball-check and that import fails closed on a conflicting re-pin;
    (4) Start each gateway — `famp-gateway --listen <addr> --tls-cert <path>
    --tls-key <path> --peer <domain>=<url> <principal-name>...` with the exact
    optional/required split and the `--peer <domain>=<url>` format; (5)
    Connect/verify — address the remote principal by name, then `famp inspect
    tasks --id <task_id> --json` on both sides to confirm the task FSM advances
    to a terminal state. SECURITY (per RESEARCH Security Domain): the export
    line is public-key-only — the guide MUST instruct copying the `peer export`
    output line and MUST NOT instruct copying `~/.famp/gateway/identity.ed25519`
    (secret key). Then edit `README.md`: add a link to `docs/GATEWAY-SETUP.md`
    from the Quick Start / federation sentence as the primary cross-host entry
    point (supplement or replace the stale migration-doc pointer); leave the
    `## Advanced: v0.8 federation CLI` history section unmodified (RESEARCH Open
    Question 2 recommendation).
  </action>
  <acceptance_criteria>
    - `docs/GATEWAY-SETUP.md` exists.
    - Every documented gateway flag appears: `for f in -- --listen --tls-cert --tls-key --peer --trust-cert --socket; do grep -q -- "$f" docs/GATEWAY-SETUP.md || echo "MISSING $f"; done` prints nothing.
    - The peer commands appear: `grep -q 'famp peer export' docs/GATEWAY-SETUP.md && grep -q 'famp peer import' docs/GATEWAY-SETUP.md`.
    - The verify step references task inspection: `grep -q 'famp inspect tasks' docs/GATEWAY-SETUP.md`.
    - No-public-relay framing present: `grep -qi 'no public relay\|own machines\|own-machines' docs/GATEWAY-SETUP.md`.
    - The guide never instructs copying the secret key file: `grep -c 'identity.ed25519' docs/GATEWAY-SETUP.md` — any mention must be in a "do not share" context (reviewer confirms; the export line, not the key file, is what gets copied).
    - README links the guide: `grep -q 'GATEWAY-SETUP.md' README.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/GATEWAY-SETUP.md && grep -q 'GATEWAY-SETUP.md' README.md && grep -q 'famp inspect tasks' docs/GATEWAY-SETUP.md</automated>
  </verify>
  <done>Guide exists with all shipping flags, the peer export/import bootstrap, the connect/verify step, no-public-relay framing, and a README link; secret key never instructed to be copied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the doc/binary accuracy gates (D-07)</name>
  <files>crates/famp-gateway/tests/gateway_usage_doc_accuracy.rs, crates/famp/tests/gateway_setup_doc_accuracy.rs</files>
  <read_first>
    - `crates/famp/tests/cli_help_invariant.rs` — the exact `Command::cargo_bin(...).args(["--help"]).output()` + substring-assert pattern to mirror; and `crates/famp/tests/readme_line_count_gate.rs` for workspace-root doc-path resolution.
    - `.planning/phases/10-test-reactivation-setup-docs/10-RESEARCH.md` §"Accuracy-gate pattern to mirror (D-07)" and §"Code Examples" — `famp-gateway` has NO `--help`; its no-args invocation exits non-zero and prints the full `usage:` string to stderr, which is the probe. `famp peer export/import --help` ARE clap-generated.
  </read_first>
  <behavior>
    - gateway_usage_doc_accuracy (in famp-gateway crate): running `famp-gateway` with no args prints a usage string to stderr containing --listen/--tls-cert/--tls-key/--peer/--trust-cert; the test asserts every gateway flag documented in docs/GATEWAY-SETUP.md is present in that usage string (guide flags ⊆ binary usage). Fails if the guide documents a flag the binary does not print.
    - gateway_setup_doc_accuracy (in famp crate): `famp peer export --help` and `famp peer import --help` succeed; the test asserts the `--as` flag (export) and the peer subcommand names documented in the guide are present in the clap help output.
  </behavior>
  <action>
    Create TWO accuracy-gate tests so each spawns only its own crate's binary
    (avoids cross-package `cargo_bin` resolution, per the [07-03] project-memory
    gotcha). (A) `crates/famp-gateway/tests/gateway_usage_doc_accuracy.rs`:
    `Command::cargo_bin("famp-gateway").output()` with no args, assert exit is
    non-success and stderr contains each gateway flag token; then read
    `docs/GATEWAY-SETUP.md` via a path from `CARGO_MANIFEST_DIR` joined with
    `../../docs/GATEWAY-SETUP.md`, and for each gateway flag the guide documents,
    assert it appears in the captured usage stderr (guide ⊆ binary). (B)
    `crates/famp/tests/gateway_setup_doc_accuracy.rs`: `Command::cargo_bin("famp")
    .args(["peer","export","--help"])` and `...["peer","import","--help"]`,
    assert success and that `--as` (export) plus the export/import subcommand
    names appear in help; read the guide via `CARGO_MANIFEST_DIR` +
    `../../docs/GATEWAY-SETUP.md` and assert the guide's `famp peer export`/
    `famp peer import` invocations match the clap surface. Assert messages must
    name DOC-04 and say "docs/GATEWAY-SETUP.md drifted from the shipping CLI —
    update the guide or the flag." Run `just lint`.
  </action>
  <acceptance_criteria>
    - Both files exist: `crates/famp-gateway/tests/gateway_usage_doc_accuracy.rs` and `crates/famp/tests/gateway_setup_doc_accuracy.rs`.
    - Both gates pass: `cargo nextest run -p famp-gateway -E 'test(gateway_usage_doc_accuracy)'` and `cargo nextest run -p famp -E 'test(gateway_setup_doc_accuracy)'` each report passed.
    - Non-vacuity (manual falsification, revert after): adding a fake flag like `--bogus-flag` to docs/GATEWAY-SETUP.md's gateway command makes gateway_usage_doc_accuracy FAIL; revert restores green.
    - Full workspace green: `cargo nextest run --workspace` reports 0 failures.
    - `just lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p famp-gateway -E 'test(gateway_usage_doc_accuracy)' && cargo nextest run -p famp -E 'test(gateway_setup_doc_accuracy)'</automated>
  </verify>
  <done>Both accuracy gates present and green; gateway gate falsified against a bogus flag; guide flags proven a subset of the live binary surfaces; `just lint` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Author 10-HUMAN-UAT.md for Ben's two-machine walkthrough</name>
  <files>.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md</files>
  <read_first>
    - `docs/GATEWAY-SETUP.md` (Task 1 output) — the walkthrough being validated.
    - `.planning/phases/10-test-reactivation-setup-docs/10-CONTEXT.md` §D-07 and §"Explicitly out of scope" — the human run is the Gate A dogfood (laptop ↔ dev server), NOT an automated CI job; DOC-04 must not be claimed done on the grep-gate alone.
    - `.planning/STATE.md` §"Deferred Items" — the `05-HUMAN-UAT.md` precedent for a pending human-UAT scenario tracked as a deferred acceptance.
  </read_first>
  <action>
    Create `.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md`
    as the DOC-04 human acceptance record (D-07). Include: (1) the scenario —
    Ben follows `docs/GATEWAY-SETUP.md` unassisted on two machines he controls
    (laptop ↔ dev server, direct or his existing VPN, no public relay) and
    reaches a working bidirectional cross-host connection; (2) a numbered
    pass/fail checklist mirroring the guide's five sections (daemon on each host
    → gateway identity → out-of-band export/import with fingerprint check →
    start each gateway → send + `famp inspect tasks` shows terminal state on
    both sides); (3) explicit acceptance criteria: a signed envelope delivers
    A→B and B→A and both task FSMs reach a terminal state; (4) a status field
    marked PENDING (awaiting Ben's real two-machine run) with a note that this
    is the milestone's Gate A dogfood and is tracked as a deferred acceptance
    (like the DAEMON-06 Linux UAT), NOT an execution blocker — the automated
    grep-gates (Task 2) cover doc/binary accuracy but explicitly do NOT cover
    the unassisted-success clause. Do NOT mark DOC-04 fully done here; leave the
    human clause PENDING for `/gsd-verify-work` / `/gsd-complete-milestone`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md` exists.
    - It references the guide and the Gate A framing: `grep -q 'GATEWAY-SETUP.md' 10-HUMAN-UAT.md` and `grep -qi 'gate a\|dogfood' .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md`.
    - It carries a PENDING status for the unassisted-success clause: `grep -qi 'pending' .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md`.
    - The checklist covers the connect/verify step: `grep -q 'famp inspect tasks' .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md`.
  </acceptance_criteria>
  <verify>
    <human-check>The UAT doc is a faithful, followable checklist of the guide's steps; the two-machine run itself is Ben's deferred Gate A acceptance, not performed during execution.</human-check>
    <automated>test -f .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md && grep -qi 'pending' .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md</automated>
  </verify>
  <done>10-HUMAN-UAT.md exists with the two-machine checklist, Gate A framing, and a PENDING unassisted-success clause; DOC-04's human clause is left for Ben's real run, not claimed on the grep-gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → operator | The setup guide instructs a human across a trust-sensitive bootstrap (out-of-band key exchange, TLS bind); wrong instructions cause insecure or failed setup. |
| doc ↔ binary | The guide's flag/command surface must match the shipping `famp-gateway` / `famp peer` binaries or an operator follows stale instructions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Information Disclosure | Guide instructs copying secret key material instead of the public `peer export` line | high | mitigate | Guide documents copying the `peer export` public-key-only line and explicitly warns never to share `~/.famp/gateway/identity.ed25519`; acceptance criterion greps for the secret-key filename to confirm it is only in a do-not-share context |
| T-10-04 | Tampering | Guide drifts from the binary; operator follows stale flags, connects insecurely or fails silently | medium | mitigate | Two compiled accuracy gates (Task 2) fail CI when guide flags are not a subset of `famp-gateway` usage / `famp peer` clap help; falsified against a bogus flag |
| T-10-05 | Spoofing | Operator skips the fingerprint (key_id) eyeball-check and pins the wrong peer key | medium | mitigate | Guide calls out the key_id fingerprint check at import; import fails closed on a conflicting re-pin (documented, from Phase 8 TRUST-01/02); human-UAT checklist includes the fingerprint verification step |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs (Markdown + two stdlib/assert_cmd tests); RESEARCH confirms zero new dependencies |
</threat_model>

<verification>
- `cargo nextest run --workspace` green, including both new accuracy gates.
- Gateway accuracy gate falsified against a bogus flag (fails, then reverts to green).
- `docs/GATEWAY-SETUP.md` documents all shipping gateway flags + peer export/import + connect/verify, own-machines-first, secret key never copied.
- README links the guide; `10-HUMAN-UAT.md` exists with a PENDING Gate A clause.
- `just lint` exits 0.
</verification>

<success_criteria>
The two-machine setup guide is written, binary-accurate (grep-gated), and linked
from README (DOC-04); the unassisted-success clause is captured as Ben's
human-verified Gate A acceptance in 10-HUMAN-UAT.md, not claimed on the gate alone.
</success_criteria>

<output>
Create `.planning/phases/10-test-reactivation-setup-docs/10-03-SUMMARY.md` when done.
</output>
