---
phase: 12-v1-0-0-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GATEWAY-SETUP.md
  - crates/famp/tests/gateway_setup_doc_accuracy.rs
  - crates/famp/src/cli/send/mod.rs
  - README.md
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "docs/GATEWAY-SETUP.md §6 names the exact fire-and-forget boundary: a zero exit code from a remote `famp send` confirms ONLY that the local broker accepted the envelope into the gateway-backed outbound mailbox on this host, and explicitly does NOT confirm egress drain/sign/relay, remote gateway verification, remote mailbox arrival, or far-side task-FSM advancement (REL-01, §16 item 8)."
    - "`cargo test -p famp --test gateway_setup_doc_accuracy` pins that statement: the new assertions FAIL against the pre-edit doc and PASS after it, so the pin is non-vacuous rather than satisfied by pre-existing text."
    - "The REL-01 anchor phrases are matched against the whitespace-normalized doc (`doc.split_whitespace().collect::<Vec<_>>().join(\" \")`), so markdown line-wrap inside the paragraph cannot break the match; every anchor is pure ASCII with no `**`/backtick/apostrophe/em-dash inside it, so a smart-quote or bold-marker edit cannot silently un-match a still-correct sentence."
    - "The new §6 paragraph is positioned between the two existing §6 anchors: its byte offset in the normalized doc is greater than that of `famp send --to agent:hostB.example/bob` and less than that of `famp inspect tasks --id`, asserted by index comparison in the same style as the existing keyring/ready ordering assertion (gateway_setup_doc_accuracy.rs:209-222)."
    - "`cargo test -p famp --test gateway_setup_doc_accuracy` fails loudly with the existing `update the guide or the code:` panic message if `docs/GATEWAY-SETUP.md` is missing, empty, or unreadable — it never passes vacuously on absent input."
    - "`famp send --help` states the same fire-and-forget boundary in one terse clap line, and README's `## Not Shipped Yet` no longer claims `famp-gateway` bridging is unshipped."
  artifacts:
    - "docs/GATEWAY-SETUP.md §6 send-confirmation paragraph"
    - "extended crates/famp/tests/gateway_setup_doc_accuracy.rs (REL-01 assertion block: 3 prose anchors + 1 ordering assertion + 1 `famp send --help` anchor + 1 README anchor)"
    - "crates/famp/src/cli/send/mod.rs SendArgs::to clap doc-comment caveat"
    - "README.md corrected federation shipped/not-shipped state + pointer to docs/GATEWAY-SETUP.md"
  key_links:
    - "GATEWAY-SETUP.md §6 prose == gateway_setup_doc_accuracy.rs anchor literals == `famp send --help` caveat == README pointer (one claim, four surfaces, one pinning test)"
    - "the documented boundary == the actual code boundary: `send/mod.rs:368` returns on `BusReply::SendOk`; relay happens later in `famp-gateway/src/egress.rs::run_egress` (egress.rs:315), a decoupled ~1s poll the CLI never awaits"
  prohibitions:
    - "MUST NOT ship a release-facing statement about what `famp send` confirms that is not true of the shipped code — in particular MUST NOT reuse design review C §16's stale wording asserting that remote sends do not initiate or complete the task FSM, which Phase 11's ADDR-02 falsified."
---

<objective>
REL-01 / §16 item 8 — state, in the three surfaces a user actually reads (`docs/GATEWAY-SETUP.md` §6, `famp send --help`, README), exactly what a successful remote `famp send` confirms and what it does not, and pin that statement with an assertion in the already-existing `crates/famp/tests/gateway_setup_doc_accuracy.rs` so it cannot silently rot.

Purpose: today `GATEWAY-SETUP.md` says nothing about send-confirmation semantics (verified by grep — neither "exit code" nor "fire-and-forget" appears anywhere in the file). A user can reasonably read a zero exit code as "my peer got it," which is false until the gateway's decoupled drain loop runs. This is the last documentation item on the §16 v1.0.0 checklist.

Output: a corrected §6 paragraph, a terse `--help` caveat, a corrected README federation state, and a non-vacuous regression pin — landing as `.rs`-touching commits that trigger a real CI run.

**This plan is the phase TRACER.** It is the only work in Phase 12 that changes code *and* docs together, and it is therefore the first commit that will actually fire `.github/workflows/ci.yml` (whose `paths-ignore` on `docs/**`, `.planning/**`, `**/*.md` means a docs-only commit gets **zero check-runs, not a pass**). Task 1 walks the whole release-gate loop end to end — edit → local targeted test → commit → push → live `gh api` check-runs attestation — before any expansion work depends on that loop working. If the loop is broken, this plan finds it after one commit rather than after the version bump.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md
@.planning/phases/12-v1-0-0-release-gate/12-PATTERNS.md
@.planning/phases/12-v1-0-0-release-gate/12-VALIDATION.md
</context>

## Artifacts this phase produces

Symbols and literals **created by this plan** (exclude these from source-drift verification — they do not exist upstream):

| Artifact | Kind | Location |
|----------|------|----------|
| `**What a successful \`famp send\` confirms — and what it does not.**` | new doc paragraph lead-in (H-less bold callout) | `docs/GATEWAY-SETUP.md` §6 |
| `A zero exit code confirms only that the local broker accepted the envelope into the gateway-backed outbound mailbox on this host.` | new doc sentence + test anchor A1 | `docs/GATEWAY-SETUP.md` §6, `crates/famp/tests/gateway_setup_doc_accuracy.rs` |
| `It does not confirm that the gateway has drained, signed, and relayed the envelope` | new doc clause + test anchor A2 | same two files |
| `That is the fire-and-forget boundary.` | new doc sentence + test anchor A3 | same two files |
| `only local acceptance into the gateway-backed outbound mailbox` | new clap `--help` clause + test anchor A4 | `crates/famp/src/cli/send/mod.rs`, test file |
| `**Federation gateway (v1.0, shipped):**` | new README bullet lead-in + test anchor A5 | `README.md`, test file |
| REL-01 assertion block (6 new `assert!`/index checks) | appended inside the EXISTING `#[test] fn gateway_setup_doc_accuracy()` — **no new test function name is created** | `crates/famp/tests/gateway_setup_doc_accuracy.rs` |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — pin the send-confirmation statement (test RED first), write the §6 paragraph (GREEN), commit, push, prove CI fires green</name>
  <files>crates/famp/tests/gateway_setup_doc_accuracy.rs, docs/GATEWAY-SETUP.md</files>
  <read_first>
    - `crates/famp/tests/gateway_setup_doc_accuracy.rs` (all 228 lines — you are appending to the single existing `#[test] fn gateway_setup_doc_accuracy()`; note the `normalized` binding at line 112 and the ordering-assertion idiom at lines 209-222)
    - `docs/GATEWAY-SETUP.md` lines 249-300 (§6 "Connect / verify" — the `famp send --to agent:hostB.example/bob` fenced block ends at line 256; `Then confirm the task's FSM reached a terminal state` is line 258; `famp inspect tasks --id <task_id> --json` is line 261)
    - `.planning/phases/12-v1-0-0-release-gate/12-PATTERNS.md` § "`crates/famp/tests/gateway_setup_doc_accuracy.rs` — EXTEND" (house style: normalize once, one assert per claim, fail message ALWAYS starts `update the guide or the code:`) and § "`docs/GATEWAY-SETUP.md` §6 — INSERT" (bold-lead-in callout convention)
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § REL-01 (the traced exit-code semantics — do not re-derive them, and do not soften them)
  </read_first>
  <behavior>
    - RED step: with the assertions added but the doc unedited, `cargo test -p famp --test gateway_setup_doc_accuracy` MUST fail on anchor A1. Neither "exit code" nor "fire-and-forget" currently appears anywhere in `docs/GATEWAY-SETUP.md`, so a pass at this step proves the anchor is vacuous (matching pre-existing text) and the anchor must be re-chosen.
    - GREEN step: after inserting the §6 paragraph, the same command passes.
    - Ordering: the index of anchor A1 in `normalized` is strictly greater than the index of `famp send --to agent:hostB.example/bob` and strictly less than the index of `famp inspect tasks --id`. Both of those anchors occur exactly once in the file (lines 255 and 261) so `.find()` is unambiguous.
    - Absent input: deleting/emptying `docs/GATEWAY-SETUP.md` makes the test panic with the existing `update the guide or the code: could not read` message — it never passes on missing input.
  </behavior>
  <action>
    First append the REL-01 assertion block to the END of the existing `#[test] fn gateway_setup_doc_accuracy()` in `crates/famp/tests/gateway_setup_doc_accuracy.rs`, immediately before its closing brace (currently line 228), under a section comment naming the requirement, matching the section-comment convention already used at lines 104-111 and 200-203. Reuse the existing `doc` and `normalized` bindings — do NOT re-read or re-normalize the file. Add three positive `assert!(normalized.contains(...), "update the guide or the code: ...")` checks for anchors A1, A2, A3 (exact literals listed in the "Artifacts this phase produces" table above; every fail message MUST start with the house prefix `update the guide or the code:`), then one ordering check built from `normalized.find(...).expect(...)` on `famp send --to agent:hostB.example/bob` and on `famp inspect tasks --id`, asserting the A1 index falls strictly between them. Run `cargo test -p famp --test gateway_setup_doc_accuracy` and CONFIRM IT FAILS — record the failing assertion in the SUMMARY as the non-vacuity proof. If it passes at this step, the anchor collided with pre-existing prose: pick a more specific anchor and repeat.

    Then insert the new paragraph into `docs/GATEWAY-SETUP.md` §6, after the fenced `famp send --to agent:hostB.example/bob --new-task --body "hello from A"` block (currently ending line 256) and before the line beginning `Then confirm the task's FSM reached a terminal state`. Use the doc's established bold-lead-in callout shape (the same shape as the existing `**Before you send, probe the live peer first.**` at line 271). The paragraph opens with the bold lead-in `**What a successful \`famp send\` confirms — and what it does not.**` and then states, in this order: (1) anchor A1 verbatim; (2) anchor A2 verbatim, continuing into the remaining negatives — that the remote gateway verified it, that it reached the remote mailbox, or that the task FSM advanced on the far side — followed by the reason, that egress is a decoupled background drain loop the CLI never waits on; (3) anchor A3 verbatim; (4) a closing sentence pointing at the `famp inspect tasks` check below as what actually confirms end-to-end delivery. Keep the three anchor sentences pure ASCII: no `**`, no backticks, no apostrophes, no em-dashes INSIDE an anchor literal (markdown emphasis and em-dashes are fine in the surrounding sentences). Do not introduce a new heading — this is prose inside the existing `## 6. Connect / verify`.

    Re-run the test (GREEN), then `just lint`, then commit both files together in ONE commit (bundling the `.rs` pin with its `.md` claim is this repo's established pattern and is what makes the commit touch a path `ci.yml` does not ignore), and push. Confirm the push produced a real CI run: query the pushed SHA's check-runs and require a non-zero total with every conclusion `success`. Do NOT treat an empty check-runs list as a pass — an empty list means the commit touched only ignored paths and the bundling failed.
  </action>
  <verify>
    <automated>cargo test -p famp --test gateway_setup_doc_accuracy</automated>
    <automated>just lint</automated>
  </verify>
  <acceptance_criteria>
    - RED proof recorded: the pre-doc-edit run of `cargo test -p famp --test gateway_setup_doc_accuracy` failed, and the failing assertion's message is quoted in the SUMMARY.
    - `cargo test -p famp --test gateway_setup_doc_accuracy` exits 0 after the doc edit.
    - `just lint` exits 0.
    - `grep -c 'fire-and-forget boundary' docs/GATEWAY-SETUP.md` returns `1`.
    - `grep -c 'gateway-backed outbound mailbox' docs/GATEWAY-SETUP.md` returns at least `1`.
    - `awk '/^## 6\. Connect \/ verify/,/^## /' docs/GATEWAY-SETUP.md | grep -c 'fire-and-forget boundary'` returns `1` — the paragraph is inside §6, not appended elsewhere.
    - `grep -n 'fire-and-forget boundary' docs/GATEWAY-SETUP.md` reports a line number strictly between the line numbers returned by `grep -n 'famp send --to agent:hostB.example/bob' docs/GATEWAY-SETUP.md` and `grep -n 'famp inspect tasks --id' docs/GATEWAY-SETUP.md`.
    - Every new fail message in the test begins with `update the guide or the code:` — verified by `grep -c 'update the guide or the code:' crates/famp/tests/gateway_setup_doc_accuracy.rs` increasing by exactly the number of new `assert!` calls added.
    - `git show --stat HEAD` lists BOTH `docs/GATEWAY-SETUP.md` and `crates/famp/tests/gateway_setup_doc_accuracy.rs` in one commit.
    - `gh api "/repos/thebenlamm/FAMP/commits/$(git rev-parse HEAD)/check-runs" --jq '.total_count'` returns a number greater than `0`, and `gh api "/repos/thebenlamm/FAMP/commits/$(git rev-parse HEAD)/check-runs" --jq '[.check_runs[] | select(.conclusion != "success")] | length'` returns `0` once every run reports `status == "completed"`.
  </acceptance_criteria>
  <reversibility rating="reversible">Prose + test assertions in two files; revert is a single `git revert` with no downstream state.</reversibility>
  <done>§6 states the fire-and-forget boundary, the doc-accuracy test pins it non-vacuously, and the commit has a real, fully-green CI run — proving the phase's edit → test → CI loop works before the version bump depends on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: expand the same claim to `famp send --help` and README, pinned by the same test</name>
  <files>crates/famp/src/cli/send/mod.rs, README.md, crates/famp/tests/gateway_setup_doc_accuracy.rs</files>
  <read_first>
    - `crates/famp/src/cli/send/mod.rs` lines 60-110 (the `SendArgs` struct; the `to` field and its current one-line doc comment are at lines 68-70; the `domain` field at lines 102-109 is the precedent for a multi-line behavioral-caveat doc comment)
    - `README.md` lines 34-90 (`## What Works Today` bullet convention `**Feature (vX.Y, shipped):** …`, and `## Not Shipped Yet` whose line 78 bullet still claims `famp-gateway` bridging is unshipped — false since Phase 7-11)
    - `crates/famp/tests/gateway_setup_doc_accuracy.rs` lines 1-18 (module doc-comment — it currently describes the file as the GATEWAY-SETUP accuracy gate only) and lines 29-60 (the `Command::cargo_bin("famp").args([...,"--help"])` subprocess idiom)
    - `.planning/phases/12-v1-0-0-release-gate/12-RESEARCH.md` § REL-01 "Exact insertion points" item 3 and § Assumptions Log A1 (option (b) — correct the stale bullet and point at the guide, rather than duplicating the full semantics into README)
  </read_first>
  <behavior>
    - `famp send --help` exits 0 and its whitespace-normalized stdout contains anchor A4.
    - `README.md` contains anchor A5, and no longer contains a `## Not Shipped Yet` bullet claiming `famp-gateway` bridging is unshipped.
    - Both are asserted from the same test binary, so `cargo test -p famp --test gateway_setup_doc_accuracy` is the single command that covers all three REL-01 surfaces.
  </behavior>
  <!-- planner-discipline-allow: bridging the local bus -->
  <action>
    In `crates/famp/src/cli/send/mod.rs`, extend the `to` field's doc comment (currently the single line at 68) with continuation lines carrying the fire-and-forget caveat, following the `domain` field's multi-line behavioral-caveat shape at lines 102-109. Keep the FIRST line unchanged so clap's short `-h` summary does not grow. The continuation must contain anchor A4 verbatim (`only local acceptance into the gateway-backed outbound mailbox`) and must point the reader at `docs/GATEWAY-SETUP.md` section 6 rather than restating the full explanation — clap help stays terse.

    In `README.md`: delete the stale `## Not Shipped Yet` bullet that lists `famp-gateway` bridging the local bus to remote FAMP-over-HTTPS (line 78) — Phases 7-11 shipped exactly that — leaving the rest of the v1.0 not-shipped list (Agent Cards, `.well-known` distribution, negotiation, delegation, provenance graph, extensions registry, replay defense/freshness, conformance matrix) intact, since those genuinely are still deferred. Then add one bullet to `## What Works Today` immediately after the v0.11 broker-daemon bullet, opening with anchor A5 (`**Federation gateway (v1.0, shipped):**`), stating in one or two sentences that `famp send --to agent:<domain>/<name>` addresses a remote principal over a signed cross-host path, that a successful send confirms only local acceptance and not remote delivery, and linking to `docs/GATEWAY-SETUP.md` using the same relative-markdown-link shape as the existing design-spec link at line 65. Do NOT restate the full boundary explanation here — one sentence plus the link, so the two copies cannot drift apart.

    In `crates/famp/tests/gateway_setup_doc_accuracy.rs`, add two more checks to the same REL-01 block: (a) a subprocess check that runs `famp send --help` via the existing `Command::cargo_bin("famp")` idiom, asserts exit 0, whitespace-normalizes stdout the same way the doc is normalized (clap wraps help text, so an un-normalized `contains` is brittle), and asserts anchor A4 is present; (b) a read of `README.md` (resolve it as `env!("CARGO_MANIFEST_DIR")` joined with `../../README.md`, mirroring `gateway_setup_doc_path()`) asserting anchor A5 is present. Update the file's module doc-comment (lines 1-9) to record that this gate now also pins REL-01's `--help` and README claims, so a future reader does not think the README check is misplaced. Same fail-message prefix as every other prose-semantic assertion in the file.

    Run the targeted test and `just lint`, then commit all three files in ONE commit and push; confirm the push produced a real, green CI run the same way Task 1 did.
  </action>
  <verify>
    <automated>cargo test -p famp --test gateway_setup_doc_accuracy</automated>
    <automated>just lint</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -q -p famp -- send --help | tr -s '[:space:]' ' ' | grep -c 'only local acceptance into the gateway-backed outbound mailbox'` returns `1`.
    - `cargo run -q -p famp -- send --help` exits 0.
    - `grep -c 'Federation gateway (v1.0, shipped)' README.md` returns `1`.
    - `awk '/^## Not Shipped Yet/,/^## Prerequisites/' README.md | grep -ci 'bridging the local bus'` returns `0`.
    - `awk '/^## Not Shipped Yet/,/^## Prerequisites/' README.md | grep -c 'Agent Cards'` returns `1` — the genuinely-deferred items were not collaterally deleted.
    - `grep -c 'docs/GATEWAY-SETUP.md' README.md` returns at least `1`.
    - `cargo test -p famp --test gateway_setup_doc_accuracy` exits 0; `just lint` exits 0.
    - `git show --stat HEAD` lists all three of `crates/famp/src/cli/send/mod.rs`, `README.md`, `crates/famp/tests/gateway_setup_doc_accuracy.rs`.
    - `gh api "/repos/thebenlamm/FAMP/commits/$(git rev-parse HEAD)/check-runs" --jq '[.check_runs[] | select(.conclusion != "success")] | length'` returns `0` with `.total_count` greater than `0`.
  </acceptance_criteria>
  <reversibility rating="reversible">Doc comment + README prose + test assertions; single-commit revert.</reversibility>
  <done>All three REL-01 surfaces (guide, `--help`, README) carry the same accurate statement, one test command covers all three, README no longer claims shipped federation is unshipped, and the commit is CI-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | The guide/`--help`/README are the operator's only model of what a successful send guarantees; a wrong model here causes an operator to believe a message was delivered when it was not. This plan changes no runtime path, so this doc→belief boundary is the only one it touches. |
| repo → CI | The commit shape decides whether `ci.yml` runs at all (`paths-ignore` on `docs/**`, `.planning/**`, `**/*.md`). |

## STRIDE Threat Register (OWASP ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Repudiation | `docs/GATEWAY-SETUP.md` §6 / `famp send --help` | medium | mitigate | An operator who believes a zero exit code means delivery cannot later distinguish "peer ignored me" from "never left my host." Task 1 states the exact boundary and Task 1's ordering assertion forces it to sit between the send command and the `famp inspect tasks` confirmation step, where it is read in sequence. |
| T-12-01-02 | Tampering | `crates/famp/tests/gateway_setup_doc_accuracy.rs` | medium | mitigate | A future edit could silently soften the statement back to an overclaim. The three literal anchors plus the ordering assertion make any such edit a red CI `test` job, not a silent drift. |
| T-12-01-03 | Information disclosure | new doc paragraph | low | accept | The paragraph names only already-public architecture (a decoupled egress drain loop) that `ARCHITECTURE.md` and `12-RESEARCH.md` already describe; it exposes no key material, host, or credential. Accepted with no mitigation. |
| T-12-01-04 | Spoofing | none | low | accept | This plan writes no authentication, identity, or signature path. INV-10, RFC-8785 canonicalization, and the `FAMP-sig-v1\0` domain prefix are untouched; the local bus stays UNSIGNED. Accepted by construction. |
| T-12-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No dependency is added by this plan — `12-RESEARCH.md` § "Package Legitimacy Audit" records N/A for the whole phase. Gate: `git diff --name-only HEAD~2..HEAD` must contain no `Cargo.toml`, no `Cargo.lock`, and no lockfile of any kind; if it does, stop and run the Package Legitimacy Gate protocol before continuing. |
</threat_model>

<verification>
- `cargo test -p famp --test gateway_setup_doc_accuracy` passes and demonstrably failed before the doc edit (non-vacuity proof recorded in the SUMMARY).
- `just lint` passes.
- Both commits have a live, fully-green CI run confirmed via `gh api /repos/thebenlamm/FAMP/commits/<sha>/check-runs` with `total_count > 0`.
- Full-workspace `cargo test --workspace` / `just ci` are NOT run locally — they hang on this machine (documented nextest `--list`-phase hang). CI is the only admissible full-suite evidence.
</verification>

<success_criteria>
REL-01 is closed: all three user-facing surfaces state the send-confirmation boundary accurately, one targeted test command pins all three, the statement is positioned where it is read, and the phase's edit → test → commit → CI-green loop is proven working on real commits.
</success_criteria>

## Probe reconciliation (spec-less fallback — REL-01)

No SPEC.md exists for this phase, so `## Edge Coverage` and `## Prohibitions` were derived by the spec-less probe fallback. The probe surfaced **6 applicable edge items for REL-01**. Reconciliation: **5 authored into `must_haves.truths` + 1 surfaced as a flagged assumption = 6.** No silent drops.

| # | Category | Disposition | Where |
|---|----------|-------------|-------|
| 1 | boundary | covered | truth 1 (the exact fire-and-forget threshold and both sides of it) |
| 2 | adjacency | covered | truth 2 (RED-first proof — the new anchor must not collide with pre-existing prose) |
| 3 | empty | covered | truth 5 (missing/empty doc panics, never a vacuous pass) |
| 4 | encoding | covered | truth 3 (whitespace-normalized matching; ASCII-only anchors, no smart-quote/bold-marker straddle) |
| 5 | ordering | covered | truth 4 (index comparison between the two existing §6 anchors) |
| 6 | precision | **flagged assumption** | Not applicable to REL-01: the requirement is a prose doc edit plus string-containment assertions. There is no arithmetic, no rounding/tie-breaking, and no fixed-width numeric field anywhere in its surface, so no precision contract exists to specify. Recorded rather than dropped. |

Prohibition probe (in-prompt, two-stage recall → precision): 1 kept for REL-01, authored descriptor-less into `must_haves.prohibitions` above (transparency class — no stale limitation statement). Routine-engineering candidates ("must not break the existing flag-grep assertions", "must not leave the doc self-contradictory") were dropped in the precision stage as ordinary correctness owned by the edge probe and code review.

<output>
Create `.planning/phases/12-v1-0-0-release-gate/12-01-SUMMARY.md` when done. `.planning/` is gitignored in this repo — use `git add -f` to commit any planning artifact.
</output>
