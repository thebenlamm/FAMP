---
phase: 09-end-to-end-cross-host-delivery
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - crates/famp-gateway/tests/e2e_cross_host_delivery.rs
autonomous: true
requirements: [GW-01, GW-02, GW-03]
must_haves:
  truths:
    - "A message from an agent on side A addressed to a principal on side B is delivered into B's local bus mailbox through the two gateways (GW-01)."
    - "B's reply within the same task/conversation is delivered back into A's local bus mailbox (GW-02)."
    - "A full request → commit → deliver → ack cycle completes across both sides and `famp inspect tasks --id <task_id> --json` shows a terminal FSM state on BOTH sides (GW-03)."
  artifacts:
    - "crates/famp-gateway/tests/e2e_cross_host_delivery.rs — two-broker + two-gateway loopback HTTPS harness with ChildGuard"
  key_links:
    - "The terminal FSM state is derived live from mailbox JSONL by `famp inspect tasks` (09-RESEARCH §6) — GW-03 passes iff the right envelope classes land byte-exact in each mailbox, no gateway FSM logic exists to assert."
    - "Two isolation axes per side: --socket for bus/mailbox isolation AND $FAMP_HOME for gateway identity/peers-keyring isolation (09-RESEARCH §7)."
---

<objective>
Prove the phase: a two-process loopback E2E that stands up two brokers on distinct sockets, two `famp-gateway` processes over loopback HTTPS, establishes mutual TOFU trust via `famp peer export`/`import`, drives a full `request → commit → deliver → ack` task cycle between an agent on each side, and asserts (1) delivery into both mailboxes and (2) a terminal task FSM state on both sides via `famp inspect tasks --id --json`.

This is the D-07 phase gate. Per 09-RESEARCH §6 there is no persisted FSM to assert — `famp inspect tasks` derives state live from mailbox content — so GW-03 is proven the moment the correct envelope classes round-trip byte-exact through the relay. The live two-physical-machine run and the `just ci`-gated variant are Phase 10 (TEST-02) and explicitly OUT of scope here.

Purpose: Turn GW-01/GW-02/GW-03 from "wired" into "observably true."
Output: One green integration test binary composing the Phase-7 subprocess harness with the fixture-cert TLS setup.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-end-to-end-cross-host-delivery/09-RESEARCH.md
@.planning/phases/09-end-to-end-cross-host-delivery/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `crates/famp-gateway/tests/e2e_cross_host_delivery.rs` — the two-process loopback gate.
- Reuses the existing `crates/famp-gateway/tests/common/child_guard.rs` (via `#[path = "common/child_guard.rs"] mod child_guard;`) and the fixture certs at `crates/famp/tests/fixtures/cross_machine/{alice,bob}.{crt,key}`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Two-broker + two-gateway loopback harness with TOFU bootstrap</name>
  <files>crates/famp-gateway/tests/e2e_cross_host_delivery.rs</files>
  <read_first>
    - crates/famp-gateway/tests/liveness.rs (Pattern A: ensure_famp_bin_built, spawn_broker_subprocess, wait_for_broker_socket, poll_until_all_live, ChildGuard usage)
    - crates/famp-gateway/tests/common/child_guard.rs (RAII kill+wait)
    - crates/famp/tests/_deferred_v1/e2e_two_daemons.rs.deferred (Pattern B: fixture-cert loading — TLS half only)
    - crates/famp/src/cli/peer/export.rs, import.rs (peer export/import CLI shapes for the TOFU bootstrap)
    - 09-RESEARCH.md §7 (harness composition + two isolation axes + cross-package binary gotcha) + §Summary (one-signing-key-per-name)
  </read_first>
  <action>
    Build the harness in a `#[tokio::test]` (or `#[test]` with a runtime): create two `tempfile::tempdir()`s (sideA, sideB). For each side use ONE tempdir for both axes: broker socket at `<tmp>/bus.sock` (passed via `--socket`) AND `FAMP_HOME=<tmp>` (env on every `famp`/`famp-gateway` child) so identity + peers keyring isolate per side (09-RESEARCH §7). Call `ensure_famp_bin_built()` (prebuild `-p famp --bin famp` — CARGO_BIN_EXE_famp does not cross package boundaries, 07-03 decision). Spawn both brokers with ChildGuard; `wait_for_broker_socket` on each (poll, never fixed sleep). Establish mutual trust: run `famp peer export --as alice` (FAMP_HOME=sideA) capturing the one-line blob, feed it to `famp peer import` (FAMP_HOME=sideB); reverse for bob. Spawn both gateways with ChildGuard: side A backs `bob` (the remote principal) with `--listen 127.0.0.1:<portA> --tls-cert <fixtures>/alice.crt --tls-key <fixtures>/alice.key --peer <bobDomain>=https://127.0.0.1:<portB> --trust-cert <fixtures>/bob.crt --socket <sideA>/bus.sock`; side B backs `alice` symmetrically with bob's cert/key and A's URL/trust-cert. Use the cross_machine fixture certs at `crates/famp/tests/fixtures/cross_machine/`. Poll gateway readiness on the "ready, backing N principal(s)" stdout line or via `famp inspect identities --json` (poll_until_all_live shape) — never a fixed sleep. Use plain `cargo test` (never nextest — project memory). Every spawned broker AND gateway child MUST be wrapped in ChildGuard (project memory: leaked tmp-socket brokers respawn).
  </action>
  <verify>
    <automated>cargo build -p famp-gateway --test e2e_cross_host_delivery</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p famp-gateway --test e2e_cross_host_delivery` exits 0.
    - Every broker/gateway subprocess is constructed through ChildGuard (no bare `.spawn()` without a guard).
    - Readiness uses poll-with-deadline helpers, not fixed `sleep` (grep the test for `thread::sleep`/`sleep(` on the readiness paths — none on convergence checks).
    - Both sides isolate on distinct `--socket` paths AND distinct `FAMP_HOME`.
  </acceptance_criteria>
  <done>The harness stands up two isolated broker+gateway pairs with mutual TOFU trust over loopback HTTPS, ready to drive a task cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the full cycle and assert delivery + terminal FSM on both sides</name>
  <files>crates/famp-gateway/tests/e2e_cross_host_delivery.rs</files>
  <read_first>
    - crates/famp-inspect-server/src/parse.rs (derive_fsm_state — the (class, mode, terminal) → state mapping GW-03 asserts against)
    - crates/famp-inspect-server/src/tasks.rs (inspect_tasks_by_id — the read-time aggregation)
    - crates/famp/tests/common/cycle_driver.rs (drive_alice/drive_bob request→commit→deliver→ack cycle shape, if present)
    - crates/famp/src/cli/send/mod.rs and the reply path (famp send / famp reply CLI to seed the conversation)
    - 09-RESEARCH.md §6 (GW-03 is mailbox-derived) + §Phase Requirements → Test Map
  </read_first>
  <action>
    Drive the cycle: from side A, `famp send` (FAMP_HOME/socket = sideA) a new-task request addressed to the gateway-backed remote principal name for `bob`, capturing the resulting `task_id`. Assert GW-01 by polling `famp inspect messages`/`inspect tasks` (or `famp inbox list --as bob`) on side B until the request envelope appears in bob's local mailbox. From side B, `famp reply` within the same `task_id` (commit, then deliver, then the ack path — reuse cycle_driver's drive_bob shape if available) so the reply relays back; assert GW-02 by polling side A until bob's reply lands in alice's mailbox. Assert GW-03 by polling `famp inspect tasks --id <task_id> --json` on BOTH sides until each shows a terminal state (`COMPLETED`/`FAILED`/`CANCELLED` per derive_fsm_state's `("deliver", mode, true)` mapping); assert both sides converge to the SAME terminal state. Every wait is poll-with-deadline (bounded, e.g. 15-30s), never a fixed sleep. Keep the test a single `#[test]`/`#[tokio::test]` so ChildGuard drop tears down all four+ children on any panic.
  </action>
  <verify>
    <automated>cargo test -p famp-gateway --test e2e_cross_host_delivery</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p famp-gateway --test e2e_cross_host_delivery` exits 0.
    - The test asserts: the request reached B's mailbox (GW-01), the reply reached A's mailbox (GW-02), and `famp inspect tasks --id <task_id> --json` shows the SAME terminal FSM state on both sides (GW-03).
    - No fixed `sleep` on any convergence assertion; all waits are bounded polls.
    - `just lint` passes.
  </acceptance_criteria>
  <done>The full request→commit→deliver→ack cycle round-trips across two gateways and both sides observe a matching terminal task state — GW-01/GW-02/GW-03 observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real subprocesses | The E2E spawns real brokers + gateways; leaked children can respawn tmp-socket brokers (project incident class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13 | Denial of Service | leaked test subprocesses | medium | mitigate | Every broker/gateway child wrapped in ChildGuard (RAII kill+wait on drop); single-test scope so one panic tears down all children. |
| T-09-14 | Tampering | flaky fixed-sleep readiness | low | mitigate | All readiness/convergence checks are poll-with-deadline, never fixed sleep (project convention) — prevents false green on a slow relay. |
| T-09-15 | Spoofing | E2E accepts an unverified relay | high | mitigate | The cycle only completes if verify_inbound_any accepted each hop against the imported peer key; a broken verify would fail delivery, not silently pass — the terminal-state-on-both-sides assertion is the proof. |
</threat_model>

<verification>
- `cargo test -p famp-gateway --test e2e_cross_host_delivery` green.
- `just lint` clean.
- GW-01 (request→B mailbox), GW-02 (reply→A mailbox), GW-03 (matching terminal FSM on both `famp inspect tasks --id --json`) all asserted in one test.
</verification>

<success_criteria>
A single green two-process loopback E2E proves the full bidirectional request→commit→deliver→ack cycle completes across two gateways with the task FSM terminal on both sides — the Phase 9 gate.
</success_criteria>

<output>
Create `.planning/phases/09-end-to-end-cross-host-delivery/09-05-SUMMARY.md` when done.
</output>
