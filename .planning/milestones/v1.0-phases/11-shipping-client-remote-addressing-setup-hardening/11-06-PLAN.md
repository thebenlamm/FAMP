---
phase: 11-shipping-client-remote-addressing-setup-hardening
plan: 06
type: execute
wave: 4
depends_on: [11-03, 11-04, 11-05, 11-07]
files_modified:
  - .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-HUMAN-UAT.md
autonomous: false
requirements: [UAT-01]
must_haves:
  truths:
    - "A live two-machine re-run with the FIXED `famp send` (NO injector) delivers into the real remote agent's mailbox and the task FSM reaches a terminal state on both sides."
    - "The deployed `~/.cargo/bin/famp` and `famp-gateway` binaries carry this phase's fix before the dogfood (binary freshness verified)."
  artifacts:
    - ".planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-HUMAN-UAT.md (the dogfood record + verdict)"
  key_links:
    - "famp send --to agent:<peer-domain>/<name> (machine A) -> gateway egress -> HTTPS -> ingress -> real remote mailbox (machine B) -> reply -> terminal FSM both sides"
  prohibitions:
    - "No hand-written injector — the real shipping `famp send` must author the envelope."
    - "This is the final human gate before tagging v1.0.0; do not auto-approve."
---

<objective>
UAT-01 — the final human gate. Re-run the Gate A two-machine dogfood (2026-07-28) with the FIXED `famp send` and NO injector: `famp send --to agent:<peer-domain>/<name>` from machine A delivers into the real remote agent's mailbox on machine B, and the task FSM reaches a terminal state on both sides (observable via `famp inspect tasks`). This closes the last gap between "the wire is proven" and "v1.0.0 is tagged."

Purpose: prove the shipping client — not scaffolding — drives the full cross-host cycle live.
Output: a signed-off `11-HUMAN-UAT.md` dogfood record.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-CONTEXT.md
@.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md
@docs/GATEWAY-SETUP.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-03-SUMMARY.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-04-SUMMARY.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy fresh binaries + verify freshness before the dogfood</name>
  <files>(no source edit — rebuild/deploy: ~/.cargo/bin/famp, famp-gateway)</files>
  <read_first>
    - CLAUDE.md "Conventions" (`just install` — the deployed `~/.cargo/bin/famp` is what agent sessions read; `target/release/famp` is not the deploy target)
    - .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-RESEARCH.md "Runtime State Inventory" (binary-freshness before docs-verify/UAT; both `famp` and `famp-gateway` must be rebuilt)
  </read_first>
  <action>
    Run `just install-all` (the plan-03 recipe that installs BOTH `famp` and `famp-gateway`) so both binaries carry this phase's fix (client `build_envelope_value` mode-branched class + gateway plan-07 egress own-domain check + ready-line). Confirm binary freshness on BOTH hosts: each deployed binary's build post-dates the plan-03/04/07 edits (compare `~/.cargo/bin/famp` and `~/.cargo/bin/famp-gateway` mtime vs the last relevant commit, or run `famp -V`). Record the confirmed binary build/version AND its git commit + a hash of each binary on BOTH UAT machines in 11-HUMAN-UAT.md (review MEDIUM — the dogfood runs `famp-gateway` from PATH, so record hash/commit on both hosts to rule out a stale binary).
  </action>
  <verify>
    <automated>just ci</automated>
  </verify>
  <acceptance_criteria>
    - `just install-all` completes; both `~/.cargo/bin/famp` and `~/.cargo/bin/famp-gateway` mtimes are newer than the latest `crates/famp/src/cli/send/mod.rs` / `crates/famp-gateway/src/{egress,main}.rs` change on EACH host.
    - `just ci` green.
    - The confirmed binary build/version + git commit + binary hash on BOTH UAT machines is recorded in 11-HUMAN-UAT.md.
  </acceptance_criteria>
  <done>Fresh binaries deployed on both hosts via install-all; commit + hash recorded per host; full CI green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: live two-machine dogfood with the fixed `famp send` (no injector) — final v1.0.0 gate</name>
  <action>Human operator re-runs the Gate A two-machine dogfood using the real shipping `famp send` (no injector) per the how-to-verify steps below, and records the pass/fail verdict + evidence in 11-HUMAN-UAT.md. This is a blocking human gate — do not auto-approve.</action>
  <what-built>
    The fixed shipping `famp send` remote-addressing path (plans 01-05): domain-qualified `from`/`to`, typed unsigned `request` via sign-then-strip, split-addressing (leaf bus Target), single-source own-domain config, the corrected GATEWAY-SETUP.md, cross-platform fixtures, and the shipping-surface CI regression net. Everything needed to drive a cross-host delivery from the real client.
  </what-built>
  <how-to-verify>
    Re-run the Gate A two-machine dogfood with the REAL client (NO injector), following the corrected docs/GATEWAY-SETUP.md:
    1. On both machines (e.g. opus laptop ↔ zed `home-devbox`, or a fresh server-side agent): set own-domain (`--domain` / `FAMP_OWN_DOMAIN` / `$FAMP_HOME/own-domain`) — REQUIRED on both, since plan-07's egress check enforces `from.authority()==own-domain` when configured; generate certs with the CA:FALSE+serverAuth recipe, exchange peer keys out-of-band and pin BEFORE launching the gateway, back the REMOTE principal per §4 (A backs bob, B backs alice), and (macOS) pre-authorize the host firewall.
    2. From machine A, OPEN the task: `famp send --to agent:<peer-domain>/<name> --new-task "<msg>"` — NO injector, the shipping binary only (emits a typed `request`).
    3. Confirm on machine B: the message lands in the real remote agent's mailbox (`famp inbox` / `famp inspect messages`), and `famp inspect tasks` shows REQUESTED.
    4. From machine B, reply WITHIN the task to drive the FSM to terminal via the shipping client: `famp send --to agent:<A-domain>/<name> --task <task_id>` (emits `commit` → COMMITTED), then `famp send --to agent:<A-domain>/<name> --task <task_id> --terminal` (emits `deliver` + terminal_status → COMPLETED). Confirm the replies land back on machine A.
    5. Confirm the task FSM reaches a TERMINAL state on BOTH machines (`famp inspect tasks --id <task_id>` on each — REQUESTED → COMMITTED → COMPLETED, converging to the same terminal state).
    6. If any step fails, the enriched egress error chain (plan 01) should name the cause on the spot — capture it in 11-HUMAN-UAT.md and route back to the relevant plan.
    Record the outcome (pass/fail + evidence) in 11-HUMAN-UAT.md. A PASS here is the final gate before tagging v1.0.0.
  </how-to-verify>
  <resume-signal>Type "approved" (dogfood reached terminal FSM on both sides, no injector) or describe the failure + which finding/plan it maps to.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two real machines over a controlled network | the live end-to-end trust + delivery path, exercised by the real client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-16 | Spoofing/Repudiation | live cross-host delivery with real trust | high | mitigate | Human dogfood validates the full signed cycle end-to-end; blocking gate, no auto-approve |
| T-11-17 | Tampering | stale binary masking the fix | medium | mitigate | Binary-freshness check (Task 1) before the dogfood |

No new dependencies — no supply-chain (T-*-SC) row.
</threat_model>

<verification>
- `just ci` green (Task 1).
- Human-verified live dogfood: delivery into the real remote mailbox + terminal FSM on both sides, driven by the shipping `famp send` (no injector) — recorded in 11-HUMAN-UAT.md.
</verification>

<success_criteria>
`famp send --to agent:<peer-domain>/<name>` from machine A reaches a terminal task state on both machines with no hand-written injector — the final human gate before v1.0.0.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-HUMAN-UAT.md` (live dogfood record + verdict)
- Deployed fresh `~/.cargo/bin/famp` + `famp-gateway` binaries
</artifacts>

<output>
Create `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-06-SUMMARY.md` when done
</output>
