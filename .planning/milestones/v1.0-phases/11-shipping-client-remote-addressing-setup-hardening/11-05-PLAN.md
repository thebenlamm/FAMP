---
phase: 11-shipping-client-remote-addressing-setup-hardening
plan: 05
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - docs/GATEWAY-SETUP.md
  - crates/famp/tests/gateway_setup_doc_accuracy.rs
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "GATEWAY-SETUP.md §4 instructs backing the REMOTE principal (A backs bob, B backs alice), not the local one."
    - "§3 pins the sender's gateway key under the sender AGENT principal (agent:{domain}/{name}), not a /gateway-suffixed label."
    - "The guide states pin-before-launch / no-hot-reload, warns on the duplicate-pubkey keyring brick, and moves the gateway 'ready' line to AFTER keyring load."
    - "The guide replaces 'self-signed is fine' with the CA:FALSE+serverAuth cert recipe that verifies on macOS AND Linux, and documents macOS host-firewall (socketfilterfw) pre-auth."
    - "The guide documents the own-domain config surface (--domain / FAMP_OWN_DOMAIN / $FAMP_HOME/own-domain) and the `famp send --to agent:<domain>/<name>` remote-addressing usage."
    - "The doc-accuracy gate performs SEMANTIC checks (wiring direction, pin-label = agent principal, ordering, cert policy present), not only --help flag-grep."
  artifacts:
    - "corrected docs/GATEWAY-SETUP.md (all 8 Gate A findings)"
    - "extended crates/famp/tests/gateway_setup_doc_accuracy.rs (semantic assertions)"
  key_links:
    - "doc pin-label authority (§3) == own-domain (plan 02) == famp send `from` (plan 03) == ingress keyring lookup"
  prohibitions:
    - "Do NOT weaken the existing --help flag-grep assertions; extend, don't replace."
    - "Documented commands/flags must match the shipping CLI (no aspirational flags)."
---

<objective>
D-07 / DOC-05 — correct `docs/GATEWAY-SETUP.md` for all 8 Gate A dogfood findings (see `.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md`) and strengthen the accuracy gate beyond flag-grep so it can catch the semantic inversions the grep missed (wiring direction, pin-label direction, ordering, keyring bricking, cert policy). Also document the new own-domain config surface and the `famp send` remote-addressing usage this phase ships.

Purpose: make the two-machine guide correct and followable on both macOS and Linux, and give CI teeth against future semantic drift.
Output: a corrected guide + an extended doc-accuracy test.

Note: the gateway "ready" line CODE move (main.rs, print after keyring/transport/listener init) is implemented in Plan 07 (which owns `crates/famp-gateway/src/main.rs`). THIS plan corrects the DOC to describe ready-after-keyring and adds a doc-level ordering assertion — it does not edit main.rs (avoids a two-plan conflict on that file). The process-readiness BEHAVIOR test also lives in Plan 07.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-RESEARCH.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-PATTERNS.md
@.planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-02-SUMMARY.md
@.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: correct GATEWAY-SETUP.md for all 8 findings + document own-domain + remote send</name>
  <files>docs/GATEWAY-SETUP.md</files>
  <read_first>
    - docs/GATEWAY-SETUP.md (§1 Prerequisites L19, §2 Gateway identity L41, §3 Out-of-band key exchange L50, §4 Start each gateway L101, §5 Connect/verify L143 — the sections to correct)
    - .planning/phases/10-test-reactivation-setup-docs/10-HUMAN-UAT.md (the canonical list of all 8 findings + the cert recipe + the recommended §5 pre-flight discipline — this is the source of truth for the corrections)
    - .planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-RESEARCH.md "Pitfall 5" (keyring load-once / duplicate-pubkey brick / ready-line ordering) + the cert recipe
    - crates/famp/src/cli/own_domain.rs (plan 02 — the config surface to document: precedence + file location) and the fixed `famp send` remote usage (plan 03)
  </read_first>
  <action>
    Edit docs/GATEWAY-SETUP.md to fix all 8 Gate A findings, using 10-HUMAN-UAT.md as the authority: (1) §4 — instruct each host to back the REMOTE principal it proxies (machine A backs bob, machine B backs alice), reversing the current inverted wiring; (2) §3 — pin the sender's gateway key under the sender AGENT principal `agent:{domain}/{name}` (the value now single-sourced from own-domain, plan 02), removing the incorrect gateway-suffixed pin label; (3) state pin-before-launch and that the keyring is loaded ONCE at startup (no hot-reload); (4) warn that re-exporting under a corrected label leaves two lines with one pubkey and bricks the keyring (`duplicate pubkey`, load fails closed) — instruct stripping the stale line before re-pinning; (5) move the documented "ready" signal to AFTER keyring load (a ready line before keyring load is a false-success); (6) replace any "self-signed is fine" guidance with the CA:FALSE+serverAuth openssl recipe from RESEARCH (the one that verifies on both Apple SecTrust and webpki); (7) add a macOS host-firewall pre-authorization step (`socketfilterfw` allow inbound for `famp-gateway`); (8) add an own-domain configuration subsection (precedence `--domain` > `FAMP_OWN_DOMAIN` > `$FAMP_HOME/own-domain`) and a remote-send usage line showing `famp send --to agent:<peer-domain>/<name>`. Also add a brief KNOWN-LIMITATION note (review LOW): a remote `--to agent:hostb/bob` routes the local bus frame to the bare leaf `bob`, so if a LOCAL holder named `bob` also exists the frame could mis-route — document that leaf names should be unambiguous per host until fully-qualified local routing lands (v1.1). Keep the documented `famp peer export`/`import` invocations matching the shipping clap surface.
  </action>
  <verify>
    <automated>cargo test -p famp --test gateway_setup_doc_accuracy</automated>
  </verify>
  <acceptance_criteria>
    - The guide contains the CA:FALSE+serverAuth cert recipe (grep for `CA:FALSE` and `serverAuth` both present).
    - The guide documents own-domain precedence (grep for `FAMP_OWN_DOMAIN` and `own-domain`) and a `famp send --to agent:` remote example.
    - The guide contains a macOS firewall pre-auth step (grep for `socketfilterfw`).
    - The pin-label instruction references the sender agent principal, and the incorrect gateway-suffixed pin label no longer appears (verified by the gate in Task 2).
    - `cargo test -p famp --test gateway_setup_doc_accuracy` green.
  </acceptance_criteria>
  <done>All 8 findings corrected; own-domain + remote-send documented; existing CLI-flag accuracy preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend the doc-accuracy gate with semantic assertions</name>
  <files>crates/famp/tests/gateway_setup_doc_accuracy.rs</files>
  <read_first>
    - crates/famp/tests/gateway_setup_doc_accuracy.rs (current gate — `--help` flag-grep only: runs `famp peer export --help`, asserts stdout contains `--as`; `gateway_setup_doc_path()` helper resolves the doc)
    - docs/GATEWAY-SETUP.md (the corrected doc from Task 1 — the assertion target)
  </read_first>
  <behavior>
    - The gate parses GATEWAY-SETUP.md and asserts, positively: the cert recipe policy tokens (`CA:FALSE`, `serverAuth`) are present; the own-domain config tokens are present; the macOS firewall step is present; the pin-label instruction names an agent principal; the §4 wiring states A backs the remote (bob) and B backs the remote (alice); the guide states the ready signal appears AFTER keyring load.
    - The gate asserts, negatively: NO pin-label of the SHAPE `agent:<something>/gateway` appears (scoped regex `agent:[^\s]*/gateway`) — so it does NOT false-positive on legitimate filesystem paths like `~/.famp/gateway/identity.ed25519`.
    - The pre-existing `--help` flag-grep assertions still run and still pass.
  </behavior>
  <action>
    Extend crates/famp/tests/gateway_setup_doc_accuracy.rs (do not remove existing assertions). Read the doc via the existing `gateway_setup_doc_path()` helper and add semantic assertions covering the corrections: presence of the cert-policy tokens, the own-domain config tokens, the macOS firewall step, and the pin-under-agent-principal instruction. Add CONCRETE wiring assertions (review MEDIUM): the doc §4 says A backs the remote principal (bob) and B backs the remote principal (alice) — assert both directional statements are present, not just that "back" appears. Add an ORDERING assertion that the ready-signal instruction is documented as after keyring load (e.g. the "ready" mention appears after the keyring-pin/load section). For the finding-#2 negative guard, use a SCOPED regex `agent:[^\s]*/gateway` (build the pattern in-test from parts so the raw pin-label shape is not written verbatim into the doc via this file) rather than a broad `/gateway` substring search — this avoids colliding with legit `~/.famp/gateway/...` paths. Keep each assertion's failure message actionable ("update the guide or the code").
  </action>
  <verify>
    <automated>cargo test -p famp --test gateway_setup_doc_accuracy</automated>
  </verify>
  <acceptance_criteria>
    - The gate asserts presence of: `CA:FALSE`, `serverAuth`, `FAMP_OWN_DOMAIN`, `socketfilterfw`, an `agent:` pin-label instruction, the A-backs-bob + B-backs-alice directional wiring, and ready-after-keyring ordering.
    - The gate asserts ABSENCE of an `agent:[^\s]*/gateway`-shaped pin label (scoped regex — does NOT trip on `~/.famp/gateway/identity.ed25519`).
    - The pre-existing `--as` flag-grep assertion still runs and passes.
    - `cargo test -p famp --test gateway_setup_doc_accuracy` green; `just lint` clean.
  </acceptance_criteria>
  <done>Doc-accuracy gate now catches semantic inversion, directional wiring, ready ordering, and cert-policy drift (scoped pin-label guard); existing checks intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator following the guide -> live trust config | a wrong pin label or wrong wiring produces an UnpinnedKey self-DoS at dogfood time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Repudiation | inverted guide wiring/pin label | high | mitigate | All 8 findings corrected; semantic gate blocks regression of the pin-label inversion |
| T-11-14 | Spoofing | "self-signed is fine" cert guidance | high | mitigate | Guide ships the CA:FALSE+serverAuth recipe verified on both platforms |
| T-11-15 | Denial of Service | keyring duplicate-pubkey brick / pin-after-launch | medium | mitigate | Guide documents pin-before-launch, no-hot-reload, and the stale-line strip |

No new dependencies — no supply-chain (T-*-SC) row.
</threat_model>

<verification>
- `cargo test -p famp --test gateway_setup_doc_accuracy` green (extended semantic + existing flag-grep).
- `just ci` + `just lint` clean.
</verification>

<success_criteria>
GATEWAY-SETUP.md is correct for all 8 findings and documents own-domain + remote addressing; the accuracy gate now fails on semantic inversion/ordering/cert-policy drift, not just missing flags.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Corrected `docs/GATEWAY-SETUP.md` (8 findings + own-domain config + remote-send usage)
- Extended `crates/famp/tests/gateway_setup_doc_accuracy.rs` (semantic assertions + finding-#2 negative guard)
</artifacts>

<output>
Create `.planning/phases/11-shipping-client-remote-addressing-setup-hardening/11-05-SUMMARY.md` when done
</output>
