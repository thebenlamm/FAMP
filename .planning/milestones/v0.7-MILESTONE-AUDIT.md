---
milestone: v0.7
milestone_name: Personal Runtime
audited: 2026-04-14
status: passed
scores:
  requirements: 32/32
  phases: 4/4
  integration: wired (all cross-phase seams verified)
  flows: 2/2 happy-path + 6/6 adversarial rows
gaps:
  requirements: []
  integration: []
  flows: []
deferred_to_v0_8_plus:
  - "TRANS-05 .well-known Agent Card distribution (explicitly out of Phase 4 scope, documented in CONTEXT.md)"
  - "TRANS-08 cancellation-safe spawn-channel send path (explicitly out of Phase 4 scope)"
  - "Subprocess cross_machine_happy_path.rs is #[ignore]'d (authorized fallback); CONF-04 gate is owned by the same-process http_happy_path.rs test which exercises real rustls + two HttpTransport instances"
  - "Wider envelope classes (ENV-04 announce, ENV-05 describe, ENV-08 propose, ENV-11 delegate, ENV-12 supersede/close, ENV-13 full 11 causal relations)"
  - "FSM-01 ConversationFsm, FSM-06 terminal precedence, FSM-07 stateright model check"
  - "CONF-08..18 broader adversarial matrix (stale commit, replay, unknown-critical, round overflow, subcontracting, competing commits, cancellation race, drop-at-await, key rotation, Level 2/3 badge scripts)"
---

# Milestone v0.7 Personal Runtime — Audit Report

**Audited:** 2026-04-14
**Status:** passed — all four phases green, 32/32 requirements satisfied, finish-line E2E verified on the running repo, no openssl/native-tls in the dep graph.

## Scope

**Milestone goal** (ROADMAP.md): A single developer runs the same signed `request → commit → deliver → ack` cycle two ways — one binary via `MemoryTransport` and two processes via a minimal HTTP binding — with trust bootstrapped from a local TOFU keyring, and the three adversarial cases (unsigned / wrong-key / canonical divergence) fail closed on both transports.

**Phases in scope:**
- Phase 1: Minimal Signed Envelope (3 plans)
- Phase 2: Minimal Task Lifecycle (3 plans)
- Phase 3: MemoryTransport + TOFU Keyring + Same-Process Example (4 plans)
- Phase 4: Minimal HTTP Transport + Cross-Machine Example (5 plans)

## Phase Verification Aggregation

| Phase | VERIFICATION status | Score | Tests | Gaps | Human_needed |
|-------|--------------------|-------|-------|------|--------------|
| 01 Minimal Signed Envelope | passed | 5/5 | 73/73 | none | none |
| 02 Minimal Task Lifecycle | passed | 4/4 | 17/17 (p=famp-fsm) | none | none |
| 03 MemoryTransport + TOFU + Example | passed | 5/5 | 226/226 (then) | none | none |
| 04 HTTP Transport + Cross-Machine | passed | 5/5 | 247/247 (then) | none | none |

All four phase reports are `status: passed` with zero declared gaps and zero `human_needed` flags.

## Finish-Line E2E Verification (re-run on audit day)

| Gate | Command | Result |
|------|---------|--------|
| Full workspace test suite | `cargo nextest run --workspace` | **253 passed, 1 skipped** (the authorized `#[ignore]` subprocess cross_machine test) |
| `personal_two_agents` builds | `cargo build --example personal_two_agents` | builds clean (dev profile) |
| `cross_machine_two_agents` builds | `cargo build --example cross_machine_two_agents` | builds clean |
| CONF-04 happy path over real rustls | `cargo nextest run -p famp --test http_happy_path` | **1/1 PASS** (`http_happy_path_same_process`, 0.259s) |
| CONF-05/06/07 × {Memory, HTTP} = 6 rows | `cargo nextest run -p famp --test adversarial` | **6/6 PASS** (0.288s) — `memory_unsigned`, `memory_wrong_key`, `memory_canonical_divergence`, `http_unsigned`, `http_wrong_key`, `http_canonical_divergence` |
| EX-01 subprocess trace | (part of workspace run) `famp::example_happy_path::personal_two_agents_exits_zero_with_expected_trace` | PASS (11.9s) |
| No OpenSSL in dep tree | `cargo tree -i openssl --workspace` | `did not match any packages` (exit 101 → empty) — PASS |
| No native-tls in dep tree | `cargo tree -i native-tls --workspace` | `did not match any packages` — PASS |

**Finish line: achieved.** All four ROADMAP finish-line conditions are verifiable on the current `main` working tree.

## Requirements Coverage (v0.7 — 32 total)

| REQ-ID | Phase | Status | Evidence |
|--------|-------|--------|----------|
| ENV-01 | 1 | SATISFIED | `crates/famp-envelope/src/envelope.rs` type-state Envelope; vector_zero.rs passes |
| ENV-02 | 1 | SATISFIED | deny_unknown_fields on all bodies; adversarial unknown-field tests pass |
| ENV-03 | 1 | SATISFIED | INV-10 enforced via type-state; `compile_fail_unsigned.rs` marker; `adversarial::missing_signature_rejected` |
| ENV-06 | 1 | SATISFIED | `body/ack.rs`; ack dispositions roundtrip |
| ENV-07 | 1 | SATISFIED | `body/request.rs`; request roundtrip |
| ENV-09 (narrowed) | 1 | SATISFIED | `body/commit.rs` has no capability_snapshot; `commit_body_rejects_capability_snapshot` passes |
| ENV-10 | 1 | SATISFIED | `body/deliver.rs` with terminal_status; interim/terminal/failed tests |
| ENV-12 (cancel-only) | 1 | SATISFIED | `ControlAction = {Cancel}` single variant; supersede/close/revert_transfer/cancel_if_not_started rejected |
| ENV-14 | 1 | SATISFIED | `BodySchema::SCOPE` const per body |
| ENV-15 | 1 | SATISFIED | `roundtrip_signed.rs` — 7 tests all 5 classes |
| FSM-02 (narrowed) | 2 | SATISFIED | Exactly 5 variants, no REJECTED/EXPIRED representable |
| FSM-03 | 2 | SATISFIED | `consumer_stub.rs` under `#![deny(unreachable_patterns)]`, no catch-all |
| FSM-04 | 2 | SATISFIED | `step(input)` with table-driven transitions, typed `IllegalTransition` error |
| FSM-05 | 2 | SATISFIED | `TaskState: Copy`; zero lifetimes in public types |
| FSM-08 | 2 | SATISFIED | `proptest_matrix.rs`, 2048 cases, independent oracle |
| TRANS-01 | 3 | SATISFIED | `famp-transport::Transport` trait (native AFIT) |
| TRANS-02 | 3 | SATISFIED | `MemoryTransport` mpsc impl, 5 unit tests |
| KEY-01 | 3 | SATISFIED | `Keyring { map: HashMap<Principal, TrustedVerifyingKey> }` |
| KEY-02 | 3 | SATISFIED | Canonical fixture round-trips byte-identical (`rt1b_canonical_fixture_round_trips_byte_identical`) |
| KEY-03 | 3 | SATISFIED | `parse_peer_flag` with 3 peer-flag tests |
| EX-01 | 3 | SATISFIED | `personal_two_agents.rs` + subprocess trace test |
| CONF-03 | 3 | SATISFIED | Happy-path MemoryTransport via subprocess test |
| CONF-05 (memory row) | 3 | SATISFIED | `memory::memory_unsigned` PASS |
| CONF-06 (memory row) | 3 | SATISFIED | `memory::memory_wrong_key` PASS |
| CONF-07 (memory row) | 3 | SATISFIED | `memory::memory_canonical_divergence` PASS |
| TRANS-03 | 4 | SATISFIED | `famp-transport-http` axum server + reqwest client |
| TRANS-04 | 4 | SATISFIED | `POST /famp/v0.5.1/inbox/{principal}` route literal in `server.rs` |
| TRANS-06 | 4 | SATISFIED | `rustls_platform_verifier::Verifier::new_with_extra_roots` in `tls.rs`; dep-tree gate green |
| TRANS-07 | 4 | SATISFIED | `RequestBodyLimitLayer::new(1_048_576)` + `body_over_1mb_does_not_enter_handler` |
| TRANS-09 | 4 | SATISFIED | `FampSigVerifyLayer` outer-to-routing; 4 middleware_layering sentinel tests + 3 HTTP adversarial rows |
| EX-02 | 4 | SATISFIED | `cross_machine_two_agents.rs` binary present, no `todo!()`, builds clean; same-process `http_happy_path.rs` owns the wire gate |
| CONF-04 | 4 | SATISFIED | `http_happy_path_same_process` PASS over real rustls (2 × `HttpTransport` + `tls_server::serve_std_listener`) |
| CONF-05/06/07 (HTTP rows) | 4 | SATISFIED | `http::http_unsigned`, `http::http_wrong_key`, `http::http_canonical_divergence` all PASS with sentinel proof |

**Coverage:** 32 / 32 declared v0.7 requirements satisfied. No orphans. The explicit exclusions (TRANS-05, TRANS-08, ENV-09 federation form, ENV-12 wider form, FSM-02 wider form) are called out inline in at least one phase doc and are tracked as v0.8+ deferrals.

**Stale REQUIREMENTS.md checkboxes (documentation hygiene, non-blocking):** ENV-09, ENV-12, and every Phase 3/4 requirement (TRANS-01/02/03/04/06/07/09, KEY-01/02/03, EX-01/02, CONF-03/04/05/06/07) still show `[ ]` in `.planning/REQUIREMENTS.md` despite the code and tests satisfying them. Phase 1 VERIFICATION already flagged the ENV-09/ENV-12 lines; the same hygiene issue applies to the entire traceability table. Surfaced here so a single post-milestone doc sweep can flip them all.

## Cross-Phase Integration Wiring

All expected cross-phase seams verified via import grep and test execution.

| Seam | From → To | Status | Evidence |
|------|-----------|--------|----------|
| Envelope → HTTP middleware | `famp-envelope` → `famp-transport-http::middleware` | WIRED | `middleware.rs:18: use famp_envelope::{peek_sender, AnySignedEnvelope, EnvelopeDecodeError};` |
| Envelope → Runtime | `famp-envelope` → `famp/src/runtime/loop_fn.rs` | WIRED | `loop_fn.rs:23: use famp_envelope::{AnySignedEnvelope, ...};` |
| `peek_sender` lifted to envelope | Runtime wrapper delegates to `famp_envelope::peek_sender` | WIRED | `runtime/peek.rs:12: famp_envelope::peek_sender(bytes)`; `envelope/src/peek.rs:22` canonical impl + 3 unit tests |
| FSM → Runtime adapter | `famp-fsm` → `famp/src/runtime/{adapter,loop_fn}.rs` | WIRED | `loop_fn.rs:24: use famp_fsm::TaskFsm`; `adapter.rs:7: use famp_fsm::TaskTransitionInput` |
| `famp-transport::Transport` trait → `famp-transport-http` impl | trait → `impl Transport for HttpTransport` | WIRED | `transport.rs:20: use famp_transport::{Transport, TransportMessage};` |
| `famp-transport::MemoryTransport` → runtime + examples + adversarial | → `personal_two_agents.rs`, `adversarial/memory.rs`, `http_happy_path.rs` indirectly via cycle_driver | WIRED | `personal_two_agents.rs:36`, `adversarial/memory.rs:14` |
| Keyring → both transports | `famp-keyring::Keyring` → MemoryTransport runtime + HttpTransport middleware + server | WIRED | `server.rs:19`, `middleware.rs:19`, `runtime/loop_fn.rs:25`, `cycle_driver.rs:26` (shared driver used by both transports) |
| Two-phase decode pattern identical across transports | `peek_sender → keyring.get → canonicalize → AnySignedEnvelope::decode` | IDENTICAL | Both `runtime/loop_fn.rs` and `middleware.rs` grep-match the same 4 call sequence; adversarial matrix proves the two code paths return the same typed error variants for the same bytes |
| Generic `cycle_driver::drive_alice/drive_bob` over `T: Transport` | shared by `personal_two_agents.rs` and `http_happy_path.rs` | WIRED | `tests/common/cycle_driver.rs:27: use famp_transport::{Transport, TransportMessage};` consumed by both |

**Orphaned exports:** none. **Missing connections:** none.

## E2E Flow Status

| Flow | Steps | Status |
|------|-------|--------|
| EX-01 / CONF-03: `personal_two_agents` MemoryTransport cycle | sign request → MemoryTransport send → peek → keyring lookup → canonical check → AnySignedEnvelope::decode → recipient cross-check → FSM step → reply (commit/deliver/ack) → exit 0 with trace | **COMPLETE** — subprocess test `personal_two_agents_exits_zero_with_expected_trace` PASS |
| EX-02 / CONF-04: `cross_machine_two_agents` HTTPS cycle | same pipeline over rustls HTTPS between two `HttpTransport` instances | **COMPLETE** — gated by same-process `http_happy_path_same_process` (real rustls, two transports, two listeners, `tokio::join!(drive_alice, drive_bob)`); subprocess binary builds clean but subprocess test remains authorized `#[ignore]` (bootstrap chicken-and-egg) |
| CONF-05 × 2: unsigned rejected on both transports | Memory: `RuntimeError::Decode(MissingSignature)`; HTTP: handler never entered (`inbox_rx.try_recv() == Empty`) | **COMPLETE** — 2/2 rows PASS |
| CONF-06 × 2: wrong-key rejected on both transports | Memory: `RuntimeError::Decode(SignatureInvalid)`; HTTP: sentinel not reached | **COMPLETE** — 2/2 rows PASS |
| CONF-07 × 2: canonical divergence rejected on both transports | Memory: `RuntimeError::CanonicalDivergence` (pre-decode check distinct from CONF-06); HTTP: sentinel not reached; shared byte-identical fixture `tests/fixtures/conf-07-canonical-divergence.json` | **COMPLETE** — 2/2 rows PASS |

## Anti-Pattern Scan (non-test source under `crates/**/src/**/*.rs`)

| Pattern | Hits | Assessment |
|---------|------|------------|
| `todo!()` (macro invocation) | 0 real hits | The single textual match (`tls_server.rs:6`) is inside a `//!` doc comment that documents closure of the B-3 checker ("no more `todo!()` for the serve helper") |
| `unimplemented!()` | 3 hits | All three are in rustdoc fenced `/// ```` examples inside `crates/famp-envelope/src/envelope.rs` (lines 80, 81, 91) — example-placeholder syntax, not runtime code |
| `FIXME` | 0 | clean |
| `TODO` | 0 | clean |
| `panic!(` | 4 hits | All four are inside `#[cfg(test)]` modules (`memory.rs` test helper, `tls.rs` test match arms, `transport.rs` test match arm) — `other => panic!("expected X, got {other:?}")` test-assertion idiom |

**No real stubs, no real panics, no deferred work left behind in v0.7 non-test source.**

## Deferred / Tech Debt Aggregation

From phase VERIFICATION docs and plan context:

- **TRANS-05** (.well-known Agent Card distribution) — out of Phase 4 scope, documented in `04-CONTEXT.md §Phase Boundary`, pointer to v0.8 Identity & Cards.
- **TRANS-08** (cancellation-safe spawn-channel send path) — out of Phase 4 scope, pointer to v0.9 Causality & Replay Defense.
- **Subprocess cross_machine test** — `crates/famp/tests/cross_machine_happy_path.rs` marked `#[ignore]` on line 59 with documented bootstrap chicken-and-egg rationale; CONF-04 gate instead owned by `http_happy_path.rs` same-process test that still exercises real rustls between two `HttpTransport` instances. Plan 04-04 authorizes this fallback; `--wait-peer-file` style solution deferred.
- **Stale REQUIREMENTS.md checkboxes** — doc hygiene, non-blocking (see Requirements Coverage section).
- **Phase 1 Observation #1** (stale ENV-09/ENV-12 boxes) rolled up into the same doc-sweep item.

No open tech debt that blocks milestone closure. The `04-REVIEW.md` / `04-REVIEW-FIX.md` cycle from Phase 4 closed with 5 fix commits on `main` (d596868, 152dd05, 505f3c2, 8640a43 et al.) all merged.

## Verdict

v0.7 Personal Runtime is **passed**. Every declared success criterion across all four phases is verifiable on the current tree: 253/253 workspace tests green (1 authorized ignore), both examples build, the CONF-04 happy-path over real rustls passes, the 3 × 2 adversarial matrix passes with distinct typed errors and handler-not-entered proofs on HTTP, the no-openssl / no-native-tls dep-tree gates are clean, cross-phase wiring is fully verified via import grep and the two-phase decode pattern is byte-identical between `MemoryTransport` runtime glue and `HttpTransport` middleware, and 32/32 requirements are satisfied. The only hygiene task is a doc sweep to flip stale `[ ]` checkboxes in `.planning/REQUIREMENTS.md` — non-blocking and explicitly called out in the Phase 1 verification report. Milestone is ready to close; v0.8 Federation Profile may proceed.

---

_Audited: 2026-04-14_
_Auditor: Claude (gsd milestone-audit integration checker)_
