---
phase: 02-minimal-task-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/famp-core/src/lib.rs
  - crates/famp-core/src/class.rs
  - crates/famp-core/src/terminal_status.rs
  - crates/famp-envelope/src/class.rs
  - crates/famp-envelope/src/body/deliver.rs
  - crates/famp-envelope/src/lib.rs
  - crates/famp-envelope/Cargo.toml
autonomous: true
requirements: []
must_haves:
  truths:
    - "famp-core exports MessageClass as its canonical home"
    - "famp-core exports TerminalStatus as its canonical home"
    - "famp-envelope re-exports both from famp-core (backward compatible)"
    - "workspace builds and all existing tests remain green"
  artifacts:
    - path: "crates/famp-core/src/class.rs"
      provides: "MessageClass enum, 5 shipped variants"
    - path: "crates/famp-core/src/terminal_status.rs"
      provides: "TerminalStatus enum, 3 variants (Completed, Failed, Cancelled)"
  key_links:
    - from: "crates/famp-envelope/src/class.rs"
      to: "famp_core::MessageClass"
      via: "pub use"
      pattern: "pub use famp_core::MessageClass"
    - from: "crates/famp-envelope/src/body/deliver.rs"
      to: "famp_core::TerminalStatus"
      via: "pub use"
      pattern: "pub use famp_core::TerminalStatus"
---

<objective>
Lift `MessageClass` and `TerminalStatus` from `famp-envelope` into `famp-core` so that `famp-fsm` (Plan 02) can consume them without depending on `famp-envelope` (locked decision D-D1 + D-B5 research resolution Option A).

Purpose: Resolve the crate-layering blocker. `famp-fsm` must not import `famp-envelope`; both types it needs (`MessageClass`, `TerminalStatus`) currently live there. This plan moves them to `famp-core` with a backward-compatible re-export from `famp-envelope`.

Output: Two new modules in `famp-core` (`class.rs`, `terminal_status.rs`), updated re-exports in `famp-envelope`, zero regressions in existing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-minimal-task-lifecycle/02-CONTEXT.md
@.planning/phases/02-minimal-task-lifecycle/02-RESEARCH.md
@crates/famp-core/src/lib.rs
@crates/famp-envelope/src/class.rs
@crates/famp-envelope/src/body/deliver.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move MessageClass from famp-envelope to famp-core</name>
  <files>crates/famp-core/src/class.rs, crates/famp-core/src/lib.rs, crates/famp-envelope/src/class.rs, crates/famp-envelope/src/lib.rs</files>
  <read_first>
    - crates/famp-envelope/src/class.rs (current MessageClass definition, serde attrs, derives — copy VERBATIM)
    - crates/famp-core/src/lib.rs (existing module list + pub use structure, mirror pattern)
    - crates/famp-envelope/src/lib.rs (current re-export of MessageClass)
    - .planning/phases/02-minimal-task-lifecycle/02-RESEARCH.md (MessageClass Placement Resolution section)
  </read_first>
  <action>
    1. Create `crates/famp-core/src/class.rs` containing the EXACT `MessageClass` enum definition currently in `crates/famp-envelope/src/class.rs` — same variants (`Request`, `Commit`, `Deliver`, `Ack`, `Control`), same derives, same `#[serde(rename_all = ...)]` attribute, same doc comments. Copy all associated `impl` blocks (e.g., `Display`, `as_str`) if present.
    2. Add `pub mod class;` to `crates/famp-core/src/lib.rs` and `pub use class::MessageClass;` alongside other pub uses (mirror the existing pattern used for `error::ProtocolErrorKind`).
    3. In `crates/famp-envelope/src/class.rs`, delete the local enum definition and replace with: `pub use famp_core::MessageClass;` plus any envelope-specific helper functions that are NOT the enum itself (keep them, re-import `MessageClass`).
    4. Verify `crates/famp-envelope/src/lib.rs` still exposes `pub use class::MessageClass;` (or equivalent) so downstream re-exports are unchanged.
    5. Do NOT change `famp-envelope`'s Cargo.toml — it already depends on `famp-core`.
    6. Run `cargo build -p famp-core -p famp-envelope` and fix any compile errors (likely imports that referenced the old location via crate-internal paths like `crate::class::MessageClass` — change to `famp_core::MessageClass` where the file is inside famp-envelope but outside class.rs).
  </action>
  <verify>
    <automated>cargo nextest run -p famp-core -p famp-envelope</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub enum MessageClass" crates/famp-core/src/class.rs` returns 1 match
    - `grep -n "pub enum MessageClass" crates/famp-envelope/src/class.rs` returns 0 matches
    - `grep -n "pub use famp_core::MessageClass" crates/famp-envelope/src/class.rs` returns 1 match
    - `grep -n "pub use class::MessageClass" crates/famp-core/src/lib.rs` returns 1 match
    - `cargo build -p famp-core -p famp-envelope` exits 0
    - `cargo nextest run -p famp-envelope` exits 0 with all pre-existing tests green (no new failures from the move)
  </acceptance_criteria>
  <done>`MessageClass` is defined in `famp-core::class` and re-exported from `famp-envelope::class`; all existing famp-envelope tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move TerminalStatus from famp-envelope to famp-core</name>
  <files>crates/famp-core/src/terminal_status.rs, crates/famp-core/src/lib.rs, crates/famp-envelope/src/body/deliver.rs</files>
  <read_first>
    - crates/famp-envelope/src/body/deliver.rs (current TerminalStatus definition — copy VERBATIM including serde attrs)
    - crates/famp-core/src/lib.rs (after Task 1 edits — use same pub-use pattern)
    - .planning/phases/02-minimal-task-lifecycle/02-RESEARCH.md (TerminalStatus Placement Resolution section, Option A)
  </read_first>
  <action>
    1. Create `crates/famp-core/src/terminal_status.rs` containing the EXACT `TerminalStatus` enum currently in `crates/famp-envelope/src/body/deliver.rs` — 3 variants (`Completed`, `Failed`, `Cancelled`), same derives (`Debug, Clone, Copy, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize`), same `#[serde(rename_all = "snake_case")]`, same doc comments.
    2. Add `pub mod terminal_status;` and `pub use terminal_status::TerminalStatus;` to `crates/famp-core/src/lib.rs`.
    3. In `crates/famp-envelope/src/body/deliver.rs`, delete the local `TerminalStatus` enum and replace with `pub use famp_core::TerminalStatus;` at the top of the file. Keep the `DeliverBody` struct and all other contents intact — `DeliverBody`'s field typed `TerminalStatus` now resolves through the re-export.
    4. Search for any other internal references (`grep -rn "TerminalStatus" crates/famp-envelope/src/`) and fix broken paths (e.g., if another module used `crate::body::deliver::TerminalStatus`, it still works via the re-export, but `super::TerminalStatus` may need updating).
    5. Run `cargo build -p famp-core -p famp-envelope` and fix errors.
  </action>
  <verify>
    <automated>cargo nextest run -p famp-core -p famp-envelope</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub enum TerminalStatus" crates/famp-core/src/terminal_status.rs` returns 1 match with exactly 3 variants (`Completed`, `Failed`, `Cancelled`)
    - `grep -c "pub enum TerminalStatus" crates/famp-envelope/src/body/deliver.rs` returns 0
    - `grep -n "pub use famp_core::TerminalStatus" crates/famp-envelope/src/body/deliver.rs` returns 1 match
    - `grep -n "pub use terminal_status::TerminalStatus" crates/famp-core/src/lib.rs` returns 1 match
    - `cargo nextest run -p famp-envelope` exits 0 with 0 new failures (all pre-existing deliver body and round-trip tests still green)
    - `just test` (full workspace) exits 0
  </acceptance_criteria>
  <done>`TerminalStatus` lives in `famp-core::terminal_status`, re-exported from `famp-envelope::body::deliver`; every existing envelope test still passes unchanged.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` exits 0
- `just test` exits 0 (full workspace suite green, no regressions)
- `grep -rn "pub enum MessageClass\|pub enum TerminalStatus" crates/famp-envelope/src/` returns 0 matches
- `grep -rn "pub enum MessageClass\|pub enum TerminalStatus" crates/famp-core/src/` returns 2 matches (one each)
</verification>

<success_criteria>
Both `MessageClass` and `TerminalStatus` are canonically defined in `famp-core` and re-exported from their former homes in `famp-envelope`. Zero behavior change for existing code; `famp-fsm` (Plan 02) can now import both from `famp-core` without touching `famp-envelope`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-minimal-task-lifecycle/02-01-SUMMARY.md`
</output>
