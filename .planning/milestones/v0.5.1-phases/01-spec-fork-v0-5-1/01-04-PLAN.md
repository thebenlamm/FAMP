---
phase: 01-spec-fork-v0-5-1
plan: 04
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - FAMP-v0.5.1-spec.md
autonomous: true
requirements: [SPEC-09, SPEC-10, SPEC-11, SPEC-12, SPEC-13, SPEC-14, SPEC-15, SPEC-16]
must_haves:
  truths:
    - "All 8 state-machine holes have explicit written resolutions in dedicated sub-sections"
    - "Ack-disposition vs terminal-state-crystallization are cleanly separated (§9.6a)"
    - "§7.3 'no body inspection' claim is retracted; FSM-observable whitelist is published (§7.3a)"
    - "Transfer-timeout race has a δ clock-skew guard band using transferring agent's clock (§12.3a)"
    - "EXPIRED vs deliver race uses same δ guard band (§9.5a)"
    - "Conditional lapse wins over delivery-wins rule (§9.6b)"
    - "COMMITTED_PENDING_RESOLUTION internal state + lex-UUIDv7 tiebreak (§11.5a)"
    - "Supersession does not reset negotiation round counter (§10.3a)"
    - "Capability snapshot is commit-time bound to committing card's card_version (§11.2a)"
  artifacts:
    - path: "FAMP-v0.5.1-spec.md"
      provides: "§9.6a, §7.3a, §12.3a, §9.5a, §9.6b, §11.5a, §10.3a, §11.2a populated"
      contains: "COMMITTED_PENDING_RESOLUTION, conditional lapse, transfer timeout tiebreak, FSM inspects, supersession, commit-time"
  key_links:
    - from: "§11.2a capability snapshot"
      to: "§6.3 card versioning"
      via: "commit-time binding references card_version from §6.3"
      pattern: "card_version"
    - from: "§12.3a transfer-timeout tiebreak"
      to: "§13.1 clock skew"
      via: "δ guard band references §13.1 clock-skew tolerance"
      pattern: "δ"
---

<objective>
Wave 2 state-machine holes: rewrite all 8 state-machine hole resolutions (SPEC-09 through SPEC-16) into dedicated lettered sub-sections. Every resolution is copied verbatim from RESEARCH §3 — the planner already pressure-tested and finalized each one. This plan depends on Plan 02 (signing semantics, clock-skew δ) and Plan 03 (card_version) already being in place.

Purpose: Phase 5 (`famp-fsm`) consumes these sections to build compile-time-safe state machines. Each resolution must be precise enough that an exhaustive Rust `match` compiles without guessing.
Output: Eight rewritten sub-sections + 8 changelog entries.

NOTE — D-19.1: CONTEXT.md D-19.1 (ratified 2026-04-13) locks the δ = 60-second clock-skew guard band against the transferring agent's clock. D-19.1 is a user-ratified decision, not planner discretion. Task 2 below implements it verbatim.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md
@.planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md
@.planning/phases/01-spec-fork-v0-5-1/01-VALIDATION.md
@FAMP-v0.5-spec.md
@docs/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §9.6a + §7.3a + §9.6b + §10.3a — Terminal precedence, FSM whitelist, conditional lapse, supersession counting (SPEC-09, SPEC-10, SPEC-13, SPEC-15)</name>
  <read_first>
    - FAMP-v0.5-spec.md §9.6 (terminal precedence), §7.3 ("no body inspection"), §11.4 (conditional commitments), §10.3 (negotiation rounds)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-17, D-18, D-21, D-23
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §3 SPEC-09 (lines 193-199), SPEC-10 (lines 201-207), SPEC-13 (lines 227-233), SPEC-15 (lines 245-251)
    - docs/PITFALLS.md (the §9.6, §7.3, §11.4, §10.3 reviewer findings cited in each resolution)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Populate four sub-sections. For each, use the RESEARCH §3 "Final resolution text" VERBATIM — do not paraphrase, do not summarize, do not re-derive.

    **§9.6a Terminal precedence (SPEC-09, D-17):**
    Paste verbatim from RESEARCH §3 SPEC-09 "Final resolution text" (lines 199 approximately): the section that begins "A terminal state crystallizes when the FSM validly processes one of: (a) a `deliver` message carrying envelope-level `terminal_status ∈ {completed, failed}`; ...". Keep every word including the final sentence about `ack` with disposition `refused` or `stale` not reversing crystallization. Prefix: "**§9.6a Terminal precedence.**"

    **§7.3a FSM-observable field whitelist (SPEC-10, D-18):**
    Paste verbatim from RESEARCH §3 SPEC-10 "Final resolution text" (line 207 approximately): the block beginning "**FSM-observable field whitelist.** The conversation and task state machines are driven by a closed set of fields: (envelope) `class`, `relation`, `terminal_status`; (body) `interim`, `scope_subset`, `target`. ...". Include the "no other fields participate" sentence and the extensions prohibition. Include the retraction sentence: "The v0.5 claim that 'no body inspection is required' is retracted; the whitelist is the normative replacement." Ensure the literal phrase "FSM inspects" appears (or the alternate anchor `envelope-level.*whitelist` — both satisfy SPEC-10 validation regex). Recommend using "The FSM inspects exactly these fields" as the anchor sentence.

    **§9.6b Conditional-lapse precedence (SPEC-13, D-21):**
    Paste verbatim from RESEARCH §3 SPEC-13 "Final resolution text" (line 233 approximately): the block beginning "**Conditional-lapse precedence.** A `control` message with relation `cancels` and disposition `condition_failed` (Section 11.4) sent by the committing agent takes precedence over a concurrent `deliver` with terminal status. ...". Include the orphaned-ack fallback and the safety-property rationale. Place this section immediately after §9.6a (they are adjacent precedence rules).

    **§10.3a Supersession does not reset round counting (SPEC-15, D-23):**
    Paste verbatim from RESEARCH §3 SPEC-15 "Final resolution text" (line 251 approximately): "**Rule 4a: Supersession does not reset round counting.** The negotiation round counter for a task is `count(messages with relation proposes_against in the task's conversation graph, including messages that have been subsequently superseded)`. ...". Ensure the literal phrase "supersession" appears within 30 chars of "round" (SPEC-15 validation anchor).

    **Changelog appends** (4 entries — use EXACT Δ numbers to preserve sequential integrity):
    - `v0.5.1-Δ16 — §9.6a — PITFALLS §9.6 terminal precedence hole / CONTEXT D-17 — Ack-disposition decoupled from terminal-state crystallization; crystallization only via deliver-with-terminal-status, control:cancels, or transfer-timeout reversion.`
    - `v0.5.1-Δ17 — §7.3a — PITFALLS §7.3 "no body inspection" contradiction / CONTEXT D-18 — Retract claim; publish FSM-observable field whitelist {class, relation, terminal_status, interim, scope_subset, target}; extensions prohibited from reusing these names.`
    - `v0.5.1-Δ20 — §9.6b — PITFALLS conditional-lapse vs delivery-wins conflict / CONTEXT D-21 — Committer-side control:condition_failed overrides delivery-wins default; late deliver gets ack:orphaned.`
    - `v0.5.1-Δ22 — §10.3a — PITFALLS supersession round-limit circumvention / CONTEXT D-23 — Round counter includes superseded messages; supersession does NOT reset counter.`
  </action>
  <verify>
    <automated>rg -q 'ack.disposition.{0,50}terminal' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'FSM.{0,20}inspects|envelope-level.{0,20}whitelist' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'conditional.{0,10}lapse' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'supersession.{0,30}round' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ16' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ17' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ20' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ22' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'ack.disposition.{0,50}terminal' FAMP-v0.5.1-spec.md` passes (SPEC-09 anchor)
    - `rg -q 'FSM.{0,20}inspects|envelope-level.{0,20}whitelist' FAMP-v0.5.1-spec.md` passes (SPEC-10 anchor)
    - `rg -q 'conditional.{0,10}lapse' FAMP-v0.5.1-spec.md` passes (SPEC-13 anchor)
    - `rg -q 'supersession.{0,30}round' FAMP-v0.5.1-spec.md` passes (SPEC-15 anchor)
    - `rg -q 'condition_failed' FAMP-v0.5.1-spec.md` passes (SPEC-13 semantic content)
    - Changelog entries Δ16, Δ17, Δ20, Δ22 all present
    - `just spec-lint 2>&1 | rg -c 'PASS'` increases by 4 (SPEC-09, SPEC-10, SPEC-13, SPEC-15)
  </acceptance_criteria>
  <done>Four sub-sections are populated verbatim from RESEARCH §3 resolutions; four changelog entries present.</done>
</task>

<task type="auto">
  <name>Task 2: §9.5a + §12.3a — EXPIRED-vs-deliver and transfer-timeout tiebreaks with δ guard band (SPEC-11, SPEC-12)</name>
  <read_first>
    - FAMP-v0.5-spec.md §9.5 (EXPIRED transition), §12.3 (transfer delegation deadline)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-19, D-19.1 (LOCKED 2026-04-13), D-20
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §3 SPEC-11 (lines 209-217, the ADJUST verdict) and SPEC-12 (lines 219-225), and §8 gap 1 (clock-skew guard band decision)
    - FAMP-v0.5.1-spec.md §13.1 (already populated in Plan 02 — provides the δ = 60s default)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Populate two δ-guard-band tiebreak sub-sections. **This task implements CONTEXT.md D-19.1 (ratified 2026-04-13), which locks the δ = 60-second clock-skew guard band against the transferring agent's clock. D-19.1 is a user-ratified decision; no planner discretion is involved.**

    **§9.5a EXPIRED vs deliver tiebreak (SPEC-12, D-20):**
    Paste verbatim from RESEARCH §3 SPEC-12 "Final resolution text" (line 225 approximately): "**EXPIRED vs deliver tiebreak.** Let `ts_expire` be the task's computed EXPIRED deadline. Let `δ` be the federation clock skew tolerance. A `deliver` message is accepted and crystallizes its `terminal_status` iff `deliver.ts ≤ ts_expire − δ`. A `deliver` with `deliver.ts > ts_expire − δ` is rejected with `stale:expired` and the task transitions to EXPIRED. This rule applies identically to interim deliveries that happen to race expiration: they are accepted if on-time, rejected if off-time."
    Add a forward link: "The default value of δ is defined in §13.1 (±60 seconds RECOMMENDED)."

    **§12.3a Transfer-timeout tiebreak (SPEC-11, D-19.1 LOCKED):**
    Paste verbatim from RESEARCH §3 SPEC-11 "Final resolution text" (line 217 approximately): "**Transfer-timeout tiebreak.** Let `ts_transfer` be the `ts` of the `delegate` message with `form: transfer`. Let `ts_deadline = ts_transfer + transfer_deadline` (default 5 minutes). Let `δ` be the federation clock skew tolerance (default 60 seconds). A `commit` from the delegate is considered on-time iff `delegate_commit.ts ≤ ts_deadline − δ`. On-time commits crystallize ownership-transfer and close the original commitment. Commits with `ts > ts_deadline − δ` are rejected with `conflict:transfer_timeout`; the transferring agent's auto-reversion wins and the original commitment reactivates (Section 12.3 item 5). The `δ` guard band ensures that no commit whose on-time status depends on clock-skew interpretation is accepted."

    Add an explicit note below §12.3a: "**Note on D-19.1.** CONTEXT.md D-19 was ratified with an addendum (D-19.1, 2026-04-13) that specifies the δ = 60-second guard band against the transferring agent's clock (it is the authority on its own timeout timer), sacrificing a 60-second window of legitimate-but-ambiguous commits for deterministic tiebreak. A delegate that self-checks and finds itself inside the guard band MUST send a `control:cancels` (target: `transfer_commit_race`) rather than a `commit`. δ matches the clock-skew tolerance from §13.1 for consistency."

    The `target: transfer_commit_race` enum value introduced here will be referenced by Plan 05's `control` body schema (SPEC-17). Add a forward reference: "See §8a (control body schema) for the `target` enumeration."

    **Changelog appends:**
    - `v0.5.1-Δ18 — §12.3a — PITFALLS transfer-timeout race / CONTEXT D-19 + D-19.1 (LOCKED 2026-04-13) — Tiebreak rule uses transferring agent's clock with δ=60s guard band from §13.1; on-time iff delegate_commit.ts ≤ ts_deadline − δ; loser gets conflict:transfer_timeout; new control target value transfer_commit_race introduced.`
    - `v0.5.1-Δ19 — §9.5a — PITFALLS EXPIRED vs deliver race / CONTEXT D-20 — Same δ guard band applied: deliver accepted iff ts ≤ ts_expire − δ; else stale:expired.`
  </action>
  <verify>
    <automated>rg -q 'EXPIRED.{0,20}deliver' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'transfer.{0,10}timeout.{0,20}tiebreak' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'δ' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'transfer_commit_race' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ18' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ19' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'EXPIRED.{0,20}deliver' FAMP-v0.5.1-spec.md` passes (SPEC-12 anchor)
    - `rg -q 'transfer.{0,10}timeout.{0,20}tiebreak' FAMP-v0.5.1-spec.md` passes (SPEC-11 anchor)
    - `rg -q 'δ' FAMP-v0.5.1-spec.md` passes (guard band symbol)
    - `rg -q 'ts_deadline' FAMP-v0.5.1-spec.md` passes (formula present)
    - `rg -q 'transfer_commit_race' FAMP-v0.5.1-spec.md` passes (new enum value for Plan 05 to consume)
    - `rg -q 'v0\.5\.1-Δ18'` AND `rg -q 'v0\.5\.1-Δ19'` both pass
    - The D-19.1 note is present: `rg -q 'D-19\.1' FAMP-v0.5.1-spec.md` passes
  </acceptance_criteria>
  <done>Two δ-guard-band tiebreak sub-sections present; D-19.1 (LOCKED) implemented verbatim; new `transfer_commit_race` target value introduced for Plan 05 to wire into the control body schema.</done>
</task>

<task type="auto">
  <name>Task 3: §11.5a + §11.2a — Competing-instance commits and capability snapshot (SPEC-14, SPEC-16)</name>
  <read_first>
    - FAMP-v0.5-spec.md §11.5 (competing instances), §11.2 (capability snapshot), §6.3 (card versioning — already populated by Plan 03)
    - .planning/phases/01-spec-fork-v0-5-1/01-CONTEXT.md D-22, D-24
    - .planning/phases/01-spec-fork-v0-5-1/01-RESEARCH.md §3 SPEC-14 (lines 235-243) and SPEC-16 (lines 253-259)
  </read_first>
  <files>FAMP-v0.5.1-spec.md</files>
  <action>
    Populate two sub-sections.

    **§11.5a Competing-instance resolution (SPEC-14, D-22):**
    Paste verbatim from RESEARCH §3 SPEC-14 "Final resolution text" (line 243 approximately): "**Competing-instance resolution.** When two instances of the same principal issue `commit` messages for the same task within the same freshness window and neither explicitly supersedes the other, the task enters the internal state `COMMITTED_PENDING_RESOLUTION`. The FSM MUST deterministically resolve this state within one transition by selecting the commit with the **lexicographically smaller envelope `id`** (which, for UUIDv7, corresponds to the earlier-created message). The winning commit crystallizes the task into COMMITTED. The losing commit is rejected with `conflict:competing_instance`; the losing instance MUST be notified via `ack` with disposition `refused`. If two commits have identical `id` values (UUIDv7 collision), both are rejected as `conflict:competing_instance` and the task remains REQUESTED. The `COMMITTED_PENDING_RESOLUTION` state is internal; it MUST NOT appear in public protocol messages or provenance records. INV-5 holds because the public observable state is always one of the five terminal states plus REQUESTED/COMMITTED."

    Add an explicit "internal state" line the Phase 5 executor can grep for: "**Internal state note:** `COMMITTED_PENDING_RESOLUTION` is an internal FSM state. It MUST NOT appear in serialized provenance records, in the FSM's public `state()` accessor (if any), or in any protocol message. Implementations SHOULD use a separate enum for internal vs public states to make this a compile-time property."

    **§11.2a Capability snapshot binding — card version clarification (SPEC-16, D-24):**
    Paste verbatim from RESEARCH §3 SPEC-16 "Final resolution text" (line 259 approximately): "**Capability snapshot binding — card version clarification.** The capability snapshot is captured at the moment the committing agent sends the `commit` message, not at the moment the underlying proposal was issued. The snapshot is bound to the committing card's `card_version` at commit time. If the committing agent's card rotates after commit, Section 6.3's continuity rule applies: the commit remains valid provided the new card's `min_compatible_version ≤ card_version_at_commit`. If the counter-party's card rotated between proposal and commit, the committing agent MAY choose to re-validate the counter-party's latest card before committing, but is not required to; a commit sent after a counter-party rotation is valid against the counter-party's card version current at commit receipt time, subject to the same `min_compatible_version` rule."

    Include the literal phrase `commit-time` (SPEC-16 validation regex is `capability.{0,20}snapshot.{0,20}commit-time`) — ensure it appears within 20 characters of "snapshot". Recommended opening sentence adjustment: "The capability snapshot is taken at **commit-time** and bound to the committing card's `card_version`." then paste the full RESEARCH text after.

    Cross-link: "See §6.3 for card rotation semantics and the `min_compatible_version` field."

    **Changelog appends:**
    - `v0.5.1-Δ21 — §11.5a — PITFALLS INV-5 hole, competing-instance commits / CONTEXT D-22 — Introduce COMMITTED_PENDING_RESOLUTION internal state; lex-smaller UUIDv7 id wins; loser gets conflict:competing_instance; internal state MUST NOT leak to wire.`
    - `v0.5.1-Δ23 — §11.2a — PITFALLS capability snapshot / card version drift / CONTEXT D-24 — Snapshot bound at commit-time to committing card's card_version; §6.3 continuity rule applies for rotations.`
  </action>
  <verify>
    <automated>rg -q 'COMMITTED_PENDING_RESOLUTION' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'capability.{0,20}snapshot.{0,20}commit-time' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ21' FAMP-v0.5.1-spec.md &amp;&amp; rg -q 'v0\.5\.1-Δ23' FAMP-v0.5.1-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q 'COMMITTED_PENDING_RESOLUTION' FAMP-v0.5.1-spec.md` passes (SPEC-14 anchor)
    - `rg -q 'capability.{0,20}snapshot.{0,20}commit-time' FAMP-v0.5.1-spec.md` passes (SPEC-16 anchor)
    - `rg -q 'lexicographically smaller' FAMP-v0.5.1-spec.md` passes (D-22 tiebreak rule)
    - `rg -q 'internal FSM state' FAMP-v0.5.1-spec.md` OR `rg -q 'MUST NOT appear' FAMP-v0.5.1-spec.md` passes (internal-state note)
    - Changelog entries Δ21, Δ23 present
    - After this task, `just spec-lint` should pass all 8 SPEC-09..SPEC-16 anchors
  </acceptance_criteria>
  <done>§11.5a and §11.2a populated; internal-state note present; all 8 state-machine holes now have normative resolutions in the spec.</done>
</task>

</tasks>

<verification>
After this plan: SPEC-09, SPEC-10, SPEC-11, SPEC-12, SPEC-13, SPEC-14, SPEC-15, SPEC-16 anchors all pass `just spec-lint`. Eight changelog entries (Δ16, Δ17, Δ18, Δ19, Δ20, Δ21, Δ22, Δ23) present.
</verification>

<success_criteria>
- All 8 SPEC-09..16 grep anchors pass
- 8 new changelog entries present
- `rg -q 'transfer_commit_race' FAMP-v0.5.1-spec.md` passes (feeds Plan 05 control schema)
- D-19.1 (LOCKED) documented inline
</success_criteria>

<output>
Create `.planning/phases/01-spec-fork-v0-5-1/01-04-SUMMARY.md` listing each sub-section populated, citing D-19.1 (LOCKED 2026-04-13), and flagging the `transfer_commit_race` enum value as a cross-task contract for Plan 05.
</output>
