---
quick_id: 260425-pc7
description: "Add scope.more_coming flag to new_task envelopes (T2.1)"
status: in-progress
created: 2026-04-25
mode: quick
---

# Quick 260425-pc7: `scope.more_coming` flag on new_task envelopes (T2.1)

## Goal

Add a `scope.more_coming: true|false` JSON convention on FAMP `request`
envelopes so a sender can signal "I'm not done briefing — wait for follow-up
`deliver`s before treating this task as ready to commit." Mirrors the
existing `body.interim` shape on `deliver` envelopes.

Addresses **Gap G4 (orchestrator starvation)** — the agent-a starvation
during the Lampert deck cycle this morning.

## Implementation shape

`RequestBody.scope` is `serde_json::Value` (freeform), so `more_coming`
is a JSON-level convention inside the scope map, alongside the existing
`scope.instructions` (ADR 0001). This matches the user's spec literally
("scope.more_coming") and preserves byte-exact backwards compat:

- **Default false → key omitted entirely.** Existing signed envelopes
  continue to verify byte-for-byte.
- Sender helper inserts `"more_coming": true` only when the caller asks.
- Receiver reads `scope.pointer("/more_coming").and_then(|v| v.as_bool())
  .unwrap_or(false)`.

## Tasks (atomic commits, TDD)

### T1 — RED: failing round-trip test

**File:** `crates/famp-envelope/tests/scope_more_coming_round_trip.rs` (new)

Mirrors `tests/provisional_scope_instructions_vector.rs`:
- Build a `request` envelope with `scope.instructions` + `scope.more_coming = true`
- Sign with RFC 8032 Test-1 key, encode, decode, verify_strict
- Assert `scope.pointer("/more_coming")` round-trips to `Some(true)`
- Second assertion: omitting `more_coming` from sender side produces
  bytes byte-identical to a parallel build that never set the field
  (proves serde-skip-when-default at the JSON layer)

Commit: `test(quick-260425-pc7): add RED round-trip test for scope.more_coming`

### T2 — GREEN: surface `REQUEST_SCOPE_MORE_COMING_KEY` constant

**File:** `crates/famp-envelope/src/body/request.rs`

Add `pub const REQUEST_SCOPE_MORE_COMING_KEY: &str = "more_coming";`
mirroring the existing `REQUEST_SCOPE_INSTRUCTIONS_KEY` constant.
Doc-comment notes the convention (default false → omit).

No struct field changes — `scope` stays freeform. The "serde-skip when
default" guarantee is enforced by the sender path (only inserts on true).

Run the RED test → must turn GREEN.

Commit: `feat(quick-260425-pc7): add REQUEST_SCOPE_MORE_COMING_KEY constant`

### T3 — CLI wiring: `famp send --new-task --more-coming`

**File:** `crates/famp/src/cli/send/mod.rs`

- Add `pub more_coming: bool` to `SendArgs` with `#[arg(long, requires = "new_task")]`
- Thread through `build_request_envelope(..., more_coming: bool)`
- Inside `build_request_envelope`, after the existing instructions
  insert, conditionally insert `"more_coming": true` when the flag is set.
  Also handle the body-less branch: if `more_coming` is true but `body`
  is `None`, still create an object with `more_coming: true`.

Commit: `feat(quick-260425-pc7): wire --more-coming through famp send CLI`

### T4 — MCP wiring: `famp_send` tool

**Files:** `crates/famp/src/cli/mcp/server.rs`, `crates/famp/src/cli/mcp/tools/send.rs`

In `tool_descriptors()`:
- Tighten the `body` description (T1.2 pattern reminder — already shipped
  in 260425-of2; do NOT re-tighten, but mirror the same crispness)
- Add `more_coming` field to `inputSchema.properties`:
  ```
  "more_coming": {
    "type": "boolean",
    "description": "OPTIONAL, new_task mode only. Set true when this is the first of multiple new_task envelopes for the same task — receiver should wait for follow-up deliver()s before committing. Default false (the task is fully briefed in this single envelope)."
  }
  ```

In `tools/send.rs::call`:
- Read `input["more_coming"].as_bool().unwrap_or(false)`
- Pass through to `SendArgs.more_coming` (only meaningful for new_task mode;
  silently ignored for deliver/terminal modes by the CLI layer's `requires`
  attribute → MCP path doesn't go through clap, so just pass it on regardless;
  the envelope layer just won't insert it for non-new_task)

Commit: `feat(quick-260425-pc7): expose more_coming in famp_send MCP tool`

### T5 — Surface in `famp inbox list`

**File:** `crates/famp/src/cli/inbox/list.rs`

In `run_list`, when shaping each entry, extract `more_coming` from the
request envelope's `body.scope.more_coming` and add a top-level
`"more_coming": <bool>` field to the JSON output (only for `class ==
"request"`; default false). This way agents/humans don't have to dig
into `body.scope.more_coming`.

Commit: `feat(quick-260425-pc7): surface more_coming in inbox list output`

### T6 — Verify clean: cargo test + clippy

```sh
PATH="$HOME/.cargo/bin:$PATH" cargo test --workspace
PATH="$HOME/.cargo/bin:$PATH" cargo clippy --workspace --all-targets -- -D warnings
```

Both must be green. No commit (verification only).

### T7 — Redeploy listeners

```sh
scripts/redeploy-listeners.sh
```

Push new envelope shape to all 8 daemons.

### T8 — Update resume doc + SUMMARY.md + STATE.md

- Write `260425-pc7-SUMMARY.md`
- Append row to STATE.md "Quick Tasks Completed"
- Update `~/.claude/plans/ok-now-analyze-and-toasty-waffle.md`: mark T2.1
  shipped with commit hashes
- Final docs commit: `docs(quick-260425-pc7): complete more_coming flag quick task`

## must_haves (truths)

- Existing signed envelopes with no `more_coming` key still decode + verify_strict
- New envelopes with `more_coming: true` round-trip and decode with the flag set
- `famp send --new-task --more-coming` emits the flag on the wire
- `famp_send` MCP tool accepts `more_coming` in new_task mode
- `famp inbox list` surfaces `more_coming` as a top-level JSON field
- cargo test --workspace passes
- cargo clippy --workspace --all-targets -- -D warnings passes

## Out of scope

- Changing auto-commit logic on the receiver side (follow-up after observable)
- Backwards-incompatible envelope changes
- Format-drift on adjacent code
