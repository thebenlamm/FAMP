---
phase: 260711-g1t
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DRIFT-01-spec-version, DRIFT-02-milestone-currency, DRIFT-03-mcp-tool-count, DRIFT-04-two-dir-doc, DRIFT-05-obsolete-script, DRIFT-06-toolchain-note, DRIFT-07-install-restart-msg]
files_modified:
  - README.md
  - CLAUDE.md
  - FAMP-v0.5.1-spec.md
  - crates/famp-crypto/Cargo.toml
  - crates/famp-inbox/Cargo.toml
  - crates/famp-taskdir/Cargo.toml
  - crates/famp-transport-http/Cargo.toml
  - crates/famp/Cargo.toml
  - Justfile
  - crates/famp/src/cli/mcp/server.rs
  - crates/famp/src/cli/mod.rs
  - crates/famp/src/cli/install/claude_code.rs
  - scripts/redeploy-listeners.sh
  - docs/history/redeploy-listeners.sh

must_haves:
  truths:
    - "Non-historical prose in README, CLAUDE.md, and the spec file states spec v0.5.2 as authoritative; historical milestone references (README version-note, changelog rows, frozen crypto vectors) still read v0.5.1."
    - "All shipping crate `description` fields state v0.5.2 (or a later milestone tag), none say v0.5.1."
    - "CLAUDE.md Architecture and README describe v0.11 as the current runtime; shipped v0.9 bus items are not under 'Not Shipped Yet'."
    - "MCP subcommand help no longer hardcodes a tool count; a unit test asserts all twelve descriptor names are present."
    - "README documents the ~/.famp vs ~/.famp-local directory split; no ~/.famp-local references were removed."
    - "redeploy-listeners.sh lives under docs/history/ and no live doc or recipe points users to it as a current tool."
    - "README Prerequisites notes the first toolchain install also pulls rustfmt+clippy."
    - "install-claude-code print distinguishes live slash commands from the MCP-server registration that needs a restart."
    - "No envelope/wire/FSM/broker runtime source changed; the banner regression test and spec-lint gate stay green."
  artifacts:
    - README.md
    - CLAUDE.md
    - FAMP-v0.5.1-spec.md
    - crates/famp/src/cli/mcp/server.rs
    - crates/famp/src/cli/mod.rs
    - crates/famp/src/cli/install/claude_code.rs
    - Justfile
    - docs/history/redeploy-listeners.sh
    - "GitHub issue proposing ~/.famp / ~/.famp-local unification"
  key_links:
    - "crates/famp-envelope/src/version.rs FAMP_SPEC_VERSION=0.5.2 <-> Justfile check-spec-version-coherence <-> crate descriptions (extended gate)"
    - "server.rs tool_descriptors() name entries <-> new unit test (twelve-name anti-drift gate)"
    - "banner regression test crates/famp/src/cli/mod.rs:205-213 MUST stay green — do NOT touch BANNER_ABOUT"
    - "scripts/spec-lint.sh SPEC=\"FAMP-v0.5.1-spec.md\" <-> spec file MUST NOT be renamed"
---

<objective>
Reconcile FAMP v0.11 documentation and dev-loop scripts with the shipped runtime.
Fix seven pre-verified drift items found on a fresh Ubuntu install: stale spec
version (0.5.1 vs authoritative 0.5.2), stale milestone framing (v0.9 vs v0.11),
a wrong MCP tool count (help says eight, registry has twelve), an under-documented
two-directory layout, an obsolete v0.8 listener script, a missing toolchain note,
and misleading install-restart messaging.

Purpose: A byte-exact reference implementation must not ship docs that contradict
its own binary and gates. This closes the drift AND wires anti-drift gates so it
cannot silently re-open.

Output: Corrected docs/scripts, an extended spec-version coherence gate, a new
twelve-tool descriptor unit test, and a GitHub issue tracking the eventual
directory unification.

Scope guard: DOC/SCRIPT/STRING HYGIENE ONLY. Binary string edits (help/print text)
and one new unit test are in-scope. Envelope/wire/FSM/broker logic is OUT of scope.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Pre-verified drift facts live in the plan tasks below. Key guardrails:
# - Banner is LOCKED by a regression test (crates/famp/src/cli/mod.rs:205-213). Do NOT touch BANNER_ABOUT.
# - Spec file MUST NOT be renamed (scripts/spec-lint.sh hardcodes the filename).
# - Frozen interop test vectors carrying "0.5.1" (famp-crypto worked-example §7.1c) MUST NOT change.
# - ~/.famp-local is LIVE backing store, NOT vestigial — document it, never remove references.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec version 0.5.2 in prose docs (README, CLAUDE.md, spec file)</name>
  <files>README.md, CLAUDE.md, FAMP-v0.5.1-spec.md</files>
  <action>
Reconcile the authoritative spec version to 0.5.2 in non-historical prose. Three edits:

1. README (~line 24): the sentence describing the stack currently calls it
   "v0.5.1-spec-conformant". Change that version token to v0.5.2. Then add ONE
   changelog row in README's milestone/changelog list (the "Current Milestones"
   block near line 690) recording the 0.5.1 -> 0.5.2 move and its one substantive
   delta: the non-FSM-firing audit_log MessageClass (shipped v0.9 Phase 1).
   LEAVE UNCHANGED: README line ~9 (the "On the version numbers" note framing
   v0.5.1 as the spec fork) and the existing "v0.5.1: spec fork, shipped"
   changelog row — both are historical milestone references.

2. CLAUDE.md (line ~16): the constraint "v0.5.1 fork is the authority for this
   implementation" -> v0.5.2 is the authority, adding a short clause noting the
   0.5.1 -> 0.5.2 move added the audit_log MessageClass (does not fire the task FSM).

3. FAMP-v0.5.1-spec.md (~lines 27-35): the "Reference-implementation note
   (non-normative)" currently says the impl "intentionally retains 0.5.1 until v0.9
   Phase 1 ships MessageClass::AuditLog." That gap has CLOSED. Rewrite the note in
   PAST tense: the impl emitted 0.5.1 on the wire until v0.9 Phase 1; it now emits
   0.5.2. Keep the normative-constant sentence intact. Because ANY edit to the spec
   file requires a changelog entry per its convention, add one v0.5.2-Delta row in
   the Delta-changelog block (~line 1106, the v0.5.2 catalog Delta29-Delta33 group)
   recording this editorial past-tense reconciliation and citing this doc-drift fix.

DO NOT rename the spec file. DO NOT touch the banner or any frozen test vectors.
  </action>
  <verify>
    <automated>grep -c 'v0.5.2-spec-conformant' README.md | grep -qv '^0$' && grep -c 'v0.5.2' CLAUDE.md | grep -qv '^0$' && cargo test -p famp --lib banner 2>&1 | tail -3</automated>
  </verify>
  <done>README stack line + CLAUDE.md authority line say v0.5.2; README changelog has a 0.5.1->0.5.2 row; spec reference-note is past-tense with a matching Delta-changelog entry; historical rows and the banner test are untouched (banner test still passes).</done>
</task>

<task type="auto">
  <name>Task 2: Crate descriptions to v0.5.2 + extend coherence gate</name>
  <files>crates/famp-crypto/Cargo.toml, crates/famp-inbox/Cargo.toml, crates/famp-taskdir/Cargo.toml, crates/famp-transport-http/Cargo.toml, crates/famp/Cargo.toml, Justfile</files>
  <action>
Five crate `description` fields still carry the stale "FAMP v0.5.1" tag:
famp-crypto, famp-inbox, famp-taskdir, famp-transport-http, and the famp umbrella
crate. Update each version token in the `description` line from v0.5.1 to v0.5.2
(preserve the rest of each description verbatim). Leave milestone-scoped
descriptions alone (famp-keyring "v0.7", the inspect crates "v0.10", and the
already-correct famp-bus "v0.5.2").

Then extend the existing `check-spec-version-coherence` recipe in the Justfile
(~lines 205-210) so the drift cannot re-open: inside the block that already runs
when version.rs declares 0.5.2, add a check that fails the recipe if any crate
`description` line under crates/*/Cargo.toml still contains the token v0.5.1
(match the literal "v0.5.1" on description lines only; emit an actionable message
naming the offending file). This makes the gate cover version.rs<->AuditLog AND
crate-description drift.
  </action>
  <verify>
    <automated>just check-spec-version-coherence && grep -h '^description' crates/*/Cargo.toml | grep -c 'v0.5.1' | grep -q '^0$'</automated>
  </verify>
  <done>The five stale descriptions read v0.5.2; `just check-spec-version-coherence` passes now and would fail if a v0.5.1 description token were reintroduced.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone currency — CLAUDE.md Architecture + README "Not Shipped Yet"</name>
  <files>CLAUDE.md, README.md</files>
  <action>
Bring milestone framing to v0.11 (the shipped, archived milestone):

1. CLAUDE.md Architecture section (line ~57 "FAMP today is local-first (v0.9)"
   and the v0.9 "today" language at ~64, 73, 75, 78, 82, 90, 95): reframe so v0.11
   is described as today's runtime (persistent broker daemon + cross-tool bootstrap,
   shipped 2026-06-06) and v0.9 is described as a shipped prior milestone (the
   local-first bus that v0.11 built on). Keep the Layer 0/1/2 model and the v1.0
   federation trigger framing intact — only the "today = v0.9" tense is stale.

2. README "Not Shipped Yet" section (~lines 65-72): the "v0.9 — Local-First Bus
   (shipping now)" subsection is stale — the UDS broker, channels, and zero-crypto
   local path are BUILT and running. Move those shipped items out of "Not Shipped
   Yet" (into the shipped/feature list above it, or delete the redundant bullets if
   already listed there). Leave genuinely-unshipped v1.0 federation items under
   "Not Shipped Yet."
  </action>
  <verify>
    <automated>grep -c 'FAMP today is local-first (v0.9)' CLAUDE.md | grep -q '^0$' && grep -c 'shipping now' README.md | grep -q '^0$'</automated>
  </verify>
  <done>CLAUDE.md Architecture frames v0.11 as current and v0.9 as shipped; README no longer lists the shipped v0.9 bus under "Not Shipped Yet / shipping now".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fix MCP tool-count help + add twelve-descriptor anti-drift test</name>
  <files>crates/famp/src/cli/mod.rs, crates/famp/src/cli/mcp/server.rs</files>
  <behavior>
    - A unit test calling `tool_descriptors()` collects every descriptor `name` and asserts all twelve names are present: famp_send, famp_await, famp_inbox, famp_channel_log, famp_peers, famp_register, famp_set_listen, famp_whoami, famp_verify, famp_join, famp_leave, famp_inspect_waiters.
    - The test asserts the descriptor count equals twelve (fails loud if a tool is added/removed without updating the test).
  </behavior>
  <action>
Two edits, both help-text/test only (NO behavior change to tool dispatch):

1. crates/famp/src/cli/mod.rs (doc-comment at lines ~88-91): the `Mcp` subcommand
   doc-comment hardcodes a wrong tool count and a partial enumeration (it names a
   count and lists a subset). The registry actually exposes twelve tools. Replace
   the hardcoded count+enumeration in the prose with a stable, drift-proof
   description that says the tool set is enumerated at runtime via the MCP
   tools/list method (so no count needs maintaining in the doc-comment). Keep the
   surrounding Content-Length/JSON-RPC framing description.

2. crates/famp/src/cli/mcp/server.rs: add a `#[cfg(test)] mod tests` block (none
   exists yet) with a unit test that exercises the private `tool_descriptors()` fn
   (fn is at line ~35): parse the returned JSON array, collect each object's `name`
   string, and assert (a) the count is twelve and (b) the exact set of twelve tool
   names above is present. This is the anti-drift GATE for the tool registry — wire
   it, do not leave the registry uncovered.

Post-merge note (NOT run by executor): server.rs is a live MCP surface — per
CLAUDE.md the orchestrator must run `just install` after merge so ~/.cargo/bin/famp
reflects the help-text change.
  </action>
  <verify>
    <automated>cargo test -p famp --lib mcp::server 2>&1 | tail -5 && grep -c 'tools/list' crates/famp/src/cli/mod.rs | grep -qv '^0$'</automated>
  </verify>
  <done>server.rs has a passing unit test asserting exactly twelve named descriptors; the mod.rs doc-comment no longer hardcodes a tool count and references runtime tools/list enumeration; `cargo build -p famp` succeeds.</done>
</task>

<task type="auto">
  <name>Task 5: Document ~/.famp vs ~/.famp-local split + toolchain note + file unification issue</name>
  <files>README.md</files>
  <action>
Three doc additions (item 4-doc, item 6, plus a GitHub issue):

1. README — add a short subsection (near the existing FAMP_LOCAL_ROOT note ~line
   490) explaining FAMP's two-directory layout: ~/.famp holds the broker socket and
   per-name mailboxes (message runtime); ~/.famp-local holds identity backing store
   (wires.tsv cwd->identity bindings) and hooks.tsv. State plainly that ~/.famp-local
   is LIVE and required for non-MCP CLI use. Do NOT remove any existing
   ~/.famp-local reference — the current text (README ~line 493) is accurate.

2. README Prerequisites — add ONE line noting the first toolchain install
   (rust-toolchain.toml) also pulls rustfmt + clippy, which `just ci`, `just lint`,
   and the pre-push git hook require — so offline/air-gapped users expect the extra
   components and are not surprised. (Do NOT edit rust-toolchain.toml; keep its
   components as-is.)

3. Run `gh issue create` to file a tracking issue proposing eventual unification of
   ~/.famp and ~/.famp-local into a single directory. Title it clearly, body it as a
   RUNTIME change explicitly OUT OF SCOPE for this doc-hygiene task, and reference
   the two-directory split just documented. Capture the returned issue URL in the
   task SUMMARY.
  </action>
  <verify>
    <automated>grep -c '.famp-local' README.md | grep -qv '^0$' && grep -Eic 'rustfmt|clippy' README.md | grep -qv '^0$'</automated>
  </verify>
  <done>README documents the ~/.famp vs ~/.famp-local split (no ~/.famp-local references removed) and the toolchain-components prerequisite; a GitHub issue exists proposing directory unification, labeled out-of-scope/runtime, with its URL recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 6: Retire obsolete redeploy-listeners.sh to docs/history/</name>
  <files>scripts/redeploy-listeners.sh, docs/history/redeploy-listeners.sh</files>
  <action>
scripts/redeploy-listeners.sh targets the retired v0.8 HTTPS-per-agent-daemon
federation model (it polls for "listening on https://" and cycles
~/.famp-local/agents/* daemons). v0.11 has one UDS broker and no network listener,
so the script is dead. `git mv scripts/redeploy-listeners.sh
docs/history/redeploy-listeners.sh` (docs/history/ already exists and holds retired
famp-local scaffolding). This is CI-safe: the check-shellcheck recipe only globs
crates/famp/assets/*.sh, not scripts/.

Then grep README.md and the Justfile for any live reference pointing users to the
script as a current tool and remove/redirect it. The only existing textual
reference is in docs/superpowers/specs/2026-04-26-windows-port-brief.md (a
historical "do not port" note) — LEAVE that; it is itself history and correctly
describes the script as scaffolding.
  </action>
  <verify>
    <automated>test -f docs/history/redeploy-listeners.sh && test ! -f scripts/redeploy-listeners.sh && grep -c 'redeploy-listeners' README.md Justfile 2>/dev/null | grep -q ':0$'</automated>
  </verify>
  <done>The script lives at docs/history/redeploy-listeners.sh, is gone from scripts/, and no live reference in README or the Justfile points to it.</done>
</task>

<task type="auto">
  <name>Task 7: Clarify install-claude-code restart messaging</name>
  <files>crates/famp/src/cli/install/claude_code.rs</files>
  <action>
crates/famp/src/cli/install/claude_code.rs (line ~126) prints "install-claude-code
complete. Restart Claude Code windows to pick up changes." This over-generalizes:
the 7 installed slash commands are picked up LIVE; only the MCP server registration
requires a Claude Code restart. Reword the printed message to distinguish
per-artifact: slash commands are available immediately in open windows; the MCP
server registration takes effect after restarting Claude Code. String-only edit —
zero behavior change.

Post-merge note (NOT run by executor): this is a binary string in famp source — per
CLAUDE.md the orchestrator must run `just install` after merge so ~/.cargo/bin/famp
prints the corrected message.
  </action>
  <verify>
    <automated>grep -c 'MCP server' crates/famp/src/cli/install/claude_code.rs | grep -qv '^0$' && cargo build -p famp 2>&1 | tail -2</automated>
  </verify>
  <done>The install-claude-code completion message distinguishes live slash commands from the MCP-server registration that needs a restart; famp still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits documentation, crate metadata, help/print strings, and adds one unit test. No new data-crossing boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g1t-01 | Tampering | Doc/gate reconciliation reintroducing drift | low | mitigate | Anti-drift gates wired: extended `check-spec-version-coherence` (crate descriptions) and new twelve-descriptor unit test; banner regression test and spec-lint filename guard remain green. |
| T-g1t-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan; no new dependencies added. |
</threat_model>

<verification>
- `cargo build -p famp` succeeds (Tasks 4, 7 touch famp source strings/tests only).
- `cargo test -p famp --lib` passes, including the banner regression test (unchanged) and the new twelve-descriptor test.
- `just check-spec-version-coherence` passes with the extended crate-description check.
- No stale `v0.5.1` token remains in any crate `description` line.
- No file under crates/*/src changed except the mod.rs doc-comment and the server.rs test module — grep the diff to confirm no envelope/wire/FSM/broker logic changed.
- Spec file NOT renamed (`scripts/spec-lint.sh` still finds FAMP-v0.5.1-spec.md); run `just spec-lint` if convenient.
</verification>

<success_criteria>
All seven drift items reconciled: spec version 0.5.2 in non-historical prose and
crate descriptions; v0.11 milestone framing; drift-proof MCP tool help + twelve-tool
test; documented two-directory split; retired listener script; toolchain note;
clarified install message. Two anti-drift gates wired (extended coherence recipe +
descriptor test). Banner test, spec-lint, and build stay green. Zero runtime behavior
change. GitHub unification issue filed.
</success_criteria>

<post_execution_notes>
POST-MERGE (orchestrator, NOT executor): Tasks 4 and 7 edit binary strings in famp
source. Per CLAUDE.md, run `just install` after merge so ~/.cargo/bin/famp (the
binary every agent session reads) reflects the corrected MCP help text and
install-claude-code message. `target/release/famp` is not the deployment target.
No broker restart needed (no wire/protocol change).
</post_execution_notes>

<output>
Create `.planning/quick/260711-g1t-reconcile-v0-11-doc-drift-spec-version-v/260711-g1t-SUMMARY.md` when done.
Record the GitHub issue URL from Task 5 in the SUMMARY.
</output>
