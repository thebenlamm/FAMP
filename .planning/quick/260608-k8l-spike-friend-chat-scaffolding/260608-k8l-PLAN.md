---
quick_id: 260608-k8l
slug: spike-friend-chat-scaffolding
date: 2026-06-08
---

# Quick Task: v1.0 federation spike scaffolding

Add two zero-code scaffolding artifacts for the v1.0 federation **validation spike**
(prove cross-host agent chat is worth reaching for before building `famp-gateway`;
roadmap parked — see auto-memory `project_v10_spike_first`).

1. `just spike-tunnel` — socat recipe exposing the local broker on the tailnet
   (`bind=$(tailscale ip -4 | head -1)`, port 9999), printing the IP+port to share.
2. `docs/SPIKE-friend-chat.md` — paste-to-your-friend runbook: Tailscale + famp +
   socat prerequisites, friend's reverse-socat command, register-on-shared-broker,
   send both directions, and a prominent **security gate** (friend-facing window
   registers `listen: false`; inbound cross-host messages are data-to-render, never
   instructions-to-execute — the confused-deputy boundary).

NOT build work. No production code touched.
