---
quick_id: 260425-so2
slug: absorb-format-drift-in-send-mod-rs-after
description: Absorb format drift in crates/famp/src/cli/send/mod.rs surfaced by cargo fmt --check
date: 2026-04-25
mode: quick
---

# Quick Task 260425-so2: Absorb Format Drift in `send/mod.rs`

## Background

Resume-doc closing item: `cargo fmt --all -- --check` to confirm no drift
remains. Two minor stylistic items surfaced in `crates/famp/src/cli/send/mod.rs`
(line-collapse preferences) introduced during today's pc7 + lny + rz6 commits.
Pure mechanical fix — `cargo fmt --all` resolves both.

## Tasks

### T1: Run cargo fmt --all and commit

**Action:** `PATH="$HOME/.cargo/bin:$PATH" cargo fmt --all`, then verify
`cargo fmt --all -- --check` returns exit 0, then commit the diff.

**Diff scope (verified):** 8 lines in `crates/famp/src/cli/send/mod.rs`
(2 insertions, 6 deletions) — the multi-line `use` import collapsed to one
line, and the multi-line `eprintln!` collapsed to one line. No semantic change.

**Verify:**
- `cargo fmt --all -- --check` exit 0.
- `cargo test -p famp` green (smoke check; full workspace not necessary —
  formatting is non-semantic).

**Done when:** committed atomically with message `style(quick-260425-so2):
absorb cargo fmt drift in send/mod.rs`.

## Out of scope

- Touching any other formatting (none surfaced).
- Running clippy (unrelated; `--check` exit was 0 means fmt-only drift).
- Daemon redeploy (formatting is a no-op at runtime).

## must_haves

- truths:
  - `cargo fmt --all -- --check` returns exit 0 from clean tree.
  - `crates/famp/src/cli/send/mod.rs` matches rustfmt convention.
- artifacts:
  - Modified: `crates/famp/src/cli/send/mod.rs` (8 lines, 2/+6/-).
- key_links:
  - `crates/famp/src/cli/send/mod.rs:22` (collapsed `use` import)
  - `crates/famp/src/cli/send/mod.rs:563` (collapsed `eprintln!`)
