---
quick_id: 260425-sl0
slug: t3-x-file-three-backlog-items-999-3-999-
description: T3.x — file three backlog items (999.3 heartbeat, 999.4 user_attention, 999.5 spec-by-path deferral)
date: 2026-04-25
mode: quick
---

# Quick Task 260425-sl0: T3.x — File Three Backlog Items

## Background

The pressure-test resume doc (~/.claude/plans/ok-now-analyze-and-toasty-waffle.md)
identifies three Tier-3 items that don't warrant immediate implementation but
must be filed in the project backlog so they aren't lost:

- **T3.1 G3 work-in-progress visibility** — propose `heartbeat` envelope class
- **T3.2 G5 human-in-loop primitive** — propose `user_attention` envelope class
- **T3.3 G2 spec-by-path deferral** — link the gap to the v1.0 federation gateway

The project's backlog convention (per ROADMAP.md:186) is to add `Phase 999.x`
entries with Goal / Context / Plans (TBD) blocks — see existing 999.1 and 999.2
for the template. No `.planning/backlog/` directory exists; the in-roadmap pattern
is the established home.

## Truths

- ROADMAP.md already has Phase 999.1 (`famp await` crash safety) and 999.2
  (multi-listener lock semantics) — both filed 2026-04-24 from the Stop hook
  adversarial review. Pattern is: header line, **Goal:** TBD-marker,
  **Context:** prose paragraph (where the gap surfaced and why it matters),
  **Plans:** stub bullet pointing at `/gsd:review-backlog`.
- Next available numbers: 999.3, 999.4, 999.5.
- These are filings, not implementations — no code, no envelope schema work,
  no test fixtures, no daemon redeploy. Pure ROADMAP.md edit.
- All three gaps already have evidence trails in the resume doc — context
  prose can quote/summarize from there for traceability.

## Tasks

### T1: Append 999.3 / 999.4 / 999.5 to ROADMAP.md "Backlog" section

**File:** `.planning/ROADMAP.md`

**Action:** After the existing Phase 999.2 block (currently ending at line 208
with `Plans: - [ ] TBD ...`), append three new Phase 999.x blocks following
the established template. Insert before the closing `---` and the
"Roadmap updated" footer line.

The three blocks (each filling Goal / Context / Plans):

#### Phase 999.3: `heartbeat` envelope class — work-in-progress visibility (BACKLOG)

- **Goal:** Define and ship a low-bandwidth `heartbeat` envelope class so a
  long-running worker can periodically signal "still alive, working on
  <one-liner>" without the originator having to poll. Eliminates the failure
  mode where 8–15 minute silent gaps in a multi-agent task look indistinguishable
  from a crashed daemon.
- **Context:** Surfaced 2026-04-25 during the first 3-agent pressure test
  (Lampert × Ha Pharma deck cycle). Symptom: agent-a starved 21 minutes
  watching agent-b silently work on a Magnus-pressure-tested artifact, then
  Ben intervened thinking it was stuck. Today there is no protocol-level
  signal between "actively working" and "crashed mid-task." Proposal: new
  envelope class `heartbeat` carrying `{ task_id, working_on: <≤120 char
  string>, ts }`; sender emits at most every N minutes (default 5) or on
  demand from a hypothetical `famp_status` MCP tool; receiver-side, the
  originator's `famp_await` surfaces "agent-b heartbeat at HH:MM, working
  on: ..." rather than rendering silence as suspicious. Sized as substrate
  work because it touches `famp-envelope` (new MessageClass) and `famp-fsm`
  (heartbeat is non-state-advancing — does not consume a slot in the 5-state
  FSM, but the inbox surface treats it like a deliver).
- **Plans:** TBD (promote with `/gsd:review-backlog` when ready).

#### Phase 999.4: `user_attention` envelope class — human-in-loop primitive (BACKLOG)

- **Goal:** Define and ship a `user_attention` envelope class so a worker
  can explicitly mark a task as "blocked pending human input" — distinct
  from `REQUESTED`, `COMMITTED`, or any of the three terminal states. The
  inbox surface and orchestrator must render this as a first-class
  human-action signal, not just another deliver.
- **Context:** Surfaced 2026-04-25 during the same pressure test. Symptom:
  agent-c (Magnus, on call) said "this needs Ben" during round-2
  escalation; agent-b had no FAMP-native primitive to forward the
  blocked-on-human state to agent-a (the orchestrator) in a way that
  would surface differently from a normal reply. Workaround used: a
  prose-tagged deliver, indistinguishable from any other reply. Proposal:
  new envelope class `user_attention` carrying `{ task_id, reason:
  <markdown blob explaining what input is needed>, suggested_actions?:
  Vec<string> }`; receiver-side, `famp_inbox list` and `famp_await` MUST
  flag these distinctly (e.g., a separate column or icon). Open design
  question: does this advance the FSM (new state `BLOCKED_HUMAN`?) or is
  it a non-state-advancing signal layered on COMMITTED? Likely the latter
  — keeps the 5-state FSM intact and matches the heartbeat pattern.
- **Plans:** TBD (promote with `/gsd:review-backlog` when ready).

#### Phase 999.5: Spec-by-path tracking — `~/Workspace/...` paths in messages (BACKLOG, deferred to v1.0)

- **Goal:** Track the spec-by-path gap explicitly so it isn't forgotten
  before v1.0. The gap is already covered structurally by the v1.0
  federation gateway design — this entry exists so there is a discoverable
  link from the pressure-test findings to the federation work, and so
  v1.0 planning explicitly verifies the gap is closed.
- **Context:** Surfaced 2026-04-25 during the first 3-agent pressure test.
  Symptom: agent-b sent absolute filesystem paths (`~/Workspace/FAMP/...`,
  `~/Workspace/Lampert/...`) inside envelope bodies because the protocol
  has no native way to address a spec/artifact by content-id or by
  federation-resolvable URL. Today this works only because all three
  agents are co-resident on the same Mac with the same `$HOME`. The
  moment any agent runs cross-host, every such reference is dead. v0.9
  (local-first bus, in design at
  `docs/superpowers/specs/2026-04-17-local-first-bus-design.md`) does
  NOT address this — it's a same-host design. v1.0's federation gateway
  is the right home for content-addressable refs (or signed-URL refs)
  because that's the layer where cross-host trust + transport already
  exists. **Action for v1.0 planning:** when scoping the federation
  gateway, include an explicit requirement that an envelope can carry
  a portable artifact reference (sha256-id or signed URL) and the
  receiver can dereference it without trusting the sender's filesystem.
  See `~/.claude/plans/ok-now-analyze-and-toasty-waffle.md` § "G2"
  for the symptom evidence and § T3.3 for the deferral rationale.
- **Plans:** TBD — to be folded into v1.0 federation gateway scope, NOT
  promoted independently. (Surface during `/gsd:new-milestone` for v1.0.)

**Verify:**
- `grep -n "Phase 999\." .planning/ROADMAP.md` returns 5 entries (1, 2, 3, 4, 5).
- `git diff --stat .planning/ROADMAP.md` shows ROADMAP.md only.

**Done when:** three new blocks present in the Backlog section in numerical
order, footer "Roadmap updated" line still present and unchanged
(it documents the 2026-04-17 v0.9 re-scope, not these filings — these are
backlog additions that don't require a roadmap-version bump).

## Out of scope

- Designing the actual envelope schemas — that's promotion work.
- Editing `docs/superpowers/specs/2026-04-17-local-first-bus-design.md`.
- Touching `ARCHITECTURE.md`.
- Code, tests, or daemon redeploy.

## must_haves

- truths:
  - All three gaps (G3, G5, G2) have a discoverable backlog entry that
    a future `/gsd:review-backlog` or v1.0 milestone planning can find.
  - The 999.5 entry explicitly notes the v1.0 federation gateway as the
    right home, so the gap is not orphaned.
- artifacts:
  - Modified: `.planning/ROADMAP.md` (3 new Phase 999.x blocks appended
    in the Backlog section).
- key_links:
  - `.planning/ROADMAP.md:186-208` (existing Backlog section + 999.1/999.2)
  - `~/.claude/plans/ok-now-analyze-and-toasty-waffle.md` § Tier-3 / T3.1-T3.3
    and § G2/G3/G5 (evidence trail)
  - `docs/superpowers/specs/2026-04-17-local-first-bus-design.md` (referenced
    from 999.5 context as the v0.9 design that does NOT cover the gap)
