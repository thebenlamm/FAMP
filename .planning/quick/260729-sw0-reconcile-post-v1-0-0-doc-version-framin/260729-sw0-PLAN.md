---
phase: quick-260729-sw0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - ARCHITECTURE.md
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260729-sw0]
must_haves:
  truths:
    - "README.md's status line names v1.0 (shipped, tagged v1.0.0), not v0.11."
    - "No sentence in README.md / ARCHITECTURE.md / CLAUDE.md frames the v1.0 federation gateway as future, unlanded, deferred, or trigger-gated."
    - "ARCHITECTURE.md states 16 workspace crates at v1.0.0 and its crate table lists all 16 including famp-gateway."
    - "ARCHITECTURE.md has a v1.0 section describing the shipped federation gateway in present/past tense."
    - "CLAUDE.md's Architecture block no longer calls v0.11 the current runtime and no longer carries the superseded 'v1.0 readiness trigger (named)' paragraph."
    - "Every place a doc previously said what comes after the current version now says the next milestone is not yet defined; Gate B is described as open, not scheduled."
    - "Exactly three files are modified; the two repo-doc test gates still pass with a non-zero matched-test count."
  artifacts:
    - README.md
    - ARCHITECTURE.md
    - CLAUDE.md
  key_links:
    - "README.md:71 'Federation gateway (v1.0, shipped)' is byte-identical after the change (asserted by crates/famp/tests/gateway_setup_doc_accuracy.rs:331)."
    - "README.md '## Quick Start' ```bash fence body is byte-identical after the change (asserted by crates/famp/tests/readme_line_count_gate.rs)."
    - "ARCHITECTURE.md's crate-count sentence and its crate table agree (16 = 16 rows)."
---

<objective>
Reconcile stale version framing in README.md, ARCHITECTURE.md, and CLAUDE.md after
v1.0.0 shipped (tagged `v1.0.0` at `5edff41`, 2026-07-29). These three docs still
present v0.11 as the current runtime and the v1.0 federation gateway as a future,
trigger-gated milestone. All three are wrong.

Purpose: a reader of any of these three files currently concludes federation has not
shipped. ARCHITECTURE.md's newest section is v0.9 and its crate table is missing
`famp-gateway` entirely; CLAUDE.md still carries a superseded "v1.0 readiness trigger"
paragraph whose 4-week clock was retired.

Output: three edited markdown files. No source, no test, no other doc.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@README.md
@ARCHITECTURE.md

Ground truth already VERIFIED by the planner — you may re-verify, do not re-derive:
- workspace `Cargo.toml` line 25: `version = "1.0.0"`
- `crates/` holds exactly 16 crates: famp, famp-bus, famp-canonical, famp-core,
  famp-crypto, famp-envelope, famp-fsm, famp-gateway, famp-inbox,
  famp-inspect-client, famp-inspect-proto, famp-inspect-server, famp-keyring,
  famp-taskdir, famp-transport, famp-transport-http
- tags `v1.0.0` and `v1.0.0-rc.1` both exist; `v1.0.0` is at `5edff41`
- `famp-gateway` intra-workspace deps (`crates/famp-gateway/Cargo.toml:15-21`):
  famp-bus, famp, famp-keyring, famp-envelope, famp-crypto, famp-transport,
  famp-transport-http
- Exactly TWO test files read repo-root docs:
  `crates/famp/tests/readme_line_count_gate.rs` (3 tests) and
  `crates/famp/tests/gateway_setup_doc_accuracy.rs` (1 test).
  No Justfile recipe and no shell script greps any of the three target files.

## FROZEN README strings — do not reword, do not delete

| Literal | Asserted by | Where it lives |
|---|---|---|
| `Federation gateway (v1.0, shipped)` | `gateway_setup_doc_accuracy.rs:331` (positive) | README.md:71 |
| `## Quick Start` header + first ```bash fence body (≤12 lines, must contain `cargo install famp`, `famp install-claude-code`, `/famp-register`) | `readme_line_count_gate.rs:44-64` | README.md:171-193 |
| `brew install famp` MUST NOT appear anywhere in README | `readme_line_count_gate.rs:77` (negative) | — |
| `/famp-msg` MUST NOT appear anywhere in README | `readme_line_count_gate.rs:94` (negative) | — |

None of the edits below touch README.md:71 or the Quick Start fence, and none
introduce `brew install famp` or `/famp-msg`. The line-count gate measures ONLY the
Quick Start fence body, so changing README's total line count is safe.

## Hard prohibitions

- `docs/GATEWAY-SETUP.md` — DO NOT OPEN FOR EDIT. It is flag-grep gated by
  `gateway_setup_doc_accuracy.rs`.
- Do not touch any fourth file. Not PROJECT.md, not docs/, not Cargo.toml
  (`crates/famp-gateway/Cargo.toml:9` says "Layer 2 skeleton" — stale, out of scope).
- Do not rewrite passages that correctly describe v0.8 / v0.9 / v0.11 in past tense.
- Do not name v1.1 features as planned or committed. Where a doc says "next is X",
  replace with an explicit "the next milestone is not yet defined".
- No drive-by copyediting, no reflowing unrelated prose, no markdown reformatting.

## Known-stale but OUT OF SCOPE (record in SUMMARY, do not fix)

- README.md:300 "exposes eight tools" — the MCP registry has twelve descriptors
  (anti-drift unit test exists). A count error, not version framing.
- README.md:704-713 "## Repo Layout" omits `famp-bus` (v0.9), the three
  `famp-inspect-*` crates (v0.10), `famp-inbox`, `famp-taskdir`, and `famp-gateway`
  (v1.0). Stale across three milestones, so not a v1.0-framing artifact.
- `crates/famp-gateway/Cargo.toml:9` description says "(Layer 2 skeleton)".

## Execution notes (repo-specific)

- `.planning/` is gitignored — run NON-ISOLATED (no worktree). A worktree run will
  FATAL on materializing this ignored, uncommitted PLAN.md.
- `cargo nextest` HANGS in this repo. Do NOT run `just ci` or `just test`. Use plain
  `cargo test` with the exact `--test` targets named below.
- No Rust source changes here, so `just lint` is NOT required for this task.
- `cargo test <filter>` exits 0 on ZERO matches. Every verify command below pipes to
  `grep -qE 'test result: ok\. N passed'` with an explicit N so a vacuous run FAILS.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Frozen-anchor audit, then reconcile README.md end-to-end</name>
  <files>README.md</files>

  <action>
STEP 1 — FROZEN-ANCHOR AUDIT (run BEFORE any edit; do not skip).

Run each command and record the output in the SUMMARY:

```
grep -rn "README\|ARCHITECTURE\.md\|CLAUDE\.md" --include='*.rs' crates/ | grep -v '_deferred_v1'
grep -n "readme.contains\|readme\.\|fence_body.contains" crates/famp/tests/gateway_setup_doc_accuracy.rs crates/famp/tests/readme_line_count_gate.rs
grep -rn "README\|ARCHITECTURE\|CLAUDE\.md" Justfile scripts/ 2>/dev/null
```

Expected result: the frozen-string table in `<context>` is complete — exactly two
test files, no Justfile/script hits. HALT and report if the audit surfaces a doc
string asserted on that is NOT in that table; that string becomes frozen too and the
replacement text below must be revised rather than applied blind.

STEP 2 — APPLY THE EDITS. Each is an exact `Edit` old_string -> new_string. Apply in
order. Do nothing not listed here.

--- R1. Status line (README.md:7) ---
OLD:
**Status:** `v0.11 Broker Daemon`
NEW:
**Status:** `v1.0 Federation Profile — Gateway Core` (shipped 2026-07-29, tagged `v1.0.0`)

--- R2. Lead paragraph (README.md:14-18) ---
OLD:
FAMP today is local-first: a UDS-backed broker for same-host agent messaging
with zero crypto on the local path. FAMP at v1.0 is federated: cross-host
messaging via a `famp-gateway` wrapping the local bus, all of v0.5.2's
signature/canonical-JSON guarantees preserved. The v1.0 trigger condition
is documented in [ARCHITECTURE.md](ARCHITECTURE.md).
NEW:
FAMP is local-first and federated. Local: a UDS-backed broker for same-host agent
messaging with zero crypto on the local path. Federated: cross-host messaging via
a `famp-gateway` wrapping that local bus, all of v0.5.2's
signature/canonical-JSON guarantees preserved. The gateway shipped in v1.0; the
layered model is documented in [ARCHITECTURE.md](ARCHITECTURE.md).

--- R3. Under-the-hood sentence (README.md:26-27) ---
OLD:
and envelope types, and a 5-state task FSM. The local bus is the v0.11
runtime path; federation transport internals remain preserved for v1.0.
NEW:
and envelope types, and a 5-state task FSM. The local bus is the same-host
runtime path; the preserved federation transport internals are now wired into
the shipped `famp-gateway`.

--- R4. Profile bullets (README.md:29-32) ---
OLD:
- **Local (v0.11)** — same-host agents through a local UDS broker (persistent daemon, or auto-spawn for unsandboxed clients).
- **Federation Profile (v1.0)** — cross-host protocol, Agent Cards,
  delegation, provenance, and remote routing via `famp-gateway`.
  See the [design spec](docs/superpowers/specs/2026-04-17-local-first-bus-design.md).
NEW:
- **Local (v0.11, shipped)** — same-host agents through a local UDS broker (persistent daemon, or auto-spawn for unsandboxed clients).
- **Federation Profile (v1.0, shipped)** — cross-host remote routing via
  `famp-gateway`, over a signed wire with two-machine TOFU trust. Agent Cards,
  delegation, and the provenance graph are not part of the shipped v1.0 surface —
  see [Not Shipped Yet](#not-shipped-yet).
  See the [design spec](docs/superpowers/specs/2026-04-17-local-first-bus-design.md).

--- R5. inbox list bullet (README.md:57) ---
OLD:
  - `famp inbox list` — inspect received messages (includes posts from joined channels; `--include-terminal` is accepted but currently a no-op — broker-side terminal filtering is deferred to v1)
NEW:
  - `famp inbox list` — inspect received messages (includes posts from joined channels; `--include-terminal` is accepted but currently a no-op — broker-side terminal filtering did not land in v1.0 and remains deferred)

NOTE: README.md:221's `--include-terminal` mention carries no version claim. Leave it.

--- R6. "Not Shipped Yet" group label (README.md:83) ---
OLD:
**v1.0 — Federation Profile** (after v0.11), still deferred:
NEW:
**Deferred past v1.0** — the next milestone is not yet defined:

Leave every bullet under it unchanged EXCEPT R7.

--- R7. Replay-defense bullet (README.md:90) — GUARDED, see below ---
FIRST run: `grep -rn "seen_nonce\|replay_cache\|nonce_cache\|nonce_seen" crates/famp-gateway/src/`
If that grep returns ANY match: SKIP R7 entirely, leave the bullet as-is, and record
the match in the SUMMARY. If it returns nothing (planner observed nothing), apply:
OLD:
- replay defense / freshness windows / idempotency scoping
NEW:
- replay defense and idempotency scoping — the cross-host envelope carries `nonce`
  and `expiry` and is format-validated at ingress, but no seen-nonce cache rejects
  a re-sent envelope

--- R8. v0.8 escape-hatch paragraph (README.md:294-296) ---
OLD:
for the migration path; the `v0.8.1-federation-preserved` git tag is the
escape hatch for users who genuinely need cross-host messaging today (frozen,
bug fixes ship via the v1.0 federation gateway when it lands).
NEW:
for the migration path; the `v0.8.1-federation-preserved` git tag is the frozen
reference point for the pre-v1.0 cross-host path. Cross-host messaging now ships
via `famp-gateway` — see [docs/GATEWAY-SETUP.md](docs/GATEWAY-SETUP.md).

--- R9. Current Milestones tail (README.md:739-744) ---
OLD:
- `v0.11`: **broker daemon & cross-tool bootstrap** — shipped, current
  runtime. `famp daemon install` runs a service-managed broker (launchd /
  systemd `--user`) so Claude Code and Codex connect to a persistent broker
  instead of relying on per-client auto-spawn; version handshake at connect.
- `v1.0`: federation profile — after v0.11. Agent Cards, delegation,
  provenance, cross-host via a `famp-gateway` process.
NEW:
- `v0.11`: **broker daemon & cross-tool bootstrap** — shipped.
  `famp daemon install` runs a service-managed broker (launchd /
  systemd `--user`) so Claude Code and Codex connect to a persistent broker
  instead of relying on per-client auto-spawn; version handshake at connect.
- `v1.0`: **federation profile — gateway core** — shipped 2026-07-29, tagged
  `v1.0.0`, current runtime. Cross-host messaging via a `famp-gateway` process:
  remote principals proxied onto the local bus over an Ed25519-signed (INV-10)
  cross-host wire with two-machine TOFU trust. Agent Cards, delegation, and
  provenance are not in the shipped surface — see
  [Not Shipped Yet](#not-shipped-yet).
- Next milestone: **not yet defined.** **Gate B** — the conformance vector pack,
  which fires when a second implementer commits to interop — remains open.

STEP 3 — Confirm README.md:71 (`Federation gateway (v1.0, shipped)` bullet) and the
Quick Start fence are byte-identical to HEAD:
`git diff README.md | grep -E '^[-+].*(Federation gateway \(v1\.0, shipped\)|cargo install famp|famp install-claude-code|/famp-register)'`
must print NOTHING. If it prints anything, you moved a frozen anchor — revert that hunk.
  </action>

  <verify>
    <automated>cargo build -p famp --bin famp 2>&1 | tail -3 && cargo test -p famp --test readme_line_count_gate 2>&1 | tee /tmp/famp-sw0-readme.log | grep -qE 'test result: ok\. 3 passed' && cargo test -p famp --test gateway_setup_doc_accuracy 2>&1 | tee /tmp/famp-sw0-gw.log | grep -qE 'test result: ok\. 1 passed' && git diff --name-only | sort | tr '\n' ',' | grep -qx 'README.md,' && git diff README.md | grep -cE '^[-+].*(Federation gateway \(v1\.0, shipped\)|brew install famp|/famp-msg)' | grep -qx 0 && echo GATES_OK</automated>
  </verify>

  <done>
README.md carries no sentence framing the v1.0 gateway as future/unlanded/trigger-gated;
the status line, lead paragraph, profile bullets, Not-Shipped-Yet label, v0.8 escape-hatch
paragraph, and Current Milestones tail all read post-v1.0.0. Both doc gates pass with
3 and 1 matched tests respectively (non-zero, verified by grep on the reported count).
`git diff --name-only` is exactly `README.md`. No frozen anchor appears on a `+`/`-` line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ARCHITECTURE.md — crate count, layer table, and a v1.0 section</name>
  <files>ARCHITECTURE.md</files>

  <action>
No test reads ARCHITECTURE.md (confirmed in Task 1 STEP 1). Apply each edit exactly.

--- A1. include_terminal deferral (ARCHITECTURE.md:34-35) ---
OLD:
the wire but currently a no-op — broker-side terminal-FSM filtering is
deferred to v1 (would require the famp-bus actor to read famp-taskdir,
NEW:
the wire but currently a no-op — broker-side terminal-FSM filtering did not
land in v1.0 and remains deferred (would require the famp-bus actor to read famp-taskdir,

--- A2. v0.9 section header (ARCHITECTURE.md:44) ---
OLD:
## v0.9 — local-first bus (shipping at v0.9.0 tag)
NEW:
## v0.9 — local-first bus (shipped at the v0.9.0 tag)

--- A3. MCP contract sentence (ARCHITECTURE.md:67-69) ---
OLD:
  `famp_peers`, `famp_join`, `famp_leave` — the same contract that will
  gain transparent remote routing in v1.0.
NEW:
  `famp_peers`, `famp_join`, `famp_leave` — the same contract that gained
  transparent remote routing in v1.0.

--- A4. Layer table row 2 (ARCHITECTURE.md:77) ---
OLD:
| 2 — Federation gateway (v1.0) | Cross-host | `famp-gateway` (new), reuses `famp-transport-http`, `famp-keyring` | HTTPS + canonical JSON + Ed25519 | Full |
NEW:
| 2 — Federation gateway (v1.0, shipped) | Cross-host | `famp-gateway`, reuses `famp-transport-http`, `famp-keyring` | HTTPS + canonical JSON + Ed25519 | Full |

--- A5. Layer-status paragraph (ARCHITECTURE.md:79-82) ---
OLD:
Layer 0 is untouched by v0.9. Layer 1 is net-new. Layer 2 is designed in
v0.9 but not built — its internals (`famp-transport-http`,
`famp-keyring`) stay compiling and tested in CI so they don't rot before
being wrapped.
NEW:
Layer 0 is untouched by v0.9. Layer 1 is net-new. Layer 2 was designed in
v0.9 but not built then — its internals (`famp-transport-http`,
`famp-keyring`) stayed compiling and tested in CI so they would not rot
before being wrapped, which `famp-gateway` did in v1.0.

--- A6. NEW SECTION — insert between the v0.9 design-spec link (ends line 85) and
`## Pre-v0.9 scaffolding` (line 87) ---
Anchor the Edit on the two lines that bracket the insertion point:
OLD:
[`docs/superpowers/specs/2026-04-17-local-first-bus-design.md`](docs/superpowers/specs/2026-04-17-local-first-bus-design.md).

## Pre-v0.9 scaffolding
NEW:
[`docs/superpowers/specs/2026-04-17-local-first-bus-design.md`](docs/superpowers/specs/2026-04-17-local-first-bus-design.md).

## v1.0 — federation gateway (shipped at the v1.0.0 tag, 2026-07-29)

Layer 2 is built. `famp-gateway` is a separate process that connects to the
existing local bus rather than replacing it. It backs each remote principal with
a `bind_as` connection reporting the gateway's own live local PID, so the
broker's same-host `kill(pid, 0)` liveness check needed no change — `famp-bus` is
untouched by v1.0. One gateway backs multiple remote principals concurrently
with no cross-talk.

- **Wire.** Every cross-host envelope is Ed25519-signed over RFC 8785 canonical
  JSON under the `FAMP-sig-v1\0` domain prefix (INV-10), and is rejected before
  it touches the local bus if unsigned or invalid. The envelope carries
  sender/receiver domain, `sender_key_id`, `nonce`, and `expiry`;
  capability/approval fields are omitted when empty. Federation fields are
  stripped before the onward local `Send`, so relayed envelopes stay decodable
  by `famp inbox` / `famp inspect` (BUS-11).
- **Trust.** Hand-copied keys via out-of-band `famp peer export` /
  `famp peer import` with TOFU pinning. Unpinned keys are rejected; there is no
  implicit peering.
- **Addressing.** `famp send --to agent:<domain>/<name>` from the shipping client
  reaches a remote principal. A successful send confirms local acceptance into
  the gateway-backed outbound mailbox, not remote delivery — verify the far side
  with `famp inspect tasks`.
- **Scope.** Two machines under one operator, over a network that operator
  already runs. No public relay, no NAT traversal, no cross-person trust, no
  signed peer directory, no capability/approval plane.

Setup guide: [`docs/GATEWAY-SETUP.md`](docs/GATEWAY-SETUP.md).

## Pre-v0.9 scaffolding

--- A7. Transport-crate guidance (ARCHITECTURE.md:104-106) ---
OLD:
- **Transport crates are federation-specific.** `famp-transport-http`,
  `famp-keyring` — will be wrapped by `famp-gateway` in v1.0, not by
  `famp-bus` in v0.9. Treat them as v1.0 internals.
NEW:
- **Transport crates are federation-specific.** `famp-transport-http`,
  `famp-keyring` — wrapped by `famp-gateway` (v1.0), not by `famp-bus`.
  Treat them as federation internals.

--- A8. Crate count + version (ARCHITECTURE.md:113-114) ---
OLD:
All 15 workspace crates at v0.11.0. Dependencies listed are intra-workspace
NEW:
All 16 workspace crates at v1.0.0. Dependencies listed are intra-workspace

--- A9. Add the missing famp-gateway table row (ARCHITECTURE.md, after the
`famp-transport-http` row, before the `famp` (binary) row) ---
OLD:
| `famp-transport-http` | 2 (v1.0) | HTTPS transport binding for federation gateway; wraps `famp-keyring` for peer auth | `famp-core`, `famp-envelope`, `famp-crypto`, `famp-keyring`, `famp-transport`, `famp-canonical` |
| `famp` (binary) | CLI |
NEW:
| `famp-transport-http` | 2 (v1.0) | HTTPS transport binding for federation gateway; wraps `famp-keyring` for peer auth | `famp-core`, `famp-envelope`, `famp-crypto`, `famp-keyring`, `famp-transport`, `famp-canonical` |
| `famp-gateway` (binary) | 2 | Federation gateway process: proxies remote principals onto the local bus; signs egress and verifies inbound envelopes before relay | `famp-bus`, `famp`, `famp-keyring`, `famp-envelope`, `famp-crypto`, `famp-transport`, `famp-transport-http` |
| `famp` (binary) | CLI |

(Truncate the OLD/NEW `famp` (binary) line at `| CLI |` only if the Edit tool needs
more anchor context; otherwise anchor on the full `famp-transport-http` row alone and
append the new row after it.)

STEP FINAL — count the table rows and confirm the claim: the crate table must have
exactly 16 data rows to match "All 16 workspace crates".
  </action>

  <verify>
    <automated>test "$(grep -cE '^\| `famp' ARCHITECTURE.md)" = "16" && grep -q 'All 16 workspace crates at v1.0.0' ARCHITECTURE.md && grep -q '^## v1.0 — federation gateway (shipped at the v1.0.0 tag, 2026-07-29)$' ARCHITECTURE.md && grep -cE 'shipping at v0\.9\.0|will[[:space:]]*$|is designed in|will be wrapped|deferred to v1[^.]|All 15 workspace crates' ARCHITECTURE.md | grep -qx 0 && git diff --name-only | sort | tr '\n' ',' | grep -qx 'ARCHITECTURE.md,README.md,' && echo ARCH_OK</automated>
  </verify>

  <done>
ARCHITECTURE.md states 16 workspace crates at v1.0.0 and its table has 16 `famp*` data
rows including `famp-gateway`; a `## v1.0 — federation gateway (shipped ...)` section
exists describing the shipped gateway in present/past tense; no "shipping at", "is
designed in", "will be wrapped", "deferred to v1", or "All 15 workspace crates" text
remains. `git diff --name-only` is exactly README.md + ARCHITECTURE.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile CLAUDE.md and prove the three-file diff boundary</name>
  <files>CLAUDE.md</files>

  <action>
WARNING to record in the SUMMARY: every edit below lands INSIDE a GSD-managed block
(`<!-- GSD:project-start source:PROJECT.md -->` at CLAUDE.md:1-19 and
`<!-- GSD:architecture-start source:ARCHITECTURE.md -->` at CLAUDE.md:54-107). A future
`/gsd-docs-update` regenerating from PROJECT.md could revert C1. Task 2's
ARCHITECTURE.md edits are the durable source for C2-C4; PROJECT.md is out of scope for
this task (three-file constraint), so C1's drift risk is accepted and noted.

--- C1. Conformance target (CLAUDE.md:15) ---
OLD:
- **Conformance target**: Staged conformance is supported — each milestone tags conformance level achieved; vector pack ships in v1.0 alongside federation gateway.
NEW:
- **Conformance target**: Staged conformance is supported — each milestone tags conformance level achieved; the vector pack did NOT ship in v1.0 — it is gated on a second implementer committing to interop (Gate B, still open).

--- C2. Architecture lead paragraph (CLAUDE.md:57-62) ---
OLD:
**FAMP today is local-first** (v0.11, built on the v0.9 local bus): a
persistent, service-managed UDS broker daemon for same-host agent messaging,
with cross-tool bootstrap (Claude Code + Codex). **FAMP at v1.0 is
federated**: cross-host messaging via `famp-gateway` wrapping the local bus.
See [ARCHITECTURE.md](ARCHITECTURE.md) for the full layered model (Layer 0
protocol primitives -> Layer 1 local bus -> Layer 2 federation gateway).
NEW:
**FAMP today is local-first AND federated** (v1.0, shipped 2026-07-29): a
persistent, service-managed UDS broker daemon for same-host agent messaging
with cross-tool bootstrap (Claude Code + Codex), plus cross-host messaging via
`famp-gateway` wrapping that same local bus.
See [ARCHITECTURE.md](ARCHITECTURE.md) for the full layered model (Layer 0
protocol primitives -> Layer 1 local bus -> Layer 2 federation gateway).

--- C3. v0.11 paragraph label (CLAUDE.md:83) ---
OLD:
**v0.11 (shipped 2026-06-06, current runtime):** `famp daemon install`
NEW:
**v0.11 (shipped 2026-06-06):** `famp daemon install`

Leave the rest of that paragraph, and the whole `**v0.9 (shipped):**` paragraph
(CLAUDE.md:78-81), and the `Note: as of v0.8.x ...` paragraph (CLAUDE.md:72-76)
UNCHANGED — they are accurate past tense.

--- C4. Superseded readiness trigger (CLAUDE.md:89-95) ---
OLD:
**v1.0 readiness trigger (named):** v1.0 federation milestone fires
when Sofer (or a named equivalent) runs FAMP from a different machine
and exchanges a signed envelope. If 4 weeks pass after v0.9.0 ships
with no movement on this trigger, federation framing is reconsidered.
Concrete forcing function for the local-case-black-hole risk; the
conformance vector pack ships at the same trigger (deferred from
v0.5.1 wrap, see `.planning/WRAP-V0-5-1-PLAN.md` DEFERRED banner).
NEW:
**v1.0 (shipped 2026-07-29, tagged `v1.0.0`, current runtime):**
`famp-gateway` proxies remote principals onto the local bus over an
Ed25519-signed cross-host wire (INV-10) with two-machine TOFU trust;
`famp send --to agent:<domain>/<name>` from the shipping client reaches a
remote principal. Gate A closed: bidirectional signed exchange between two
machines one operator controls (macOS <-> Linux). **Gate B** — the conformance
vector pack, which fires when a second implementer commits to interop — is
still open. **The next milestone is not yet defined.**

Leave CLAUDE.md:102-106 (`**When working here:** ...`) UNCHANGED — it is accurate
now that both v0.9 and v1.0 have shipped.

STEP FINAL — three-file boundary proof. Run and record:
```
git diff --name-only | sort
git status --porcelain
```
`git diff --name-only | sort` must be exactly:
ARCHITECTURE.md
CLAUDE.md
README.md
and `git status --porcelain` must show exactly those three ` M` lines (`.planning/` is
gitignored, so plan/SUMMARY files must not appear). If a fourth path appears, revert it.
  </action>

  <verify>
    <automated>git diff --name-only | sort | tr '\n' ',' | grep -qx 'ARCHITECTURE.md,CLAUDE.md,README.md,' && test "$(git status --porcelain | wc -l | tr -d ' ')" = "3" && grep -cE 'current runtime\):\*\* `famp daemon install`|v1\.0 readiness trigger|vector pack ships in v1\.0|FAMP at v1\.0 is' CLAUDE.md | grep -qx 0 && grep -q 'The next milestone is not yet defined' CLAUDE.md && cargo build -p famp --bin famp >/dev/null 2>&1 && cargo test -p famp --test readme_line_count_gate 2>&1 | grep -qE 'test result: ok\. 3 passed' && cargo test -p famp --test gateway_setup_doc_accuracy 2>&1 | grep -qE 'test result: ok\. 1 passed' && echo ALL_GATES_OK</automated>
  </verify>

  <done>
CLAUDE.md's Architecture block presents v1.0 as shipped and current, no longer calls
v0.11 the current runtime, and carries no "v1.0 readiness trigger" paragraph; the
Conformance-target constraint says the vector pack did not ship in v1.0 and names Gate B
as open; "The next milestone is not yet defined" is present. `git diff --name-only`
is exactly the three target files, `git status --porcelain` has exactly 3 lines, and
both doc gates pass with 3 and 1 matched tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator action | A reader follows README/GATEWAY guidance to configure cross-host trust; a wrong claim here becomes a misconfiguration. |
| docs -> test gates | Two Rust tests grep repo-root doc literals; doc text is load-bearing on CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sw0-01 | Tampering | README.md frozen literals | high | mitigate | Task 1 STEP 1 audits every test-asserted doc string before editing; Task 1 STEP 3 + the `git diff` grep in every `<verify>` prove no frozen anchor appears on a +/- line. |
| T-sw0-02 | Information disclosure | New ARCHITECTURE.md v1.0 section | medium | mitigate | Every claim in A6 is sourced from `.planning/STATE.md` phase map + existing doc text. No new security-property claim; the "confirms local acceptance, not remote delivery" boundary is restated, never softened. |
| T-sw0-03 | Repudiation | Overstating shipped security posture | medium | mitigate | R7 is grep-guarded: the replay-defense claim is only narrowed after `grep` proves no seen-nonce cache exists; if a cache is found the edit is skipped. "Not Shipped Yet" bullets are otherwise left untouched rather than re-adjudicated without code verification. |
| T-sw0-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — no dependency is added, removed, or installed. Zero package-manager operations in this plan. |
</threat_model>

<verification>
1. `cargo build -p famp --bin famp` succeeds (needed by `gateway_setup_doc_accuracy`,
   which shells out to `famp peer/send --help`).
2. `cargo test -p famp --test readme_line_count_gate` reports `3 passed` — the count is
   asserted by grep so a zero-match run cannot pass silently.
3. `cargo test -p famp --test gateway_setup_doc_accuracy` reports `1 passed` — same
   non-vacuity guard.
4. `cargo test --workspace docs` — run it and record the reported count. The planner
   expects this filter to match ZERO tests (no test name contains `docs`), so a `0
   passed` result is NON-INFORMATIVE, not a pass. Items 2 and 3 are the real gates.
   Do not report item 4 as evidence of anything.
5. `git diff --name-only | sort` is exactly `ARCHITECTURE.md`, `CLAUDE.md`, `README.md`.
6. `git status --porcelain | wc -l` is exactly 3.
7. Do NOT run `just ci` / `just test` (cargo-nextest hangs in this repo). Do not run
   `just lint` — no Rust source changed.
</verification>

<success_criteria>
- Zero sentences across the three files frame the v1.0 federation gateway as future,
  unlanded, deferred, or trigger-gated.
- ARCHITECTURE.md: 16 crates at v1.0.0, table has 16 rows incl. `famp-gateway`, and a
  v1.0 section exists.
- Every "what's next" statement says the next milestone is not yet defined; Gate B is
  described as open, never scheduled; no v1.1 feature is named as planned.
- v0.8 / v0.9 / v0.11 past-tense passages are byte-identical to HEAD except where a
  stale "current runtime" / "deferred to v1" / "will be wrapped" claim was inside them.
- Both doc gates green with non-zero matched test counts; exactly three files modified.
</success_criteria>

<output>
Create `.planning/quick/260729-sw0-reconcile-post-v1-0-0-doc-version-framin/260729-sw0-SUMMARY.md` when done.

The SUMMARY MUST record:
- The STEP 1 frozen-anchor audit output (verbatim command results).
- Whether R7 was applied or skipped, and the guard grep's output.
- The three out-of-scope stale items listed in `<context>` (README "eight tools",
  README Repo Layout crate omissions, `famp-gateway` Cargo.toml "Layer 2 skeleton"),
  so they can be picked up as a follow-up quick task.
- The GSD-managed-block drift warning for CLAUDE.md C1 (PROJECT.md is the upstream
  source and was deliberately not edited).

Commit with `git add README.md ARCHITECTURE.md CLAUDE.md` — force-add nothing under
`.planning/` unless the orchestrator asks.
</output>
