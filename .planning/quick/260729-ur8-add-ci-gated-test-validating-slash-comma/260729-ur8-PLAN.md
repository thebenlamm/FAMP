---
phase: quick-260729-ur8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/famp/tests/slash_command_assets.rs
  - .github/workflows/asset-gate.yml
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]
must_haves:
  truths:
    - "On a clean tree `cargo test -p famp --test slash_command_assets` reports `8 passed; 0 failed` (3 pre-existing + 5 new). A `0 passed` run is a FAILURE, not a pass."
    - "With famp-send.md's `peer` bullet replaced by the historical broken `to` bullet, `slash_command_assets_prescribe_only_real_argument_keys` FAILS naming the offending key and the tool it was prescribed for."
    - "`test_famp_who_allowed_tools_lists_only_famp_peers` passes in BOTH the broken and the restored state (control proving the harness still executes)."
    - "Every `mcp__famp__famp_*` tool an asset names resolves to a descriptor in the registry parsed out of server.rs AND to a `dispatch_tool` match arm."
    - "Every numeric tool-count claim written in an asset equals the parsed registry length; a stale count fails the build."
    - "Adding an 8th file to assets/slash_commands/ without registering it in the test fails `slash_command_asset_harness_is_not_vacuous`."
    - "Both commit paths reach the gate: a `.rs`-touching commit via ci.yml's existing `test` job (`cargo nextest run --workspace`), and an assets-only `.md` commit via the new `.github/workflows/asset-gate.yml`."
    - "`.github/workflows/ci.yml` is byte-identical to HEAD after this plan — `git diff --exit-code -- .github/workflows/ci.yml` is clean."
  artifacts:
    - "crates/famp/tests/slash_command_assets.rs — extended in place, module doc de-staled, existing 3 tests untouched"
    - ".github/workflows/asset-gate.yml — NEW, purely additive workflow triggering on the assets-only .md commit path; ci.yml untouched"
    - "SUMMARY.md — records the observed broken-state failure text AND the restored-state pass count"
  key_links:
    - "include_str!(\"../src/cli/mcp/server.rs\") -> the `serde_json::json!([...])` array literal inside `fn tool_descriptors()` is the ONLY source of truth for tool names, inputSchema properties, and required keys."
    - "asset `mcp__famp__famp_X` reference -> registry descriptor `name` -> `\"famp_X\" =>` arm in `dispatch_tool` (a descriptor with no arm is a lie the asset would act on)."
    - "asset-gate.yml `paths` -> must list the asset subtree, the test file, AND itself; a path omitted here is a silently ungated commit shape."
    - "asset-gate.yml `concurrency.group` MUST NOT equal ci.yml's `ci-<ref>` group, or the two workflows cancel each other on the same ref."
---

<objective>
Close the detection gap that let `crates/famp/assets/slash_commands/*.md` ship instructions
the real MCP parser never honours. Two bugs already escaped this way: `famp-send.md` /
`famp-channel.md` prescribed a `to: {"kind":…, "name":…}` shape `famp_send` never reads
(producing `EnvelopeInvalid: missing required field: mode` for any user who followed it), and
`famp-who.md` advertised a stale tool count for months. The assets are fixed; the GATE is not.

Purpose: make the asset text mechanically answerable to the tool schema the model actually
receives, so this bug class cannot recur silently.
Output: 5 new tests in the existing `slash_command_assets.rs` integration target, plus the CI
path-filter fix that makes an asset-only edit actually run them.

Note on TDD: the deliverable IS a test suite, so task-level `tdd="true"` does not apply.
Task 2's falsification protocol supplies the equivalent RED/GREEN evidence.
</objective>

<execution_context>
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/workflows/execute-plan.md
@/Users/benlamm/Workspace/FAMP/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/benlamm/Workspace/FAMP/CLAUDE.md
@/Users/benlamm/Workspace/FAMP/crates/famp/tests/slash_command_assets.rs

# READ-ONLY reference, for its job idiom and concurrency group only.
# Per GT-5a this file must NOT be modified by this plan.
@/Users/benlamm/Workspace/FAMP/.github/workflows/ci.yml
</context>

<verified_ground_truth>
The planner executed these checks against the working tree on 2026-07-29. Trust them; they are
the load-bearing premises of the whole plan. Re-run any one you build on if you touch it.

**GT-1 — the `json!` body is strict-JSON parseable.** Slicing server.rs from
`serde_json::json!(` inside `fn tool_descriptors() -> serde_json::Value {` up to the unique
terminator `\n    ])\n}` yields a string starting `[` and ending `]` that `json.loads`
accepted, giving 12 descriptors. No Rust interpolation, no comments, no trailing commas inside
the literal. The terminator occurs EXACTLY ONCE in the whole file.

**GT-2 — parsed registry (order preserved):** famp_send, famp_await, famp_inbox,
famp_channel_log, famp_peers, famp_register, famp_set_listen, famp_whoami, famp_verify,
famp_join, famp_leave, famp_inspect_waiters. `famp_send.inputSchema.properties` =
{body, channel, expect_reply, mode, more_coming, peer, task_id, title};
`famp_send.inputSchema.required` = ["mode"]. All 12 names also appear as `"famp_X" =>` arms in
`dispatch_tool` (5 free-pass arms at server.rs:447-451, 7 gated arms at 464-470).

**GT-3 — the extraction rules in Task 2 were run against all 7 real assets and produced:**
famp-channel.md tool=famp_send keys={body,channel,mode,title};
famp-send.md tool=famp_send keys={body,mode,peer,title};
famp-join.md tool=famp_join keys={channel};
famp-leave.md tool=famp_leave keys={channel};
famp-inbox.md tool=famp_inbox keys={} (`include_terminal` is prose-only, correctly not captured);
famp-register.md tool=famp_register keys={};
famp-who.md tool=famp_peers keys={} count-claims=[12, 12].
Every asset references exactly ONE distinct tool. Under the falsification patch, famp-send.md
yields keys={body,mode,title,**to**} — and `to` is not a famp_send property, which is the RED.

**GT-4 — `famp-register.md` prescribes NO keys yet `famp_register` requires `identity`** (the
asset expresses it as prose, not a key). This is why the required-coverage check in Task 2 MUST
be conditional on a non-empty extracted key set. Unconditional = instant false failure.

**GT-5 — one of the two commit paths already reaches the gate; the other is dark.**
Independently re-read by the coordinator on 2026-07-29; treat as ground truth.

- COVERED (`.rs`-touching commits): ci.yml's `test (${{ matrix.os }})` job runs
  `cargo nextest run --workspace --profile ci` (ci.yml:104-119, `needs: [test-canonical,
  test-crypto]`) on ubuntu + macos. `--workspace` picks up
  `crates/famp/tests/slash_command_assets.rs` automatically as an integration target — no
  registration needed. `clippy` (ci.yml:40-49) runs `--all-targets`, so the new test code is
  lint-gated too. nextest works fine on the CI runners; only the LOCAL invocation hangs
  (see `<verification>`).
- DARK (assets-only `.md` commits): ci.yml lines 3-15 exclude `**/*.md` from both `push` and
  `pull_request`. The slash-command assets ARE `.md`, so an asset-only edit produces ZERO
  check-runs and the gate never fires on the exact change it exists to catch.

**GT-5a — DECISION (Ben, 2026-07-29): ci.yml is NOT to be touched.** Its exclude-list block
stays exactly as-is. Rewriting its trigger surface into a hand-maintained include-list would put
every job in that file behind a list where one omitted path silently un-gates the whole repo.
Task 3 instead adds a NEW, purely additive workflow covering only the dark path — zero blast
radius on existing CI.
</verified_ground_truth>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end registry gate — parse server.rs as text, validate every asset tool name, kill the vacuous pass</name>
  <files>crates/famp/tests/slash_command_assets.rs</files>
  <read_first>
crates/famp/src/cli/mcp/server.rs lines 34-52 (the `json!` opening, first descriptor, and the
shape of an inputSchema properties block), line 174 (`    ])` terminator), lines 446-475
(`dispatch_tool` free-pass + gated match arms).
  </read_first>
  <behavior>
    - `slash_command_asset_harness_is_not_vacuous` fails if: the asset dir holds a number of
      `.md` files other than the number the test embeds; the parsed registry length is not 12;
      any asset references a number of distinct tools other than exactly 1.
    - `slash_command_assets_reference_only_dispatchable_mcp_tools` fails if an asset names a
      tool absent from the parsed registry, or present in the registry but with no
      `dispatch_tool` arm.
    - Both pass on the current tree.
  </behavior>
  <action>
Extend the EXISTING file. Do not create a second test file. Do not touch any file under
`crates/famp/src/` — `tool_descriptors()` is private and another window is editing server.rs;
if you conclude a src change is required, STOP and report instead of editing.

Keep the two existing inner attributes at the top of the file verbatim
(`unused_crate_dependencies`, and the `unwrap_used`/`expect_used` pair) — `just lint` denies
both clippy lints workspace-wide.

Rewrite the `//!` module doc. It currently pins a hardcoded surface count in prose, which is
the same rot this task exists to prevent. The replacement must state, WITHOUT writing any
digit-followed-by-"tools" phrase: that the module gates all slash-command assets against the
MCP tool registry; that the registry is parsed out of `src/cli/mcp/server.rs` text because
`tool_descriptors()` is private and this is an integration-test crate; that the count is pinned
in code by `slash_command_asset_harness_is_not_vacuous` rather than in prose; and that the
three original CC-07 famp-who tests below are retained. Note the paired unit test
`tool_descriptors_has_exactly_twelve_named_tools` inside server.rs so a future editor changing
the registry sees both gates.

Add `const SERVER_RS: &str = include_str!("../src/cli/mcp/server.rs");`.

Add a private helper `registry() -> serde_json::Value` implementing GT-1: locate
`fn tool_descriptors() -> serde_json::Value {`, then within that suffix locate
`serde_json::json!(` and take the byte just after it as the slice start; locate the terminator
`"\n    ])\n}"` and take its index plus 6 as the slice end (the terminator's first 5 bytes are
the newline and four spaces, so index+5 is the `]` and index+6 is one-past-it). Every lookup
uses `.expect(...)` with a message that names the anchor that went missing and says the parser
needs updating — a formatting change in server.rs must fail LOUD, never silently match less.
Assert the slice starts with `[` and ends with `]`, then `serde_json::from_str` it, with an
expect message instructing the reader to check for a non-JSON Rust expression inside the
`json!` literal. `serde_json` is a regular dependency of the famp package, so it is available
to this integration target with no Cargo.toml change.

Add helpers over that value: `tool_names(&Value) -> BTreeSet<String>`,
`properties_of(&Value, &str) -> BTreeSet<String>` reading
`inputSchema.properties` keys (empty set when the properties object is empty, as for
famp_peers / famp_whoami / famp_inspect_waiters), and `required_of(&Value, &str) ->
BTreeSet<String>` reading `inputSchema.required` (empty when absent).

Add `const ASSETS: &[(&str, &str)]` with one entry per asset: the file name and its
`include_str!("../assets/slash_commands/<name>.md")`. All 7 per GT-3. Keep the existing
`FAMP_WHO_MD` const and the 3 tests that use it byte-identical.

Add `referenced_tools(&str) -> BTreeSet<String>`: for every occurrence of the literal
`mcp__famp__`, collect the immediately following run of `[a-z0-9_]` characters. No regex crate
needed; a `match_indices` + `char_indices` walk is enough.

Add `asset_dir_md_count() -> usize` using `std::fs::read_dir` over
`concat!(env!("CARGO_MANIFEST_DIR"), "/assets/slash_commands")` — `env!` resolves at compile
time so there is no runtime env dependency. Count entries whose `Path::extension()` equals
`Some(OsStr::new("md"))`. Do NOT use `.ends_with(".md")`: clippy pedantic
`case_sensitive_file_extension_comparisons` is denied here and will fail `just lint`.

Write `slash_command_asset_harness_is_not_vacuous` asserting (a) `asset_dir_md_count()` equals
`ASSETS.len()`, with a message telling the reader to add the new asset to `ASSETS` — this is
what stops a newly added asset from being silently ungated; (b) `tool_names(&registry()).len()
== 12`, message cross-referencing server.rs's own
`tool_descriptors_has_exactly_twelve_named_tools` so a deliberate registry change fails in two
named places at once instead of drifting; (c) for each asset, `referenced_tools` has exactly
one element, message explaining that a multi-tool asset makes per-key validation ambiguous and
needs an explicit asset-to-tool mapping added before it can ship.

Write `slash_command_assets_reference_only_dispatchable_mcp_tools`: for each asset and each
referenced tool, assert the name is in `tool_names(&registry())`, then assert
`SERVER_RS.contains(&format!("\"{tool}\" =>"))` so a descriptor that exists but has no
`dispatch_tool` arm also fails. Use inlined format args throughout (clippy pedantic
`uninlined_format_args` is denied).
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; cargo test -p famp --test slash_command_assets 2>&amp;1 | tail -20</automated>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; grep '^//!' crates/famp/tests/slash_command_assets.rs | grep -nE '[0-9]+ tools' ; test $? -eq 1</automated>
  </verify>
  <done>
`cargo test -p famp --test slash_command_assets` reports `5 passed; 0 failed` (3 pre-existing +
2 new). Report the literal count from the output — an empty or `0 passed` run is a FAILURE, not
a pass. The `//!` doc block contains no digit-followed-by-`tools` phrase (second verify command
exits 0 only when that grep found nothing). The 3 original famp-who tests are unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Argument-key + count-claim gates, then falsify them with a named must-fail and a named must-pass control</name>
  <files>crates/famp/tests/slash_command_assets.rs, crates/famp/assets/slash_commands/famp-send.md (temporary, reverted)</files>
  <behavior>
    - `slash_command_assets_prescribe_only_real_argument_keys` fails when an asset prescribes a
      key that is not a property of the tool it names. This is the gate that would have caught
      the shipped `to` bug.
    - `slash_command_assets_prescribe_every_required_argument_key` fails when an asset that
      prescribes at least one key omits one of that tool's `required` keys. Skipped entirely for
      assets with an empty key set (GT-4).
    - `slash_command_asset_tool_count_claims_match_registry` fails when a numeric tool-count
      claim in asset prose diverges from the parsed registry length.
    - All three pass on the current tree; the first FAILS under the falsification patch.
  </behavior>
  <action>
Add `is_key_ident(&str) -> bool`: non-empty, first char is `a-z` or `_`, every char is `a-z`,
`0-9`, or `_`.

Add `prescribed_keys(&str) -> BTreeSet<String>` using the backtick-split trick — for a line,
`line.split('`')` alternates outside/inside spans, so odd indices are the backtick-quoted
spans. Two rules, unioned, verified against all 7 real assets per GT-3:

Rule A (bullet form, e.g. the `peer` / `mode` / `title`-or-`body` bullets in famp-send.md): if
the trimmed line starts with `- ` and the line contains `": "`, take the portion before the
FIRST `": "` and collect every odd-index backtick span of that portion that satisfies
`is_key_ident`. Taking the head before the first `": "` is what makes the broken
`to`-with-JSON-blob bullet yield `to` rather than the keys nested inside its JSON value, and
the odd-index sweep is what catches BOTH keys on the `title`-or-`body` bullet.

Rule B (inline form, e.g. famp-join.md / famp-leave.md): for every odd-index backtick span on
any line, if the span contains `:` and the portion before the first `:` satisfies
`is_key_ident`, that portion is a key. This deliberately skips spans like the
`{ online: [...] }` shape in famp-who.md (head is not an identifier) and bare spans with no
colon such as tool names or `include_terminal` in prose.

Write `slash_command_assets_prescribe_only_real_argument_keys`: for each asset, resolve its
single referenced tool (Task 1's helper), take `properties_of` that tool, and assert every
prescribed key is a member. The failure message MUST name the asset file, the offending key,
the tool, and the sorted set of keys that tool actually accepts — the whole point is that the
next person sees the fix without opening server.rs.

Write `slash_command_assets_prescribe_every_required_argument_key`: for each asset whose
prescribed key set is NON-EMPTY, assert every entry of `required_of(tool)` is present. Add a
comment explaining the non-empty guard: famp-register.md expresses `identity` as prose rather
than as a prescribed key, so an unconditional check is a false failure (GT-4).

Write `slash_command_asset_tool_count_claims_match_registry`: for each asset, scan
`match_indices` of the literals `-tool` and ` tools`; for each hit take the text before it and
read the trailing run of ASCII digits; if there is no trailing digit run, skip the hit (this is
what makes `allowed-tools` and the bare phrase `tool listed` inert). Parse each captured digit
run and assert it equals `tool_names(&registry()).len()`. Accumulate the claims found and
assert the total across all assets is at least 1, so a future rewrite that removes every
countable claim cannot turn this test into a silent no-op. GT-3 says famp-who.md yields exactly
two claims today, both 12.

Do not edit any asset except the temporary falsification patch below, and do not change any
asset's file mode (they are 0644 and three existing tests pin famp-who.md's content).

FALSIFICATION (mandatory, both observations get recorded in SUMMARY.md):
1. In `crates/famp/assets/slash_commands/famp-send.md`, replace the bullet that prescribes
   `peer` with the historical broken bullet: a `- ` bullet whose backticked key is `to` and
   whose backticked value is the JSON object with `kind` set to agent and `name` set to `$1`.
2. Run `cargo test -p famp --test slash_command_assets`. REQUIRED observation:
   `slash_command_assets_prescribe_only_real_argument_keys` FAILS, naming `to`. Copy the
   verbatim failure line. Also REQUIRED: `test_famp_who_allowed_tools_lists_only_famp_peers`
   still PASSES in this same run — that is the control proving the harness itself executed
   rather than the target being skipped. If the run is green, or if the control did not run,
   the gate is worthless and this task is NOT satisfied.
3. `git checkout -- crates/famp/assets/slash_commands/famp-send.md`, then re-run and confirm
   all tests pass. Record the exact pass count.
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; git checkout -- crates/famp/assets/slash_commands/famp-send.md &amp;&amp; cargo test -p famp --test slash_command_assets 2>&amp;1 | tail -20</automated>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; git diff --name-only -- crates/famp/assets/slash_commands/ ; test -z "$(git diff --name-only -- crates/famp/assets/slash_commands/)"</automated>
  </verify>
  <done>
Clean tree reports `8 passed; 0 failed` (3 pre-existing + 5 new) — report the literal count.
The broken-state run was observed to FAIL specifically on
`slash_command_assets_prescribe_only_real_argument_keys` while
`test_famp_who_allowed_tools_lists_only_famp_peers` PASSED, and both observations are written
into SUMMARY.md. Green under both states means zero information and does not satisfy this task.
`git diff` over `crates/famp/assets/slash_commands/` is empty — every asset is byte-identical
to HEAD.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cover the dark commit path with a new additive workflow, then lint</name>
  <files>.github/workflows/asset-gate.yml</files>
  <read_first>.github/workflows/ci.yml lines 20-27 (concurrency + top-level env) and lines 29-49 (the fmt-check / clippy job idiom to mirror: checkout, toolchain, cache, run). READ ONLY — ci.yml must not be modified.</read_first>
  <reversibility rating="reversible">New file, additive only. Revert is `git rm`. No existing workflow, job, or trigger is altered, so blast radius on current CI is zero.</reversibility>
  <precondition>`.github/workflows/asset-gate.yml` does not already exist (this task creates it; if a file is there, stop and reconcile rather than overwrite).</precondition>
  <action>
Per GT-5/GT-5a: the `.rs` commit path is ALREADY covered by ci.yml's existing `test` job via
`cargo nextest run --workspace --profile ci`, and `clippy --workspace --all-targets` already
compiles the new test. Do not re-litigate that. The only dark path is an assets-only `.md`
commit, and ci.yml is OFF LIMITS — do not edit it, not even a comment. Cover the dark path with
a NEW file instead.

Create `.github/workflows/asset-gate.yml` with exactly this structure:

- `name`: a short human name for the gate, e.g. `asset-gate`.
- Top-level `env` mirroring ci.yml: `CARGO_TERM_COLOR: always` and `RUSTFLAGS: "-D warnings"`.
- `concurrency`: `group` set to an asset-gate-prefixed expression interpolating
  `github.ref`, plus `cancel-in-progress: true`. The group MUST NOT be ci.yml's `ci-`-prefixed
  group — a collision makes the two workflows cancel each other on the same ref.
- `on.push` and `on.pull_request`, each with `branches: ["**"]` (mirroring ci.yml) and an
  identical `paths` list of exactly three entries: `crates/famp/assets/slash_commands/**`,
  `crates/famp/tests/slash_command_assets.rs`, and `.github/workflows/asset-gate.yml`. The
  third entry is self-gating so an edit to the gate is itself gated. Use a plain include-list —
  there are no negated patterns here, so no ordering subtlety applies.
- Exactly ONE job, `runs-on: ubuntu-latest` (single OS is correct: this test is pure text
  parsing of in-repo files with no OS-dependent behavior, unlike ci.yml's matrixed jobs).
  Steps, in ci.yml's idiom: `actions/checkout@v4`, then `dtolnay/rust-toolchain@stable`, then
  `Swatinem/rust-cache@v2`, then a `run` of `cargo test -p famp --test slash_command_assets`.

Do NOT add `cargo-nextest` or `taiki-e/install-action` to this file — plain `cargo test` on the
one test binary, no `--workspace`. That keeps the gate fast and independent of the nextest
install step.

Add a short YAML comment at the top explaining why this file exists separately from ci.yml:
ci.yml excludes `**/*.md` from its triggers, the slash-command assets are `.md`, so an
assets-only commit would otherwise produce zero check-runs; and reference quick-task id
260729-ur8 plus `crates/famp/tests/slash_command_assets.rs` as the gate being kept reachable.

Then verify the CI wiring claim on BOTH paths and record both in SUMMARY.md — do not guess:
(1) the `.rs` path — quote the ci.yml line showing the `test` job's
`cargo nextest run --workspace --profile ci` invocation as the evidence that `--workspace`
already reaches the new test target; (2) the `.md` path — the parser check below proves
asset-gate.yml's `paths` covers the assets subtree.

Validate the YAML with a parser, not by eyeball. GOTCHA: PyYAML follows YAML 1.1, where the
bare key `on` parses as the BOOLEAN `True`, not the string `"on"` — so a lookup must fall back
from `"on"` to `True` or it will spuriously report the trigger block missing. The verify command
below already does this; keep that fallback if you rewrite it.

Do NOT run `just install`; no tool surface changed, so the installed binary is irrelevant here.
State that explicitly in SUMMARY.md rather than running it.

Finally run `just lint` (which is `cargo clippy --workspace --all-targets -- -D warnings`, and
this repo denies clippy `pedantic` and warns `nursery`). Plain clippy is not a substitute. Fix
any finding in the new test code only; do not touch other crates.
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; python3 -c 'import yaml; d=yaml.safe_load(open(".github/workflows/asset-gate.yml")); t=d.get("on", d.get(True)); p=set(t["push"]["paths"]); need={"crates/famp/assets/slash_commands/**","crates/famp/tests/slash_command_assets.rs",".github/workflows/asset-gate.yml"}; assert need.issubset(p), p; assert set(t["pull_request"]["paths"])==p; g=d["concurrency"]["group"]; assert not g.startswith("ci-"), g; assert len(d["jobs"])==1, list(d["jobs"]); print("asset-gate.yml OK, group:", g)'</automated>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; git diff --exit-code -- .github/workflows/ci.yml &amp;&amp; echo "ci.yml untouched"</automated>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; just lint 2>&amp;1 | tail -5</automated>
    <automated>cd /Users/benlamm/Workspace/FAMP &amp;&amp; cargo test -p famp --test slash_command_assets 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
`asset-gate.yml` parses as YAML; both event blocks carry the identical three-entry `paths` list
(assets subtree + test file + itself); its `concurrency.group` does not collide with ci.yml's;
it declares exactly one job using plain `cargo test` with no nextest install.
`git diff --exit-code -- .github/workflows/ci.yml` is clean — ci.yml is byte-identical to HEAD.
`just lint` is clean. `cargo test -p famp --test slash_command_assets` still reports
`8 passed; 0 failed` — literal count reported. SUMMARY.md records the two-path CI-wiring
evidence (quoted ci.yml `--workspace` line for the `.rs` path, parser output for the `.md` path)
and the explicit "no `just install` needed, test-only change" note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo asset -> `~/.claude/commands/` | `famp install-claude-code` copies these `.md` files onto the operator's machine, where they become instructions the model executes. Asset text is a model-instruction supply chain. |
| asset instruction -> MCP `tools/call` arguments | The asset decides what JSON the model sends to `famp_send` et al. A wrong instruction is indistinguishable from a broken parser from the user's seat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ur8-01 | Tampering | `assets/slash_commands/*.md` drifting from the real tool schema | medium | mitigate | Tasks 1+2: assets are gated against the registry parsed from `server.rs`; unknown keys, unknown tools, undispatchable tools, and stale count claims all fail the build. |
| T-ur8-02 | Denial of Service (of the gate itself) | the new test harness | high | mitigate | Task 1 `slash_command_asset_harness_is_not_vacuous` + Task 2's minimum-one-claim assertion + Task 3's additive asset-gate workflow (including its self-gating `paths` entry). A gate that silently matches zero things, or never runs, is worse than no gate — this is the failure mode that let both original bugs ship. |
| T-ur8-03 | Tampering | dependency installs | low | accept | No package-manager installs in this plan; `serde_json` is already a `[dependencies]` entry of the `famp` package and reachable from the integration target with no Cargo.toml change. |
</threat_model>

<verification>
1. `cd /Users/benlamm/Workspace/FAMP && cargo test -p famp --test slash_command_assets` reports
   `8 passed; 0 failed`. Report the literal count from stdout. Note the repo trap: a filtered
   `cargo test` EXITS 0 ON ZERO MATCHES, so an empty run reads as success — do NOT add a name
   filter, run the whole target, and treat any count other than 8 as a failure to investigate.
2. `cargo nextest` HANGS **locally** in this repo — `just ci` and `just test` are UNUSABLE on
   this machine. Use the plain `cargo test` invocation above only. This is a local-only
   pathology: nextest runs fine on the CI runners, which is why ci.yml's `test` job is sound and
   needs no change (GT-5).
3. `just lint` clean (NOT plain clippy — `just lint` is the recipe CI mirrors and this repo
   denies clippy `pedantic`).
4. `git diff --name-only` plus `git status --porcelain` together list exactly
   `crates/famp/tests/slash_command_assets.rs` (modified) and
   `.github/workflows/asset-gate.yml` (new, untracked until added). Any `crates/famp/src/**`
   path, and any diff on `.github/workflows/ci.yml`, is a constraint violation — revert it.
5. CI wiring, both paths, asserted rather than assumed: the `.rs` path is covered by ci.yml's
   existing `test` job (`cargo nextest run --workspace --profile ci`, ci.yml:104-119) with the
   quoted line in SUMMARY.md; the assets-only `.md` path is covered by asset-gate.yml, proven by
   the YAML-parser check in Task 3.
6. Falsification evidence present in SUMMARY.md: broken-state failure line naming
   `slash_command_assets_prescribe_only_real_argument_keys`, plus the control
   `test_famp_who_allowed_tools_lists_only_famp_peers` observed passing in that same run.
</verification>

<success_criteria>
- 5 new tests in the existing integration target; 3 pre-existing famp-who tests unmodified.
- No file under `crates/famp/src/` modified. No `pub` added to `tool_descriptors()`.
- `.github/workflows/ci.yml` byte-identical to HEAD — not one comment, not one path entry.
- No asset file modified in the final tree (falsification patch reverted, modes still 0644).
- The module doc no longer pins a tool count in prose.
- The gate fires in CI on BOTH commit shapes: `.rs` edits via ci.yml's existing `--workspace`
  test job, assets-only `.md` edits via the new additive asset-gate workflow.
- Both falsification observations recorded in SUMMARY.md, not just the green one.
</success_criteria>

<output>
Create `.planning/quick/260729-ur8-add-ci-gated-test-validating-slash-comma/260729-ur8-SUMMARY.md` when done.
</output>