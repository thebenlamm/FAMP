---
quick_id: 260425-re1
slug: t2-2-readme-redeploy-verification-spot-c
description: T2.2 — README redeploy section spot-check + add "how to verify" paragraph
date: 2026-04-25
mode: quick
---

# Quick Task 260425-re1: T2.2 — README Redeploy Verification Spot-Check

## Background

Resume doc (~/.claude/plans/ok-now-analyze-and-toasty-waffle.md, T2.2) asked
for a spot-check of the "Redeploying after daemon code changes" section that
quick-260425-m0f added to README.md. Three checklist items:

1. Where `daemon.pid` lives — ✅ already covered (line 185).
2. Link to the redeploy script — ✅ already covered (section title + 4 invocation examples on lines 175–180).
3. How to verify a redeploy succeeded — ❌ **NOT covered.**

The script itself prints a per-agent verification table on completion
(STOP/RESTART/PID/LOG columns) and writes a fresh `listening on https://...`
beacon line into each `daemon.log`, but the README doesn't tell the operator
to look at either of those signals. This task closes that gap.

## Truths

- Current section: README.md lines 170–186 ("## Redeploying after daemon code changes").
- Script verification artifacts (observed when redeploy ran earlier today):
  - Final summary table: `AGENT / STOP / RESTART / PID / LOG` rows + a final
    "all N agent(s) cycled cleanly" line.
  - Per-agent: `tail -1 ~/.famp-local/agents/<name>/daemon.log` → `listening on https://127.0.0.1:<port>`.
  - PID file at `~/.famp-local/agents/<name>/daemon.pid` updated to the new process.
  - Binary timestamp at `~/.cargo/bin/famp` reflects the rebuild.
- No code change needed — README-only.

## Tasks

### T1: Add "Verifying a redeploy" paragraph to README

**File:** `README.md`

**Action:** After the existing paragraph that ends with "logs at
`~/.famp-local/agents/<name>/daemon.log` (appended, not truncated)." (currently
line 186), append a new paragraph that tells the operator the four concrete
ways to confirm a redeploy actually took:

1. The script's exit code (0) and final "all N agent(s) cycled cleanly" line.
2. The per-agent summary table (STOP=clean, RESTART=success).
3. `tail -1 ~/.famp-local/agents/<name>/daemon.log` shows a fresh `listening on https://127.0.0.1:<port>` line newer than the rebuild.
4. `ls -l ~/.cargo/bin/famp` shows a binary timestamp matching the rebuild.

Keep it tight (≤6 lines of prose). Do NOT touch other README sections.

**Verify:**
- `grep -n "Verifying a redeploy\|listening on" README.md` returns the new paragraph.
- Diff is README.md only; no other files changed.

**Done when:** new paragraph present, no whitespace drift elsewhere.

## Out of scope

- Touching `scripts/redeploy-listeners.sh` itself.
- Adding new flags or behaviors.
- Reorganizing the rest of the README.

## must_haves

- truths:
  - Operator reading the README can identify a successful redeploy without
    needing to read the script source.
- artifacts:
  - Modified `README.md` (one new paragraph appended after line 186).
- key_links:
  - `README.md:170-186` (existing redeploy section)
  - `scripts/redeploy-listeners.sh` (verification artifacts the README should reference)
  - `~/.claude/plans/ok-now-analyze-and-toasty-waffle.md` (T2.2 spec)
