//! Pure, transport-agnostic gateway ingress verification (WIRE-01, TRUST-02).
//!
//! `verify_inbound` is added in the next commit (Task 2 of this plan). This
//! module placeholder exists so `famp-gateway`'s `famp-keyring` /
//! `famp-envelope` dependencies and the `RejectReason` enum (added in this
//! commit) compile and lint clean before the verification logic lands.
