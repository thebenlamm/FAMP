//! `EnvelopeDecodeError` — placeholder, fully populated in Plan 01-01 Task 3.

/// Placeholder — Task 3 replaces this with the full wire-level variant list.
#[derive(Debug, thiserror::Error)]
pub enum EnvelopeDecodeError {
    /// Placeholder variant; replaced in Task 3.
    #[error("envelope decode error (placeholder)")]
    Placeholder,
}
