//! Async UDS `BusClient` — Phase 02 placeholder.
//!
//! Full implementation lands in plan 02-01 task 2. This task-1 stub exists
//! only so the workspace builds with the new module declaration in `lib.rs`.

pub mod codec;
pub mod spawn;
