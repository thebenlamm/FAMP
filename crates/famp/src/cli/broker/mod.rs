//! `famp broker` subcommand module — Phase 02 skeleton.
//!
//! The broker run loop and CLI args are owned by plan 02-02. Plan 02-01
//! lands only the `nfs_check` submodule (BROKER-05) so other plans in this
//! wave can reference it without ordering hazards.

pub mod nfs_check;
