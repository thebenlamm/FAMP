# Rust
target/
Cargo.lock.bak
**/*.rs.bk

# macOS
.DS_Store

# Editor
*.swp
*.swo
.idea/
.vscode/
.claude/worktrees/

# Local Claude Code MCP server config (user-specific paths).
.mcp.json
# ...but each host packaging's .mcp.json IS part of the plugin and must ship.
# An installed plugin is a git clone; without this the plugin loads with no MCP
# server and therefore no tools, and it fails silently because the file still
# exists in the author's working tree.
!plugins/*/.mcp.json

# Auto-generated by Corporate Brain context generator (per-user).
.claude/context.md

# GSD framework install (per-user dev tooling, regeneratable via GSD install).
# The repo tracks only the curated FAMP-specific .claude files (famp-* commands/
# skills, settings.json); everything GSD/Codex installs locally is ignored.
.claude/.gsd-profile
.claude/agents/
.claude/commands/gsd-*.md
.claude/gsd-core/
.claude/gsd-file-manifest.json
.claude/gsd-install-state.json
.claude/gsd-migration-journal/
.claude/hooks/
.claude/package.json
.claude/scripts/
.codex/

# Maintainer's private working artifacts. The curated public history
# lives in docs/history/. See docs/history/README.md for what stays
# public and why.
.planning/
.codebase-review/
learned-rules.md
HANDOFF-*.md

# Defensive: never commit secrets, keystores, or per-identity FAMP_HOME state
.env
.env.*
*.pem
!crates/famp/tests/fixtures/**/*.pem
signing.key
identity.json
FAMP_HOME/
