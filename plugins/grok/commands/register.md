---
description: Register an identity with the FAMP local broker for this Claude Code session. Use first in any new window — every other FAMP slash command is gated until register succeeds.
allowed-tools: famp__famp_register
argument-hint: <name>
---

Use the `famp__famp_register` tool to register identity `$ARGUMENTS` with the local FAMP broker.

If the user passes no argument or a name like `#channel`, ask them for a plain identity name first.
